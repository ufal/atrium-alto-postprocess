# Extra dependencies for the offline config-constant importance sweep
# (tools/const_importance_sweep.py). NOT needed by the production pipeline or by
# the test suite — install only when running a sweep.
#
#   pip install -r setup/requirements-sweep.txt
#
# pandas/numpy already come from requirements.txt.
#
# (12-factor II) NOTE: the scikit-learn and matplotlib ranges below were narrowed
# in 8fadeb9 from `scikit-learn>=1.9.0` / `matplotlib>=3.11.1` to the capped
# ranges here. The new ranges are DISJOINT from the old ones — they exclude every
# version the previous constraint required — so a sweep run before and after that
# commit uses different libraries. The ceilings are kept because they reflect a
# real interpreter constraint somewhere, but the reason should be recorded here
# rather than inferred; if it is only a local Python version, this belongs in
# that environment, not in the repo's dependency set.
scikit-learn>=1.7,<1.8      # RandomForest surrogate + permutation importance (sklearn backend)
optuna>=4.9.0            # TPE/random search + fANOVA importance (optuna backend)
matplotlib>=3.10,<3.11   # optional: param_importance.png bar charts
SALib>=1.5.2           # Morris screening + Sobol global sensitivity (model-free backends)
