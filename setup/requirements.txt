# API service dependencies.
#
# (12-factor II) Pinned with the same constraints as setup/requirements.txt.
# torch, transformers, fasttext, numpy, lxml, fastapi, uvicorn and
# python-multipart were previously unpinned entirely, so an API-only install
# resolved to whatever the index served that day and could differ from the
# batch pipeline's own resolution for the same package.
fastapi>=0.141.1,<1.0
uvicorn>=0.52.4,<1.0
python-multipart>=0.0.32
# (atrium-project#10 D4) /process accretes into a document record through
# document_hook.py, which validates it against atrium_document.schema.json before
# writing. Declared here too because setup/setup_api_server.sh installs ONLY this
# file — without it the Layer D gate degrades to a warning on an API-only install.
jsonschema>=4.26.0
torch>=2.1,<3.0
transformers>=4.57.6,<5.0
fasttext>=0.9.3
numpy>=2.4.6
lxml>=6.1.2
