# (#50) `alto-tools` is NOT a dependency any more. The two code paths this repo used
# — `-t` (text) and `-s` (statistics) — are vendored in ../alto_tools.py, from upstream
# commit 1f4f01e5f6ac3562740e39948442973b9cb94be4, Apache-2.0, with the licence text in
# ../LICENSES/alto-tools-Apache-2.0.txt. It was declared here as a `git+` requirement
# (PyPI's 0.1.0 wheel has no `-s` flag at all), which meant the published image
# re-resolved a GitHub URL to build itself and the E2E lane re-installed a moving
# `master` inside the released image at run time. Vendoring removes both.
fasttext>=0.9.3
# (atrium-project#10 D4) RUNTIME dependency, not a test one: document_hook.py calls
# atrium_document.validate_document() on every record this repo writes — the Layer D
# gate ("no doc.json is emitted if validation fails"). validate_document() raises
# RuntimeError rather than passing when jsonschema is absent, precisely so a gate
# cannot silently become a no-op; the hook then degrades to one loud warning per run.
# Declared here as well as in requirements-test.txt so the published image has it.
jsonschema>=4.26.0
lxml>=6.1.3
# (12-factor II) numpy is a DIRECT import of classify_TEXT.py,
# extract_LytRdr_ALTO_2_TXT.py, tools/recategorize_from_csv.py and
# tools/const_importance_sweep.py — it was reaching them only transitively
# through pandas/torch, which is an undeclared dependency by any other name.
# Pin mirrors setup/requirements-test.txt so the two cannot resolve differently.
numpy>=2.4.6
pandas>=3.0.5,<4.0
pillow>=12.3.0
torch>=2.1,<3.0
tqdm>=4.70.0
transformers>=4.57.6,<5.0
httpx2>=2.12.0
bitsandbytes>=0.50.2
accelerate>=1.14.0
