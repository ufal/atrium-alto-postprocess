__pycache__/
*.py[cod]
.venv/
venv/
env/
/venv-alto/
# setup_api_server.sh / Docker build artifacts (fetched, not source)
v3/
models/
lid.176.bin
# caches & local env
.pytest_cache/
.coverage
coverage.xml
.cache/
*.log
*.zip
# (#31 Phase 4) the ZIP-bundle sample of the text-lines inputs is committed.
!data_samples/TEXT/*.zip
.env
.idea/
./.idea/
# Paradata emitted by local runs. The handful of sample logs already committed
# under paradata/ stay tracked (gitignore does not untrack tracked files) —
# this only stops every local pipeline run from showing up as untracked noise.
paradata/*.json
