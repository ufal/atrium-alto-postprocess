[EXTRACT]
INPUT_CSV          = ./data_samples/test_alto_stats.csv
OUTPUT_TXT         = ./data_samples/PAGE_TXT
WORKERS_MAX        = 16
OUTPUT_TXT_LR      = ./data_samples/PAGE_TXT_LR
WORKERS_MAX_LR     = 1
LR_MODEL           = hantian/layoutreader
LR_CHUNK_SIZE      = 350
LR_MIN_CHUNK_SIZE  = 50
OUTPUT_TXT_LLM     = ./data_samples/PAGE_TXT_LLM
WORKERS_MAX_LLM    = 1
LLM_MODEL          = THUDM/glm-4v-9b
LLM_MAX_RESOLUTION = 1344
LLM_MAX_NEW_TOKENS = 4096
LLM_PROMPT         = OCR: Transcribe all text on this page exactly as it appears.

[CLASSIFY]
INPUT_CSV = data_samples/test_alto_stats.csv
TEXT_DIR = data_samples/PAGE_TXT_LR
OUTPUT_LINES_LOG = data_samples/DOC_LINE_CATEG
BATCH_SIZE = 128
WORKERS_MAX = 32
# (#7) Most-expected language MUST be first (force-remap default).
EXPECTED_LANGS = ces,deu,eng
# (#3) slk added on DK's request: FastText routinely confuses Czech<->Slovak.
# NOTE the consequence: with slk trusted, remap_lang keeps such lines labelled
# `slk` (original confidence preserved) and NEVER reaches the slk->ces special
# case.
# The score-driven quality logic is identical either way; only the stored
# `lang`/`main_lang` label differs.
# Drop slk here if you prefer slk->ces relabel.
TRUSTED_FOREIGN_LANGS = deu,eng,fra,pol,ita,slk
MODEL_NAME = Qwen/Qwen2.5-0.5B

[AGGREGATE]
RAW_LINES_CSV = data_samples/DOC_LINE_CATEG
OUTPUT_STATS = samples_page_stats.csv
OUTPUT_DOC_DIR = data_samples/DOC_LINE_STATS

[TEXT_UTILS]
LANG_SCORE_ROUGH = 0.45
LANG_SCORE_CLEAR = 0.75
ALLOWED_INTERNAL = .-,+()"'—–:%;?!/
STRIP_CHARS = .,;:!?()[]"'\/\

QS_WEIGHT_VALID_WORD  = 0.30
QS_WEIGHT_WEIRD       = 0.16
QS_WEIGHT_PERPLEXITY  = 0.15
QS_WEIGHT_LENGTH      = 0.05
QS_WEIGHT_GARBAGE     = 0.15
QS_WEIGHT_VOWEL       = 0.07
QS_WEIGHT_LANG        = 0.05
QS_WEIGHT_GIBBERISH   = 0.04
QS_WEIGHT_FUSED       = 0.03
QS_LENGTH_MAX         = 100
PERPLEXITY_THRESHOLD_MAX  = 1000.0

SHORT_PPL_CAP               = 850.0
CLEAN_PROSE_PPL_MAX         = 400.0
PPL_INVERTED_MIN            = 200.0

CATEG_GARBAGE_DENSITY_HIGH  = 0.35
# (#3 Phase 1) NOISY_MAX 0.90 -> 0.85: recovers ~24 clean Czech sentences scoring
#   0.85-0.90 as Clear.
# Zero garbage risk (garbage maxes at ~0.78 in simulation).
CATEG_NOISY_SCORE_MAX       = 0.85
# (#3 Phase 1) TRASH_MAX 0.50 -> 0.55: balanced garbage reduction.
# DIAL: 0.50 =
#   zero-collateral, 0.60 = aggressive. The Phase-2 structural Trash routes
#   (hard-sweep / inverted / short-garbage) now catch most garbage on their own,
#   so re-validate with tools/recategorize_from_csv.py --report-only before
#   raising this further;
# 0.55 vs 0.50 is the safe range here.
CATEG_TRASH_SCORE_MAX       = 0.55

ROT_RATIO_INVERTED_MIN      = 0.55
WEIRD_RATIO_INVERTED_MIN    = 0.35
ROT_HIGH_LANG_CONF          = 0.90

CLEAN_PROSE_MIN_SCORE       = 0.65
CLEAN_PROSE_WEIRD_MAX       = 0.08
CLEAN_PROSE_WC_MIN          = 4

# (#3) Short-fragment guard. Holds a very short line at Noisy when it reaches the
# Clear band but also carries noise (weirdness or garbage), since QS/perplexity
# are unreliable on tiny fragments. 0 = disabled (current behaviour). Recommended
# value 3: it holds 1-2 word noisy fragments (značky., taxo vojenské.) at Noisy
# while sparing clean short prose (republiky československé) and the LM-certain
# low-ppl fast-track. Verified collateral-free against the calibration fixtures.
CLEAR_BAND_WC_MIN           = 0

LANG_SCORE_REMAP             = 0.75
# (#3 A1) Cap for remapped NON-Latin-script guesses (Hangul/Cyrillic/CJK).
LANG_SCORE_REMAP_FAR        = 0.50
SINGLE_CHAR_ALLOWED         = aAiIuUvVzZkKsS
# (#3 C) Short Czech function words counted as valid before the 3-char gate.
SHORT_VALID_WORDS           = a,i,k,o,s,u,v,z,se,si,po,na,za,ze,do,od,ke,ku,ve,ní,mi,ti,by,je,to,co,ač,my,ty,on,ji,jí,už,až
REPEAT_ALLOWED_CHARS        = oOuU
REPEATED_DOUBLE_MIN         = 2
VOWEL_RATIO_LOW             = 0.20
VOWEL_RATIO_HIGH            = 0.70
ACADEMIC_TITLES             = PhDr,MUDr,JUDr,MVDr,RNDr,PaedDr,CSc,DrSc,Ing,Mgr,Bc,PhD,DiS,prof,doc
LDL_ALLOWED_FOLLOW          = .,/:%-;?)=
LDL_UNITS                   = m,cm,mm,g,kg,km,ha,l,ml
GARBAGE_KEEP_CHARS          =
FUSED_VOWEL_RUN_MIN         = 3
WX_REPEAT_MIN               = 2
ISOLATED_CHAR_RATIO_MAX     = 0.40
ISOLATED_CHAR_MIN_TOKENS    = 3
SYM_LET_DIG_NONTEXT         = true
MOSTLY_READABLE_VALID_MIN   = 0.85
SHORT_NOISY_QS_PENALTY      = 0.20

# (#3 Phase 2) Categoriser override / structural-route thresholds — previously
# hardcoded inside determine_category;
# surfaced here so issue #3 is actually
# calibratable. Defaults reproduce the prior hardcoded behaviour exactly.
LOWPPL_CLEAR_MAX            = 50.0
HARD_SWEEP_LANG_MAX        = 0.45
HARD_SWEEP_PPL_MIN         = 1000.0
GHOST_DOMINATED_MIN_RATIO  = 0.5
# Per-word penalty score_word() adds for tokens containing 'w' (rare in Czech;
# strong mirror/inverted-OCR signal, DK request).
WORD_W_PENALTY             = 0.20

# (#3 A3) Page-level inverted-scan sweep (apply_document_postprocessing).
INVERTED_RUN_MIN           = 4
INVERTED_PAGE_MAJORITY     = 0.60

[PIPELINE]
METHOD        = layoutreader
INPUT_DIR     = data_samples/ALTO
PAGE_ALTO_DIR = data_samples/PAGE_ALTO
SKIP_SPLIT    = false
PARADATA_DIR  = paradata