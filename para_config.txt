# para_config.txt — atrium-alto-postprocess
# Repository-specific paradata facts. Edit this file (not the Python) when
# components or versions change.
[tool]
program = alto-postprocess
version = v0.20.0
repository_fallback = https://github.com/ufal/atrium-alto-postprocess

[components]
# name = <license> ; <always|conditional> ; <role>
# "always"      -> component is part of every run (worst-case baseline)
# "conditional" -> only counts toward the license when log_component() is called
#
# Baseline (worst-case) components are flagged "always" so every run inherits at
# least the project-wide CC BY-NC 4.0 floor set by the FastText LID weights —
# the shared invariant asserted by tests/test_paradata.py across all four repos.
#
# The perplexity model and the GPU-bound extraction backends are conditional:
# they are recorded only by the script that actually loads them.

alto_tools     = Apache-2.0      ; always      ; alto-tools statistics + text extraction (alto-tools method)
fasttext       = CC BY-NC 4.0    ; always      ; FastText language identification weights (langID_classify)
qwen2.5_0.5b   = Apache-2.0      ; conditional ; Qwen2.5-0.5B multilingual perplexity model (default)
distilgpt2     = Apache-2.0      ; conditional ; distilgpt2 English-only perplexity model (alternative)
layoutlmv3     = CC BY-NC-SA 4.0 ; conditional ; LayoutLMv3 layout-aware reading-order extraction (LayoutReader method)
glm4v_9b       = glm-4           ; conditional ; GLM-4v-9b generative OCR from page images (GLM method)
