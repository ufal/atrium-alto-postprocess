# 📓 Digest — [\#30](https://github.com/ufal/atrium-alto-postprocess/issues/30) `open`: Algorithm change for 2010+ years documents

**Labels**: `enhancement`, `development`, `integration`
**Status**: ✅ **`Merged`**. @david-spacil's PR [#48](https://github.com/ufal/atrium-alto-postprocess/pull/48) landed on `test` as [`070620f`](https://github.com/ufal/atrium-alto-postprocess/commit/070620f) on 2026-09-09 at 15:37 UTC, green — `954 passed` clean, `952 passed / 2 xfailed` with the patch. `test` HEAD is `3afaddd`; the release path is `v1.4.7-beta` 🛑 *(2026-09-23: it shipped in `v1.5.0-beta` — the merge was 15:37 UTC, six hours after `v1.4.7-beta` was published at 09:34)*. `rule_short_garbage` no longer convicts on shape alone; it now requires `_has_strong_garbage_evidence()`, exactly as proposed.

The seven-week argument is over and the numbers are in. What is left is a **known, quantified, deliberately accepted debt** and one narrowing step that the merge has just unblocked. Full reasoning in [`../plans/30.plan.md`](../plans/30.plan.md).

---

## 🆕 2026-09-24 — the two answers that came after the last revision

* **Q8 (@david-spacil, 2026-09-23 13:08):** "mostly German, but misread Czech is systematic, so neither pure option
  fits." 26,889 short umlaut lines without a Czech accent in the full corpus; a weighted 150-line sample gives ~73 %
  German, ~13 % misread Czech (`ZPRÄVA`, `ÄEZY` = `ŘEZY`, `äslo` = `číslo`), ~10 % rubbish, ~5 % unclear. An exemption
  for German letters would keep a mild existing error (legible misreadings already `Clear`) and wrongly protect only
  umlaut rubbish (`PM=Ää`, `57 Ä`). So the left-out veto is worth measuring; nothing is switched on.
* **Q3 (@DanaKriv, 2026-09-23 16:08):** the 357 decisions will be done by the end of next week (≈ 2026-10-02).
* Still open: Q1 (what `Trash` means), Q2 (the de-duplication trade), Q4–Q6. `test` = `master` = `2e2794d`; release
  `v1.5.1-beta`.

## 📍 Where this stands — read this first (2026-09-22)

> ✅ **2026-09-23 — the re-check is confirmed, and the tree is synced to it.** @david-spacil
> re-scored his 508 lines on `3b02959`: **336/508**, `S-VIIIb` `Clear` again, `Lokolieace: •VIII,`
> the only break (15 moved: 11 fixed, 1 broken, 3 wrong either way), and 0/508 change with the flag
> off — so D45's restored guard is confirmed, and inert until the flag flips. The docs, config
> comments, the advisory and the tool texts were then brought in line with the settled answers,
> and the witness report now passes each row's language (D44 parity). Nothing is armed. What
> remains is people: Q1–Q6, which he is taking through with @DanaKriv by e-mail, and the new,
> optional Q8 (the plan's H14).

Everything below this section is the working record, in the order it was found,
with its corrections attached. This is the state it adds up to.

**What shipped.** `rule_short_garbage` no longer convicts a short line on shape
alone. Over both collections that moved **367,208 lines** toward `Clear` and
demoted none; a human graded 484 of them **76.5% right / 9.6% borderline /
13.9% wrong**. The price is roughly **26,000 garbage lines now sitting in
`Clear`**, accepted deliberately and tracked by a strict xfail that goes red the
day it is paid down.

**What stage 8 settled.** Four of the five questions the stage-8 table asked,
read directly off `logs8.log` rather than a summary of it:

| run     | question                                                    | outcome                                                                                                                                         |
|---------|-------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------|
| 08a     | is the de-gemination cap portable per-collection (D30)?     | **No.** `ppole` is 3 ARUP / 226 ARUB — the *most* concentrated of the eight, not the least. The last candidate for a portable signal is dead.   |
| **08b** | does `SHORT_GARBAGE_WITNESS_ENABLE` pass the adoption gate? | **Yes.** errors 513→503, `Clear`-loss **40→40**, cost 0.2917→0.2829, McNemar p=0.013. The 42→41→40 drift resolves to 40/40 in one paired run.   |
| 08c     | is the document cascade what makes it adoptable?            | **Yes** — with `--no-postprocessing`, `Clear`-loss goes 55→56 on the same flag flip. Confirms S4/S5 below.                                      |
| 08d     | D26, `QUALITY_VOCABULARY_ENABLE` — void in 07b, now armed   | **Decisively REJECT.** 540 breaks against 212 fixes, `Clear`-loss 40→**180** — 13.8% of all gold `Clear` destroyed to buy `Trash`-recall 78.3%. |
| 08e     | D27, `STRIP_SYMBOL_GLYPHS` — void in 07c, now armed         | **Null**, not void: 5 fixes / 5 breaks, p=1. Closed.                                                                                            |

**And it found a defect nobody had noticed: the exposure pass and the
annotation ask it built (08f/08g) were run with the vocabulary lexicon OFF,
while `setup/config.txt` is explicit that the witness only ever ships with it
ON — "these two keys are one decision".** That is no longer an argument, it is
a measurement: **08f and 08g have been re-run with `SHORT_GARBAGE_LEXICON_PATH`
configured**, and the ask re-cut from the result landed on `test` as
[`d9c41a5`](https://github.com/ufal/atrium-alto-postprocess/commit/d9c41a5).
Stages **9b and 9c are done**. The estimate this document carried was
directionally right and numerically wrong, the same way twice: T1 put the
exempt share at **73.1%** and it measures **82.1%** (37,555 → **6,714**
at-risk lines); T2 said a 592-row census would buy **30.7%**, and the re-cut
**157**-row census buys **18.1%**. Both estimates are kept below, next to what
replaced them.

**So the flag decision is, for the first time, sized against the configuration
that ships.** 08b passes the adoption gate on gold, cleanly, and that was never
in question — `08b`–`08e` are gold-scored A/Bs on the existing 2,064-line
sidecar and no part of this touches them. What blocked was the exposure figure
and the ask built from it. The re-run leaves **5,563 strings / 6,714 lines**
whose category the flag can actually change, and the successor ask is **357
decisions** (157 census + 200 sample) rather than 792. **The hold on
@DanaKriv's request is lifted in this revision.**

**And the re-run changed what the argument is about.** With the lexicon armed
the at-risk head is no longer the archive's own vocabulary — it is ordinary
words this archive happens to use rarely. The largest single item is
**`Dauerleihe`**, the German museum term for a permanent loan: **267 `Clear`
lines**, scanned perfectly, unprotected because a document-frequency table
built from this archive's Czech text has never seen it. One clause carries
almost all of that damage — **`vowel_run` is the sole reason for 4,305 of the
6,668 at-risk lines (64.6%)** — and the knob that narrows it,
`SHORT_GARBAGE_WITNESS_VOWEL_RUN_MIN`, already exists and is already decoupled
from everything else it could break. That is the new shape of the question, and
it is § V5 below.

**Two predicate questions were also open, and both are closed, in opposite
directions.** `Lepus europaeus`, `Mammalia indet.` and `Triticum monococcum`
all convict on the shipped predicate with no lexicon configured — but so does
every string like it that has any real document frequency, because
`_has_vocabulary_support()` (D14) already reaches all of them once the lexicon
is armed. That is not a predicate gap; it is the lexicon working as designed,
measured here rather than assumed. The genuine gap was **URLs and e-mail
citations quoted once** — a document-frequency table can never attest a
citation that appears in a single document, so the 144 such lines in the
stage-8 delivery survived even a configured lexicon. **Fixed as D33**, landed
in this commit: `is_domain_notation()` now recognises the shape (0 false
positives measured against the other 7,289 at-risk survivors). A third class —
`Kaukasus`, `Hallstatthaus`, `Schuhleistenkeilbruchstueck`: real, unattested,
too rare to be reached by either mechanism — is measured and left as named,
accepted debt (**D34**): no length or case-shape threshold separates them from
real `low_variety` garbage of the same length and ratio, and retuning one from
two named examples would repeat the exact mistake D25/D30 already made with the
geminate cap.

**And stage 6 has now landed too, closing the oldest open question in this
issue.** R3 — that every `DEAD` / `LOAD-BEARING` / `clear_loss` verdict ever
quoted here was scored against the pipeline's own output — is **answered**: the
87-hour sweep re-scored all 23 rules against the 2,064-row gold sidecar. Its
baseline gold `Clear`-loss reads **40**, the same number 08b reports from a
separate job with a different tool, which is the first cross-run consistency
check this issue has had. 🛑 **Superseded 2026-09-22 by stage 10d: the figure is
now 38, with `errors` unchanged at 503.** The cross-run agreement above still
stands for the tree it was measured on; see X1 for why the two are consistent
rather than contradictory, and for the check that has not been done.

Two things came out of it that change what is written elsewhere. **`rule_short_garbage`
— the rule this whole issue narrowed — is itself responsible for 7 of those 40
`Clear`-losses**, so the debt the witness is judged against is partly the product
of the rule the witness was built to narrow. And the sweep **nominated
`rule_short_garbage_witness` for retirement**, because a rule gated by a flag
that ships off reports `fire_count == 0` on every corpus and the classifier had
no way to say so. Fixed as **D35** (a fourth class, `INERT`); the full reading is
in the stage-6 section below.

**What is actually blocking, as of 2026-09-22 evening.** **One answer, from one
person.** @david-spacil has answered every open item (§ W below), so what remains
is **@DanaKriv's 357 decisions** and her agreement with his `Trash` definition.
Stage 10 is running on the cluster and covers the rest.
🛑 *Superseded later the same evening: stages 10 and 11 have both finished (Z1–Z6 below), and the
measurement programme is complete. What remains is decisions — @DanaKriv's two, plus the
questions put to the thread after comment 61 (§ "After the tag" directly below).*

Three things his answers added to our side of the list, none of them blocking:
the de-gemination guard comes out (**D40 resolves**, W3); the vowel-run clause
should be gated on language rather than blunted (**D44**, W5) — which needs the
language label plumbed into `determine_category()`, so it is a change and not a
setting; and the gold sidecar needs an `ssut*` check (**W6**), because he
annotated it before discovering that three of those spellings are real language.
That last one touches the `Clear`-loss figure, which is the number the whole
decision turns on, so it is first in stage 11 even though the exposure is small.
(That figure is **38** as of stage 10d, not 40 — X1.)

**What is NOT true, though earlier revisions of this document said so.** Two
prior read-throughs each found the previous stage's summary did not survive
reading the delivery itself (S1–S5 below, T1–T15 in the new section). That
pattern held a third time here — T6/T7 were first read as predicate defects and
turned out to be a measurement run in the wrong configuration, exactly like
T1 itself. The standing instruction stands: read the artefact, not the report
of the artefact, and that now includes this document's own stage-8 read before
it is trusted for stage 9.

**A fourth instance, and this one is about this document's own arithmetic.**
T1 and T2 were published as estimates and labelled as estimates, which is the
right way round — but both were wrong in the *same* direction, and the
direction is worth naming: **every estimate this issue has made of how far the
vocabulary exemption reaches has been too conservative.** T1 guessed 73.1% and
the answer is 82.1%. Whatever the next such estimate is, assume the lexicon
reaches further than it looks.

---

## 🧭 D45 — @david-spacil's 508 re-check, and two guards that had been gone since 2026-09-18 (2026-09-23)

[His answer to Q7](https://github.com/ufal/atrium-alto-postprocess/issues/30#issuecomment-5793165361):
`master`, with the tag run as well. Whole documents re-scored with `recategorize_from_csv.py`,
witness on, **no lexicon** (the shipped default):

| revision                                        | agreement with gold |
|-------------------------------------------------|--------------------:|
| `4ed309a` witness off                           |             326/508 |
| `09c9640` (`v1.5.1-beta`) witness on            |             334/508 |
| `4ed309a` witness on                            |             335/508 |
| `4ed309a` witness on, `VOWEL_RUN_EXEMPT_LANGS=` |             334/508 |

16 lines move on `master`: **11 fixed, 2 broken, 3 wrong either way** — H5 closed, and the "≤ 2 by
set inclusion" bound measured directly. The two breaks are `S-VIIIb` and `Lokolieace: •VIII,`, and
he asked whether `_RE_FUSED_GRID_REF`'s removal in `cc4990e` was intended.

**It was not, and it was not alone.** `9bc218b` (09-18 08:03) added two witness-local guards,
`_RE_FUSED_GRID_REF` and the German-diacritic veto `has_expected_lang_diacs()`, and round 2's
stage-5a re-run measured them in — 14 fixed / 1 broken, p = 0.00098, the figure the plan still
quotes. `cc4990e` (09-18 17:14, a lexicon-parsing commit) deleted both; `031fa58` (17:55) restored
the pre-round-2 config witness block that round 2 had replaced. That is a stale working copy, not a
decision, and no test pinned either guard, so the suite stayed green. This document and the plan
went on describing the `S-VIIIb` exemption as present.

**What it did to the record, read off the deliveries rather than assumed:**

* **Every run from 5f onward scored the guard-less witness.** Stage 8 never saw `S-VIIIb` break
  because its runs carried a lexicon (`viiib` attested); the shipped config has none.
* **S4's 05a-vs-05f table crossed the deletion.** 05f's two extra breaks — `Frauenzimmerbad` and
  `S-VIIIb` — are exactly the two guarded lines, so "the cascade removes two of the witness's
  errors" is confounded. The precondition conclusion survives on same-tree A/Bs (08c, 10f).
* **D44 re-solved a line the lost veto had solved.** `Frauenzimmerbad` carries `ä` twice. D44 is
  still the right rule for the phonotactics, but its fix of that line rides on a `deu` label at
  0.359 (Z5), where the veto rode on the letters.

**Landed (maintainer decision): the grid guard only**, pinned by the seven-string series, a
narrowness test and an end-to-end `S-VIIIb` row in the wiring test — which fail with the guard
removed (9 failures) and pass with it. Expected on his run: **336/508, one break**
(`Lokolieace: •VIII,`, gold `Noisy` — the decipherable-damage case Q6 asks about).
✅ **Confirmed** by him on `3b02959` (2026-09-23, 11:05 UTC): 336/508, 15 moved — 11 fixed, 1
broken, 3 wrong either way — and 0/508 change with the flag off against `4ed309a`. **Not
restored:** `has_expected_lang_diacs()` — it overlaps D44, it moves ~239 corpus lines (83 now
`Trash`), and its zero-argument cache would need registering in `_CACHES_FROM_FLAG`; it needs its
own gold A/B and exposure pass. **Flagged, not changed:** the config lexicon block `031fa58` put
back is no longer right in either version — round 2's rebuttal of it assumed `ppole` was garbage,
which W2 overturned.

Q1–Q6 are with @DanaKriv: @david-spacil is taking them through with her by e-mail.

---

## 📬 After the tag — comment 61, one overstatement in it, and the questions still open (2026-09-22, evening)

[Comment 61](https://github.com/ufal/atrium-alto-postprocess/issues/30#issuecomment-5783543756)
(20:22Z, edited 20:53Z) summarised the post-`v1.5.1-beta` work for @DanaKriv and @david-spacil. A
read of the tree against it, and against the collaborator documents, found no behavioural defect —
and one claim the code does not support.

**AT1 — `[allowed]` reaches less far than comment 61 says.** The comment (and
`docs/issue30/README.md`) told the data providers that listing a word stops the program discarding
it. `shape_garbage_clauses()` never consults the list. Probed through `classify_TEXT.score_line()`
(`ces_Latn` at 0.40, perplexity 900) with `ssuti` listed:

| line             | witness off | witness on  | clauses                                     |
|------------------|-------------|-------------|---------------------------------------------|
| `ssuti`          | `Clear`     | `Clear`     | `initial_geminate` — but gate 5 never opens |
| `ssuti vfetennl` | `Clear`     | **`Trash`** | `initial_geminate`, from `ssuti` alone      |

A line made only of listed words is safe, because listing zeroes the gibberish and weirdness that
open `rule_short_garbage`. A mixed line is not. That is exactly what the word-list file's own header
and `tests/test_word_lists.py` say — "how far a listed word should reach is an open question for
@DanaKriv and @david-spacil" — but **the question had never been put to them**, and the summary they
were given described the answer as already yes. Now asked (Q5b / plan H12).

**AT2 — three more questions recorded as theirs and never asked.** What every recognised address
should be once W4 holds (`DOMAIN_NOTATION_CATEG`, D43 — Q4); which commented `[allowed]` candidates
to arm, including the four @david-spacil has confirmed as real language (Q5a); and what
`rule_short_garbage` should answer on a short line that might be decipherable, which
`categorization_logic.md` called "a decision with an owner … not put to one" (Q6). Plus two that
were asked but have moved: which revision @david-spacil re-checks the 508 against, now that D40 and
D44 landed after the tag (Q7), and @DanaKriv's two (Q1 agreement with W4, Q2 dedup dissent).

**AT3 — the collaborator documents had drifted from the tree, and from each other.** Fixed in
place, each with its correction marked: `categorization_logic.md` described a
`rule_domain_notation_clear` / `SHORT_LINE_URL_CLEAR_ENABLE` that does not exist (the real route is
`rule_domain_notation_categ`, now gate 0b) and a witness row without D44 or its exemptions; the
review request transcribed "1940s" as "1950s", dropped the Krofta clause, and still presented Y4's
superseded prediction as current; the annotation guide's worked example row (`1 fraament okraie`,
558 lines) is not in the 157-row census, its "94% `edit1`" had no source (the two files give 102 of
113, 90%), and three of its § 6 questions asked about usefulness while its § 4 says usefulness does
not count; `annotation_ask_README.md` still listed `Non-text` as a label to write and still promised
"a single number we can change"; the root `README.md` said the repository has no gold labels.

**Nothing in the code or the flags changed.** The full suite is unchanged (1,422 passed, 13 skipped,
2 xfailed) and `ruff` is clean.

---

## 🧪 Stage 10f + stage 11 — the cascade is a precondition, and Y4 was wrong in our favour (2026-09-22)

10f closed stage 10; stage 11 was three joins over the gold corpus, minutes rather
than hours, and it answered every question stage 10 left open. Six findings, and
two of them correct this document.

### Z1 — 10f: per line the witness FAILS its own gate

|                        | errors |   cost | `Clear`-loss | `Trash`-recall |
|------------------------|-------:|-------:|-------------:|----------------|
| per-line, off          |    502 | 0.3055 |           53 | 23.3%          |
| per-line, on           |    492 | 0.2977 |       **54** | 30.0%          |
| with cascade (10e), on |    503 | 0.2810 |       **38** | 18.9%          |

Same 12 fixes / 2 breaks, same p = 0.01294 — and the tool prints
`REJECT - macro_f1 rises but Clear-loss worsen`. This reproduces 08c
(`Clear`-loss 55 → 56) on the post-D33 tree, so it is now confirmed twice:
**the document-level dedup is not a refinement of the witness, it is a
precondition for it.** @david-spacil's § 2 answer — keep the step as it is — is
therefore load-bearing for the flag decision, not the tidy-up it looked like.

One row moves between the two runs: `HHCCJCJ` is a fix in 10e and absent in 10f,
where `ymeememe, mieme?` takes its place. Small, but it makes "the cascade is
doing the work" checkable rather than asserted.

### Z2 — W6 closes. `Clear`-loss 38 carries no asterisk

**Zero** of the 2,064 gold rows contain `ssuti`, `ssutí` or `ssutě` in any
spelling. The correction does not reach the gold set, so the number the adoption
gate turns on needs no revision. Expected, and now seen rather than argued.

### Z3 — X1 is CONFIRMED, and the check that confirmed it printed the wrong verdict

11c found **3** gold-`Clear` rows matching `_RE_NOTATION_URL` and its pass
condition wanted 2, so it printed *"3, not 2. The direction is right and the size
is not."* **The pass condition was mis-specified and the answer was in the rows it
printed:**

| stored      | text                                                 |
|-------------|------------------------------------------------------|
| `Noisy`     | `tel.: 327 511 730, e-mail: archeologiekh@seznam.cz` |
| **`Trash`** | `http://www.arub.cz`                                 |
| **`Trash`** | `http://www.arub.cz`                                 |

D33 moves lines `Trash` → `Noisy`. Only the rows stored `Trash` can move, and
there are **exactly two**. The third was already `Noisy` and never contributed a
`Clear`-loss at all. So D33 removes exactly two gold-`Clear` rows from the
`Clear`→`Trash` cell, which is precisely the 40 → 38 shift, with `errors`
unchanged because both remain wrong.

**The 42 / 41 / 40 / 38 drift is accounted for at last** — the first three were
three working versions of the code, and the fourth is D33, named and checked.

The stage should have filtered on `stored == "Trash"` rather than counting every
URL-shaped row. Recorded because it is this issue's own failure mode in miniature:
the artefact was right, the summary written into it was wrong, and reading the
rows beat reading the verdict.

### Z4 — the language split's effect, measured: **12 fixes / 1 break**, and Y4 was wrong

Y4 predicted 11 fixes / 1 break — errors 503 → 504, `Clear`-loss 38 → 37 — on the
assumption that `deutendes. Alhimiaal` would be detected German and its fix lost.
The labels say otherwise:

| row                                                 | detected       | conf. | exempt? | outcome           |
|-----------------------------------------------------|----------------|------:|---------|-------------------|
| `Frauenzimmerbad", sämtlic …` (break, gold `Clear`) | `deu_Latn`     | 0.359 | **yes** | **break removed** |
| `deutendes. Alhimiaal` (fix, gold `Trash`)          | **`afr_Latn`** | 0.738 | no      | **fix kept**      |
| `Lokolieace: •VIII,` (break, gold `Noisy`)          | **`fin_Latn`** | 0.883 | no      | break stays       |
| `MI*I\ EOOCO` (fix, gold `Trash`)                   | `eng_Latn`     | 0.343 | no      | fix kept          |

So the split gives **12 fixes and 1 break**: errors 503 → **502**, `Clear`-loss
38 → **37**, both down. **The adoption gate passes — it does not need to be
re-argued at all**, and the "the gate is what needs deciding" framing in Y4 and in
the plan's 11d row is withdrawn.

**Y4's arithmetic was sound and its premise was not.** A line that reads German to
a person was called Afrikaans by FastText, and that is the only reason the fix
survived.

### Z5 — and that is exactly why the result is fragile

The split passes on this gold set partly by accident:

* `deutendes. Alhimiaal` keeps its conviction because the detector said Afrikaans
  rather than German. Had it said German — the human reading — the fix would be
  gone and Y4's worse arithmetic would hold.
* `Frauenzimmerbad", sämtlic …` is exempted on a `deu` label at **0.359**
  confidence, barely above a coin toss.
* `http://www.arub.cz` is detected **`yue_Hant`** — Cantonese in traditional
  Chinese script.

**The detector's label on a short, damaged, diacritic-free line is largely noise**,
which is precisely the population the witness exists for. Gating a rule on it is
defensible only with that stated.

**Open design question this raises, not answered here:** `_vowel_run_min_for()`
reads the language and ignores `orig_lang_score`. A `deu` label at 0.359 is weak
evidence for suspending a clause. A confidence floor would make the exemption
harder to earn — and would, on these four rows, put `Frauenzimmerbad` back at
risk. That is a real trade and it needs its own measurement, not a default.

### Z6 — 44.5% is the wrong number to read; 5.8% is the right one

The gold rows' raw detected language, over the 1,777 that carry one (the other 287
are `Empty` or `Non-text`, which never reach the detector):

| base                                     |     raw |    share | after `remap_lang` |
|------------------------------------------|--------:|---------:|-------------------:|
| `ces`                                    |     986 |    55.5% |      1,379 (77.6%) |
| `slk`                                    |     125 |     7.0% |                125 |
| **`deu`**                                | **103** | **5.8%** |                103 |
| `eng`                                    |      97 |     5.5% |                 97 |
| `vie`                                    |      89 |     5.0% |              **0** |
| `pol`                                    |      49 |     2.8% |                 49 |
| `est`, `slv`, `fin`, `nld`, `xho`, `uzn` |     112 |     6.3% |              **0** |

**Not detected Czech: 791 of 1,777 = 44.5%.** The decision rule written down
before the run said a few per cent means the split buys nothing and tens of per
cent means it is the whole answer — and 44.5% looks decisive.

**It is the wrong number.** Vietnamese, Estonian, Xhosa and Uzbek in a Czech
archaeological archive are not foreign text; they are the detector failing, which
is what `remap_lang()` exists to absorb (it folds all of them back to Czech, which
is why the stored column reads 77.6%). The share that the split actually acts on
is **`deu` + `fra` = 5.8%**, and that is the figure to quote.

So both halves of the decision rule are satisfied at once, by different numbers,
and the one that governs is the small one. On this gold set 5.8% was enough,
because the one row it reaches is the one break that mattered.

---

## 🧪 Stage 10e — the adoption gate, post-D33 (2026-09-22)

10e is the run stage 9a owed and stage 10 carried forward: the flag A/B re-run on
the post-D33 predicate. It delivered `10e_flag_ab_postd33.log` and 14 discordant
rows. 10f (`--no-postprocessing`) was still running.

### Y1 — the gate passes, and it passes wider than before

| `SHORT_GARBAGE_WITNESS_ENABLE` | macro_f1 |  errors |       cost | `Clear`-loss | `Trash`-recall                  |
|--------------------------------|---------:|--------:|-----------:|-------------:|---------------------------------|
| False                          |   0.6174 |     513 |     0.2897 |           38 | 22/180 = 12.2% [8.2, 17.8]      |
| True                           |   0.6345 | **503** | **0.2810** |       **38** | **34/180 = 18.9%** [13.8, 25.2] |

Fixes 12, breaks 2, effective n = 14, exact McNemar **p = 0.01294**, significant
at 0.05. Errors −10, cost −0.0087, `Clear`-loss unchanged. All three arms of the
acceptance test hold, which is what 08b said on the pre-D33 tree and what 9a was
owed to confirm.

### Y2 — X1's hypothesis survives its first real test

X1 read `Clear`-loss 38 against 08b's 40 and proposed D33 as the cause, on the
grounds that `errors` was unchanged at 503. **10e adds the evidence that matters:
`Clear`-loss is 38 in BOTH arms**, and 08b reported 40 in both of its arms. So the
shift is flag-independent — it belongs to the tree, not to the witness — which is
exactly the shape a D33-style change predicts and not the shape a witness change
could produce.

That is corroboration, not proof; the confirming check is still the cheap one X1
named. But the alternative explanations have narrowed considerably.

### Y3 — the witness's only two mistakes are both vowel-run cases, and they are 10d's two fixes

The 14 discordant rows read cleanly. The **12 fixes** are unambiguous rubbish that
gold calls `Trash` — `utlll`, `sektlll`, `NWIIIIIIIIIIII`, `SEEEEEEEEEEEE:`,
`HHCCJCJ`, `tirrttitt`, `NINNNIC` and five more. The **2 breaks** are:

| text                                            | gold    | clause      |
|-------------------------------------------------|---------|-------------|
| `Frauenzimmerbad", sämtlic Gesellschastsbäder,` | `Clear` | `vowel_run` |
| `Lokolieace: •VIII,`                            | `Noisy` | `vowel_run` |

**These are the same two rows 10d reported as its fixes at min=4.** One run's fix
is the other run's break, because they are the same two lines seen from the two
sides of the same clause. Everything the witness gets wrong on this gold set is
the vowel-run clause, and nothing else it does is in dispute.

### Y4 — what the language split would do to the gate, computed rather than guessed

Running the 14 rows through the predicate as it now stands, with the split built:

* **Both breaks are `vowel_run`-only**, so both are spared when the line is
  detected German or French. `Frauenzimmerbad" …` is unmistakably German.
* **Of the 12 fixes, exactly two are `vowel_run`-only** and could therefore be
  lost: `deutendes. Alhimiaal` (which also reads German) and `MI*I\ EOOCO`. The
  other ten fire `triple`, `initial_geminate` or `low_variety` and are untouched
  by the split at any threshold.

So the split turns on the detected language of **four lines**. The likely case —
`Frauenzimmerbad" …` and `deutendes. Alhimiaal` detected German, the other two
not — gives **11 fixes and 1 break** instead of 12 and 2. Errors 503 → 504,
**`Clear`-loss 38 → 37**.

**Read that trade before running it.** The break it removes is a gold-`Clear`
line — a readable German sentence sent to `Trash`, the most expensive error this
project makes. The fix it loses is a gold-`Trash` line that merely stays `Noisy`,
the cheapest. So the composition improves while the count does not.

**And the adoption gate as written would reject it.** The gate requires a
candidate to raise *neither* total errors, cost, *nor* `Clear`-loss, and +1 error
fails that test whatever the `Clear`-loss does. That is worth knowing before the
A/B runs rather than after: if the split is wanted on the strength of trading an
error for a `Clear`-loss, the gate is the thing that needs the argument, not the
split. Recorded as a prediction with its condition — it rests on four language
labels nobody has read yet (X4).

---

## 🧪 Stage 10d read against its own delivery (2026-09-22)

The vowel-run gold A/B finished and delivered `10d_vowelrun_gold_ab.log` and
`10d_discordant.csv` (4 rows). 10e is running. Both were pulled down and
recomputed rather than read from the summary written when they ran. Five
findings, and the first one moves the number this whole decision turns on.

### X1 — `Clear`-loss is **38**, not 40, and this drift is not like the earlier ones

| run             | date       | errors | `Clear`-loss |
|-----------------|------------|-------:|-------------:|
| 08b, witness ON | 2026-09-21 |    503 |       **40** |
| 10d, both arms  | 2026-09-22 |    503 |       **38** |

This document has now carried four values for this quantity — 42, 41, 40, 38 —
and the first three were resolved as three working versions of the code rather
than three answers (stage 8 § 1). **This one is different, and the difference is
readable off the two lines above: `errors` is identical at 503.** Two rows left
the `Clear`→`Trash` confusion cell without leaving the error count, which means
they are now predicted `Noisy` — still wrong, and wrong in a way `_clear_loss()`
does not count, because it sums `Clear`→`Trash` and `Clear`→`Non-text` only
(`tools/ab_constant_eval.py:54-57`).

**D33 is the obvious candidate and it landed in that window.** The URL/e-mail
exemption moves lines `Trash`→`Noisy` and nothing else; two such lines being
`Clear` in gold would produce exactly this signature.

**Stated as a hypothesis, with its check, because that is the lesson of this
issue.** The check is cheap and needs no cluster run: pull the two rows out of
the confusion matrix at 08b and at 10d and look at them. Until that is done,
**38 is the number to quote and the reason for it is unconfirmed.** If it holds,
the adoption gate's headline improved on its own and nothing needs re-running —
which would be the first time in this issue that a number moved in our favour
for a reason we could name in advance.

### X2 — a global threshold of 4 is dead, and 10d is what kills it

| `VOWEL_RUN_MIN` | macro_f1 | errors |   cost | `Clear`-loss | `Trash`-recall              |
|-----------------|---------:|-------:|-------:|-------------:|-----------------------------|
| 3 (shipped)     |   0.6345 |    503 | 0.2810 |           38 | 34/180 = 18.9% [13.8, 25.2] |
| 4               |   0.6325 |    503 | 0.2810 |           38 | 32/180 = 17.8% [12.9, 24.0] |

Fixes 2, breaks 2, effective n = 4, exact McNemar **p = 1**. The adoption gate is
indifferent — errors +0, cost +0, `Clear`-loss +0 — and **both secondary
measures move the wrong way**: macro_f1 −0.0020 and two fewer `Trash` lines
recalled.

So the blunt move is a null that costs recall, which is what V5's estimate could
not have told us and what W5's correction already implied. **10d was written into
the plan as "a regression guard rather than the decision" and it turned out to be
the decision after all** — it closes the threshold question in the direction
@david-spacil's *other* suggestion pointed.

### X3 — the split's effect on gold hinges on four language labels nobody has

The four discordant rows, run through the predicate as it now stands:

| dir   | gold    | text                                            | min=3 | min=4 | `ces` | `deu`/`fra` |
|-------|---------|-------------------------------------------------|-------|-------|-------|-------------|
| fix   | `Clear` | `Frauenzimmerbad", sämtlic Gesellschastsbäder,` | fires | —     | fires | —           |
| fix   | `Noisy` | `Lokolieace: •VIII,`                            | fires | —     | fires | —           |
| break | `Trash` | `deutendes. Alhimiaal`                          | fires | —     | fires | —           |
| break | `Trash` | `MI*I\ EOOCO`                                   | fires | —     | fires | —           |

All four behave identically at 3 and at 4, and **the language split reproduces
min=4 exactly for `deu`/`fra` and min=3 exactly for `ces`.** So on this gold set
the split is not a different experiment from the threshold — it is the same
experiment, conditioned on a label.

**Which way it lands is decided entirely by FastText's verdict on four lines, and
nobody has looked.** Row 1 is unmistakably German and is a **fix**; row 3 reads
German (`deutendes`) and is a **break**. If both are detected German the split
keeps one and keeps the other — **a wash**. That expectation should be on the
record now rather than discovered after the run, because the alternative is a
fourth summary that did not survive its own delivery.

### X4 — 11b has a much cheaper form than the one the plan carries

The plan sizes D44 by re-emitting the exposure queue with a `lang` column. X3
shows a smaller measurement answers the question that actually matters:
**emit `original_lang` for the 2,064 gold rows.** That scores D44 against gold
directly, settles the four rows above, and gives the 3%-or-40% sizing figure as a
by-product. It needs the corpus, so it is cluster-side, but it is a join and not
a run.

### X5 — German lines reach the witness, which is why D44 had to exist

`has_cz_diacs()` is **False** on all four rows, including row 1, which carries
`ä`. `ä` is in `DEU_DIACS`, not `CZ_DIACS`, so the whole-line diacritic veto at
the head of `shape_garbage_clauses()` does not protect German at all. The clause
was therefore applying a fact about Czech phonotactics to German text with
nothing in between, which is the category error W5 names — corroborated here from
the predicate rather than from the word list.

### X6 — provenance

`10d_vowelrun_gold_ab.log`, `10d_discordant.csv` (4 rows), delivered 2026-09-22.
Gold sidecar matched 2,064 of 2,067 labels (3 unmatched), unchanged. The banner
still prints the `SHORT_GARBAGE_LEXICON_GEMINATE_MAX_DF` scale advisory, because
the cluster tree predates the guard's removal (D40, landed in-tree the same day).
10e was still running when this was written.

---

## 🔬 Stage 8 f/g re-run read against its own delivery (2026-09-22)

The run that stage 9b and 9c specified: `08f_exposure_full` and
`08g_annotation_ask` re-executed with `ATRIUM_TEXT_UTILS_SHORT_GARBAGE_LEXICON_PATH`
pointed at the 113,100-document table, which is the only configuration
`setup/config.txt` permits the witness to ship in. Same corpus, same 56,599,631
lines read. Everything below is recomputed from `logs8.log`,
`08f_dedup_groups.csv`, `08f_witness_candidates.csv`, `08f_witness_distinct.csv`,
`08g_distinct_evidence.csv` and the re-cut `docs/issue30/{census,sample}.csv` /
`frame.json` — not read off the log's own summary lines.

### V0 — the delivery, in one table

|                                | 08f/08g, lexicon **off** | 08f/08g, lexicon **on** |              change |
|--------------------------------|-------------------------:|------------------------:|--------------------:|
| lines read                     |               56,599,631 |              56,599,631 |                   — |
| in scope (≤3 words, no vetoes) |                7,493,429 |               7,477,924 |         **−15,505** |
| witnessed                      |                  100,824 |                  48,909 |              −51.5% |
| distinct strings               |                   50,042 |                  42,853 |              −14.4% |
| **at risk** (`Clear`/`Noisy`)  |    8,529 str / 37,555 ln |    5,563 str / 6,714 ln | **−34.8% / −82.1%** |
| census rows → at-risk settled  |              592 → 82.8% |         **157 → 18.1%** |                   — |
| sample rows → precision        |            200 → ±6.9 pp |           200 → ±6.9 pp |                   — |

By stored category, in scope → witnessed: `Clear` 3,175,396 → **4,103** (0.1%),
`Noisy` 1,751,369 → **2,565** (0.1%), `Trash` 2,551,159 → **42,241** (1.7%).
The witness fires on 0.7% of what it can see, and 86.4% of what it fires on is
already `Trash`.

### V1 — T1 and T2 were estimates, and both were too conservative

This document's stage-8 read did the honest thing — it estimated the lexicon's
reach from 08g's `token_status` column and said in terms that the number would
become a measurement when 09b ran. It has, and the estimate missed in the same
direction twice:

| quantity                         | T1/T2 estimate |        measured |                   error |
|----------------------------------|---------------:|----------------:|------------------------:|
| at-risk exposure exempted        |      **73.1%** |       **82.1%** |   understated by 9.0 pp |
| at-risk lines surviving          |          9,876 |       **6,714** |       overstated by 47% |
| at-risk strings surviving        |          7,433 |       **5,563** |       overstated by 34% |
| census coverage of the survivors |    30.7% (592) | **18.1% (157)** | not comparable — see V9 |

The last row is not an error in the estimate: T2 projected what the *delivered
592-row* census would buy on the real population. The re-cut census is a
different instrument with 157 rows, so 30.7% and 18.1% do not measure the same
thing. The first three rows are errors, and they matter for calibration rather
than for any decision: **nothing that follows from T1/T2 changes sign.** The
ask still had to be re-cut, and it still came out smaller.

The generalisable lesson is the direction. Every estimate this issue has made of
how far `_has_vocabulary_support()` reaches has been low. A
document-frequency table over 113,100 documents attests far more than it looks
like it should.

### V2 — D33's corpus-wide effect, measured at last, as a floor

The stage-8 read recorded D33 (`_RE_NOTATION_URL` wired into
`is_domain_notation()`) as removing 8,082 lines from the *witness-reachable*
population, and marked the corpus-wide effect **unmeasured**, because the
measurement available at the time was circular (see U7).

The re-run supplies it for free. Both runs read the same 56,599,631 lines. The
scope filter is the same three vetoes. In-scope went **7,493,429 → 7,477,924**:

> **D33 removes 15,505 lines from the witness's scope, corpus-wide.**

Two caveats, both load-bearing:

1. **This is a floor, not the production effect.** The witness's scope filter
   also demands `word_count <= 3`. `rule_domain_notation` does not. Every line
   longer than three words that now matches the URL/e-mail shape is invisible to
   this count and still changes category in production.
2. **It is an inference from a difference, not a direct count.** It is sound
   only because D33 is the *only* change to the three scope vetoes between the
   two runs — D32 and D34 produced no code change, and D35–D37 are confined to
   `tools/rule_coverage_report.py`. If that ever stops being true, this number
   stops meaning what it says.

**Consequence for the release.** The `unreleased` row in `CONTRIBUTING.md`
claims "no categorisation change (every flag involved ships `false`)". That is
false and was already known to be false — `is_domain_notation()` is not behind
any flag, and URL/e-mail lines move `Trash`/`trash_hard_sweep` →
`Noisy`/`noisy_threshold`. The re-run puts a lower bound of 15,505 lines under
it. The row is rewritten in this revision and the release is a **minor** bump,
not a patch.

### V3 — one clause carries almost all of the damage

`shape_garbage_clauses()` returns which of the four clauses a string satisfies.
Splitting the 42,853 distinct strings by whether they still carry a `Clear` or
`Noisy` line today, and attributing each string to the clause that is its **sole**
reason for being convicted:

| sole clause        | at-risk strings | at-risk lines | share of at-risk | already-`Trash` lines | clause agrees with pipeline |
|--------------------|----------------:|--------------:|-----------------:|----------------------:|----------------------------:|
| `vowel_run`        |       **3,478** |     **4,305** |        **64.6%** |                17,003 |                   **79.8%** |
| `low_variety`      |           1,209 |         1,311 |            19.7% |                 5,351 |                       80.3% |
| `initial_geminate` |             573 |           603 |             9.0% |                 4,571 |                       88.3% |
| `triple`           |              77 |            77 |             1.2% |                 3,553 |                   **97.9%** |

The last column is the share of a clause's sole-reason lines that the pipeline
*already* calls `Trash` — the only precision proxy available before annotation,
and a weak one, because it measures agreement with the pipeline rather than with
truth. Read carefully, it still says something clean:

* **`triple` is the best-targeted clause in the witness and the smallest.** It
  is the sole reason for 77 at-risk lines in 56.6 million, and 98% of what it
  fires on is already discarded.
* **`vowel_run` is the biggest and the least discriminating.** It is the sole
  reason for two thirds of everything the flag could damage.
* The four clauses barely overlap. Of 5,695 at-risk strings, **5,337 (93.7%)
  satisfy exactly one clause**, so the table above accounts for essentially all
  of the population rather than a slice of it.

**This reframes the flag decision.** Up to now the question has been "arm the
witness or not". The measurement says the witness is close to being one clause
with three small companions, and the interesting question is what `vowel_run`
should be, not whether the witness should exist.

### V4 — the at-risk head is no longer the archive's vocabulary. It is ordinary words

T3 recorded what the lexicon-off at-risk head was made of: `ppole` (42.7%),
`ARCHAIA` (10.7%), Latin taxonomy (4.7%), footer URLs. Every one of those is
the archive talking about itself, and every one of them is exempt once the
lexicon is armed. What is left is a different kind of thing entirely:

| at-risk lines | string                     | current | clause        | what it is                                   |
|--------------:|----------------------------|---------|---------------|----------------------------------------------|
|       **286** | `Dauerleihe` *             | `Clear` | `vowel_run`   | German: *permanent loan*. A museum term.     |
|           125 | `J. Vysoean`               | `Noisy` | `vowel_run`   | a personal name, damaged (`Vysočan`)         |
|            47 | `Dated=Dated (relatively)` | `Clear` | `low_variety` | structured metadata, read correctly          |
|            46 | `lenaye`                   | `Clear` | `vowel_run`   | damaged                                      |
|            26 | `Poeitaecxy soubor`        | `Clear` | `vowel_run`   | damaged (`Početový soubor`)                  |
|            14 | `eaual to:`                | `Clear` | `vowel_run`   | damaged (`equal to:`)                        |
|            13 | `FEUILLETON.`              | `Noisy` | `vowel_run`   | French/German: a newspaper section. Correct. |
|            10 | `B/ POSTKRANIAINY SKELET:` | `Noisy` | `vowel_run`   | damaged (`POSTKRANIÁLNÍ SKELET`)             |

\* `Dauerleihe` as one census decision folds **24 spellings** and settles 305
lines, of which 286 are at risk (`Clear:277|Trash:19|Noisy:9`).

**The mechanism, and it is worth stating plainly because it will recur.** The
exemption lexicon is built from the archive's own text. It therefore protects
whatever the archive says *often*, and offers nothing to a word that is
perfectly correct but rare *here*. German museum vocabulary, French loanwords
and foreign place names are exactly that class. `Dauerleihe` is `unattested`;
so are `auerleihe`, `Bauerleihe` and `Jauerleihe`, which are the same word
damaged — the table cannot tell them apart, because it has never seen any of
them.

This is the same failure that produced the `Linum`/`ilium` result at stage 7 and
the Latin-species result at stage 8, arriving a third time through a third door.
The corpus profile already tells David and Dana that dictionary-style tests
misfire on this archive; this is the sharpest example of it yet, and the first
where the misfire is on a language rather than on a register.

**And the recoverability column behaves correctly here, which is worth
recording after T11 was so critical of it.** `Dauerleihe` scores **0.00** — no
attested neighbour, which is exactly right for a word the table has never seen.
`J. Vysoean` scores **1.00** — correctly identified as a damaged copy of
something real. Across the whole at-risk population: 48.8% score 0.00 and 24.8%
score 1.00, so the column is bimodal rather than uninformative. T11's criticism
stands as stated (it cannot distinguish *correct and rare* from *damaged beyond
recognition*, and both land on 0.00) but it is not useless, and the annotation
rows carry it for a reason.

### V5 — the vowel-run threshold, and it is the one lever that fits the damage

`vowel_run` fires on a run of `SHORT_GARBAGE_WITNESS_VOWEL_RUN_MIN` vowels,
default 3. `Dauerleihe` has `aue`. `FEUILLETON` has `eui`. `Vysoean` has `oea`.
`lenaye` has `aye`. Every item in V4's table that was read *correctly* is a
three-vowel run, and the page-stamp family that the witness exists to catch —
`OUUITN`, `OUOISP`, `OUUIUO`, `OUOIRS` — is four or more.

Estimated over the delivered strings by applying the clause's own regex at a
higher threshold:

| `..._VOWEL_RUN_MIN` | at-risk strings | at-risk lines |      vs. 3 | already-`Trash` lines confirmed |  vs. 3 |
|--------------------:|----------------:|--------------:|-----------:|--------------------------------:|-------:|
|     **3** (shipped) |           5,695 |         6,668 |          — |                        42,143 * |      — |
|               **4** |       **2,671** |     **2,891** | **−56.6%** |                          29,058 | −31.0% |
|                   5 |           2,277 |         2,436 |     −63.5% |                          25,999 | −38.3% |

\* Lines belonging to strings with no at-risk line. `logs8.log` reports 42,241,
which counts 98 further `Trash` lines sitting inside strings that also carry a
`Clear` or `Noisy` line; see V8.

~~At 4, the strings spared include every correctly-read item in V4 —
`Dauerleihe`, `J. Vysoean`, `FEUILLETON.`, `POSTKRANIAINY SKELET`, `lenaye` —
and the strings still convicted include the whole `OUUITN`/`OUOISP`/`OUUIUO`
stamp family and `eaual to:` (`eaua`, a four-run). That is the trade one would
design if one were designing it by hand, which is precisely why it should not
be adopted on the strength of this table.~~

> 🛑 **WRONG, corrected by @david-spacil on 2026-09-22, and it contradicted V4
> two sections above.** `J. Vysoean` and `B/ POSTKRANIAINY SKELET:` are **not**
> correctly read. They are scanning errors — `Vysočan` and `POSTKRANIÁLNÍ
> SKELET` — so the three-vowel clause convicts them **correctly**, and 4 lets
> them through. `lenaye` is damaged too. V4's own table says "damaged" for both
> of the first two; this paragraph then listed them as correctly read. Nothing
> new was measured between the two sections — the second one simply reached for
> a tidier sentence than the data supported.

**The honest version of the trade at 4.** It spares **two** strings confirmed
correctly read — `Dauerleihe` (286 at-risk lines) and `FEUILLETON.` (13) — and
releases confirmed scanning errors with them: `J. Vysoean` (125), `lenaye` (46),
`Poeitaecxy soubor` (26), `noienm k.` (19), `B/ POSTKRANIAINY SKELET:` (10). The
`OUUITN`/`OUOISP`/`OUUIUO` stamp family and `eaual to:` (`eaua`, a four-run) are
still convicted. **299 confirmed-good lines against at least 226 confirmed-bad
ones** is not the clean trade this section originally described, and it is the
reason W4 below records a different direction entirely.

**Three reasons this is a proposal and not a result:**

1. **It is a regex estimate, not the predicate.** `shape_garbage_clauses()`
   applies its vetoes and its own text normalisation before the clause runs;
   this recomputation applies the vowel-run pattern to the delivered `text`
   column. It needs the real predicate over `08f_witness_distinct.csv` —
   **local, no cluster, minutes** — before any number here is quoted as
   measured.
2. **Nothing here is scored against gold.** 08b's adoption gate (errors
   513→503, `Clear`-loss 40→40, McNemar p=0.013) was measured at min_run 3.
   Changing the threshold invalidates it until re-scored. That is also local:
   `tools/ab_constant_eval.py` against the 2,064-line sidecar.
3. **The tuning failure mode of this issue is exactly this.** D25 and D30 were
   both thresholds fitted to a handful of named examples that did not survive
   scale. Choosing 4 because it spares four words one can name is the same move.
   The examples are a hypothesis; the gold A/B is the test.

**What is genuinely safe about it**, and worth recording so it is not
re-litigated: the knob exists already and was deliberately decoupled from
`FUSED_VOWEL_RUN_MIN` for this purpose, so moving it cannot touch
`detect_fused_words()`, `fused_ratio` or the `fused_words` CSV column; and
`SHORT_GARBAGE_WITNESS_ENABLE` ships `false`, so changing its default has **no
production effect at all**.

**UPDATE, same day — point 1 is largely discharged, and cheaply.**
`classify_line()` hands the raw `text` to `shape_garbage_clauses()` and writes
that same string into the distinct CSV, so the min=4 queue really is a subset of
the min=3 queue and the predicate can be re-run over the delivered file rather
than over the corpus. Done, inside `override_constants()` **used as a context
manager**:

| quantity        |   min=3 |     min=4 |     change |     min=5 |
|-----------------|--------:|----------:|-----------:|----------:|
| strings         |  42,853 |    27,129 |     −36.7% |    23,816 |
| lines           |  48,909 |    32,027 |     −34.5% |    28,496 |
| at-risk strings |   5,695 |     2,677 |     −53.0% |     2,283 |
| at-risk lines   |   6,668 | **2,897** | **−56.6%** |     2,442 |

The regex estimate said 2,891 at-risk lines and −56.6%. The predicate says 2,897
and −56.6%. **The estimate was right to within six lines.**

**It is still a lower bound, and the reason is worth keeping.** This recompute
ran with **no lexicon configured**, because the table lives on the cluster. With
one armed, `_has_vocabulary_support()` exempts *sub-tokens*, so a multi-token
line loses the clauses its attested tokens would have contributed. Measured
against the delivered `clauses` column, the lexicon-off recompute disagrees on
**191 of 42,853 rows (0.45%)** and in every single case it finds *more* clauses
than the armed run recorded. So the armed figure sits at or slightly below 2,897,
and stage 10a re-reads the corpus with the table armed rather than inheriting a
number that is nearly right. Point 2 — that none of this is scored against gold —
is unchanged and is the one that matters.

**And the threshold question does not need a new annotation round.** Intersecting
the spared set with the ask already in @DanaKriv's hands:

| file         | rows |           spared at min=4 | still convicted | partly spared |
|--------------|-----:|--------------------------:|----------------:|--------------:|
| `census.csv` |  157 | **81** (51.6%), 870 lines |   76, 347 lines |         **0** |
| `sample.csv` |  200 |           **103** (51.5%) |              97 |         **0** |

**184 of 357 decisions (51.5%) touch a string the move would spare, and not one
row is split.** That last column is the load-bearing one: the census folds
spellings into a single decision, and a row half-spared could not be scored
either way. So when the answers come back they can be scored twice — once over
every row, once over the rows that survive at min=4 — and the difference *is* the
trade, at no cost to anyone.

### V6 — the dedup cascade's sign flipped at scale, and H6 option 2 is dead

S4 measured the document-level modal dedup on the 822-document queue and found
it **rescued 31 `Trash` lines while destroying 17 `Clear` ones** — a net gain,
but an uncomfortable one, and H6 asked whether a rule blocking a bare plurality
from demoting `Clear` → `Trash` would improve it. Over both collections:

| contested groups                             | 822-doc queue |         both collections |
|----------------------------------------------|--------------:|-------------------------:|
| vote lands on `Trash`, carrying `Clear` down |      17 lines |  **10 lines** (8 groups) |
| vote lands off `Trash`, lifting `Trash` out  |      31 lines | **51 lines** (41 groups) |
| **net**                                      |           +14 |                  **+41** |
| groups H6 option 2 would change              |             — |                    **0** |
| `Clear` lines H6 option 2 would save         |             — |                    **0** |

**H6 option 2 is closed by measurement.** It changes nothing, because at full
scale no group is decided by a bare plurality that lands on `Trash` — the eight
that do are strict majorities, and a tie already resolves off `Trash` by the
alphabetical accident recorded in T14.

The cascade is also much less busy than its fixture profile suggested: 43,103
`(document, string)` groups, of which **43,052 are unanimous**. 34 are strict
majorities and 17 are ties, together 244 lines of 48,909 (0.5%). And 86.6% of
witnessed lines sit in a group of one, where the cascade cannot act at all.

The tie-break remains load-bearing and remains an accident: `x.mode()[0]`
returns tied values sorted, `Clear` < `Noisy` < `Trash`, so a tie never lands on
`Trash`. 17 groups / 40 lines depend on it now rather than T14's 180. Smaller,
still undefended by any test that names it.

### V7 — the eight doubled-letter tokens are not in the queue at all

Searched across all 42,853 strings, whole-token, case-insensitive:

| token     | rows in the lexicon-on queue |
|-----------|-----------------------------:|
| `ppole`   |                        **0** |
| `ssuti`   |                        **0** |
| `ssutí`   |                        **0** |
| `ssutě`   |                        **0** |
| `llocm`   |                        **0** |
| `vvkop`   |                        **0** |
| `jjámy`   |                        **0** |
| `oobjekt` |                        **0** |

Two separate reasons, and they should not be conflated. `ssutí`, `ssutě` and
`jjámy` carry Czech diacritics, so `has_cz_diacs()` vetoes them out of scope
before any clause runs — they could never have been in this queue in any
configuration. The rest are exempt by document frequency: at full scale
`_is_geminate_artefact()`'s cap of 10 is below all of their df values, so
`_has_vocabulary_support()` protects them.

**Consequence.** § 4 of the review request — the question only @david-spacil can
answer — **no longer blocks the annotation ask**, because no annotation decision
depends on it. It remains open, and remains worth his one line, but it is now a
question about whether the de-gemination guard should exist at all, not about
what Dana is being asked to label. That is a smaller, slower question, and the
review request is rewritten to say so.

> ✅ **Answered 2026-09-22 (W2/W3), and in both halves.** Three of the eight are
> not damage at all, and on the guard: *"if the dictionary already covers all
> eight, it looks redundant – no objection to dropping it."* This table is the
> measurement that says it does. **D40 resolves to: remove the guard.** The code
> change is deliberately not made in the same pass as the reading.

### V8 — two reconciliation gaps between the artefacts, both small, one of them quoted to collaborators

Recomputing the log's own summary figures from the CSVs it wrote does not
reproduce them exactly:

| figure                  |            `logs8.log` says | recomputed from `08g_distinct_evidence.csv` |             gap |
|-------------------------|----------------------------:|--------------------------------------------:|----------------:|
| queue, distinct strings | 42,248 (08g) / 42,853 (08f) |                                      42,853 | **605 strings** |
| at risk, strings        |                       5,563 |                                       5,695 |        **−132** |
| at risk, lines          |                       6,714 |                                       6,668 |         **+46** |

The 605 is 08g's own normalisation dropping rows from 08f's queue before it
counts. The −132/+46 has the shape of a string-level rather than line-level
`Clear`/`Noisy` test: 58 strings carry *both* at-risk and `Trash` lines, and
counting all their lines rather than only the at-risk ones moves the totals in
exactly these directions.

**Neither changes a conclusion** — 2.3% of strings, 0.7% of lines — and both are
recorded rather than chased. But one of them is being quoted to collaborators:
**`annotation_ask_README.md` states 18.1% against a denominator that cannot be
reproduced from the delivered CSVs alone.** The document now says so in one
line rather than presenting the figure as exact.

### V9 — the census settles less than 18.1% of what can actually change

`logs8.log` reports `census.csv    157 decisions ->   1,217 lines (18.1% of at-risk)`.
Split by what the rows are:

| tranche            | rows |  lines | can a label change the decision? |
|--------------------|-----:|-------:|----------------------------------|
| `census_head`      |   60 |    934 | yes                              |
| `census_contested` |   97 |    283 | partly                           |
| **by stratum**     |      |        |                                  |
| `at_risk/*`        |  118 |  1,068 | **yes**                          |
| `confirms_trash/*` |   39 |    149 | **no** — already `Trash`         |

So 1,217 / 6,714 = 18.1% counts 149 lines whose label cannot move anything.
Counting only the at-risk rows: **1,068 / 6,714 = 15.9%**.

The arithmetic closes cleanly, which is the reassuring part: 5,563 at-risk
strings − 118 in the census = **5,445**, exactly the string total of the three
strata in `frame.json`. The 39 `confirms_trash` rows are a deliberate control —
they ask whether the witness is right where the pipeline already agrees with it —
and they are worth having. They are just not coverage, and the number quoted to
Dana should not imply they are.

**The instrument has changed character, and that is the population's fault, not
the tool's.** There is no head left to take a census of: 1.1 lines per distinct
string across the whole queue and **95.7% of at-risk strings occur exactly once**.
Over the whole witnessed queue the top 500 strings settle 12.5% of lines (top 1:
3.5%; top 20: 7.7%; top 100: 9.8%). Restricted to the at-risk population, which
is the one the ask is drawn from, the curve is flatter still:

| top N at-risk strings | at-risk lines settled | share |
|----------------------:|----------------------:|------:|
|                    10 |                   585 |  8.8% |
|                    60 |                   828 | 12.4% |
|                   100 |                   928 | 13.9% |
|                   500 |                 1,473 | 22.1% |

A frequency-ranked census was the right instrument for the lexicon-off queue and
is nearly the wrong one here. 60 head rows is what is left of it — and the
variant folding is what makes even those worth taking, since they settle 934
lines rather than the 828 the bare strings would. The 200-row stratified sample
now carries the estimate.

One thing genuinely improves: **357 decisions cover 516 distinct spellings**,
because the census folds punctuation and spacing variants into one row —
`Dauerleihe` alone folds 24.

### V10 — the weight trap survived the re-cut, with new numbers

T15 recorded that `sampling_weight` counts *strings*, so multiplying it by
`lines_settled` gives the wrong total. Re-measured on the new frame:

* weight is exactly `strings_in_stratum / sampled`: 1310/48 = **27.2917**,
  2668/98 = **27.2245**, 1467/54 = **27.1667**.
* `Σ(lines_settled × weight)` = **5,717.3** against the frame's own **5,646**.
  The old files tripped the same way at 10,051 against 9,906.

The correct method is unchanged and is still stated in the README: estimate a
rate per stratum, then combine the three by their **line** counts. The strata are
much more alike than they were (1.03–1.05 lines per string, against 1.30–1.38
before), so the error is smaller — 1.3% rather than 1.5% — and no less wrong.

### V11 — provenance

Re-run delivered 2026-09-22. `08f_exposure_full` + `08g_annotation_ask` only;
08a–08e were not re-executed and are unaffected. Artefacts:
`logs8.log`, `08f_dedup_groups.csv` (51 contested groups), `08f_witness_candidates.csv`
(48,909 lines), `08f_witness_distinct.csv` (42,853 strings),
`08g_distinct_evidence.csv` (42,853 strings with evidence columns). The re-cut
ask is in the repository at `docs/issue30/census.csv`, `sample.csv` and
`frame.json` as of [`d9c41a5`](https://github.com/ufal/atrium-alto-postprocess/commit/d9c41a5).
08g was run at `--strong-df 10`, **not** the `--strong-df 113` that T11 and 09c
recommended; its own log repeats the recommendation. That remains open, and it
affects the `nearest_attested` / `recoverability` columns the annotator sees,
not which rows were selected.

---

## 👤 @david-spacil's answers, and the two of them that correct us (2026-09-22)

He answered `docs/issue30/issue30_review_request.md` at
[`b691e07`](https://github.com/ufal/atrium-alto-postprocess/commit/b691e07) section by section,
without waiting for the tag. Four items close; two of them close by telling us we were wrong.

**Both corrections are of the same kind, and it is the kind this document keeps
producing: not something we could not see, but something we had measured and
then read past.** `ssuti` appears in 195 separate documents, and this document
said in terms that 195 documents is not what an artefact looks like — and then
went on calling it an artefact. V4's own table calls `J. Vysoean` damaged, and
V5 listed it two sections later among the correctly-read. Neither needed new
data.

### W1 — the small ones, closed

| item  | answer                                    | effect                                                           |
|-------|-------------------------------------------|------------------------------------------------------------------|
| § 2   | option 1; drop option 3                   | **H6 is now closed by the owner as well as by measurement** (V6) |
| § 3   | the 508-line re-check comes after the tag | recorded, nothing blocked                                        |
| § 8.1 | close the raw-perplexity thread           | closed                                                           |

### W2 — `ssuti` / `ssutí` / `ssutě` are an old spelling of *suť*, not scanning errors

> *"I was ready to say it's an OCR artefact, but upon further investigation, it's actually an old
> way of spelling the word 'suť'. So these are, in fact, `Clear`."*

**The split is four against four**, not one against seven: `ppole` and the three `ssut*` forms are
real language; `llocm`, `vvkop`, `jjámy` and `oobjekt` are damage. Corrected in six places across
this repository, each marked 🛑 where it stood.

**The conclusion survives and its reason improves.** The per-collection table (08a) used to read as
an inversion — the one legitimate token was the most one-sided of the eight, so concentration
pointed exactly the wrong way. It is not an inversion. **Both kinds of real language are house
conventions, and each house is a different one**: `ppole` is ARUB's (3 / 226), `ssut*` is ARÚP's
(152 / 43). The four genuine errors are small and split across both. Concentration cannot separate
language from damage because what it separates is *institutions*, and each institution has its own
vocabulary. That is a cleaner statement of D30's negative result than the one it replaces.

It also sharpens the reading of the ratio table: the "genuinely empty gap from 2.60× to 4.66×" that
`SHORT_GARBAGE_LEXICON_GEMINATE_MAX_DF` is fitted to has **real language on both sides of it**. The
gap separates doubled-letter *shapes*, not damage from vocabulary.

### W3 — D40 resolves: the de-gemination guard comes out

> *"if the dictionary already covers all eight, it looks redundant – no objection to dropping it."*

V7 is the measurement that says it does: **zero of 42,853 queue rows** carry any of the eight, so
at full scale the guard fires on none of them. It is a threshold fitted to an 822-document table
that now changes nothing — and with `ssut*` turning out to be real language, keeping it is a
standing risk of convicting real words for no measured benefit.

**Not changed in this pass**, deliberately: this round was the reading, and the code change belongs
with a test and a re-measurement rather than with a document edit.

### W4 — the category definition is settled, in favour of legibility

> *"`Trash` = illegible. Anything legible is `Clear`, easily decipherable is `Noisy` – regardless of
> how useful the line is to us."*

**T9 resolves**, pending @DanaKriv — he framed it as his view and she is the other half of it. Four
consequences:

1. **`http://www.arub.cz` is `Clear`.** D33 moved it `Trash` → `Noisy`, the right direction and one
   step short. 5,309 lines.
2. **The annotation guide holds the correct definitions** and stops being one of two candidates.
3. **The root `README.md` row that looked wrong is right.** It says `Trash` "should be re-processed
   by another OCR tool", which is exactly his delete-versus-re-OCR answer — *"most of it is probably
   an HTR candidate, so re-process rather than delete"*. Only the legibility semantics needed
   reconciling, not the processing column. T9 had treated the whole row as suspect.
4. **`Non-text` is out of scope for annotation.** *"I can't tell it from `Trash` without the source
   image."* He labelled only `Clear`/`Noisy`/`Trash` apart from a few hundred early lines with
   complete documents (*"1940s ARÚB"*) — *"with a few exceptions, most of them originate from Tomáš
   Krofta's annotations"* — **which finally explains the shape of the gold set**: 45 `Non-text` rows
   against 1,302 `Clear`. *(Added 2026-09-22, evening: the sidecar agrees with his account. All 45
   are `calibration_1567` rows; 24 sit in 1940s ARÚB documents (`MTX194…`), and the other 21 in just
   seven later documents — 18 in four ARÚP ones from the 1970s–2000s — which would fit the Krofta
   exceptions. The review request had transcribed his "1940s" as "1950s"; corrected there.)* That asymmetry has been visible in every class-support table this issue
   has printed and was never accounted for. @DanaKriv is now told to do the same.

### W5 — the vowel-run clause: right rule, wrong scope, and we mislabelled two examples

> *"For Czech, 3+ vowels in a row is a good rule – Czech has no triphthongs. For German and French
> it does damage. So either split by language, or try 4+."*
>
> *"One correction: `J. Vysoean` and `POSTKRANIAINY SKELET` are not correctly read – they're OCR
> errors. The 3-vowel test catches them correctly; 4+ would let them through."*

**The correction first, because it changes the arithmetic.** V5 is amended in place. At min=4 the
spared set contains **299 confirmed-good lines** (`Dauerleihe` 286, `FEUILLETON.` 13) and **at least
226 confirmed-bad ones** (`J. Vysoean` 125, `lenaye` 46, `Poeitaecxy soubor` 26, `noienm k.` 19,
`POSTKRANIAINY SKELET` 10). That is not the clean trade V5 described.

**And the framing is wrong, not just the threshold.** A three-vowel run is a fact about Czech
phonotactics. Applying it to German and French is a category error, and raising the threshold does
not fix a category error — it buys the two German words by releasing a great deal of Czech damage.
D44 records the direction: **gate the clause on the detected language.**

**What that costs, checked rather than assumed.** `DOC_LINE_CATEG` already carries `lang`,
`lang_score`, `original_lang` and `orig_lang_score` per line. But `determine_category()` receives
`lang_score`, `orig_lang_score` and `is_upright_czech` — **not the language label** — and the
nearest existing proxy does not discriminate here:
`is_upright_czech = has_cz_diacs(text) or real_hits > 0`, and the witness's scope has already vetoed
every diacritic line, so on this population it reduces to a Czech word-list hit. `Dauerleihe` fails
that test; so does the damaged `J. Vysoean`. The split therefore needs the label plumbed into
`determine_category()` and its own A/B. It is a change, not a setting.

**The cheapest next measurement needs no design decision at all:** emit the `lang` distribution of
the witness queue. If 3% of at-risk lines are detected non-Czech the split buys nothing and 4 is the
pragmatic answer after all; if it is 40%, the split is the whole answer. One column in the exposure
report. Stage 11.

### W6 — the consequence nobody asked about: the gold set predates W2

He annotated `tools/gold/sidecars/issue30_gold_2067.csv` while believing `ssut*` were damage. **If
any of its 2,064 rows is an `ssut*` line carrying `gold_categ = Trash`, that row is now wrong** —
and this is the sidecar every `Clear`-loss figure in this issue is measured against, **including the
40 that the adoption gate turns on**.

The exposure is small: 1,667 `ssuti` lines in 56.6M, and the gold corpus is 822 documents, 0.7% of
the collections. The sidecar carries locators and no text, so the check needs the corpus and is
cluster-side. **That is exactly why it should be ruled out by looking rather than by arithmetic**,
and it is the first row of stage 11. Until it is done, `Clear`-loss 40 carries an unquantified
asterisk — a small one, and an honest one.

---

## 🔬 Stage 6 read against its own delivery (2026-09-22)

The 87-hour gold-scored rule sweep finished and delivered `logs6.log` and
`06_rule_coverage_gold.json`. Both were pulled down and recomputed rather than
read from the summary written when they ran. Eight findings. **It closes R3**,
the oldest open instrument question in this issue — and it contains a ninth
instance of the failure mode R3 was about, this time in the classifier's own
output.

> **What R3 asked.** Every `DEAD` / `REDUNDANT-HERE` / `LOAD-BEARING` /
> `clear_loss` verdict this issue ever quoted was scored against the pipeline's
> own stored `categ`, whose baseline is zero by construction. The plumbing was
> fixed on 2026-09-18; the numbers were not regenerated. **They are now.**

### U0 — it cross-validates stage 8, which nothing had done before

Baseline gold `Clear`-loss reads **40** — the same number 08b reports for the
shipped configuration, on the same 2,064-row sidecar, from a completely separate
cluster job with a different tool. Two independent runs, one number. This issue
has never had a cross-run consistency check before and it is worth recording
that the first one passes.

### U1 — `rule_short_garbage_witness` is classified `DEAD`, and that verdict is an artefact of a flag

This is the finding that matters, because acting on it would have deleted the
feature this issue exists to build.

The delivered summary says, verbatim:

```
DEAD rules (fire_count == 0 — safe to retire after full-corpus confirmation):
  - rule_mid_uppercase
  - rule_short_garbage_witness
```

And the tool's own docstring defines that class as *"unreachable dead code [that]
can be permanently deleted without a gold label set, because deletion provably
changes nothing"*.

**It is not dead. It is switched off.** `_fire("rule_short_garbage_witness")` sits
behind `SHORT_GARBAGE_WITNESS_ENABLE` (`text_util.py:1504-1506`), which ships
`false`. Its `fire_count` is therefore 0 on *any* corpus, for reasons that have
nothing to do with its population. Meanwhile:

* **08f measured the same predicate reaching 100,824 lines** across both
  collections;
* **08b flipped the flag and passed the adoption gate** — errors 513 → 503,
  `Clear`-loss 40 → 40, McNemar p = 0.01294.

So the one rule the sweep nominated for retirement is the rule with a measured
six-figure population and a passing adoption gate.

Three things made this reachable rather than merely possible:

1. `_classify()` is three lines and reads `fire_count == 0 → DEAD`,
   short-circuiting before anything else is consulted.
2. `RULE_COVERAGE.md` called `fire_count == 0` *"the only gold-free,
   **config-independent** retirement criterion"*. "Config-independent" is the
   false word and the whole criterion rested on it.
3. The tool **exits 1** whenever any rule is `DEAD`, so a flag that ships off
   makes this instrument return non-zero to any pipeline driver.

**And this was known — in two places, neither of which travels with the
artefact.** `tests/test_pipeline_parity.py::UNREACHABLE_RULES` has separated
*"unreachable BY CONFIGURATION"* (the witness) from gate shadowing
(`rule_mid_uppercase`) for months, in a test file no instrument can consult. And
`issue30_stage6_job.sh` says it outright in its own epilogue:

```
  A rule DEAD here is a retirement CANDIDATE, not a retirement:
  tools/RULE_COVERAGE.md holds the criterion. rule_short_garbage_witness will
  read DEAD for as long as SHORT_GARBAGE_WITNESS_ENABLE ships false — that is
  the flag, not the rule, and it is listed in UNREACHABLE_RULES for it.
```

That is exactly right, and it was written before the run. **It is printed to the
SLURM `.out` file, not into `06_coverage.log`**, which is the file `tee` captures
and the file that gets attached to the issue. So the delivered artefact carries
the `DEAD` verdict and the words *"safe to retire"* with no qualification, while
the correction sits in a file nobody pastes.

This is not a case of nobody knowing. It is a case of the knowledge living
anywhere except in the thing a reader receives — which is the same shape as
every other instrument finding in this issue, one level up. D35 moves it into
the tool, so it travels with the output instead of beside it.

**Fixed as D35**, landed here: `text_util.CONFIG_GATED_RULES` declares the rule →
flag mapping next to the flag itself, and the report gains a fourth class,
`INERT`, consulted before the `fire_count == 0` branch. Reproduced live on the
smoke fixture:

```
rule_short_garbage_witness | 0 | 0.0000 | 0 | -- | 0 | INERT
    ← INERT (SHORT_GARBAGE_WITNESS_ENABLE is off — not a retirement signal)
```

The gate is consulted **only when the count is zero**, so a gated rule that
somehow fires is still reported normally — a flag must not be able to hide a real
finding. `RULE_COVERAGE.md`'s criterion gains the matching clause.

### U2 — the other `DEAD` rule is genuinely unreachable, and neither is retirable

`rule_mid_uppercase` fires 0 times because gate 9d requires `word_count <= 2` and
gate 7 has the same condition and always returns first — structural shadowing,
already pinned in `UNREACHABLE_RULES`. So **neither** of the two rules the sweep
nominated is a retirement candidate, and both were already documented as
unreachable elsewhere in the tree. Stage 6's retirement advice has no valid
target at all.

There is also direct evidence that the `DEAD` class is sample-sensitive in
practice and not only in principle. The 2026-09-09 sweep, over 1,471 scored
lines, called **three** rules DEAD:

| rule                 | 2026-09-09 (1,471 scored) | stage 6 (4,886,492 scored) |
|----------------------|--------------------------:|---------------------------:|
| `rule_bigram_run`    |                0 — "DEAD" |                     **52** |
| `rule_vowelless`     |                0 — "DEAD" |                  **1,086** |
| `rule_mid_uppercase` |                0 — "DEAD" |               0 (shadowed) |

Two of three came back to life when the corpus grew 3,300×, exactly as
`SWEEP_NOTES.md` predicted when it called them *"retirement candidates, not
retirements"*. That caution is now a measurement, and it is recorded in the
retirement criterion.

### U3 — seven rules score better on gold when removed, and five of them are noise

The gold-scored LOO is R3's answer, and it produces a table that reads like an
invitation to delete rules. It is not one.

| rule                        | ΔmacroF1 (removed) | gold `Clear`-loss 40 → | ≈ gold rows |
|-----------------------------|-------------------:|-----------------------:|------------:|
| `rule_short_line`           |            +0.0068 |           71 (**+31**) |        ≈4.0 |
| `rule_trailing_fill_rescue` |            +0.0022 |                41 (+1) |        ≈1.3 |
| `rule_hard_sweep`           |            +0.0007 |                38 (−2) |        ≈0.4 |
| `rule_inverted`             |            +0.0007 |                 40 (0) |        ≈0.4 |
| `rule_wqx_rot`              |            +0.0004 |                39 (−1) |        ≈0.3 |
| `rule_lowppl_clear`         |            +0.0003 |                 40 (0) |        ≈0.1 |
| `rule_zero_alpha`           |            +0.0001 |                 40 (0) |        ≈0.1 |

**The report carries no significance testing of any kind** — no McNemar, no
effective-n, no p-value. `rule_coverage_report.py` imports no statistics module,
and `evaluate_dataframe`'s `return_correctness` mask, the thing that makes a
paired test possible, is left at its default `False` by both `_loo_metrics`
calls. The repo's exact-McNemar lives in `ab_constant_eval.py:71` and is never
reached from here.

That matters because this issue already learned to read `effective n` before a
macro-F1 delta (08b's own log says so). Calibrating on the same 2,064-row set —
08b moved macro-F1 **+0.0171 over 10 net-corrected rows** — gives roughly
**0.0017 macro-F1 per gold row**. On that scale **five of the seven move less
than a single gold row.** Only `rule_short_line` (≈4 rows) and
`rule_trailing_fill_rescue` (≈1.3) are even potentially real.

The conversion is an average and the true per-row value is class-dependent — a
`Non-text` row (support 45) moves macro-F1 far more than a `Clear` row (support
1,302) — which pushes the sub-0.001 deltas further below one row, not closer to
it. The direction of the error is the safe one.

**Nothing in this table is actionable as a retirement.** The two that clear the
noise floor are candidates for a proper paired A/B with McNemar, which is what
`ab_constant_eval` exists for, and both of them *cost* `Clear` lines when removed.

### U4 — what each rule costs and protects, in gold `Clear` lines

The other half of the same table, and the more useful half, because `Clear`-loss
is the criterion the flag decision has actually turned on for two months.

| rule                   | effect on gold `Clear` |
|------------------------|------------------------|
| `rule_short_line`      | **protects 31**        |
| `rule_reference_floor` | protects 10            |
| `rule_short_garbage`   | **destroys 7**         |
| `rule_hard_sweep`      | destroys 2             |
| `rule_wqx_rot`         | destroys 1             |
| `rule_allcaps`         | destroys 1             |

**`rule_short_garbage` — the rule this entire issue narrowed — is itself
responsible for 7 of the 40 gold `Clear`-losses in the shipped configuration.**
Nobody has had that number before. It reframes the debt: the 40 is not a
background constant the witness is judged against, it is partly the product of
the very rule the witness was built to narrow.

`rule_short_line` protecting 31 is the mirror image, and explains why its
ΔmacroF1 in U3 is the largest of the "remove me" column: removing it buys
macro-F1 by destroying `Clear`.

### U5 — `gold_clear_loss` is an absolute count printed beside a delta

Twelve rules in the delivery report `gold_clear_loss = 40`. That is not twelve
rules each destroying 40 `Clear` lines — it is the LOO arm's absolute count, and
40 is what the arm reads when the rule has no gold effect at all.

The baseline it must be read against **is computed and then discarded**:
`evaluate_dataframe` builds `baseline_vs_gold` from the shipped labels against the
same human labels, and `_loo_metrics` kept only `gold_delta_macro_f1` and
`line_count` from it. So a rule reporting 40 might contribute all 40, or none,
and the output could not tell you which. Every figure in U4 above is inferred by
taking 40 as the modal baseline — defensible, and cross-checked against 08b, but
inference where the tool had the number in hand.

**Fixed as D36**: the baseline is extracted and emitted as
`gold_clear_loss_baseline`, in the JSON and in the table, at zero extra cost —
the pass has already run. The table now prints `gold clear_loss N (baseline B,
±D)`.

Also worth knowing when reading the two side by side: `clear_loss` is measured
over the whole frame and `gold_clear_loss` only over the annotated rows. They are
different populations in adjacent columns.

### U6 — `decisive_cascade` is not the quantity three places say it is

The docstring, the table label (`page-cascade residual`) and `RULE_COVERAGE.md`
all describe `decisive_cascade` as the page-level cascade a rule's removal sets
off. It is not, because **there is no all-rules-on / smoothing-off baseline pass
anywhere in the tool**. `decisive_line` therefore carries the entire footprint of
removing document smoothing, not the rule's share of it, and
`decisive_cascade = decisive_count − decisive_line` is a residual between two
flip counts that are both scored against post-smoothing labels.

The two zero-firing rules make the floor visible: both report `decisive_line =
165,482` and `decisive_cascade = −165,482`, netting to zero. A rule with no
effect cannot set off a cascade, so **165,482 is the smoothing footprint itself**,
sitting as a constant under every other row in the table. Large negative values
are the expected shape for every rule, not a signal about any of them.

Corrected in the docstring, the table label (now `smoothing residual`) and
`RULE_COVERAGE.md` as **D37**. Adding the missing fourth pass would cost another
full recategorize over 12.7M lines and is recorded as an option rather than
proposed.

### U7 — one row of the delivery is already stale, and my first attempt to size it was circular

`d4b9973` landed on `test` at 2026-09-22 08:26 carrying **D33**, which widens
`is_domain_notation()`. Stage 6 ran before it. Its `rule_domain_notation` row —
fire 35,390, `clear_loss` 677, `gold_clear_loss` 41 — is therefore pre-D33.

I tried to put a number on how stale, by comparing the predicate before and after
D33 over the stage-8 witness queue, and got "0 before, 8,082 after". **That
measurement is circular and means nothing**: every line in that queue is there
*because* the pre-D33 `is_domain_notation()` returned `False` for it —
`shape_garbage_clauses()` returns `[]` early on a notation match. A tautology
dressed as a delta, which is the house failure mode of this issue in miniature.

What the number does legitimately say, read the other way round, is that **D33
removes 8,082 lines (808 distinct strings) from the witness-reachable
population**. The corpus-wide effect on `rule_domain_notation`'s own fire count
is **unmeasured** and needs the cluster. It is stage 9e.

### U8 — provenance, so the next run is reproducible

The delivered report carries `decisive_line` / `decisive_cascade`, which only
appear with `--split-cascade`. `run_optim_pipeline.sh` never passes that flag, so
this report was invoked by hand rather than by the driver. Worth recording before
someone tries to reproduce it from the driver and gets a narrower table.

---

## 🔬 Stage 8 read against its own delivery (2026-09-21)

The stage-8 logs and all eleven attached CSVs were pulled down and analysed
directly rather than from the summaries written when they were produced.
Fifteen findings. Five settle a question the stage-8 table asked; one is a
defect in the delivery itself that would have cost @DanaKriv two to four hours
of work on the wrong rows; two overturn findings **this read had already
written down**, for the same reason the delivery's own defect exists.

> **Method note, because it is the point, again.** Nothing below needed the
> cluster, a GPU, or new annotation. Every figure is either quoted from
> `logs8.log` or recomputed from files attached to the thread on 2026-09-21,
> and the ones that are recomputed say so. This is the third consecutive stage
> where reading the delivery rather than the write-up of the delivery changed
> a decision cell — and the first where it also corrected a finding made
> earlier in the same session.

### T1 — the exposure pass and the annotation ask were run with the lexicon OFF

This is the one that matters, and it is the reason stage 9 exists.

`setup/config.txt:199` is explicit about the configuration this feature ships
in: *"AND DO NOT TURN IT ON WITHOUT A LEXICON. These two keys are one
decision."* `uncoupled_witness_warning()` says the same thing on stderr, and
the measured 1:3 ratio behind it is why. The witness will never ship shape-only.

**08f and 08g were run shape-only.** Three independent confirmations:

1. `08f_exposure_full.log` prints its configuration as
   `witness constants: MIN_ALPHA=4 VARIETY_MIN_ALPHA=7 VARIETY_MAX=0.5 TRIPLE_MAX_ALPHA=8`
   — the four shape constants, and no lexicon path.
2. 08b, 08c and 08d each open with the `SHORT_GARBAGE_LEXICON_GEMINATE_MAX_DF`
   scale warning, which `geminate_cap_scale_warning()` emits only when a table
   is configured. 08f does not carry it.
3. **`ppole` is in the queue at all.** At full scale its document frequency is
   229, above the cap of 10, so `_is_geminate_artefact()` returns `False`,
   so `_has_vocabulary_support('ppole')` returns `True`, so the witness cannot
   reach it. A lexicon-on queue cannot contain `ppole` — and `ppole` is the
   single largest row in it, 15,466 lines.

Point 3 is the load-bearing one: it is not an inference from a missing log
line, it is the code.

**What that costs.** Estimated from 08g's own `token_status` column — a string
whose every token is `attested` cannot fire any clause once the table is
configured, because the exemption sits above all four:

| population                                 |   strings |     lines | share of at-risk |
|--------------------------------------------|----------:|----------:|-----------------:|
| at risk as 08f/08g measured it             |     9,717 |    36,744 |             100% |
| exempt once the lexicon is armed           |     2,284 |    26,868 |        **73.1%** |
| **actually at risk in the shipped config** | **7,433** | **9,876** |        **26.9%** |

The whole head of the delivered queue is in the exempt column: `ppole`,
`ARCHAIA` and its variants, `Lepus europaeus`, `Mammalia indet.`, `vodovod`,
`Triticum monococcum`, the boilerplate footer URLs. Every one is attested by
its own document frequency.

**CONFIRMED FROM THE JOB SCRIPT (2026-09-22).** The three points above were
inference from the delivered artefacts. `issue30_stage8_job.sh` settles it
directly, and it is visible in one glance across the stage definitions:

```
stage 08b_witness_flag_ab   'ATRIUM_TEXT_UTILS_SHORT_GARBAGE_LEXICON_PATH="$LEXICON" \ ...
stage 08c_witness_flag_perline 'ATRIUM_TEXT_UTILS_SHORT_GARBAGE_LEXICON_PATH="$LEXICON" \ ...
stage 08f_exposure_full     'python tools/short_garbage_witness_report.py \ ...     <- no ATRIUM_* at all
```

**The gold-scored A/Bs arm the lexicon; the stage that measures the population
does not.** 08g then passes `--lexicon` to `ocr_neighbours.py` for the evidence
columns, but its input is 08f's queue, so the ask inherits the lexicon-off
population regardless. No inference remains: 08b–08e are sound, and 08f/08g
describe a configuration that does not ship.

**The 73.1% itself is still an estimate**, because `token_status` is computed by
`ocr_neighbours.py` with its own tokenisation, not guaranteed identical to
`_split_subtokens()`. That number becomes a measurement when 09b re-runs 08f
with `ATRIUM_TEXT_UTILS_SHORT_GARBAGE_LEXICON_PATH` set — a one-line change to
the stage command.

> ✅ **MEASURED, 2026-09-22 — and the estimate was low.** 09b ran. The exempt
> share is **82.1%**, not 73.1%, leaving **5,563 strings / 6,714 lines** rather
> than the 7,433 / 9,876 projected here. The conclusion this section drew is
> unchanged and the error is one of calibration, not of direction. Kept as
> written, because the estimate being visible next to the measurement is the
> only way the next estimate gets made better. Full reading in
> § "Stage 8 f/g re-run read against its own delivery", V1.

### T2 — the census is the right instrument for the wrong population

`build_annotation_sample.py` is built around a real property of the lexicon-off
queue: it is dominated by a few enormous repeated strings, so a
frequency-ranked census of the head settles most of the exposure in very few
decisions. On that queue, 592 decisions reach 82.8% — and that is correct.

On the population that is actually at risk, the same instrument collapses:

| top N strings | lines settled | share of surviving at-risk exposure |
|--------------:|--------------:|------------------------------------:|
|            10 |         1,349 |                               13.7% |
|           100 |         2,092 |                               21.2% |
|           592 |         3,035 |                           **30.7%** |
|         2,000 |         4,443 |                               45.0% |

The survivors run **1.33 lines per string** and are **93.7% singletons**. There
is no head to take a census of. Stage 9's ask has to be predominantly a
stratified sample, and it will settle proportionally less per decision — which
is not a regression, it is what the real population looks like once the strings
that were never at risk are removed from it.

> ✅ **MEASURED, 2026-09-22 — the shape of the prediction held, the numbers did
> not.** The survivors run **1.17** lines per string and are **95.7%**
> singletons, so the "no head left" call was right and if anything understated.
> The re-cut ask is **157 census + 200 sample**, and the census keeps only 60
> frequency-ranked rows; the other 97 are contested strings. The 30.7% in the
> table above is not comparable to the delivered 18.1%, because they size
> different instruments — see V9, which also separates the 18.1% from the 15.9%
> that can actually change a decision.

### T3 — what the delivered at-risk head was actually made of

> ⚠️ **This is the lexicon-OFF head, and it is now historical.** Every class in
> the table below except "everything else" is exempt once the lexicon is armed.
> For what the rule actually reaches in the shipped configuration — German
> museum vocabulary, foreign place names and structured metadata rather than the
> archive's own words — see V4. The table is kept because it is what the
> delivered 592-row ask was built from.

Recomputed from `08f_witness_distinct.csv`, over the 36,744 at-risk lines as
08f measured them:

| class                                   |  lines | strings | share |
|-----------------------------------------|-------:|--------:|------:|
| `ppole` and its spelling variants       | 15,707 |      23 | 42.7% |
| everything else                         | 14,974 |   9,209 | 40.8% |
| `ARCHAIA` — the excavator's own name    |  3,915 |     196 | 10.7% |
| Latin taxonomy                          |  1,737 |      82 |  4.7% |
| URL / e-mail                            |    411 |     207 |  1.1% |

**Roughly 59% of the queue was an abbreviation, an organisation's name, a
species name, or a web address** — four classes where the text is correctly
transcribed and the only question is whether it is prose. That is the same
population T1 shows the lexicon removes, arrived at from the other direction.

### T4 — the era split, and it is this issue's title

`08f_witness_candidates.csv` carries the document id, and the id carries the
year. Over the 98,917 witnessed lines whose id parses:

|   decade |  witnessed |    at risk | at-risk rate |
|---------:|-----------:|-----------:|-------------:|
|     1950 |      6,444 |      2,493 |        38.7% |
|     1970 |      4,902 |      1,177 |        24.0% |
|     1990 |      5,353 |      1,121 |        20.9% |
|     2000 |     14,593 |      5,508 |        37.7% |
| **2010** | **40,926** | **20,083** |    **49.1%** |
|     2020 |     16,489 |      3,817 |        23.1% |

**65.9% of all at-risk exposure sits in 2010-or-later documents**, and the
2010s carry the highest at-risk rate of any decade. The clause mix inverts
across the same boundary:

| era      | dominant clauses                                                           |
|----------|----------------------------------------------------------------------------|
| pre-2010 | `vowel_run` 76%, `low_variety` 14%, `initial_geminate` 8%, `triple` 2%     |
| 2010+    | **`initial_geminate` 66%**, `vowel_run` 23%, `low_variety` 9%, `triple` 1% |

Of the 2010+ `initial_geminate` at-risk lines, **96.1% are `ppole`**. The issue
is titled *"Algorithm change for 2010+ years documents"*, and this is the most
direct measurement of that population anyone has made in it: the modern half of
the archive is a different document genre — structured excavation forms with
short field values — and the rule that fires hardest on it fires almost
entirely on one legitimate abbreviation.

### T5 — which collection is which, and when `ppole` was born

| probe                | CTX    | MTX    |
|----------------------|-------:|-------:|
| `ppole`              |     11 | 15,662 |
| `http://www.arub.cz` |      0 |  5,309 |
| `Mammalia`           |  1,142 |    159 |
| `Lepus europaeus`    |    902 |     92 |

MTX is ARUB and CTX is ARUP — consistent with 08a's per-collection table, where
`ppole` is 3 ARUP against 226 ARUB. And `ppole` has a date:

| decade | 1990 | 2000 | 2010       | 2020  |
|--------|-----:|-----:|-----------:|------:|
| lines  |    2 |   45 | **13,144** | 2,482 |

It is a convention that arrives with the 2010s forms, in one collection. That
is exactly the profile `_has_vocabulary_support()`'s docstring warns about — a
templated string accruing document frequency like a word — and exactly why the
carve-out for it exists.

### T6 and T7 — two predicate defects, and only one of them was real

**Both were first measured here with no lexicon configured, which is T1's own
mistake.** They are written up together because the correction is the finding.

Against the shipped predicate with `SHORT_GARBAGE_LEXICON_PATH=""`:

```
Lepus europaeus              vowel_run
Mammalia indet.              low_variety
Triticum monococcum          low_variety
http://www.arub.cz           triple
www.archaiabrno.cz           vowel_run
e-mail: mhauer@zip-ops.cz    vowel_run
Poaceae                      — exempt          (the -aceae suffix rule)
```

Read as two predicate gaps: the taxonomy exemption covers family suffixes but
not binomials, and there is no notation shape for a URL. **Re-measured with a
table armed at the real full-collection document frequencies, six of the seven
change answer:**

```
Mammalia indet.              — exempt          mammalia:attested  indet:attested
Triticum monococcum          — exempt          triticum, monococcum attested
Lepus europaeus              — exempt          lepus, europaeus attested
Arvicola terrestris          — exempt
Linum usitatissimum          — exempt
http://www.arub.cz           — exempt          attested by repetition
Kaukasus                     low_variety       unattested — still convicted
```

**T6 is therefore not a defect (D32).** The binomial class is reached by D14's
vocabulary veto once the lexicon is armed, which is the mechanism that exists
for exactly this. A generic "capitalised genus + lowercase epithet" exemption
was drafted and then **measured against the corpus and rejected**: the pattern
matches 1,465 witness-fired strings, and the sample includes `Chenopoaium
hycnaum`, `Loua (oxkuku`, `Ooini hrana osira` and `Laaid. bazic 64999/` — it
would exempt garbage to rescue text the lexicon already rescues. Inventing that
pattern would have been the fifth instance in this issue of widening the
predicate by reading rather than by measuring, and the predicate's own comment
block names the previous four.

**T7 is a defect, and a narrow one (D33, fixed).** The boilerplate URLs are
attested by repetition — `http://www.arub.cz` appears on 5,309 lines of page
footer. What a document-frequency table can *never* attest is a citation quoted
once, and those survive a configured lexicon:

```
(zdroj: http://www.turistika.cz/mista/krakovec-mistni-cast-laskova).
1 https://www.mza.cz/indikacniskici/skica/detail/1669
roku 1820 (http://www.hrady.cz/index.php?OID=1291).
e-mail: officeauappmost.cz          <- OCR cost the `@`; the label survives
```

144 such lines in the stage-8 at-risk population, none appearing more than
three times. `is_domain_notation()` now recognises the shape: **0 misses on the
144, 0 false positives against the other 7,289 at-risk survivors**, and 0
against the predicate's own documented garbage fixtures. It is `.search()`
rather than an anchored `.match()` — the only shape in that predicate that is —
because a citation sits inside a sentence; the comment above it says so and
says why that is safe here and nowhere else.

### T8 — the class that neither mechanism reaches, measured and left alone (D34)

`Kaukasus`, `Hallstatthaus`, `Schuhleistenkeilbruchstueck`. Real words, all
convicted by `low_variety`, all **unattested** at full scale because they are
rare across *documents* — and `issue30_annotation_guide.md` already uses two of
them as its canonical "recoverability zero is not evidence of garbage" example.

The obvious fix is a length cap, mirroring
`SHORT_GARBAGE_WITNESS_TRIPLE_MAX_ALPHA`. **Measured on the delivered queue, it
does not separate the classes.** Per convicting word, at the length the clause
actually fires on:

| length | real words                       | garbage at the same length and ratio |
|-------:|----------------------------------|--------------------------------------|
|      7 | `vodovod` (0.43)                 | `PSSPPOP` (0.43)                     |
|      8 | `Mammalia`, `Kaukasus` (0.50)    | `AltSattl.` (0.50)                   |
|      9 | `Vertreter` (0.44)               | `STRATSTAR`, `RARRRPRIR` (0.44)      |

Case shape does not separate them either — `VODOVOD` (real) and `PSSPPOP`
(garbage) are both ALLCAPS at ratio 0.43. **Not retuned, deliberately.**
Fitting `SHORT_GARBAGE_WITNESS_VARIETY_MAX` or `_MIN_ALPHA` to two named words
is what D25 and D30 already did with the geminate cap, and what stage 07a then
refuted at scale. This is named, accepted debt with a test pinning it, in the
same form as the geminate guard's remaining gap.

### T9 — `Trash` is being asked to mean two different things

> ✅ **RESOLVED 2026-09-22, in favour of legibility (W4).** @david-spacil:
> *"`Trash` = illegible. Anything legible is `Clear`, easily decipherable is
> `Noisy` – regardless of how useful the line is to us."* Pending @DanaKriv's
> agreement, since he framed it as his view. Consequences in W4; the section
> below is kept because the question it poses is the one that got answered.

Under the definitions this project works to — `Clear` is correct text, `Noisy`
is damaged but recoverable by a reader, `Trash` is *nobody can read this* —
the pipeline currently labels as `Trash`:

* `http://www.arub.cz`, 5,309 lines. Correctly transcribed. Perfectly legible.
* `e-mail: mhauer@zip-ops.cz`, 181 of its 241 lines.
* `ARCHAIA Brno o.p.s.`, 316 lines — while labelling `Archaia Brno o.p.s.`
  `Noisy` and `ARCHAIA` `Clear`.

**76.9% of witnessed `Trash` lines carry no repeated-character run and no
unusual glyph at all** — 49,256 of 64,080. Whatever put them in `Trash`, it was
not illegibility.

This is not a bug with a patch. It is that two different questions are being
answered by one label: *can a human read this* (legibility) and *is this
running text worth indexing* (usefulness). A URL, a form label, an inventory
code and a species name are all perfectly legible and none of them are prose.
The five-way scheme already has a place for the second question — `Non-text` —
and it is not being used for it. **Whether that matters is @david-spacil's and
@DanaKriv's call, not this issue's**, but it has to be stated before annotation
starts, because an annotator applying the definitions as written will label
`http://www.arub.cz` as `Clear`, the metric will move, and nobody will know
whether that was the annotator or the definition.

### T10 — what the vocabulary veto actually destroyed (08d)

08d rejects `QUALITY_VOCABULARY_ENABLE` on the numbers: 540 breaks against 212
fixes, `Clear`-loss 40 → 180. The *shape* of those 540 is the part worth
keeping.

The expected story was a Czech dictionary convicting the archive's German. It
is not: **3% of the breaks carry German markers, 55% carry Czech ones**, and
43% carry no language marker at all. A sample of what it destroys:

```
Bezmírov /s.o. Kroměříž/.            Málek, J., 1966: Vegetační vymezení ...
Věc : Stará Kouřim / Kolín/          větší soubor z eneol.výš.sídliště - publ. ...
Čtverec: 13/14 Sonda: - Jiné: -      NASEZN. 1I/650/7086
sonda/kontext: JZ                    V Hradci Králové, 27.111.1974
J-IXb                                Dra I.L. Červinky.
```

Site names with administrative qualifiers, form fields, bibliographic
citations, inventory codes, dates, personal names. Median break: 3 words, 21
characters. **The archive's dominant register is not prose — it is abbreviated,
coded, proper-noun-heavy field documentation, and a test that asks "is this
word in a dictionary?" convicts the archive's most characteristic content.**
That is a corpus fact, and it outlives this particular flag.

### T11 — the recoverability column is weaker than its name

From `08g_annotation_ask.log`'s own warning: `--strong-df 10` is 0.0088% of
113,100 documents and admits 158,503 correction targets, *"a thin target is
weak evidence that some OTHER string is a damaged copy of it — the
`Linum`/`ilium` failure, scaled"*. The log recommends `--strong-df 113` and
declines to change it silently, correctly.

It was run at 10. And of the 13,972 rows that got a suggestion, **12,511 (90%)
rest on `edit1`** — the weakest method, the one the annotation pack's own
documentation says "may be coincidence":

> 🔁 **The re-run was also made at `--strong-df 10`** — 09c's recommendation to
> raise it to 113 was not carried out, and 08g's log repeats the warning
> verbatim. On the smaller queue 9,689 of 42,853 rows get a suggestion. This
> still affects what the annotator reads, not which rows were chosen. **One
> thing softens the criticism**: V4 shows the column is bimodal rather than
> uninformative (48.8% score 0.00, 24.8% score 1.00), and it is right about both
> `Dauerleihe` (0.00, a word the table has genuinely never seen) and
> `J. Vysoean` (1.00, genuinely a damaged copy of a real name).

| method          | rows   |
|-----------------|-------:|
| `edit1`         | 12,511 |
| `confusion`     |    826 |
| `diacritics`    |    488 |
| `stronger-twin` |    436 |

Stage 9 re-runs this at 113. Until then `recoverability` in the delivered
census and sample should be read as a hint, not evidence — and its asymmetry
still holds in the other direction: `Dauerleihe` and `radioulna` score 0.00 and
are real words.

### T12 — document frequency punishes text that repeats inside one document

`Dauerleihe` appears on 267 lines and is unattested. `Hallstatthaus`, 58 lines,
unattested. The veto counts *documents*, not lines, for a good reason — but the
consequence is that a term concentrated in a handful of documents looks exactly
like a scanning error to it, however often it occurs there. Register entries,
museum loan records and form labels all have that profile. It is the mirror of
the `ppole` problem: that one is a templated artefact accruing document
frequency like a word, this one is a word that cannot accrue any.

### T13 — the same string, answered three ways, inside one document

`Mammalia indet.` appears **416 times in one document** and is stored
`Trash:209 | Noisy:207`. Identical text, same page range, near coin-flip. And
across the corpus the excavator's own name is answered three ways depending on
how it was typeset:

| string                    | stored                              |
|---------------------------|-------------------------------------|
| `ARCHAIA`                 | `Clear:1008 ; Trash:116 ; Noisy:52` |
| `ARCHAIA Brno o.p.s.`     | `Noisy:680 ; Trash:441`             |
| `Archaia Brno o.p.s.`     | `Noisy:359`                         |
| `ARCHAIA Olomouc, o.p.s.` | `Noisy:183 ; Trash:6`               |

This is what the census's `variants` column exists to collapse, and it is the
strongest argument for asking about strings rather than lines.

### T14 — the tie-break is alphabetical, and 180 groups depend on it

Confirmed corpus-wide by 08f, where S4 had it on the 5,222-group queue: of
61,682 (document, string) groups, 180 are ties, and **none of them can land on
`Trash`** because `apply_document_postprocessing()` resolves with `x.mode()[0]`,
`mode()` returns tied values sorted, and `Clear` < `Noisy` < `Trash`. Renaming
a category, or sorting that result differently, would silently flip 180 groups
to `Trash`. It is load-bearing and it is an accident of the alphabet.

> 🔁 **Re-measured with the lexicon armed (V6): 17 ties over 40 lines**, of
> 43,103 groups. The mechanism and the risk are unchanged; the exposure is an
> order of magnitude smaller than this section states, because most of what was
> being deduplicated was vocabulary the witness can no longer reach.

### T15 — the sample cannot be projected by multiplying

`sampling_weight` is a **string** weight — 3,252 strings / 80 sampled = 40.65.
Applying it to `lines_settled` gives 10,051 lines against the frame's own
9,906. The README already states the correct method (stratum estimates
recombined by `lines`, not by `strings`); this is a note that the obvious
shortcut is wrong by about 1.5% and should not be used as a check.

> 🔁 **Still true on the re-cut frame, with new numbers (V10):** weights are
> 27.2917 / 27.2245 / 27.1667, and `Σ(lines_settled × weight)` = 5,717.3
> against the frame's 5,646 — wrong by 1.3% now rather than 1.5%, because the
> three strata are much closer in lines per string than they were.

### The corpus-wide dedup denominator — 08f answers H6

S4 sized the modal-dedup blast radius inside the 20,078-line witness queue and
found 22 contested groups. 08f is the corpus-wide version H6 needed, over both
archives:

| unit                                         |                                  count |
|----------------------------------------------|---------------------------------------:|
| (document, string) groups                    |                                 61,682 |
| unanimous                                    |                                 61,359 |
| strict majority                              |                                    143 |
| tie                                          |                                    180 |
| vote lands on `Trash`, carrying `Clear` down |       16 groups / **24** `Clear` lines |
| vote lands off `Trash`, lifting `Trash` out  |     244 groups / **305** `Trash` lines |
| **net**                                      | **+281 lines in the cascade's favour** |
| groups H6 option 2 would change              |                                  **0** |

At 12× the group count, S4's conclusion strengthens rather than weakens: the
cascade is a net rescuer, by a wider margin than on the small queue (where it
was 31 rescued against 17 destroyed), and the rule proposed to curb it has a
population of zero. **H6 option 2 can be retired on a corpus-wide denominator**,
which is what it was always waiting for. Option 1 — accept — is what the
measurement supports. The choice remains @david-spacil's.

### 08a — the last portable de-gemination signal is dead

D30 left one candidate standing: an artefact belongs to the scanning run that
produced it, an abbreviation is a convention and should appear in both
collections, so per-collection *concentration* might separate them where global
document frequency no longer does. 08a measured it:

| token       |    ARUP |    ARUB | what it is                       |
|-------------|--------:|--------:|----------------------------------|
| **`ppole`** |   **3** | **226** | **abbreviation** — ARUB's forms  |
| **`ssuti`** | **152** |  **43** | **old spelling of *suť*** — ARÚP |
| **`ssutí`** | **116** |  **26** | **old spelling of *suť*** — ARÚP |
| **`ssutě`** |  **46** |  **18** | **old spelling of *suť*** — ARÚP |
| `llocm`     |      44 |      11 | artefact                         |
| `vvkop`     |       1 |      29 | artefact                         |
| `jjámy`     |       2 |       8 | artefact                         |
| `oobjekt`   |       2 |       2 | artefact                         |

> 🛑 **Four rows corrected 2026-09-22 (W2).** `ssuti`/`ssutí`/`ssutě` were
> recorded here as artefacts on this document's own reading. @david-spacil:
> they are **an old way of spelling *suť***. The split is four legitimate
> against four damaged, not one against seven.

`ppole` — the one confirmed abbreviation, confirmed by @david-spacil — is the
**most** collection-concentrated token of the eight. Under the proposed test it
looks more like an artefact than any actual artefact does. The hypothesis is
not weakened, it is inverted, and T5 explains why: `ppole` is a convention of
*one institution's* 2010s forms, so it is concentrated by institution exactly
as a scanner artefact would be.

**Nothing separates these two classes on frequency evidence.** D25's guard has
no portable signal left, and the honest options are now to accept it as fitted
to one table, or to remove it (`SHORT_GARBAGE_LEXICON_GEMINATE_MAX_DF = 0`) and
fall back to the plain attestation veto. That is a decision for
@david-spacil — ✅ **answered 2026-09-22: `ssuti` is not a scanning error at all**
(W2). The passage below is kept as written. It turns on whether `ssuti` at 195 documents is still a scanning
error at this scale, which only a reader of the archive can say.

---

## 🔬 Stage 7 read against its own delivery (2026-09-20)

The stage-7 logs and all thirteen attached CSVs were pulled down and analysed
directly rather than from the summaries written when they were produced. Five
findings; three overturn a claim this document makes above, one closes a
long-running argument, and one is a defect in the tree.

> **Method note, because it is the point.** Nothing below needed the cluster, a
> GPU, or new annotation. Every figure is recomputed from files that have been
> attached to the thread since 2026-09-19. Four of the six instrument-level
> errors this issue has logged would have been caught by reading the delivery
> instead of the write-up of the delivery, and these five are the same lesson a
> seventh time.

### S1 — D25 does not hold at full scale. The run that was meant to confirm it refutes it

§"Measured against gold, stage 7" above says *"D25 (the geminate cap) holds at
full scale"* and *"`ppole` still sits at the lowest ratio of the eight"*. The
delivered `07a_geminate_lookup.log` says otherwise, and the same sentence that
makes the claim contains the counterexample — it gives the artefact range as
starting at **7.6×**, below `ppole`'s **37.6×**.

Read straight off the log, over 113,100 documents / 2,764,632 tokens:

| token                | own df  |  base df |     ratio | what it is                  |
|----------------------|--------:|---------:|----------:|-----------------------------|
| **`ppole`**          | **229** |    8,600 |  **37.6** | **abbreviation**            |
| **`ssuti`**          | **195** |    1,667 |   **8.5** | **old spelling of *suť***   |
| **`ssutí`**          | **142** |    1,617 |  **11.4** | **old spelling of *suť***   |
| **`ssutě`**          |  **64** |      900 |  **14.1** | **old spelling of *suť***   |
| `llocm`              |      55 |      417 |       7.6 | confirmed artefact          |
| `vvkop`              |      30 |    1,400 |      46.7 | confirmed artefact          |
| `jjámy`              |      10 |   14,799 |   1,479.9 | confirmed artefact          |
| `oobjekt`            |       — |        — |         — | no longer attested          |

> 🛑 **Four rows corrected 2026-09-22 (W2).** And the correction sharpens the
> finding rather than weakening it: the "empty gap from 2.60× to 4.66×" that
> `SHORT_GARBAGE_LEXICON_GEMINATE_MAX_DF` is fitted to now has **real language on
> both sides of it**. It does not separate damage from vocabulary at all — it
> separates doubled-letter *shapes* from everything else.

* **By ratio there is no separation at all.** `ppole` is fifth of seven, with
  four confirmed artefacts *below* it. This was already known — it is why D25
  exists — but the log was read as though the ratio still ordered them.
* **By document frequency the separation has collapsed.** On the 822-document
  table `ppole` was 35 against artefacts at 8 or below: a **4.4× gap**, and
  `SHORT_GARBAGE_LEXICON_GEMINATE_MAX_DF = 10` sits inside it. At full scale it
  is 229 against 195 — **1.17×**. A threshold placed there is fitted to one true
  positive, not read off a gap.
* **And the mis-calibration is live, not theoretical.** The constant is an
  *absolute document count*, so it does not rescale when
  `SHORT_GARBAGE_LEXICON_PATH` is pointed at the bigger table — which is exactly
  what stage 7 did. At the shipped `10`, on the full-collection table, the guard
  is right on **2 of 7** tokens instead of 7 of 7: every artefact except `jjámy`
  clears the cap and keeps a vocabulary exemption it should not have.

Also in that sentence: *"down from the 822-document table's 4.7× reading"*. 4.7
to 37.6 is up.

**Not retuned.** Eight tokens, seven of them labelled from one reading, is not a
population to fit a production threshold to, and picking ~200 off this table
would be the fourth time in this issue a constant was set from the data meant to
test it. What landed instead: `text_util.geminate_cap_scale_warning()`, which
says this on stderr whenever a table more than 4× the calibration corpus is
configured; the corrected figures in `_is_geminate_artefact`'s comment and in
`setup/config.txt`; and a test that pins both tables so the claim cannot drift
back. **The separating signal is per-collection concentration, not a global
count** — an artefact belongs to the scanning run that produced it, an
abbreviation does not — and `build_token_lexicon.py` already emits
per-collection columns. That is stage 8c, an experiment rather than an edit.

### S2 — 07b is void, and it is D27's bug wearing different clothes

§"Measured against gold, stage 7" reads 07b's null as *"a fact about the
measuring stick, not the fix"* — the gold set does not reach D26's population —
and explicitly distinguishes it from 07c: *"This is a different bug from D26's —
D26's null result stands on a coverage argument that still holds."*

It does not. Two flags, 07b and 07c, produced **byte-identical tables** on the
same 2,064 rows: same 513 errors, same 40 `Clear`-loss, same 0.2917 cost, same
`KL` to five decimals, 0 discordant rows. Two unrelated constants agreeing to
five decimal places is not two null results; it is the signature of neither flag
being armed.

`quality_word_set()` (`text_util.py`) is decorated
`@functools.lru_cache(maxsize=1)` and **takes no arguments**. It reads
`QUALITY_VOCABULARY_ENABLE` from module scope on its first call and answers from
the cache forever after. `tools/ab_constant_eval.py` runs both arms **in one
process**, reference value first, so the `False` arm caches `None` and the
`True` arm is handed the same `None` back. Demonstrated directly:

```
arm False -> quality_word_set() = None
arm True  -> quality_word_set() = None       <- the 07b result
arms identical?  True

with the cache cleared between arms:
arm False -> None
arm True  -> frozenset(...)                  <- what 07b was supposed to measure

compute_valid_ratio('oueussd edelite sektlll', None)       = 1.00
compute_valid_ratio('oueussd edelite sektlll', <word set>) = 0.00
```

`_DERIVED_FROM_FLAG` fixed *one instance* of "a flag's value gets frozen", not
the class: a module constant built at import. A zero-argument `lru_cache` is the
second instance and it was already in the tree. **Fixed** by `_CACHES_FROM_FLAG`
in `override_constants()`, clearing on entry and on exit, plus a source-level
guard test that fails if a new zero-argument cache appears unregistered —
`_read_token_lexicon` and `_compile_vowel_run` need no entry, being keyed on
their arguments.

**07b's "zero effect on gold" is void, not confirmed.** D26's coverage argument
may well still be true; it has not been tested. Stage 8b is the re-run.

### S3 — the stage-7 annotation pack is not a second population

§"What is needed" calls the 07f pack *"a different and larger population"* and
*"not more of the same queue"*, and H2 in the plan says the same. Joined against
`04_witness_distinct.csv`:

| tab                               |      rows |      lines | in the witness queue |
|-----------------------------------|----------:|-----------:|---------------------:|
| `A_discarded_but_recoverable.csv` |       690 |      1,791 |           690 (100%) |
| `B_clear_but_unrecoverable.csv`   |       615 |      1,234 |           615 (100%) |
| `C_clear_and_recoverable.csv`     |       969 |     13,807 |           969 (100%) |
| `D_trash_and_unrecoverable.csv`   |       150 |        803 |           150 (100%) |
| **total**                         | **2,424** | **17,635** |     **2,424 (100%)** |

`07f_annotation_pack.log` says it plainly — *"wrote 5,005 rows to
07f_distinct_evidence.csv"*, then the four tabs are cut from those 5,005. The
pack is **2,424 of the same 5,005 strings and 17,635 of the same 20,078 lines**,
re-partitioned by recoverability with tab D down-sampled. It is a better *view*
of the queue; it is not a second ask, and presenting it as one has been asking
@DanaKriv to do the same work twice.

The recoverability cross-tab in §"the full-collection lexicon" has the same
scope problem. Its figures reproduce exactly from `07f_distinct_evidence.csv` —
743 strings / 1,791 lines `Trash`-and-fully-recoverable, 415 / 1,007
`Clear`-and-zero-recoverable — so the table is right, but it is a measurement
over **the 20,078-line witness candidate queue**, not over the corpus. *"This is
real content being thrown away"* is a true statement about lines the shape
witness flags. Whether it generalises to the other 71.8M lines is unmeasured,
and that is exactly the "what population is this a delta of?" error this
document has already corrected twice.

### S4 — the dedup blast radius has a size, and it is 22 lines

§"Round 2" argues that **per-line precision does not bound `Clear`-loss**,
because the modal dedup rewrites every row to the modal category of its
identical text within a document, so *"convicting a few occurrences of a
repeated string can flip the majority and carry a correct occurrence with it"*.
It gives the mechanism, the 5,222 (document, string) votes, and the observation
that 44 groups of ≥20 lines cover 69.9% of the queue. It never measures how many
of those votes are actually *contested*. Computed from
`04_witness_candidates.csv`:

| unit                                                              |              count |
|-------------------------------------------------------------------|-------------------:|
| lines                                                             |             20,078 |
| (document, string) groups — what the dedup votes on               |              5,222 |
| of those, **unanimous** in their stored category                  |              5,200 |
| of those, contested                                               | **22** (755 lines) |
| of the contested, decided by a **strict majority**                |                 15 |
| of the contested, decided by a **tie**                            |                  7 |
| of the contested, decided by a **bare plurality** — H5's question |              **0** |

4,903 of the 5,222 groups are a single line. And the witness is a pure function
of the line's text, so within a group it convicts **all or none** — it cannot
create a split, only move a group that is already unanimous, wholesale.

**And the ties cannot demote either, for a reason nothing in production says out
loud.** `apply_document_postprocessing()` resolves a group with `x.mode()[0]`;
`pandas.Series.mode()` returns its tied values **sorted**, so `[0]` is the
alphabetically first — and `Clear` < `Noisy` < `Trash`. A tie therefore never lands on
`Trash`. An accident of the alphabet, load-bearing, and it makes the entire class H5
asks about impossible.

**So option 2 — "stop a bare plurality demoting `Clear` → `Trash`" — has a population of
exactly zero here.** What the cascade actually does on this queue:

|                                                       | groups |                           lines |
|-------------------------------------------------------|-------:|--------------------------------:|
| vote lands on `Trash`, carrying a `Clear` member down |  **4** |                          **17** |
| vote lands off `Trash`, lifting a `Trash` member out  | **17** |                          **31** |
| **net**                                               |        | **+14 in the cascade's favour** |

The mechanism is real; on this population it is a net rescuer, and the rule proposed to
curb it would change nothing.

**The delivered discordant files point the same way, and it is the opposite of
how this document reads them.** Compare the two runs directly:

| run                   | fixes | breaks | the breaks                                                                      |
|-----------------------|------:|-------:|---------------------------------------------------------------------------------|
| 05a — cascade **on**  |    14 |  **1** | `Lokolieace: •VIII,`                                                            |
| 05f — cascade **off** |    14 |  **3** | the above, plus `"Frauenzimmerbad", sämtlic…` and `S-VIIIb` — both gold `Clear` |

🛑 *Confounded, 2026-09-23 (D45): 05a ran with the round-2 guards and 05f without them — `cc4990e`
deleted both in between — and 05f's two extra breaks are exactly the lines the guards vetoed. So the
table does not show the cascade removing the witness's errors. The cascade-as-precondition
conclusion stands on same-tree evidence instead (08c, 10f).*

The cascade does not add the witness's errors, it **removes two of them**, and
it removes 15 from the baseline as well (`Clear`-loss 55 → 40). On this gold set
`apply_document_postprocessing()` is protective. **Option 2 of H8 — restricting
a bare plurality from demoting `Clear` → `Trash` — is therefore a production-wide
change motivated by 22 local lines, in a direction the measurement argues
against.** That is a recommendation, not a decision: H8 is still @david-spacil's,
and it is now costed on both sides.

### S5 — 07d says the adoption gate passes

`07d_vowelrun_rerun.log`, both arms:

```
3.0: macro_f1=0.6344 (+0.0170 vs shipped)  errors=503  Clear-loss=40  cost=0.2829  -> ADOPT-CANDIDATE
4.0: macro_f1=0.6324 (+0.0151 vs shipped)  errors=503  Clear-loss=40  cost=0.2829  -> ADOPT-CANDIDATE (NOT SIGNIFICANT: n=4, p=1)
```

Against shipped (513 errors / 40 `Clear`-loss / 0.2917): errors down 10, cost
down, **`Clear`-loss unchanged**. The gate D24 installed — *"raises neither total
errors, operational cost, nor `Clear`-loss"* — is satisfied, and the tool says so
in its own words. Every account above still reads the flag as rejected on
`Clear`-loss +1.

Two caveats, and they are why this is a run to schedule rather than a result to
quote:

1. **07d's base configuration is not printed.** It is inferred to be witness-on
   with a lexicon configured, because `SHORT_GARBAGE_WITNESS_VOWEL_RUN_MIN` is
   the constant under test and the `Trash` recall (34/180) matches the veto arms
   rather than 05a's 36/180. An inference, not a log line.
2. **07d is not an A/B over the witness flag.** Both its arms are witness-on, so
   there is no paired McNemar against flag-off in that run, and the `-> ADOPT-CANDIDATE`
   verdict is against the *stored* labels.

**Stage 8a is the run that settles it**, and it also settles the drift: one
`ab_constant_eval` over `SHORT_GARBAGE_WITNESS_ENABLE` with `--values false,true`,
in the stage-7 tree, from one output directory, with a `--no-postprocessing`
companion. If it reproduces `Clear`-loss 40 the flag decision is made on
evidence; if it reproduces 41 or 42, the drift is the finding and the flag stays
off until it is explained. Either outcome is worth more than the current state,
and neither needs annotation.

---

## ⚠️ Revision of 2026-09-17 — three findings, one of them about this document

Work on `claude/practical-davinci-phgjbk` ([`11c6335`](https://github.com/ufal/atrium-alto-postprocess/commit/11c6335)). Suite 1120 → 1175 passing, 0 failed; flag-off re-score changes 0 categories; `ruff` clean.

| #      | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | What it supersedes                                                                                                                                                                                                                                                                  |
|--------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **R1** | **The loan false-positive class was measured, and it fails.** The `vowel_run` clause convicts the Latin botanical **family suffix** `-aceae` wholesale — 10 of 10: `Poaceae`, `Rosaceae`, `Fabaceae`, `Brassicaceae`, `Cyperaceae`, `Chenopodiaceae`, `Asteraceae`, `Betulaceae`, `Fagaceae`, `Polygonaceae` — plus `Naiade`, `Beuern`, `Oueste`. Not an occasional error on a rare spelling: the naming convention of an entire taxonomic rank, in a corpus of archaeobotany and osteology reports. A taxonomic-suffix exemption landed above all four clauses, at **zero cost in convictions** | §5 *"Most carry `weird_ratio == 0.0` and so never reach gate 6 at all … Measuring this class is a precondition for enabling the flag, not a follow-up."* The margin was real and, as that sentence itself warned, **thin** — it rested on a signal the predicate does not own. §3.7 |
| **R2** | **The blocker was not real.** Four places in this repository recorded the remaining work as blocked because *"the gold is here; the rows it labels are not"*. All 2,067 gold rows across all 816 gold documents are in the cluster corpus. **Confirmed by the join on 2026-09-17: 2,064 of 2,067 keys matched (99.9%), 0 documents absent, 3 locator-absent**                                                                                                                                                                                                                                    | the header note above; "What actually remains" items 1 and 2; [`../plans/30.plan.md`](../plans/30.plan.md) risks and checklist; `tools/gold/GOLD.md`                                                                                                                                |
| **R3** | **The instrument that classifies the rules was scoring against itself.** Of the five drivers calling `evaluate_dataframe`, `rule_coverage_report._loo_metrics` was the only one that never passed `gold_category_column` — while accepting `--gold-sidecar`, joining it, and printing a reassuring "N labels matched" on the way past. Every `DEAD` / `REDUNDANT-HERE` / `LOAD-BEARING` / `clear_loss` verdict this issue has produced, including the 23-rule sweep posted to the thread, was agreement-with-self, whose baseline is zero by construction. No test covered it                    | §7 *"Every offline tool in `tools/` grades against the pipeline's own stored `categ`"* — which was true of one tool and false of four, and the one it was true of is the one `RULE_COVERAGE.md` cites as the retirement criterion                                                   |

Two consequences worth stating separately, because both are the kind of thing that
looks like a successful run:

* **The fix for R3 was not a forwarded keyword.** With a gold column, `flip_count`
  counts *disagreements with gold*, not lines the rule moved, and the baseline is
  no longer zero — so forwarding it would have silently redefined `decisive_count`
  into a different quantity under the same name. The structural figures are
  unchanged; `gold_delta_macro_f1` is added beside them.
* **A corpus-level guard was being applied per document.** `attach_gold_sidecar`'s
  zero-match raise asks "wrong batch, or the keys do not correspond" — a question
  about a corpus. Asked per document it is almost always answered wrongly: 2,067
  rows over 816 of 822 documents is ~2.5 rows each. It took an 822-document
  cluster run down on file 1, a synthetic sample document that sorts first and has
  no gold by construction. Fixed, and the zero check now happens once, on the
  total.

**Also landed:** D14, the lexical signal, built from the corpus's own token
document-frequency rather than an external dictionary (§6); the vowel-run length
decoupled from `FUSED_VOWEL_RUN_MIN`; `--split-cascade` and `gate_marker` in the
coverage report; `ab_constant_eval --values` parsing booleans, without which the
tool written for "measure it against gold before flipping it" could not express
either of the flags that instruction is about; and `load_csvs` skipping CSVs with
no `text` column, which is the gold-sidecar footgun `GOLD.md` could previously
only warn about.

## 📐 Measured on the cluster — runbook stages 1–4 (2026-09-17)

Stages 1–4 ran on the 822-document corpus; stage 5 was still in flight when this was written, so nothing below depends on it (it has since landed — see the stage 5 block that follows). These figures **supersede the fixture-level readings** in the revision block above wherever the two disagree, and they disagree twice.

**The blocker is dead at line level, not just by listing.** Pre-flight: **2,064 of 2,067 gold keys matched (99.9%)**, **0 documents absent**, 3 locator-absent (`CTX198401467A:184:42`, `MTX200300587:1517:7`, `MTX200500295:47:354`). `calibration_1567` 1,559/1,559; `issue30_508` 505/508. R2 is now confirmed by the join itself.

**The witness's corpus profile is nothing like its fixture profile.** 20,324 lines witnessed of 1,480,119 in scope (1.4%). By clause:

| clause             |  fires | currently `Clear` | currently `Trash` |
|--------------------|-------:|------------------:|------------------:|
| `initial_geminate` | 12,685 |  **11,872 (94%)** |               686 |
| `vowel_run`        |  5,417 |             1,458 |             3,599 |
| `triple`           |  2,157 |               670 |             1,390 |
| `low_variety`      |  2,260 |               735 |             1,323 |

Three clauses behave as designed — they mostly re-confirm lines already in `Trash`. `initial_geminate` inverts that, and **`ppole` alone is 11,562 of its 11,872 `Clear` hits, across 25 documents**: one doubled-`p` form label, 57% of the entire exposure. Behind 20,324 lines there are **5,243** distinct strings, 94% of them hapax. That inverted profile was first read as the clause being unreliable, then as the clause being right; **the first reading was correct** — see the 2026-09-19 correction. `ppole` is an abbreviation and `initial_geminate` convicts it wrongly. **And it convicts more than `ppole` wrongly (W2, 2026-09-22): `ssuti`/`ssutí`/`ssutě` are an old spelling of *suť*, so three more of the clause's eight known targets are real language too.**

> **Any exposure figure from this corpus must be de-duplicated before it is read.** Archival tables repeat one string thousands of times, and `apply_document_postprocessing`'s dedup pass settles such a string once per document anyway. This is a general caution, not a `ppole` anecdote.

**The vocabulary veto cuts exposure by 85% — and that number turned out not to mean what this section said it meant. Read the correction that follows the table before quoting it.**

|                     | confirms existing `Trash` | new convictions on `Clear`/`Noisy` | ratio               |
|---------------------|--------------------------:|-----------------------------------:|---------------------|
| witness alone       |                     5,107 |                         **15,217** | 1 : 3.0 **against** |
| + veto, `min_df` 2  |                     3,757 |                              1,987 | 1.9 : 1 for         |
| + veto, `min_df` 3  |                     3,905 |                              2,306 | **1.7 : 1 for**     |
| + veto, `min_df` 10 |                     4,874 |                              2,882 | 1.7 : 1 for         |

An 85% cut in false-positive exposure for 24% of the confirmations, flat across thresholds.

> ### ⚠️ Correction (2026-09-17, later the same day). That table does not say what it was read as saying.
>
> It was read here as *"the veto is the difference between arming the witness and not"*. Two measurements against the queue itself say otherwise.
>
> **1. 90.5% of the veto's effect is one token.** `ppole` is 11,690 of the 20,324 witnessed lines (57.5%) and 11,685 of the 15,217 supposed false positives (76.8%); the veto removes 12,911 convictions in total, and 11,685 of them are `ppole`. ~~It is an OCR doubling of Czech `pole` in the pre-printed form label `KULTURA: ppole`, it fires `initial_geminate`, and **the clause is right about all 11,690 of them**. The veto was suppressing a correct conviction at scale.~~ **WRONG — see the 2026-09-19 correction below.** It fires `initial_geminate`, and the clause is wrong about it: `ppole` is an *abbreviation*, and the veto was protecting real vocabulary.
>
> **2. The premise under `_has_vocabulary_support` is false for templated documents.** Its docstring justifies document frequency on the grounds that *"OCR noise is idiosyncratic to the scan that produced it"*. For a pre-printed form scanned across the collection the misread recurs once per document and accrues df exactly like a word. **The class is real — `oobjekt` (df 3 vs `objekt` 378), `jjámy` (5 vs 356), `ssuti`, `ssutí`, `ssutě`, `vvkop`, `llocm`, all confirmed as scanning artefacts by @david-spacil — but `ppole` is not in it**, and using it as the headline example was the error corrected below.
>
> **3. And the ratio is scored against `categ`, so it could not have settled the flag either way.** The report prints the warning itself — *"NOT an error rate — `categ` is the pipeline's answer, not gold"* — and it was quoted as a verdict regardless. It cannot separate *the witness is wrong* from *the pipeline is wrong* — and on `ppole` the answer turned out to be the first, 11,562 times, which is the opposite of what was written here. **This is the same circularity the sweep objective already had**, named two paragraphs above in this very file. Supporting sign: 24 of the 298 repeated strings carry inconsistent categories (`Linum usitatissimum` 22 `Clear` / 40 `Trash`, `sektlll` 539 / 4).
>
> **Fixed** by `_is_geminate_artefact` (`SHORT_GARBAGE_LEXICON_GEMINATE_RATIO`, default 4.0): an attested doubled-initial token whose de-geminated form is ≥4× more frequent does not count as vocabulary. On the real table the populations do not overlap — 8 tokens at ≥4×, all templated artefacts; 36 below 2×, all Roman numerals (`xxiii` vs `xiii` are different numerals) and character runs. Veto-only: it withdraws an exemption, it can never convict.
>
> **What survives unchanged:** the *coupling* decision below (a veto that moves ~12.9k lines either way must not be armed blind), and the veto's real work on Latin binomials — `Triticum monococcum` (121 lines) and `Lepus europaeus` (86) are `Clear` taxonomy that shape convicts and attestation rescues.
>
> Logged as the **third** inversion this round, after `-aceae` and the fixture reading of the veto — and the first where the fault was the *metric* rather than the intuition. §6 measured the veto on 31 fixtures and called it a marginal trade; the corpus said load-bearing; the corpus was being scored against itself.

**Decision: `SHORT_GARBAGE_WITNESS_ENABLE` and `SHORT_GARBAGE_LEXICON_PATH` are one decision.** The witness must never be armed with no table. `text_util.uncoupled_witness_warning()` says so at import, advisory rather than fatal because runbook step 5a measures the shape-only configuration deliberately.

**The false-positive class is Latin binomials, and §3.7's exemption is necessary but nowhere near sufficient.** The candidate set carries `Triticum monococcum` (`low_variety`), `Lepus europaeus` (`vowel_run`), `Mammalia indet.`, `Papaver rhoeas/dubium`, `Stachys arvensis/annuua` — real archaeobotanical and zoological vocabulary. The `-aceae` rule catches *family* names; this corpus's taxonomy is genus + epithet, whose endings (`-us`, `-um`, `-as`) are not enumerable. Attestation clears every one (df 20, 31, 5, 3). **Vocabulary is a lexical question; shape should stop being asked it**, and no further shape rule should be proposed for this class.

**One defect in the delivered code, found by reading the table.** 27 tokens begin with `#` (`#rdisico`, `#žkami`) because `#` is not in `_STRIP_CHARS`, and `_read_token_lexicon` skipped `^#` lines as comments — dropping them at load with no symptom. Impact nil (all df 1, below any threshold), fixed by treating a line as a comment only when it starts with `#` **and** carries no tab. Verified: the loader now reads all 347,097 rows and the candidate measurement is byte-identical at `min_df` 3.

## 🧪 Measured on the cluster — runbook stage 5, the gold-scored A/B (2026-09-18, round 1)

**These runs predate the guards.** 5a has since been re-run and its row below is superseded — see the round-2 block that follows. 5a-bis, 5b and 5c have *not* been re-run, so those three rows still stand as delivered and should be read as pre-guard.

**First, what n is.** With `--gold-column` set, `evaluate_dataframe` scores **only the annotated rows** (`annotated_mask`). Every figure in those logs is therefore computed on **2,064 rows**, not the 12,716,706 the header advertised. Gold carries 180 `Trash`, so every reported rate is an exact line count — and `flip_rate` against gold is simply the error rate:

| config                       |   macro_f1 | **lines wrong** |    cost | Trash found /180 | Clear lost | predicted Trash |
|------------------------------|-----------:|----------------:|--------:|-----------------:|-----------:|----------------:|
| shipped (both flags off)     |     0.6173 |             513 |     602 |               22 |         40 |              63 |
| **5a — witness, NO lexicon** |     0.6366 |         **502** | **587** |               36 |         42 |              82 |
| 5a-bis — witness + veto      |     0.6338 |             504 |     591 |               34 |         42 |              80 |
| 5b — vowel_run 4             |     0.6319 |             504 |     591 |               32 |         42 |              77 |
| 5c — + `LEXICON_CONVICT`     | **0.6570** |         **521** | **676** |           **78** |         63 |             183 |

**1. macro_f1 ranked first the only config that makes things worse.** 5c has the best macro_f1 on the board and gets **8 more lines wrong than changing nothing** (521 vs 513), at 12% higher operational cost (676 vs 602). It buys that score by more than tripling `Trash` recall while breaking more common-class lines; macro_f1 averages per-class F1, so fixing the rare class pays for itself regardless of net accuracy. Its `Trash` *precision* is 42.6%, **below** 5a's 43.9% — 5c is not more discriminating, it just calls 183 lines `Trash` instead of 63. `_print_gold_verdict` gated on macro_f1, so it printed *"better on gold"* for the worst arm measured.

> **This is the third instrument-level error in this issue, and they are the same error.** The sweep objective scored the pipeline against itself; the witness exposure ratio scored it against `categ`; the A/B verdict ranked by a metric that rewards moving the boundary toward the rare class. Each time the intuition was fine and the criterion was wrong. Fixed: the verdict now gates on errors, cost and Clear-loss, and computes the exact McNemar it used to merely ask for.

**2. The veto is not beneficial on gold.** 5a (shape only) beats 5a-bis (shape + veto) on all three — macro_f1 0.6366 vs 0.6338, 502 vs 504 wrong, 587 vs 591 cost. The effect is **2 lines**, so this is not evidence of harm; it is the absence of any evidence of benefit, arriving from a direction independent of the `ppole` finding. Two separate measurements now contradict *"the veto is the difference between arming the witness and not"*.

**3. The whole 5a result is 25 lines.** (Round 2: 23 of 20,078 — the shape of the finding is unchanged.) Joining the gold sidecar against the witnessed candidate queue: **25 of the 20,324 witnessed lines carry a gold label.** Of those, 16 are `gold=Trash` not yet `Trash` (the upside) and 5 are `gold=Clear/Noisy` not yet `Trash` (the risk). The observed +14 recall and +2 Clear-loss is 14 of those 16 and 2 of those 5 — the accounting closes exactly. **A four-decimal macro_f1 was being quoted off nineteen changed lines.** Exact McNemar on 14/2 is p≈0.0042, which is real; but it is 19 rows, and the gold set is the binding constraint on every number in this issue.

**4. ~~The guard cannot be validated by stage 5, so do not re-run it.~~ — WRONG, see round 2.** The reasoning held for the de-gemination guard alone: zero of the gold-labelled witnessed rows contain `ppole`, only 2 fire `initial_geminate`, and it withdraws attestation from 8 tokens corpus-wide. But a further exemption (`S-VIIIb` fused grid references) landed the same day and *does* reach those rows, so the re-run moved Clear-loss 42 → 41 and errors 502 → 500. **Predicting a re-run from one of the changes in it is not prediction.**

**5. ~~5b confirms the shipped default.~~ — overstated; the paired test says nothing.** With the McNemar now computed: `vowel_run` 4 against 3 is **fixes 2, breaks 2, effective n = 4, p = 1**. Identical error count (504) and cost. The default stands because **nothing argues for moving it**, which is a weaker and more accurate claim than "3 beats 4". `SHORT_GARBAGE_WITNESS_VOWEL_RUN_MIN` stays 3, on the absence of a reason rather than on evidence.

> ⚠️ **And 5b's own baseline does not reconcile.** Its `3.0` arm is nominally the same config as 5a-bis's `True` arm and reports **504 errors / Clear-loss 42** against 5a-bis's **502 / 41**. Those 504/42 figures are exactly the *round-1, pre-guard* numbers, and the two runs came from different output directories (`issue30_out` and `issue30_outs`) with overlapping stage names. The likeliest reading is that 5b ran against an older tree. **Until that is settled, 5b's numbers are not comparable with 5a's** — and two directories sharing stage names is how a stale figure gets quoted as current.

**Where that leaves the flags.** 5a is the best arm measured and it rests on 19 changed rows (15 after the guards). Both flags stay `false`. The blocker is no longer instrumentation — it is that 2,064 annotated lines reach only 23 of the population under test, which is precisely what the `--distinct` annotation queue exists to fix. The detailed ask is in **What is needed from @DanaKriv and @david-spacil** below.

## 🔁 Round 2 — the guarded re-run (2026-09-18)

Stage 4 and stage 5a were re-run with every guard in the tree. **The block above predicted this re-run would reproduce its figures. It did not**, and the prediction was wrong in an instructive way: it reasoned only about the de-gemination guard, which really cannot reach the gold rows, while a further exemption landed the same day — `S-VIIIb`-style fused grid references, where a trailing sub-letter is joined to the roman segment. That one *does* reach them. Predicting a re-run from one of the changes in it is the same mistake in miniature as the three instrument errors below.

**The corpus-scale predictions were exact.** Witnessed lines 20,078, `Trash` confirms 5,023, `Clear`/`Noisy` candidates 15,055, distinct strings 5,005 — all four as predicted. The guards removed **246 lines** from the queue (20,324 → 20,078) and 238 strings (5,243 → 5,005).

**Stage 5a, guarded, against the 2,064-row gold set:**

| arm                | macro_f1 | lines wrong |       cost | Clear lost | Trash found /180 |
|--------------------|---------:|------------:|-----------:|-----------:|-----------------:|
| flag off (shipped) |   0.6173 |         513 |     0.2917 |         40 |               22 |
| **flag on**        |   0.6371 |     **500** | **0.2822** |         41 |               36 |

`fixes 14, breaks 1, effective n=15, exact McNemar p = 0.00098`. **Errors down 13, cost down, significant at 0.001 — and still REJECTED, on Clear-loss +1.** That is the stricter criterion working as designed rather than a disappointment.

**5a-bis, guarded, completed 2026-09-18:** macro_f1 0.6343, **502 errors**, Clear-loss 41, `Trash` 34/180, `fixes 12, breaks 1, n = 13, p = 0.0034`. So with both guards in the tree the veto still costs 2 fixes and gains nothing — the third independent reading in the same direction. **That conclusion now needs re-checking against the 2026-09-19 correction**, because the veto's job on `ppole` was to protect an abbreviation, and the de-gemination guard was taking that protection away.

**@david-spacil's break class is closed.** His measurement was the gate: flag on moved 26 of the 508, *"fixing 12 and breaking 12 … every broken one carries a roman numeral"* ([`#issuecomment-5620689006`](https://github.com/ufal/atrium-alto-postprocess/issues/30#issuecomment-5620689006)). The guarded run breaks **1** line across the whole 2,064-row gold set, and his 508 are a subset of it, so breaks within his own sample are bounded at 1. Verified independently here rather than taken from the log: joining the sidecar against the regenerated queue, **23 of the 20,078 witnessed lines carry a gold label** — 16 `gold=Trash` not yet `Trash`, 3 `gold=Clear`/`Noisy` at risk. All three remaining risk rows are `gold=Noisy` (`Poue`, `Lokolieace: •VIII,`, `Iokalisqce: HVIII,`), and the only `gold=Clear` rows the witness still reaches are **already** `Trash` — two `http://www.arub.cz` occurrences already inside the baseline 40.

> **So the witness can no longer produce a new Clear-loss anywhere in its own population**, and the 41st does not belong to it.

**Where the 41st comes from, and why it is bigger than one line.** By elimination it is `apply_document_postprocessing()`'s modal dedup — the only branch that can move a line *out* of `Clear` (the other four require `categ == "Noisy"` or promote). It rewrites every row to the modal category of its identical text within a document, so convicting a few occurrences of a repeated string can flip the majority and carry a correct occurrence with it.

The consequence is the part worth keeping: **per-line precision does not bound Clear-loss.** The blast radius is the (document, string) group, not the line. The 20,078 lines sit behind **5,222 (document, string) pairs** — that many dedup votes — 44 groups of ≥20 lines cover 69.9% of the queue, and `ppole` alone is 22 votes controlling 11,562 lines. **This is not specific to the witness; any rule that convicts a repeated string inherits it.** Stage 5f (5a with `--no-postprocessing`) confirms or refutes the mechanism in one run; `classify_TEXT.py`'s dedup is deliberately unchanged until it reports.

> ✅ **Sized 2026-09-20 (S4), and the size changes the recommendation.** The mechanism
> needs a group whose occurrences disagree. Of the 5,222 votes, **5,200 are unanimous**
> and 4,903 are a single line; **22 are contested** (755 lines) — 15 decided by a strict
> majority, 7 by a tie, and **0 by a bare plurality**, so the exact configuration H5 asks
> about does not occur. A tie cannot demote either: `mode()[0]` takes the alphabetically
> first tied category and `Clear` < `Noisy` < `Trash`. The
> witness is a pure function of the line's text, so within a group it convicts all or
> none: it cannot create a split, only move an already-unanimous group wholesale.
> Stage 5f has since reported, and it points the other way too — cascade **on**, the
> witness breaks **1** line; cascade **off**, it breaks **3**, and the baseline
> `Clear`-loss is 55 instead of 40. On this gold set the cascade is protective. The
> mechanism is real; on this queue the vote destroys 17 `Clear` lines and rescues 31
> `Trash` ones, net +14 in its favour.

**A second finding, and it was blocking an 87-hour job.** `tools/rule_coverage_report.py` did not produce a gold-scored `clear_loss` even when `--gold-column` was passed: `_loo_metrics` ran the gold pass, kept `gold_delta_macro_f1`, discarded the confusion matrix, and derived `clear_loss` from the *self-scored* pass. Against stored `categ` that column only counts lines the pipeline already called `Clear`, and at the shipped config the offline re-score reproduces those labels exactly — so it reads **0 by construction** however many readable lines a rule is really destroying. Demonstrated on a fixture where gold and stored disagree: `clear_loss = 0` while `gold_clear_loss = 2`. Fixed at zero extra passes, from the confusion already computed.

**And a correction to this document's own next-step 3.** It said every `DEAD` / `LOAD-BEARING` / `clear_loss` verdict "measured agreement with the pipeline's own output". Half right, and the halves matter:

- **`decisive_count`, and so `DEAD` / `REDUNDANT-HERE` / `LOAD-BEARING`, are self-scored and should be.** They measure how many lines a rule moves — a property of the rule, not a claim about truth. They never needed gold and were never wrong.
- **`clear_loss` is a correctness claim** and did need gold, which is the bug above.

The JSON's `decisive_scored_against` compounded it by reading `"gold"` on any `--gold-column` run — the one field that exists so a self-referential report can never be mistaken for a gold one.

## 🛑 Correction of 2026-09-19 — `ppole` is an abbreviation, and this document said otherwise

@david-spacil, [in thread](https://github.com/ufal/atrium-alto-postprocess/issues/30):

> *"`ppole` is not an OCR artefact (at least not every time), it's an abbreviation for urnfield
> culture, which also explains its frequency (and also the same position on pages with forms).
> Other examples of words with doubled first letter in `issue30_gold_ab_findings.md` truly are
> OCR artefacts."*

`pp` for *popelnicová pole* is a standard Czech doubling-for-plural abbreviation — the same
convention as `pp.` for pages or `ss.` for sections. **So `initial_geminate`'s stated premise,
*"no European orthography opens a word that way"*, is false for abbreviations** — and that is the
clause firing on 11,562 lines.

**What this reverses.** The 2026-09-17 block above argued the vocabulary veto was shielding a
conviction the clause got right. The clause got it **wrong**, and the veto was protecting real
vocabulary. The reading that block was written to correct was closer to the truth than the
correction. Logged as the **fourth inversion** in this issue, and the first that no internal
measurement could have produced: it needed someone who knows what the archive says.

Note the hedge — *"at least not every time"*. The abbreviation and genuine scanning doublings
occupy the same string, so `ppole` cannot be settled by a single label either, and the annotation
ask has to allow splitting it by context.

**What survives.** The other seven are confirmed artefacts on his own reading, so the
de-gemination guard is right on 7 of 8 and wrong on the one that matters most by volume.

**The separation is available, and it is not the ratio.** On the 822-document table:

| token                       | own df | base df |   ratio |
|-----------------------------|-------:|--------:|--------:|
| **`ppole`** — abbreviation  | **35** |     163 | **4.7** |
| `ssuti`                     |      8 |      42 |     5.2 |
| `ssutí` / `vvkop` / `ssutě` |    4–5 |   23–42 | 5.8–8.4 |
| `jjámy`                     |      5 |     356 |      71 |
| `oobjekt`                   |      3 |     378 |     126 |

`ppole` carries the **highest own frequency and the lowest ratio**; every confirmed artefact sits
at df ≤ 8. A scanning error appears in a handful of documents, an abbreviation in many. 4.7
against 5.2 is not a separation, but 35 against ≤ 8 is — so the fix is an **absolute cap on the
geminate's own document frequency**, not a higher ratio. To be re-checked on the
113,100-document table before the constant is set.

---

## 🧪 Stage 5f lands — the per-line effect, isolated from the dedup cascade (2026-09-19)

`--no-postprocessing` reruns stage 5a's gold-scored A/B with `apply_document_postprocessing()`
skipped, so every figure below is the witness's effect on a line *before* any modal-dedup vote
touches it — the measurement "Round 2" (§ above) deferred to in order to close the H7 decision.

| arm (cascade off)  | macro_f1 | lines wrong | Clear lost | Trash found /180 |
|--------------------|---------:|------------:|-----------:|-----------------:|
| flag off (shipped) |   0.6362 |         502 |         55 |       42 (23.3%) |
| **flag on**        |   0.6512 |     **491** |         57 |       56 (31.1%) |

`fixes 14, breaks 3, effective n = 17, exact McNemar p = 0.01273` — significant at 0.05, not at
0.01. 17 discordant rows written to `issue30_outs/05f_discordant_perline.csv`.

**This is not the number H7 was drafted around.** H7's option 1, and this document's own "what
actually remains" list, quoted *"+1 Clear-loss for −13 errors at p = 0.001"* — those are Round
2's **with-cascade** figures, used as a placeholder for what 5f would report before 5f had run.
The real per-line number is **+2 Clear-loss for −11 errors at p = 0.013**: a smaller error
reduction and an order of magnitude less significant than the placeholder implied.

**And it complicates Round 2's own account of where the 41st line comes from.** That section
concluded the witness "can no longer produce a new Clear-loss anywhere in its own population" —
argued from the sidecar join, not from a cascade-off run — and placed the 40→41 move entirely on
`apply_document_postprocessing()`'s modal dedup. With the cascade removed altogether, Clear-loss
still moves (55→57). Two readings stay open and 5f alone cannot choose between them: either the
witness does convict a small number of gold-`Clear` lines on its own and the join undercounted
them, or the cascade-off and cascade-on runs draw Clear-loss from different, non-overlapping
baselines (55 vs 40) and the two deltas are not the same quantity. Flagged rather than resolved —
reading a delta before checking what baseline it is a delta *of* is the same mistake this section
has corrected twice already.

**Where the two runs disagree with each other, not just with gold.** 5f's 17 discordant rows
overlap 5a-guarded's 15 in 13 fixes; 5f fixes `CTX199103314A` where 5a-guarded does not,
5a-guarded fixes `MTX198301331` where 5f does not, and 5f carries two breaks 5a-guarded does not:
`CTX194503132` (`"Frauenzimmerbad", sämtlic Gesellschastsbäder,`) and `MTX197902086`
(`S-VIIIb` — the fused grid-reference exemption from Round 2, breaking here rather than fixing).
Both are lines where the reference arm already agrees with gold; the flag alone turns them wrong,
on rows the cascade would otherwise have smoothed over.
🛑 *Corrected 2026-09-23 (D45): not "on rows the cascade would otherwise have smoothed over". 5f ran
after `cc4990e` had deleted both round-2 guards, and these two breaks are exactly the two lines the
guards vetoed. 5a-guarded and 5f differ in code as well as in the cascade — see § "D45" at the top.*

**Where this leaves H7.** The three options are now costed on the population the flag will
actually run over, not on a borrowed number:

1. **Accept** — +2 Clear-loss for −11 errors, p = 0.013. The margin H7 was scoped against was
   overstated by one line of cost and an order of magnitude of significance.
2. **Stop a bare plurality demoting `Clear` → `Trash`** in the modal dedup — still a production
   change needing its own parity re-score and A/B, now motivated by a concrete pair of lines
   (`CTX194503132`, `MTX197902086`) rather than the mechanism alone.
3. **A/B the eight page-smoothing keys** — unchanged, still config-driven, still not run.

The choice among them is still @david-spacil's (H7); what changed is that it can now be made
against measured numbers instead of numbers borrowed from a different arm.

**Two lexicon thresholds, re-checked on gold rather than fixtures.** Neither changes a decision
already on the books, but both were previously argued from the 822-document table or from
fixtures alone.

- **`SHORT_GARBAGE_LEXICON_GEMINATE_RATIO`, 0.0 vs 4.0 (default): identical on every scored row**
  — same macro_f1 (0.6338), same 504 errors, same Clear-loss (42), same `Trash` recall (34/180).
  The de-gemination guard's effect is real — the `ppole` correction above measured it on the full
  corpus — but none of it lands inside the 2,064 annotated rows, consistent with only 2
  gold-labelled witnessed rows firing `initial_geminate` and neither of them being `ppole`. This
  gold set cannot tune the ratio; that needs the annotation queue's `initial_geminate` rows
  specifically, not the pooled 2,064.
- **`SHORT_GARBAGE_LEXICON_MIN_DF`, 2 vs 3 (default): one row.** `min_df` 2 scores marginally
  better (503 errors / Clear-loss 41) than 3 (504 / 42) — `fixes 0, breaks 1, n = 1, p = 1`, not
  significant, and the reverse of the direction the fixture-level reading argued (`min_df` 3
  convicting 5 of 31 pinned vocabulary fixtures was grounds to raise the threshold, not lower it).
  Gold neither confirms nor overturns it; `SHORT_GARBAGE_LEXICON_MIN_DF` stays 3 on the fixture
  evidence alone.

---

## 📚 The full-collection lexicon (2026-09-18)

**113,100 documents, 72,306,182 lines, 2,764,632 distinct tokens** — against 822 / 12.7M /
347,097. Built over both collections with per-collection columns.

**The thin-corpus argument was mostly wrong.** This document argued the 822-document table
"cannot settle the threshold" at 0.7% of the collections. At 137× the documents the distribution
barely moved: attested-in-≥3 **15.7% → 15.2%**, hapax **74.3% → 75.9%**. OCR noise scales with
the corpus — every document brings its own unique garbage, so the residue does not dilute. What
the rebuild bought is **per-token resolution**, not a different shape: `fragment` df 250 → 6,375,
correction targets 17,619 → 158,503, suggestion coverage 909 → **1,298 strings (69.6% of lines)**.
A large gain, and a different one from the one claimed.

**87% of tokens are collection-exclusive** (1,589,104 ARÚP-only + 816,095 ARUB-only). Only ~13%
appear in both.

### The mirror of Dana's concern: readable text is being discarded

> ⚠️ **Scope, added 2026-09-20 (S3).** This table is computed over the **20,078-line
> shape-witness candidate queue**, not over the corpus — it reproduces exactly from
> `07f_distinct_evidence.csv`, whose 5,005 rows are the witness's distinct strings. It
> sits under a section about the full-collection lexicon and reads as a corpus finding;
> it is not one. Every sentence below is true *of lines the witness flags*. Whether it
> generalises to the other 71.8M lines is unmeasured.

Cross-tabulating recoverability against the pipeline's own category:

| `categ_current` |         fully recoverable |   partial |            none |
|-----------------|--------------------------:|----------:|----------------:|
| Clear           |    526 str / 13,044 lines | 281 / 345 | 414 / **1,005** |
| Noisy           |                 165 / 301 | 101 / 112 |       214 / 231 |
| Trash           | **743 str / 1,791 lines** | 356 / 367 |   2,205 / 2,882 |

**743 strings / 1,791 lines currently `Trash` are fully recoverable** — `1 fraament okraie` (530)
and `1 fraoment okraie` (163) are "1 fragment okraje"; `Eouus caballus` is `Equus caballus`;
`http://www.arub.cz` (158) is an institutional URL; `os hyoideum`, `Mammalia ind.`,
`Linum usitatissimum` are ordinary osteology and botany. **This issue has been about garbage
reaching `Clear`; this is real content being thrown away**, and no figure in the thread measures
it.

### The recoverability signal is asymmetric

The `Clear` + zero-recoverability cell (414 strings / 1,005 lines) holds real garbage
(`sektlll`, `IOIAL`) **and real vocabulary**: `Kaukasus`, `Feuersteinspan`,
`Schuhleistenkeilbruchstueck` — correct German archaeological terms too rare for any
corpus-derived lexicon. **High recoverability is strong evidence of real text; zero
recoverability is weak evidence of anything**, because that is exactly what a rare true word
looks like. No threshold separates them, because what separates them is knowledge this corpus
does not contain.

---

## 🔍 The vocabulary signal the pipeline already has, and does not use (2026-09-19)

`text_util.compute_valid_ratio(text, word_set=None)` takes a vocabulary and does a real lookup.
`classify_TEXT.py:245` calls it **without one**, so it falls back to shape: *≥3 characters, ≥70%
alphabetic, no odd characters → valid*. Measured:

```
compute_valid_ratio('oueussd edelite sektlll')  ->  1.00
```

**Pure garbage scores as 100% valid words.** `valid_word_ratio` feeds `compute_quality_score`,
which drives every threshold in this pipeline — so its primary *is-this-text* signal is
shape-only, and maximally fooled by the exact population issue #30 is about. With a word set the
same call returns `0.00`.

The hook is not a bolt-on; it is the parameter the function was written for, wired to nothing.
This is the largest available use of the collection lexicon, and also the largest change this
issue could make: it moves `quality_score` on every line in the corpus. It ships behind a flag,
off, and gets measured on gold first.

**One design constraint.** A strict word-set lookup scores `1 fraament okraie` at 0.00, but that
line is readable text. The word set must admit *attested OR one known transformation from
attested*, or `valid_word_ratio` punishes damaged-but-usable lines exactly as hard as garbage.

**A cheap gap alongside it.** `_STRIP_CHARS` is ``.,;:!?()[]"'\/`` — no symbol glyphs. So
`♦zkoumaná`, `✓stopy`, `•nevelká` enter the lexicon fused and the real word is unfindable at
lookup. Both ends of the same table are affected; the fix is one constant, and it changes
production tokenisation, so it is measured like everything else.

### Measured against gold, stage 7 (2026-09-19)

> 🛑 **This whole sub-section was superseded on 2026-09-20 by § "Stage 7 read
> against its own delivery". D25 does not hold (S1) and D26's null is void
> rather than a null (S2).** Kept as written, because how it was misread is the
> point: both readings came from the summary of the run rather than the run, and
> the refuting evidence was inside the delivered log in both cases.

**~~D25 (the geminate cap) holds at full scale.~~ — it does not, see S1.** The 07a lookup re-ran all eight doubled-initial
tokens over the 113,100-document, 2,764,632-token full-collection lexicon. `ppole` still sits at
the lowest ratio of the eight — 37.6x now (own df 229 against `pole`'s 8,600), down from the
822-document table's 4.7x reading but still the outlier by an order of magnitude against the
confirmed artefacts, which range 7.6x (`llocm`/`locm`) to 1479.9x (`jjámy`/`jámy`). One token
moved category rather than magnitude: `oobjekt` no longer resolves directly to `attested` at
full scale, only `recoverable` via a one-edit distance to `objekty`/`objektu`/`objektů` — a
weaker but still-correct signal. The separation the correction above argued for on a thin table
is the same separation on 137x the documents; `SHORT_GARBAGE_LEXICON_GEMINATE_MAX_DF` can be set
from it.

**~~D26 measured zero effect on the current gold set — and that is a fact about the measuring
stick, not the fix.~~ — VOID, not zero. See S2.** `QUALITY_VOCABULARY_ENABLE` (07b) is
bit-identical, true vs false, on all 2,064 gold-scored rows: same 513 errors, same 40 Clear-loss,
same cost, **0 discordant rows** — and so is 07c, to five decimal places, because **neither A/B
armed the flag it was testing**. `quality_word_set()` is a zero-argument `lru_cache`, both arms
run in one process, and the second arm read the first arm's cached answer. Fixed 2026-09-20.

The paragraph this replaces argued the null was a coverage fact and distinguished it from 07c's
instrumentation bug. That distinction was wrong: it is the same bug, one indirection further in.
The coverage argument may still be true — the population D26 targets does look real and large —
but it is now **untested**, not confirmed, and the honest statement is that D26 has no
measurement at all. Stage 8b is the re-run; the annotation ask below is what would let it be
scored on the right population afterwards.

**D27's zero effect was not that — it was the harness never arming the flag, found and fixed
2026-09-19.** `_STRIP_CHARS` is not read fresh from `STRIP_SYMBOL_GLYPHS` on every call; it is a
DERIVED constant, built once at import time (`if STRIP_SYMBOL_GLYPHS: _STRIP_CHARS = _STRIP_CHARS
+ _SYMBOL_GLYPHS`), the same way `ROT_GHOSTLIST` and `_LANG_DIACRITICS` are already documented as
built once and exempted from `override_constants()`'s rebuild. `_STRIP_CHARS` was the same
exemption in substance, just never named as one — so 07c's A/B, which overrides
`STRIP_SYMBOL_GLYPHS` and nothing else, moved a flag that no `.strip(_STRIP_CHARS)` call site
(28 of them) could ever see move. Checked directly against this repository: overriding the flag
to `True` left `♦` out of `_STRIP_CHARS` on every inspection, with or without postprocessing,
before the fix. **Fixed** by teaching `override_constants()` a small `STRIP_SYMBOL_GLYPHS` →
`_STRIP_CHARS` mapping that rebuilds and restores the derived value alongside the flag, plus a
regression test that exercises the override path specifically — the existing ships-off test only
ever checked the flag at its default, which is exactly the gap that let this hide for as long as
it did. **07c's reported "zero effect on gold" is void, not confirmed, and needs re-running.**
This is a different bug from D26's — D26's null result stands on a coverage argument that still
holds; D27's does not, because nothing about it was ever actually measured.

**07d still hasn't closed the directory-staleness question it was written to settle, and this
D27 bug is not the explanation for it.** `SHORT_GARBAGE_WITNESS_VOWEL_RUN_MIN` is read directly
wherever it's used, not through a derived intermediate the way `_STRIP_CHARS` is — so the same
mechanism cannot be what moved 07d's number. A clean, single-directory re-run of the vowel-run
threshold comparison (3.0 vs 4.0) gives parity again — 503 errors / Clear-loss 40 for both arms,
`fixes 2, breaks 2, n=4, p=1` — the same non-result, and 3.0 still stands on the absence of a
reason to move. But the baseline figure itself is now the **third** distinct number for what
should be one config: 504 errors / Clear-loss 42 in round 1 (pre-guard), 502 / 41 in round 2
(guarded, but from the overlapping-name directory), and 503 / 40 here, from one clean directory
with the guards applied. Removing the directory variable did not stop the number moving, and
finding one real derived-constant bug elsewhere is reason to suspect another rather than to stop
looking — but `_STRIP_CHARS` specifically is ruled out. Not yet root-caused.

**07e's lookup on the R6 tokens is genuinely mixed, not a close.** `Skelettmaterial` turns out to
be real vocabulary at full scale (`attested`, not the df-2 thin-corpus artefact it looked like on
the 822-document table) — the full-collection rebuild answering exactly the kind of question it
was rebuilt for. `Naiade`, `Beuern`, `Oueste` each get an `edit1` `recoverable` suggestion
(`naaden`, `bauern`/`beuten`, `ouest`), which is weak evidence at best — this document has
already warned that `edit1` may be coincidence (`Linum`/`ilium`), and `Pinii`'s own suggestions
(`linii`, `pizni`, `indii`) read like exactly that coincidence rather than a real correction.
`Uniocrassus`, `Kaukasus` and `Feuersteinspan` stay `unattested` at any scale, which — as
established above — is not evidence of garbage either. R6 is updated, not closed, in the table
below.

---

## ⚠️ Corrections of 2026-09-10 — read before quoting anything below

@david-spacil returned a claim-by-claim review of `30.audit.md` / `30.strategy.md` /
`30.handover.md` ([`#issuecomment-5620689006`](https://github.com/ufal/atrium-alto-postprocess/issues/30#issuecomment-5620689006)),
together with **both annotation sets** and **raw perplexity for the whole changed population**.
Analysis by Claude Opus 5, re-measured on `7356f3a` where a claim concerns current code.

Three corrections land underneath figures this digest states as fact, and one new measurement
**refutes** §3.7's central projection. The corrected numbers are carried into each section
below; this is the index.

| #     | what was wrong                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | effect                                                                                                                                                                                                                                          |
|-------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **1** | **The per-line gold behind every "agreement" figure was partly *derived*, not annotated.** The 508 lines were graded *right / borderline / wrong* on the **direction** of the move; the category label was then set automatically (*right* → promoted category, *wrong* → original). That mapping is invalid — *right* means the move went the right way, not that it landed right. Now hand-labelled: **55 of the 508 labels changed**, 38 of them the same pattern (a `Trash → Noisy` lift graded right whose line belongs in `Clear`) | every agreement figure; **the patch drops 76.7% → 68.8%**. Direction of every conclusion unchanged                                                                                                                                              |
| **2** | The scripts that scored the 508 passed `expected_langs=["ces"]` where production passes `ces,deu,eng` — the D11 trust-tier bug again                                                                                                                                                                                                                                                                                                                                                                                                     | 3 lines, all German (`Silexabsplisse`, `messen,`, `0,2-0,4 m`). **Not an in-tree defect**: the repo's own tools resolve through `_load_lang_config()` / `LANGID_CONFIG`, and the 367,208 recompute ran through `tools/recategorize_from_csv.py` |
| **3** | The borderline figures re-normalised stratum weights over the 19 strata containing borderline lines — weighting by population size instead of by the borderline domain                                                                                                                                                                                                                                                                                                                                                                   | §3.4's "39.5% vs 30.5%" reverses to **33.5% vs 37.9%**, still indistinguishable                                                                                                                                                                 |

**Not affected by any of the three:** the quality figures 76.5 / 9.6 / 13.9 (5.5 : 1), which are
computed from the grades directly, and the 367,208 recompute.

**Corrected agreement.** Same categories as sent, only the gold replaced (old weights, N = 378,735):

| variant                                     | as sent                  | corrected gold                     |
|---------------------------------------------|--------------------------|------------------------------------|
| upstream `b1271a6`                          | 15.8% [8.0; 23.6]        | 16.1% [8.2; 23.9]                  |
| **+ patch**                                 | **76.7% [67.9; 85.6]**   | **68.8% [58.9; 78.7]**             |
| patch + uncapped                            | 75.6%                    | 67.8%                              |
| patch + lifts floored at `Noisy`            | 31.1%                    | 23.2%                              |
| paired, floored vs patch                    | −172,942                 | −172,654 [−213,351; −131,957]      |

On current code — `7356f3a`, production languages, weights from the 367,208 recompute:

| variant                                      | agreement, weighted    | paired vs HEAD, lines          |
|----------------------------------------------|------------------------|--------------------------------|
| upstream `f8ad4d8` without #48               | 17.5% [9.6; 25.5]      | −189,543 [−249,285; −129,801]  |
| **HEAD**                                     | **69.1% [59.2; 79.1]** | —                              |
| HEAD, cap lifted                             | 70.8% [61.4; 80.3]     | +6,285 [−31,924; +44,494]      |
| HEAD, lifts at `wc <= 2` floored at `Noisy`  | 23.4%                  | −167,788 [−207,442; −128,134]  |
| HEAD, cap lifted, demotions floored          | 67.5%                  | −5,880 [−15,578; +3,818]       |

**The picture holds; only the size moves.** The patch is far ahead of upstream, no cap variant
separates from it, the `Noisy` floor is clearly worse. The patch's own agreement is about
**8 points lower** than every figure sent before this date.

### Smaller verdicts from the same review

- **Audit #10** — the notation predicate accepts all 8 lines at the shipped config, but on real
  rows with the cap lifted **6 of 8** stay out of `Trash`, not 7: `radius prox.sin.`
  (`orig_lang_score` 0.24) and `Lokalisace: MM-III` (0.25) go `Trash`.
- **Audit #16** — notation fires on 30 of 508 ✅, but **25 of 30** match the corrected gold, not 29.
- **Audit #17** — `Bokalisace: B-XII-c` is no longer lifted to `Clear`, but it now lands in
  `Trash`, and its hand label is `Noisy`. Since #48, **0 of the 508** differ between
  `f8ad4d8 + #48` and `7356f3a`.
- **Audit hdr** — `002c4e2` and `6399270` are not in the public history; `fec8537` and `cdcdf57` are.
- **Suite** — `7356f3a` runs 1008 passed / 11 skipped / 2 xfailed, not the 1007/6/2 claimed.

## 🎯 What the issue was, and what shipped

The issue was opened as "the 2010+ documents are categorised badly". The cause was one rule.

`rule_short_garbage` (gate 6 of `determine_category`, `text_util.py:1353-1371`) returned `Trash` **unconditionally** for any 1–3 token line with no Czech diacritics, weak language ID and any weirdness. It was the largest single `Trash` producer in the corpus — 6,333 firings, **28.4% of all `Trash` lines**, 62.5% on the two documents @DanaKriv flagged. It concentrates on post-2000 documents for a structural reason: those documents are tabular, and one value per cell is precisely its firing profile.

The merged change adds the second witness its six §9 neighbours already required:

```python
_fire("rule_short_garbage")
if qs < CATEG_TRASH_SCORE_MAX + 0.35 and _has_strong_garbage_evidence(...):
    return "Trash", "trash_threshold"
```

Two hunks, `text_util.py` +11 / −1, plus the fixture updates that record the flip as a diff rather than a claim.

## 📊 Algorithm accuracy

This is the part worth reading. It is assembled from six measurement rounds that were previously scattered across the thread.

### 3.1 Corpus effect

Both collections recomputed twice on the same base, so the difference is the patch alone.

|                            | value                 |
|----------------------------|-----------------------|
| documents / lines          | 113,101 / 71,815,774  |
| **promotions / demotions** | **367,208 / 0**       |
| `Trash` delta              | **−247,252 (−8.68%)** |
| `Non-text`, `Empty`        | untouched             |

The **367,208** figure supersedes the widely-quoted **378,735**. The earlier number was measured before `rule_domain_notation` landed; notation is now lifted *without* the patch, removing 11,532 lines (3.0%) from the population it acts on. The earlier per-collection split, on the 378,735 base: ARÚP 197,218 changes (0.62% of its lines), ARUB 181,517 (0.45%).

### 3.2 Quality of the lifts

Human-annotated, key held aside on shuffled samples, design-weighted with decade strata and 95% CIs.

> ⚠️ **Every figure in this section is PER-LINE.** @david-spacil stated the caveat once, in the
> 2026-09-04 method note — *"Per-line only — document postprocessing changes the category of about
> **27%** of these lines and is absent here"* — and it was never carried into this digest or the plan.
> It belongs on every number below. `apply_document_postprocessing()` (dedup, surrounded-trash,
> page-majority, inverted-run sweep) runs in production and in
> `recategorize_from_csv.recategorize_dataframe()`, but not in the per-line grading harness, so the
> 76.5 / 9.6 / 13.9 split describes the gate's decision rather than the pipeline's final answer on
> those lines. The direction of the correction is unknown: page smoothing could recover wrongly
> lifted lines or propagate them. Re-grading document-aware is possible today —
> `evaluate_dataframe` already applies the smoothing — and needs only the annotation files.

| round                    | base              | n       | right              | borderline | wrong     | ratio       |
|--------------------------|-------------------|---------|--------------------|------------|-----------|-------------|
| **final**                | `f8ad4d8`         | **484** | **76.5%**          | **9.6%**   | **13.9%** | **5.5 : 1** |
| weighted ⚠️ superseded   | `8fadeb9 + patch` | 508     | 76.7% [67.9; 85.6] | —          | —         | —           |
| raw, unweighted          | `b1271a6 + patch` | 508     | 69.7%              | 15.4%      | 15.0%     | 4.7 : 1     |
| reweighted, ARÚP         | `b1271a6 + patch` | 508     | 75.6%              | 8.5%       | 16.0%     | 4.7 : 1     |
| reweighted, ARUB         | `b1271a6 + patch` | 508     | 78.3%              | 10.5%      | 11.2%     | 7.0 : 1     |
| earlier blind, ARÚP only | —                 | 260     | 78%                | 8%         | 14%       | —           |

⚠️ **The `76.7%` row used the derived gold and is superseded — it is `68.8%` [58.9; 78.7] hand-labelled, `69.1%` on HEAD (Corrections §1).** The `right / borderline / wrong` columns are computed from the grades directly and are unaffected, so the **484-line `final` row still stands as sent**.

Four independent gradings across three base commits land in the same place. That consistency is the main reason these figures are trusted at all. The raw 508-line sample deliberately over-weights pre-2000 documents — the harder ones — so its unweighted shares are pessimistic for the corpus.

Upstream scores 15.8% [8.0; 23.6] on the same set, which is low **by construction**: the population *is* where the patch disagrees with upstream. Only differences between rows carry information.

### 3.3 The interim cost, stated plainly

Of 230 `Trash`→`Clear` transitions in the graded sample, 37 were judged wrong — **16.1%, CI [11.9; 21.4]**. Projected onto the corpus that is roughly **26,000 garbage lines now reaching `Clear`**, CI [19,300; 34,600].

> ✅ **Holds on the corrected gold** (2026-09-10): 37 of 213 graded `Trash → Clear` lines wrong → **~26,600**, CI [19,700; 35,300], unweighted as before. This is the one headline figure the re-annotation left standing.

`Clear` is the top category, not a reviewable middle state. This is the debt, it is deliberate, and it is why the narrowing in §6 is not optional.

### 3.4 Counter-evidence, from the project's own older annotations

The sharpest thing measured against the patch, and it belongs in the ledger rather than buried:

1,567 lines annotated during earlier calibration rounds; 1,560 matched line-for-line in both recomputations. Baseline agrees with the annotation on **79.2%**; with the patch, **78.8%**. The patch moves 9 of those 1,560 lines and **not one lands on the annotated category** — six move away (`NINNNIC`, `Slaot-o hiezazzt`, `Ch. i6dn.283/54`, `p.nA o.`, `cuxoaid ,` twice), two move partway toward it (`sektor B`, `0,2-0,4 m`, both annotated `Clear`, lifted `Trash`→`Noisy`), one partway away (`IDIDIDIDIDIDUOID`, annotated `Non-text`).

It is not decisive: those sets were built by sampling disputed lines from earlier rounds, so they are enriched for garbage and barely overlap this rule's population — only 73 of the 1,560 are `Trash` in the baseline at all. But it points at exactly the weakness in §3.3.

### 3.5 Alternatives measured and rejected

All on the same 508 lines, weighted agreement with the annotation.

| variant                         | agreement              | fixes / breaks | verdict                                                                        |
|---------------------------------|------------------------|----------------|--------------------------------------------------------------------------------|
| **patch, cap 850 (shipped)** ⚠️ | **76.7% [67.9; 85.6]** | —              | baseline — **68.8% on the corrected gold**; every row below shifts with it     |
| patch, cap 1001                 | 78.6% [70.3; 86.8]     | 8 / 10         | not separable                                                                  |
| patch, cap 3001                 | 76.9% [68.2; 85.6]     | 37 / 55        | not separable                                                                  |
| patch, cap removed              | 75.6% [66.9; 84.4]     | 46 / 66        | not separable                                                                  |
| **lifts floored at `Noisy`**    | **31.1% [21.4; 40.7]** | **28 / 202**   | rejected — catastrophic                                                        |
| cap removed + demotions floored | 72.1% [62.7; 81.5]     | 9 / 41         | rejected                                                                       |
| narrowing to `wc ≥ 2`           | —                      | —              | 4.7 → 5.4 : 1, but discards ⅓ of the correct lifts to remove 42% of the errors |

Two results here settle long-running questions. **No perplexity-cap variant is separable from the patch as it stands** — the expectation that lifting `SHORT_PPL_CAP` would recover the garbage class does not hold. And the **`Noisy` floor**, which reads as the obvious safety valve, costs 202 breaks against 28 fixes: it applies to 227,536 of the changed lines (60%) and caps the good lifts along with the bad.

### 3.6 Error structure — flat, and that is the finding

| tokens | n   | errors | | characters | n   | errors |
|--------|-----|--------|-|------------|-----|--------|
| 1      | 167 | 31%    | | 0–4        | 63  | 38%    |
| 2      | 220 | 28%    | | 5–7        | 79  | 42%    |
| 3      | 114 | 25%    | | 8–10       | 103 | 24%    |
|        |     |        | | 11–14      | 109 | 29%    |
|        |     |        | | 15–20      | 113 | 22%    |
|        |     |        | | 21+        | 41  | 17%    |

No monotone trend in either dimension. The tightest `wc ≤ w, chars ≤ c` box holding 95% of the errors is **`3, 24`** — and it encloses 482 of the 508 lines, 343 of them correct. `sektlll` and `edelite` (wrong) sit in the same band as `Kaaden` and `Pinii` (right).

**This is why the narrowing had to be a shape predicate and not a word-count or character boundary.** The question "can we draw a box around the problematic short lines?" was asked directly in the thread and answered in the negative by measurement.

An earlier 3.6 / 5.1 / 6.8 : 1 correct-to-wrong gradient by token count is not a "shorter is riskier" law either: at `wc == 3` the `SHORT_PPL_CAP` no longer applies, raw perplexity reaches the sweep, and garbage still gets caught. It is a constant interaction, not a property of short lines.

By decade, the oldest scans fare worst — ARÚP 1930s grade 15 correct against 12 wrong, 1920s 11 against 8, while the 1950s and 1960s run 25:2 and 23:1. Those two worst decades are 1,108 lines out of 197,218, so they do not move the total; they are where to look first.

### 3.7 The shape witness — separation, re-verified here

`_has_shape_garbage_evidence()` (`text_util.py:1770`) is in the tree behind `SHORT_GARBAGE_WITNESS_ENABLE` (**default false**, still **no call site**). It is a pure function of the line's text, so it can be measured without the scoring stack — re-run for this digest:

| set                                              | n  | flagged                                                                                                                 |
|--------------------------------------------------|----|-------------------------------------------------------------------------------------------------------------------------|
| thread garbage, reachable                        | 9  | **9** — `oueussd`, `cuxoaid`, `IDIDIDIDIDIDUOID`, `nnnooo`, `sektlll`, `NINNNIC`, `Tthts I`, `rragment`, `vansasaasasa` |
| vocabulary / notation / toponyms / names / units | 37 | **0** — incl. the whole `vrstva` family, `Skelettmaterial`, `Aachen`, `Schifffahrt`, `malakofauna`, `II/C`, `1 ks`      |
| phonotactically legal residue                    | 6  | **0** — by construction                                                                                                 |
| known loan false-positive class                  | 7  | **5** — `Poaceae`, `Naiade`, `Beaune`, `Radiouhlik`, `Sauerstoffflasche`                                                |

Against exactly the lines @david-spacil printed in his grading tables: **8 of 15** labelled *wrongly lifted* are flagged, **0 of 25** labelled *correctly lifted*, **0 of 4** borderline, and **0 of 37** committed positive fixture rows.

**Read this as a smoke test, not an error rate.** Those are the lines this thread happened to print, not a random draw. The projection this section used to carry — *"the wrong-lift share would fall from 13.9% toward 6–7% at no cost to the correct lifts"* — has now been measured, and it was wrong.

#### The witness measured on the 508 — the projection does not hold

`ATRIUM_TEXT_UTILS_SHORT_GARBAGE_WITNESS_ENABLE=true` on `7356f3a`, production languages
(@david-spacil, 2026-09-10). It moves **26 of the 508**, all to `Trash`:

| outcome                   | n      |
|---------------------------|--------|
| graded **wrong** → fixed  | **12** |
| graded **right** → broken | **12** |
| borderline                | 2      |

**A 1 : 1 trade, not a free 6–7%.** The fixes are the expected class (`sektlll`, `utlll`,
`tirrttitt`, `IAALAILIL`, `SEEEEEEEEEEEE:`). *Every one of the twelve breaks carries a Roman
numeral*: `Sonda VIII/3`, `12.VIII.1977,`, `CCV. CCVI.`, `205; CCLXII).`, `166. Hr.XLIII.1.`,
`Lokalisace: I-VIII-eneol.II`, `w XVIII.`. Unweighted counts on a sample of changed lines, not
a population estimate.

This is the measurement §5 and the plan's Phase 3 were both blocked on, and it is exactly the
argument for having demanded it: the smoke test's 0/42 on vocabulary and notation contained no
Roman numerals at all, so the class was invisible to it.

#### The cause, and the narrowing that answers it

Traced clause by clause on `1fca8eb` — **the thread's diagnosis is incomplete**. It attributes
the breakage to the triple-character clause reading `III` as a stutter. That reaches 5 of the 7
named lines; the full trace is:

| line                                                                                           | clause(s) that fire                       |
|------------------------------------------------------------------------------------------------|-------------------------------------------|
| `Sonda VIII/3`, `12.VIII.1977,`, `166. Hr.XLIII.1.`, `Lokalisace: I-VIII-eneol.II`, `w XVIII.` | vowel run **and** triple character        |
| `CCV. CCVI.`, `205; CCLXII).`                                                                  | **initial consonant geminate** (the `CC`) |

`I` is a vowel, so `III` is also a 3-vowel run. **Exempting the triple clause alone would fix
nothing** — the vowel-run clause still convicts `VIII`.

`_has_shape_garbage_evidence()` now carries a subtoken-level Roman-numeral exemption placed
**above all four clauses**, reusing the existing `_RE_ROMAN_TOKEN` (`^[IVXLCDM]{1,7}$`). Measured
here: **all 7 named broken lines stop firing, all 5 named fixed lines keep firing**, and every
positive the predicate's comment block names survives — `oueussd`, `cuxoaid`,
`IDIDIDIDIDIDUOID`, `NINNNIC`, `Tthts`, `rragment`, `vansasaasasa`, `lllll`.

Uppercase-only is deliberate: `RE_ROMAN_NUMERAL` accepts lowercase, and every letter of `lllll`
is a numeral glyph, so it would exempt real garbage. Known cost: a 7-glyph all-numeral stutter
(`DIDIDID`) is now exempt; the length cap keeps the real `IDIDIDIDIDIDUOID` convicted. Pinned in
`tests/test_text_utils.py::TestShapeGarbageWitness`.

**This narrows the predicate; it does not enable it.** `SHORT_GARBAGE_WITNESS_ENABLE` remains
`false`, the four constants stay in `_DELIBERATELY_NOT_TUNABLE`, and the flag-off suite is
unchanged (1038 → 1053 passing, the 15 new tests; 13 skipped / 2 xfailed either way). **The
12/12 measurement has not been re-run against the narrowed predicate** — that needs the 508
scored through `_rescore_row`, which needs the delivered batch. Until it is, the claim here is
only that the named broken class no longer fires, not that the flag is ready.

> **2026-09-17:** the re-score is no longer blocked — the rows are on the cluster (R2). The
> measurement is step 5a of [`../../30.runbook.md`](../../30.runbook.md), and it is now able to
> express the flag at all: `ab_constant_eval --values` parsed floats only, so the one tool written
> for this comparison could not take `false,true`.

#### The second false-positive class, and the exemption that answers it

The roman numerals were the class the 508 exposed. The one below was named in the code, left
unmeasured for a month, and is larger.

The design block above `_has_shape_garbage_evidence()` recorded it as a known weakness and
explained why it was tolerable: *"Most carry `weird_ratio == 0.0` and so never reach the route at
all — a real but THIN margin, since it depends on a signal outside this predicate."* Measured on
the predicate itself, 2026-09-17:

| class                             | result                                                                                                                                                             |
|-----------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| botanical family names (`-aceae`) | **10 of 10 convicted** — `Poaceae`, `Rosaceae`, `Fabaceae`, `Brassicaceae`, `Cyperaceae`, `Chenopodiaceae`, `Asteraceae`, `Betulaceae`, `Fagaceae`, `Polygonaceae` |
| other loans                       | `Naiade`, `Beuern`, `Oueste` convicted                                                                                                                             |
| binomials, genera, osteology      | `Equus caballus`, `Pinus silvestris`, `Occipitale`, `Phalanx proximalis`, `Ossa tarsi`, `radius prox.sin.` — clean, before and after                               |

`-aceae` is the botanical **family** suffix. The clause was therefore not making an occasional
mistake about a rare spelling; it was convicting the naming convention of a taxonomic rank, in
the collections whose archaeobotany reports are where that rank appears. Relying on `weird_ratio`
to keep it out of `Trash` is relying on a signal this predicate does not control, which is what
the original note said and is the reason it was listed as a precondition rather than a follow-up.

**The response is an exemption, placed exactly where the roman-numeral one is and for the same
reason** — `-aceae` is a vowel run now, and on a longer name it is one clause away from the
others, so exempting a single clause fixes nothing. Closed pattern over the token's letters
(`aceae|oideae|eae|iae|aea|oidea|idae|inae`), with a length floor.

Measured cost: **zero**. Every named garbage line still fires — `oueussd`, `sektlll`, `cuxoaid`,
`rragment`, `vansasaasasa`, `NINNNIC`, `Tthts I`, `IDIDIDIDIDIDUOID`, `lllll` — and every
roman-numeral line the earlier exemption cleared still passes. Pinned by
`tests/test_shape_witness_vocabulary.py`.

What it deliberately does **not** reach: `Naiade` (`aia`), `Beuern` (`eue`), `Oueste` (`Oue`).
Those are name-shaped rather than suffixed, and inventing a "legal vowel sequence" list to catch
them is the same guessing-by-reading that cost twelve lines the last time this predicate was
widened.

> **They were handed to the lexicon, and the lexicon does not take them.** Measured against the
> real table built on the cluster (822 documents, 12.7M lines, 2026-09-17): `naiade` df 0,
> `oueste` df 0, `beuern` df 1 — none reaches `SHORT_GARBAGE_LEXICON_MIN_DF`. They are my own
> hand-built probes, not corpus lines, so attestation has nothing to say about them and the
> predicate convicts all three exactly as it did before. **This class is open, not solved.** It
> may close on the full collections (this table is 822 of 113,101 documents), and the honest
> statement until then is that three named loans remain convicted with no mechanism reaching
> them. See §6.

One knob came with it: `SHORT_GARBAGE_WITNESS_VOWEL_RUN_MIN`, defaulting to `FUSED_VOWEL_RUN_MIN`
so behaviour is unchanged. The two were the same constant, and `detect_fused_words()` feeds
`fused_ratio` in the quality score and the `fused_words` CSV column — so the only clause that
discriminates on this population could not be tuned without moving scores on every line in the
corpus. The trade at 4 is measured and asymmetric: `oueussd` (`oueu`) stays convicted, `cuxoaid`
(`oai`) escapes with no other clause catching it, and the three unsuffixed loans stop firing.
Gold decides it (runbook step 5b), not a reading.

### 3.8 Supporting corpus statistics

`is_domain_notation()` over both collections, 113,101 documents, 25.6M scored lines:

|                                                            | lines          |
|------------------------------------------------------------|----------------|
| notation, scored                                           | 237,820        |
| of those, `orig_lang_score` < `HARD_SWEEP_LANG_MAX` (0.45) | 77,936 (32.8%) |
| of those, currently `Clear`                                | 58,427         |
| of those, `perplex` ≥ `HARD_SWEEP_PPL_MIN`                 | **0**          |
| of those, sitting exactly at `SHORT_PPL_CAP`               | 50,221         |

Against a corpus base rate of 11.7% below 0.45, notation is ~2.8× more likely to fall under `HARD_SWEEP_LANG_MAX`. Keeping `rule_hard_sweep` armed is therefore safe — but **only while the cap stands**: those 58,427 lines are `Clear` because of the cap, not because the language gate cleared them. Anyone lifting `SHORT_PPL_CAP` inherits that dependency.

#### How large that dependency is — answered

Audit #8 left this open (*"raw perplexity … can give exact volumes"*). The delivery now
answers it, over the 367,208 changed lines, via `tools/issue30_perplex_report.py`:

|                                                 | lines               |
|-------------------------------------------------|---------------------|
| lines the cap applies to (`wc <= 2`)            | 299,575             |
| of those, **raw perplexity above the 850 cap**  | **168,104 (56.1%)** |
| of those, within ±5% of the cap — **undecided** | 3,445 (1.1%)        |

**The cap is load-bearing for well over half the short-line population.** Lifting
`SHORT_PPL_CAP` changes what the short-line route reads on 168,104 lines, not on a fringe.

> ⚠️ Inherits the delivery's fidelity caveat: `perplex_raw` was recomputed at batch 32 rather
> than the configured 128, and reproduces the stored value to a median 2.9% / p99 13.5% in whole
> bf16 steps. The ±5% band is reported precisely so nothing within reproduction noise of the
> threshold is quoted on one side of it.
>
> **The source file is not in this repository and must not be published** — it is the whole
> scored corpus with document names and line text. `.gitignore` blocks the filename; the report
> tool emits counts and shares only.

## 🚫 What this change is *not*

Two framings were tried and abandoned, and both are worth keeping on record because both survived review once.

**The symmetry argument does not hold.** The proposal was framed as "`rule_short_garbage` is the seventh sibling of six gated rules". Reading `50472fb`'s diff, it added `qs < thresh_trash` to sites that were *already* gated and never touched this rule. Those six live in §9, "explicit diagnostic hard gates" — late, weak, corroborating signals. `rule_short_garbage` sits in §6 among eight **early-exit** rules that all return unconditionally. It was never a guard that got skipped; it is a rule in the decisive family, which is a heavier burden, not a lighter one. The patch is not symmetric with those six anyway: they end in `check_rescues()`, this one returns `Trash` directly.

**"367,208 lifted / 0 demoted" is a theorem, not a measurement.** `determine_category` returns only `Empty`/`Trash`/`Noisy`/`Clear`, so removing an unconditional `return Trash` cannot demote anything. The direction was never in question. That figure is correct and carries **no evidence about whether the change is right** — only §3.2 does.

## ⚠️ Known edge cases

### Accepted debt, tracked

- **`oueussd`** — genuine OCR garbage that now reaches `Clear`. Recorded as a `strict=True` xfail keeping `Trash` as the expected answer (`tests/calibration_fixtures.py:156-170`), *not* a re-baselined expectation. It becomes `XPASS(strict)` — deliberately red — the moment the narrowing lands, in **both** `test_inverted_trash_stays_trash_at_default` and `test_inverted_trash_stays_trash_swept_bounds`.
- **The three de-blinded golden pins** — `malakofauna`, `Equus caballus` and `oueussd` all move to `Clear` in `tests/fixtures/pipeline_characterization.json`. They share every signal the pipeline computes, so all three move together under any gate on this rule. Two are the intended effect; the third is not. The trade in a single artifact.
- **`VOCABULARY_SHORT`** — the same flip pinned at fixture level, `Trash` → `Clear` for all three rows.

### Deliberate non-reuses — each with its counterexample and its test

Four signals look like they should feed the witness and must not. Each was measured, and each has a test naming the counterexample.

| rejected signal                                               | breaks on                                                                        | test                                        |
|---------------------------------------------------------------|----------------------------------------------------------------------------------|---------------------------------------------|
| `_RE_FUSED_CONSONANT_RUN` (5+ consonants, `text_util.py:769`) | `vrstva`, `vrstvy`, `vrstvou`, `vrstvami`, `ctvrtek`, `ctvrt`, `zmrzl`, `scvrkl` | `test_consonant_run_czech_is_not_witnessed` |
| `len(core) > 14` — `detect_fused_words`' third clause         | `Skelettmaterial`                                                                | `test_long_compound_is_not_witnessed`       |
| `_has_repeated_run()` / `detect_repeated_chars()`             | `Kaaden` (aa), `Pinii` (ii) — both #30 fixtures                                  | —                                           |
| bare `^(.)\1`; uncapped triple test; ungated geminate test    | `Aachen`; `Schifffahrt`; `mm`                                                    | `TestShapeGarbageWitness`                   |

The first is the important one. **`vrstva` ("layer") is the commonest noun in archaeological field documentation**, and `vrstva 3` satisfies every condition of gate 6 at production signals — so a witness carrying the consonant-run clause would send clean Czech to `Trash`, which is the exact outcome this issue exists to prevent. Nothing is lost by dropping it: `sektlll` is reached by the triple-character test and `Tthts I` by the geminate test.

`detect_fused_words()` itself is left untouched for a separate reason: it feeds the `fused_ratio` term of the quality score and the `fused_words` CSV column, so narrowing it would move scores on every line in the corpus.

### Known false positives that remain

- ~~**Latin/French/German loans with a 3+ vowel run** — `Poaceae`, `Naiade`, `Beaune`, `Radiouhlik`, `Sauerstoffflasche` (verified 5/7 above). Most carry `weird_ratio == 0.0` and so never reach gate 6 at all. That is a real margin but a **thin** one, because it depends on a signal outside the predicate. Measuring this class is a precondition for enabling the flag, not a follow-up.~~
  **Measured 2026-09-17 (R1) and largely closed.** The precondition was met, and the class turned out to be the botanical family suffix `-aceae` in its entirety — 10 of 10 — not a scattering of loans. A taxonomic-suffix exemption landed at zero cost in convictions; see §3.7. What remains of this bullet is the three name-shaped loans the suffix rule deliberately does not reach — `Naiade`, `Beuern`, `Oueste` — which are now the lexicon's business rather than a future shape rule's, because deciding them by shape means deciding which vowel sequences a language is allowed to contain.
- **Label-lexicon dropouts** — closing `_RE_NOTATION_LABELLED`'s label set narrowed `is_domain_notation()` from 30 of the 508 graded lines to **24**, and all six that dropped out land in `Trash`:

| line                  | `8fadeb9` | `f8ad4d8` | annotation |
|-----------------------|-----------|-----------|------------|
| `Orientace: SZ-JV`    | Clear     | **Trash** | Clear      |
| `Orientace: SV-JZ`    | Clear     | **Trash** | Clear      |
| `Komponenta: H`       | Noisy     | **Trash** | Noisy      |
| `XIV: 7`              | Noisy     | **Trash** | Noisy      |
| `XII: 2`              | Clear     | **Trash** | Clear      |
| `Bokalisace: B-XII-c` | Clear     | **Trash** | Noisy      |

  Five of the six were right before. `orientace` and `komponenta` are not in the 22-item set; `XIV` / `XII` satisfy the label shape without being words. Weighted agreement of upstream alone moves 19.9% → 17.3%. It does **not** reach the patch — `f8ad4d8 + patch` is identical to `8fadeb9 + patch` on all 508 lines.
- **Notation is not exempt from `rule_hard_sweep`**, deliberately. The predicate accepts >50% of *capitalised* dot-chained garbage (`Vvbn.Slaot.Vansas.` against `Reg.Bez.Aussig.`), so it cannot carry a hard-sweep exemption. A vowel-per-segment rule leaves ~40% and kills `Kr.Hr.`/`St.Pol.`; a short-segment majority leaves ~64%. Pinned by `TestNotationIsNotExemptFromHardSweep`.

### Out of reach by construction — needs a lexicon, and now has one

`edelite`, `vfetennl k.`, `Slaot-o hiezazzt`. Phonotactically legal, so only word knowledge separates them from real vocabulary. Asserted as **part of the contract** in `test_the_disjunction_the_gate_will_evaluate_separates_the_population`, rather than quietly omitted — a lexicon-free approach has to own this residue, not pretend it away.

The honest form of the long-running claim is therefore: separating `malakofauna` from **`edelite`** needs a lexicon. Separating `malakofauna` from **`oueussd`** does not — that was the blocking claim for three rounds and it is false.

**2026-09-17 — the lexicon exists (D14).** Not an external dictionary: the Czech ones are
CC BY-NC-SA and the clause would follow this pipeline's output, and a Latin binomial list would
cover `Equus caballus` while missing `Reg.Bez.Aussig.` — and none of them contains the site codes,
context labels and excavation shorthand that make up most of this corpus's short lines.

The corpus is its own dictionary, and it was already on disk:

> A token that appears in many **distinct documents** is vocabulary.
> A token that appears in one is what that scan did to some ink.

**Document** frequency, not line frequency — OCR noise repeats freely inside the scan that
produced it, the same misread header on forty pages, and almost never across scans made years
apart by different operators. Counting lines would let one badly-scanned document vote its own
garbage into the dictionary. And it is a property of the **data**, not of the classifier: nothing
in the builder reads `categ` or `quality_score`, which matters because this issue has already
built one circular objective and a lexicon filtered by "lines we called `Clear`" would be the
same mistake wearing a different hat.

It is used two ways, and they are gated separately because they carry different risk:

| use                                       | what it does                                                                                                                                                                 | ships                                                   |
|-------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|---------------------------------------------------------|
| **veto** — `_has_vocabulary_support()`    | an attested token cannot be convicted. Exemption-only, so it can never add a `Trash` verdict. Reaches `Naiade`, `Beuern`, `Oueste`                                           | inert unless `SHORT_GARBAGE_LEXICON_PATH` names a table |
| **evidence** — the `no_vocabulary` clause | an **un**attested token convicts. The only mechanism in the module that reaches `edelite` and `vfetennl k.` — and the only clause of the witness that can *add* a conviction | `SHORT_GARBAGE_LEXICON_CONVICT`, its own key, `false`   |

The second is where the caution belongs: **"unattested" is not "not a word".** A genuinely rare
term, a personal name, or a table built too narrowly all land in the same bucket as the residue.

#### Measured, 2026-09-17 — the hypothesis holds, the threshold does not

@K4TEL built the table on the cluster: **822 documents, 12,716,706 lines, 347,097 distinct
tokens** at `min_alpha 4`, of which 54,462 (15.7%) reach df ≥ 3. Scored through the real
predicate:

|                            | vocabulary wrongly convicted (of 31 pinned fixtures) | named garbage not convicted (of 10)  |
|----------------------------|------------------------------------------------------|--------------------------------------|
| shipped default — no table | 0                                                    | 3 — `edelite`, `vfetennl k.`, `olie` |
| + veto, `min_df` 3         | 0                                                    | **4** — the above plus `cuxoaid`     |
| + conviction, `min_df` 3   | **5**                                                | 2                                    |

**The mechanism works and the setting is wrong.** Three separate readings:

* **Document frequency does separate the two populations.** `vrstva` 429, `kontext` 216,
  `malakofauna` 63, `quercus` 55, `corylus` 45, `equus` 44 / `caballus` 38, `maxilla` 33,
  `occipitale` 23 — against `oueussd` 1, `sektlll` 1, `rragment` 1, `ninnnic` 1, `edelite` 1,
  `vansasaasasa` 0, `tthts` 0, `vfetennl` 0. The residue **is** reached: turning the clause on
  convicts `edelite` and `vfetennl k.`, which nothing in this module could do before.
* **The botanical class is attested independently** — `poaceae` 23, `rosaceae` 5, `fabaceae` 4 —
  so the veto would have protected R1's class on its own. Two mechanisms agreeing from different
  directions is worth more than either.
* **At `min_df` 3 it breaks five of the 31 pinned vocabulary fixtures**: `Pinii`,
  `Uniocrassus`, `Aachen`, `Hannah`, `Emma`, and `Skelettmaterial` (df 2) outside that list.
  Personal names, place names and a rare taxon — precisely the failure the code comment
  predicted, arriving on schedule. In the other direction the veto **protects** `cuxoaid` (df 4)
  and `olie` (df 4), this repository's own canonical short-garbage example.

`SHORT_GARBAGE_LEXICON_CONVICT` therefore **stays false**, on evidence rather than on caution.
And the fix is probably not to tune `min_df` on this table: 822 of 113,101 documents is 0.7% of
the collections, so `Skelettmaterial` at 2 and `Pinii` at 1 are thin-corpus artefacts as much as
they are rare words. **Rebuild over both full collections before touching the threshold** — the
same command, and still no GPU.

One number to carry into that decision: **74.3% of distinct tokens are hapax** (seen in exactly
one document), 10.0% appear in two. The conviction clause fires on absence, so its reach at the
type level is three-quarters of the vocabulary of this corpus. That is not three-quarters of
*lines* — common words dominate lines — but it is the right order of magnitude to be frightened
of, and it is why this clause has its own key.

Built with `tools/build_token_lexicon.py` (CPU-only, no model, no GPU), which imports its
tokenisation from `text_util` rather than copying it — a table keyed differently from the way the
predicate looks tokens up would miss every lookup, and a missed lookup reads as "no vocabulary
support", i.e. as a witness that convicts *more*. That is the direction that costs real lines, and
nothing in the output would say so. Four harness divergences in this repository came from a copied
implementation (§5); this one is pinned by a test that compares the two paths token for token.

### Structural facts that constrain any future fix

- **No perplexity rule can fire at `wc ≤ 2`.** `SHORT_PPL_CAP` (850) < `HARD_SWEEP_PPL_MIN` (1,000) < `PPL_EXTREME_MIN` (3,000) < `PPL_GARBAGE_ABSOLUTE` (30,000). Perplexity is structurally unable to intervene at any value.
- **Section 7 (`rule_short_line`, `text_util.py:1380-1439`) is total for `wc ≤ 2`.** Every branch returns, so §8–§10 and `check_rescues()` are unreachable for that population. Its `damage` branch returns **`Noisy`, never `Trash`** (`text_util.py:1424-1425`) — which is why a witness placed there could never restore a `Trash` verdict.
- **`check_rescues()` (`text_util.py:1462`) cannot supply a `Noisy` floor.** All three of its floors — `_trailing_fill_rescued()`, `is_forgiven_headline()`, `is_clean_reference()` — evaluate `False` on all 11 lines tested, so it returns `Trash` for every one, `malakofauna` and `1 ks` included. Routing gate 6 through it buys symmetry and changes nothing.
- **`compute_valid_ratio()` (`text_util.py:2405`) is shape-only** — a token counts as valid on length ≥ 3, ≥ 70% alphabetic, no strange character, no mid-word uppercase. So `valid_word_ratio == 1.0` for this entire population, `oueussd` included. This is the suppression at the heart of the issue: section 7 already reads the discriminating witness inside its `damage` term and then vetoes it with `structurally_clean = valid_word_ratio >= 1.0`.
- **`rule_mid_uppercase` is unreachable**, shadowed by gate 7's identical `word_count <= 2` condition. Recorded in `UNREACHABLE_RULES` and pinned by `test_mid_uppercase_is_shadowed_by_short_line`.
- **Dot-chains still reach `Clear`** — periods collapse a line to `wc == 1`, the cap flattens perplexity, and gate 7 reads `valid_word_ratio >= 1.0` because `.` is in `ALLOWED_INTERNAL`. Same root cause as #30; deserves its own issue.
- **`service/text_api.py` double-imports its own app** — `uvicorn.run("text_api:app", ...)` re-runs the module body under a second name, so `app` / `_state` / `TextModelManager()` exist twice and the served `_state` is not the one `__main__` holds. Unrelated to #30; deserves its own issue.

### Harness divergence — the recurring failure mode

Three separate harnesses in this repository have fed `categorize_line` a value production never computes, and each produced confident wrong numbers before it was caught:

1. `tests/test_rotation_regression.py` fed the `LANG_SCORE_REMAP` cap (0.75) where production feeds the trust score (0.9163 on the `oueussd` fixture) — every assertion in the module was about a value that does not exist.
2. `_categ` / `_process_mocked_line` hardcoded `original_lang="ces_Latn"`, granting trust tier 1.0 where production applies `TRUST_TIER_UNKNOWN` (0.50). This is what made `Equus caballus` and `diapozitiv` look "green on master, therefore non-discriminating" — scored properly, all three are `Trash` and all three discriminate.
3. `tests/test_smoke.py` carried the same bug a third time, and additionally omitted `orig_lang_score` and `garbage_density`, leaving them at `1.0` / `0.0` and silently disabling `rule_hard_sweep`, `rule_extreme_ppl` and the density branch of `_has_strong_garbage_evidence`.

All three are fixed; `test_smoke.py` now routes through `_rescore_row`. `tools/short_garbage_witness_report.py` was described here as text-only *by construction* and therefore immune — and then produced the fourth instance anyway.

**4. `tools/short_garbage_witness_report.py` carried its own copy of the witness clauses.** It re-implemented all four so it could name which one fired, guarded by an assertion in `classify_line` that its diagnosis matched the predicate. The roman-numeral exemption (2026-09-10) landed in the predicate and not in the copy. The guard did exactly what it was written to do — it fired — on **0.74% of real lines**, which is every line carrying a roman numeral: `XVIII. '`, `23. VIII.1947.`, `580. Hr. VIII.4.`. The tool aborts on the first, so it was unusable on real data for precisely the exposure measurement the flag decision is gated on.

Fixed structurally rather than by patching the copy: `text_util.shape_garbage_clauses()` is now the single implementation, `_has_shape_garbage_evidence()` is `bool()` of it, and the report imports both the vocabulary and the logic. Verified over the full 367,208-line delivery: **0 crashes**. `tests/test_short_garbage_witness_report.py` gained the roman-numeral corpus and a test that fails if a local clause implementation reappears.

The lesson is the one this section already taught, with a sharper edge: *"text-only by construction"* was true and irrelevant. The bug was duplication, not signal reconstruction, and the guard caught it only because someone wrote the guard. The discipline is what prevents a fifth.

**5. The instrument that classifies the rules was scoring against itself** (2026-09-17, R1–R3 block). Not a harness fed a wrong value — a harness fed the *right* value and asked the wrong question. `rule_coverage_report._loo_metrics` never passed `gold_category_column`, so every `DEAD` / `REDUNDANT-HERE` / `LOAD-BEARING` / `clear_loss` figure was measured against the pipeline's own stored `categ`, which the offline re-score reproduces exactly — a baseline of zero, by construction.

It belongs in this section because the shape is identical: **the run completes, prints a full and plausible table, and is worth nothing.** The same file accepted `--gold-sidecar`, joined it, and printed "N labels matched" on the way past, so a gold run and a self-scored run were indistinguishable from the output. `tests/test_rule_coverage.py` contained no occurrence of the word `gold`.

Fixed with the same structural instinct as #4: the gold verdict is *added* beside the structural figures rather than substituted for them (forwarding the keyword would have redefined `decisive_count`, since `flip_count` counts disagreements-with-gold once a gold column is present), the JSON payload now records `gold_column` and `decisive_scored_against`, and a source-level test asserts across **all five** `evaluate_dataframe` callers that none of them can drop the keyword again.

A sixth of the same family, found in the same round and worth recording because it is the cheapest to repeat: pointing a recursive driver at `tools/gold/` swept the 2,067-row sidecar in as corpus lines and reported **2,082 lines instead of 15**, with a complete table and no error. `GOLD.md` had warned about it in prose since the sidecar arrived; `load_csvs` now skips CSVs with no `text` column and says so.

## ✅ What is settled

- **Perplexity cannot arbitrate this population**, and it runs backwards: FastText scores `oueussd` at 0.9163 and `malakofauna` at 0.56; with the cap lifted, real notation demotes at perplexity 3,000 while `oueussd` survives to 30,000. Recomputed values corroborate — `II/C` ≈ 6·10⁷ (correct) against `oueussd` ≈ 4,600 (garbage). No cap variant is separable (§3.5).
- **Notation is recoverable by shape**; vocabulary is not. `is_domain_notation()` handles grid refs, labelled refs, counts and abbreviation chains at 24 of 508 lines, 4.2% of the changed population.
- **`check_rescues()` cannot provide the middle state**, and section 7 — not gate 6 — is where 1–2 token lines are decided. **That is precisely why the witness does not belong there**: section 7 cannot return `Trash`.
- **No `wc`/character boundary isolates the errors** (§3.6). The question was asked and answered by measurement.
- **The suite can now see this class of change.** 814 → 891 → 954 passing; three golden edge cases that can actually see a gate on this rule; `_has_strong_garbage_evidence()` being `False` on the whole disputed population pinned on production vectors; a `Trash`-side counterpart to the swept-bounds guard.

## ❓ What is not settled

**Claims from the thread that were never closed.** Recorded here because "nothing else outstanding"
(2026-09-09) did not match what the other side was waiting on:

| #  | Claim                                                                                                                                                                          | State                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
|----|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| B2 | *"59.4% of hard-sweep-family firings land exactly on `wc == 3`. Not measured further, just noting it."*                                                                        | **Instrument now exists** — `tools/rule_coverage_report.py --by-wc` attributes every rule fire to the word count of the line that caused it. The figure itself still needs a real collection                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| B3 | Document post-processing moves ~27% of the graded lines and is absent from every quality figure                                                                                | **Caveat recorded** (§3.2); **no longer blocked** (R2). The annotation is in the tree and the rows are on the cluster, and `evaluate_dataframe` applies the smoothing by default, so scoring the sidecar through it *is* the document-aware figure. `rule_coverage_report --split-cascade` gives the same boundary from the rule side, separating each rule's per-line effect from the page cascade its removal triggers. Runbook step 7. Still unmeasured — but for want of a run, not a delivery                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| B4 | *"the residual looks like bf16 batch nondeterminism, though that is a hypothesis, not verified against the delivered run"* — 61.6% bit-exact, 0.30% cross a decision threshold | **Open, needs a GPU.** No owner                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| B5 | *"the 1930s grade 15 correct against 12 wrong … if this ships, that is the population to look at first"*                                                                       | ✅ **Closed 2026-09-10.** Computed from the delivery, no GPU: decade parses from the document identifier (36,137 of 36,268 documents, all years 1920–2024, zero outliers). Post-patch agreement with the hand gold by decade — 1920s **50%**, 1930s **35%**, 1940s 73%, 1950s 63%, 1960s 62%, 1970s 64%, 1980s 54%, 1990s 66%, 2000s 70%, 2010s **77%**, 2020s **78%**. The old scans really are the worst, and they are 0.3% of the changed population                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| B6 | Joint sweep `SHORT_PPL_CAP` 950 / `HARD_SWEEP_PPL_MIN` 500                                                                                                                     | **Open, now guarded and SIZED.** `short_cap_arms_hard_sweep()` reports when the move is made. Measured 2026-09-10: the joint move makes **202,037** `wc <= 2` lines (**67.4%** of that population) reach `rule_hard_sweep`'s perplexity condition, **141,261 of them currently `Clear`**. Upper bound — the delivery carries no `orig_lang_score`, so the rule's language half is not evaluable here. This is not a fringe experiment: it needs a Clear-loss guard and a per-trial arming flag, not just permission to run                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| B8 | The `qs < CATEG_TRASH_SCORE_MAX + 0.35` conjunct is *"measurably inert here … the first thing to drop"*                                                                        | **Kept.** It is inert on this population but not corpus-wide, and it is the only score gate on the route. Recorded rather than removed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| B9 | PR #48's *Known limitation* still asserts *"no character-level rule separates them"*                                                                                           | **False, and uncorrectable in place.** The two in-tree copies were reworded; a merged PR body cannot be edited. Noted here as the standing correction                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| A4 | 484 vs 508 graded lines — the 24 dropped lines are never explained                                                                                                             | ✅ **Closed.** Answered in [`#issuecomment-5619021486`](https://github.com/ufal/atrium-alto-postprocess/issues/30#issuecomment-5619021486) and now **verified against the delivered files**: exactly **484 of the 508** keys are present in the 367,208 population. The 24 absent are the `is_domain_notation()` lines upstream now lifts on its own — the same 378,735 → 367,208 shrink. All 24 had been graded correct lifts, so dropping them lowered the patch's figure slightly                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| R1 | The loan false-positive class — *"a precondition for enabling the flag, not a follow-up"*                                                                                      | ✅ **Closed 2026-09-17.** Measured, and it failed: 10 of 10 botanical family names convicted, plus `Naiade`, `Beuern`, `Oueste`. Taxonomic-suffix exemption landed at zero cost in convictions (§3.7). The three unsuffixed loans move to the lexicon                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| R3 | Whether any figure this issue produced about rule importance means what it says                                                                                                | ✅ **CLOSED 2026-09-22 by stage 6.** All 23 rules re-scored against the 2,064-row gold sidecar; baseline gold `Clear`-loss **40**, matching 08b from a separate job with a different tool. The numbers are regenerated and read in the stage-6 section. Two cautions travel with them: the LOO deltas carry **no significance testing at all**, and five of the seven "better without it" rules move less than a single gold row; and the sweep's own `DEAD` verdict on `rule_short_garbage_witness` was a flag artefact, fixed as D35. Historical reading follows. **Split in two on 2026-09-18, and only one half was ever wrong.** `decisive_count` and the `DEAD`/`LOAD-BEARING` classification are self-scored **and should be** -- they measure how many lines a rule moves, a property of the rule, not a claim about truth. `clear_loss` IS a correctness claim, and it was silently self-scored even with `--gold-column`, reading 0 by construction; fixed, and it had been blocking the 87h stage 6. The rest stands: **open until the gold-scored run exists.** `_loo_metrics` never passed the gold column, so the 23-rule sweep posted to the thread, and every `DEAD` / `LOAD-BEARING` verdict behind `RULE_COVERAGE.md`'s retirement criterion, measured agreement-with-self. The plumbing is fixed; the *numbers* have not been regenerated. Runbook step 6 produces the comparable pair |
| R4 | Does document frequency actually separate this corpus's vocabulary from its OCR noise?                                                                                         | ✅ **Answered 2026-09-17, and the answer is "yes, at the wrong threshold".** Table built on the cluster: 822 documents, 12.7M lines, 347,097 tokens. Vocabulary is strongly attested (`vrstva` 429 … `occipitale` 23) and the residue is not (`edelite` 1, `vfetennl` 0), so the clause **does** reach what nothing else could. But at `min_df` 3 it convicts 5 of the 31 pinned vocabulary fixtures — `Pinii`, `Uniocrassus`, `Aachen`, `Hannah`, `Emma` — and the veto protects `cuxoaid` and `olie`. Flag stays false on evidence. Next step is a rebuild over both full collections (this table is 0.7% of them), not a threshold tweak. §6. **The corpus-scale reading appeared to reverse the fixture one for the VETO half, and then did not survive de-duplication** — see the correction in the stages 1–4 block                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| R6 | `Naiade`, `Beuern`, `Oueste` — the loans the taxonomic-suffix rule deliberately leaves to the lexicon                                                                          | **Still open, updated 2026-09-19 (07e).** On the full 113,100-document collection all three now get an `edit1` `recoverable` suggestion (`naaden`, `bauern`/`beuten`, `ouest`) instead of df 0 — but `edit1` is the weakest signal this document uses, flagged elsewhere as possibly coincidental (`Linum`/`ilium`), so this is evidence worth having, not a close. They still have no mechanism reaching them and stay convicted. The wider finding is that the class is **Latin binomials generally**, not three loans — stages 1–4 block. `Skelettmaterial`, checked alongside them, turned out to be genuinely `attested` at full scale, so it was a thin-corpus artefact rather than a member of this class after all                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| R5 | The rotation-heavy offline-vs-live divergence — 95 lines in ARÚP, 10,258 in ARUB                                                                                               | **Open, and the largest item that appears in no other document.** Raised 2026-08-24, never re-measured after the `score_line` unification and the `slk` trust-tier fix. Runbook step 2 is that measurement: a plain re-score reporting non-zero *is* the figure. It also underlies @DanaKriv's review, which compared against the shipped `2026-07-29` batch rather than a re-run                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |



**The one measurement that matters most has still not been made.** The witness's 9/9-vs-0/37 separation is a smoke test on the lines this thread printed. The in-tree corpus is **15 lines, 5 of them 1–3 tokens**, and of those exactly 2 reach the route's entry condition — both already `Trash`, with no rare-vocabulary line among them to expose a false positive. Nothing about this change can be validated locally. The flag stays `false` until it is measured on the 508.

~~Every offline tool in `tools/` grades against the pipeline's own stored `categ`, so a label-changing improvement scores as pure damage on all of them. Without a gold set wired in, the narrowing cannot tell success from regression.~~

**Superseded 2026-09-17 (R3), and it was wrong in both directions.** Four of the five drivers had
been wired to `--gold-column` since the sidecar arrived. The fifth —
`rule_coverage_report._loo_metrics`, the one that classifies rules `DEAD` /
`REDUNDANT-HERE` / `LOAD-BEARING` and the one `RULE_COVERAGE.md` cites as the
retirement criterion — never passed it, while accepting `--gold-sidecar` and printing
"N labels matched" on the way past. So the sentence was too pessimistic about the sweep
drivers and far too optimistic about the instrument behind every rule verdict in this
issue. All five are wired now and a source-level test holds them there; the *numbers*
still need regenerating (R3 in the table above).

## 👥 What is needed from @DanaKriv and @david-spacil

Written out in full because the asks have been vague before, and because one of them is now
the single binding constraint on this issue. **Everything else here is measurable without
them; these are not.**

> 🔁 **Rewritten 2026-09-22, and this time the files exist.** The ask has been rewritten
> twice. On 2026-09-20 five overlapping requests were measured down to **293 decisions**.
> On 2026-09-21 that ask was put **on hold**, because T1 showed it had been sized against
> a population the project does not ship. 09b/09c have now re-cut it from the lexicon-on
> queue: **357 decisions**, delivered in the repository, **hold lifted**. The earlier
> paragraphs are kept below the line for the record.

### The one that unblocks everything — @DanaKriv

**357 decisions**, in `docs/issue30/census.csv` and `docs/issue30/sample.csv` as of
[`d9c41a5`](https://github.com/ufal/atrium-alto-postprocess/commit/d9c41a5). The
population they are drawn from is **5,563 strings / 6,714 lines** — every line the
pipeline keeps today that arming the witness would newly discard. Everything else the
witness touches is already `Trash`, where a label cannot move the decision.

| file         | rows |                                                        settles / represents | how to read it                                                                 |
|--------------|-----:|----------------------------------------------------------------------------:|--------------------------------------------------------------------------------|
| `census.csv` |  157 | 1,217 lines (**18.1%**; 15.9% if the 39 `confirms_trash` rows are excluded) | Complete, not sampled. 60 frequency rows + 97 contested + 39 controls.         |
| `sample.csv` |  200 |                                                      represents 5,646 lines | Random, stratified. Keep `frame.json` — without it the 200 rows are anecdotes. |

**357 decisions cover 516 distinct spellings**, because the census folds punctuation and
spacing variants into one row: `Dauerleihe` alone folds 24.

**Fill `gold_categ` only** (`Clear` / `Noisy` / `Trash` / `Non-text` / `Empty`); leave
anything uncertain blank — blank is skipped, never guessed. `confidence` and `note` are
free text. The full instructions, written for a reader who has not followed this thread,
are in [`../../docs/issue30/issue30_annotation_guide.md`](../../docs/issue30/issue30_annotation_guide.md),
with the folder index at [`../../docs/issue30/README.md`](../../docs/issue30/README.md).

**Why this is the constraint, still.** Of the 48,909 lines this rule reaches in the
shipped configuration, the ones that carry a human label are the 2,064 in the existing
gold sidecar, and they overlap this queue barely at all. No amount of further engineering
moves that; only annotation does.

**What the head looks like now, and it is the reason the ask is worth doing.** The
largest single item is `Dauerleihe` — German for *permanent loan* — on 286 at-risk lines,
scanned perfectly correctly, convicted because a three-vowel run looks like damage and a
document-frequency table built from Czech archaeology has never seen the word. `FEUILLETON.`
is the same story. If the answers come back saying these are `Clear`, the conclusion is not
"the witness fails" but "the `vowel_run` clause is set one notch too low", which is a
one-constant change with a knob already built for it (V5).
🛑 *Superseded 2026-09-22 (X2, Z4): the one-constant change — a global `VOWEL_RUN_MIN` of 4 — was
measured against gold and is dead (errors unchanged, `Trash`-recall down). The answer is D44's
language split, built and measured at 12 fixes / 1 break; `Clear` answers on `Dauerleihe` and
`FEUILLETON.` would confirm the split, not a threshold.*

<details><summary>The 2026-09-20 version of this ask, superseded</summary>

**293 decisions, not 5,005 and not 20,078.** The reduction came from three measured
facts about the lexicon-off queue:

1. **Two thirds of it cannot change the answer.** Of 4,807 spelling families, **3,223 are
   already `Trash`**. Only **1,584 families / 15,041 lines** were *at risk*.
2. **The at-risk head is tiny and enormous.** 60 families reach **87.8%** of at-risk
   exposure; adding every contested family takes it to **93 decisions / 94.8%**.
3. **The tail must be sampled with a recorded frame, or it is anecdotes.** 1,506
   families averaging 1.1 lines each; 200 drawn at random estimate it to **±6.9 pp**.

Every figure in that list is measured over the population T1 showed the project does not
ship. The instrument was right; the population was wrong.

</details>

**Reading the evidence columns.** `nearest_attested` gives the closest word in the
archive's own vocabulary and how it was reached — `diacritics` (accents restored) is
near-certain; `confusion` (a known scanner confusion, `j`/`i`, `rn`/`m`) is strong;
`edit1` (one character different) **may be coincidence** — `Linum`/`ilium` are one edit
apart and both real Latin, and 07e's `Pinii` lookup landed exactly this failure mode.
`recoverability` is asymmetric the same way it is everywhere else in this document: high
means probably real text, but zero means only that this corpus has nothing to say —
`Kaukasus` and `Schuhleistenkeilbruchstueck` score zero and are perfectly good words.
Recoverable does not mean usable either: `ppole` recovers to `pole` and is an
abbreviation, not a scan error. Nothing proposes an answer; the labels are joined back
separately and inspected before anything is derived from them.

**The recoverability pack (`A`/`B`/`C`/`D`) is still worth reading, as a different view
of the same 2,424 strings.** It is the best available picture of *where the pipeline and
the corpus disagree*, and tab `A` — 690 families the pipeline discarded that the corpus
can reconstruct, `1 fraament okraie` for "1 fragment okraje" — is the concern nothing in
this issue has measured. It is **not** a second ask, and it should not be labelled
separately from `census.csv` / `sample.csv`; those two already cover the strings in it
that can change a decision.

🛑 *Status 2026-09-22: the dedup call below was answered by @david-spacil (§ 2, option 1 — keep the
step; W1) and is open only for @DanaKriv's dissent (guide § 7.1; Q2 in the thread). Its figures are
superseded by the full-archive ones in V6: 43,103 groups, 10 readable lines pulled down against 51
rescued.*

**Second — a policy call only an archivist can make.** Convicting a few occurrences of a
repeated string demotes **all** its occurrences in that document, correct ones included.
Is that acceptable for the archive, or should a bare plurality never be allowed to demote
`Clear` → `Trash`? **The measured reach of this is 7 groups / 22 lines in the witness
queue (S4)**, and removing it would also give up the cascade's protective effect (it
removes 15 baseline `Clear`-losses and two of the witness's three breaks). So the honest
form of the question is not "is this dangerous" but "is 22 lines of this kind of error
acceptable, given that fixing it costs elsewhere". Still a decision about what the output
is for, not a measurement.

**Third — re-review the rotation-heavy divergence (R5) against a fresh re-run.** The
2026-07-29 review compared against the shipped batch rather than a re-run, so 95 lines in
ARÚP and 10,258 in ARUB have never been assessed on current code. K4TEL produces the
re-run first; this ask is queued behind that.

### @david-spacil

🛑 *Status 2026-09-22: item 1 is "after the tag" (W1) — and since D40 and D44 landed after
`v1.5.1-beta`, which revision to re-check against is Q7 in the thread. Item 2 is **answered**:
option 1, keep the step (W1; H6 closed). Item 4 is closed (W1).*
✅ *2026-09-23: **item 1 is closed.** Q7 answered `master`, and his direct re-score of the 508
reads 336/508 on `3b02959` — 11 fixed, 1 broken, 3 wrong either way — after the D45 restore
(§ "D45"). One new, optional question is his alone: Q8, the plan's H14.*

**1. Independently confirm your own break class is closed.** The round-2 run bounds breaks
within your 508 at ≤ 1 by set inclusion (they are a subset of the 2,064), which is sound but
indirect. It was your finding and your tooling; a direct re-score of the 508 alone closes it
properly. What changed since your measurement: the roman-numeral exemption, the
taxonomic-suffix exemption, the `S-VIIIb` fused-grid-reference exemption, and the
de-gemination guard.

**2. The dedup decision — three options, now costed on BOTH sides, and the choice is not
K4TEL's.** Stage 5f has landed: the per-line effect, cascade removed, is **+2 Clear-loss
for −11 errors at p = 0.013** — not the +1/−13/p = 0.001 placeholder this list originally
quoted from Round 2's with-cascade run. What is new since (S4) is the size of the thing
option 2 would fix and the direction of the cascade's own effect:

|                                                            |                |
|------------------------------------------------------------|----------------|
| groups the modal dedup votes on, in this queue             | 5,222          |
| of those, contested at all                                 | 22 (755 lines) |
| of those, a **bare plurality** — what option 2 targets     | **0**          |
| `Clear` lines the vote destroys / `Trash` lines it rescues | 17 / **31**    |
| the witness's breaks, cascade **on**                       | **1**          |
| the witness's breaks, cascade **off**                      | **3**          |
| baseline `Clear`-loss, cascade on / off                    | 40 / 55        |

1. **accept** +2 Clear-loss for −11 errors, p = 0.013 — and note that with the cascade in
   place the observed cost is +1, not +2;
2. **stop a bare plurality demoting `Clear` → `Trash`** in the modal dedup — **a no-op
   on this population.** Not one of the 5,222 votes is decided by a bare plurality, and a
   tie already cannot land on `Trash`. It would also give up a cascade currently rescuing
   31 `Trash` lines for the 17 `Clear` ones it destroys, and removing two of the
   witness's three breaks. The two lines that motivated it (`CTX194503132`,
   `MTX197902086`) are among the ones the cascade already smooths over;
3. **A/B the page-smoothing keys**, all eight already config-driven — the option that
   costs nothing to try and is still not run.

On the delivered numbers the ranking is 1, then 3, then 2, and option 2 looks like a
production change bought at 22 lines. But changing a smoothing step that touches ~27% of
this population on a reading rather than a measurement is how this issue has gone wrong
three times, so nothing moves before @david-spacil chooses.

**3. Gold on the witnessed population, if @DanaKriv cannot take the whole queue.** The 508
plus calibration 1,567 reach 23 of 20,078 witnessed lines. Same file, same format; the top
100 strings would be a substantial contribution on their own.

**4. Confirm whether anything further is expected from the raw-perplexity delivery.**
✅ **Closed 2026-09-22 (W1, § 8.1): nothing further was intended.** The 367,208 changed lines
arrived via filesender and were read through `tools/issue30_perplex_report.py`. Kept here as the
record of what was asked; it is no longer an open action.

### What K4TEL does, so the split is clear

🛑 *Superseded 2026-09-22: stage 6, stage 8 in full, 9b/9c, stage 10 and stage 11 have all run and
been read (U, T, V, X, Y, Z sections). What is left on this side is the R5 re-run, the second half
of 9c (a re-cut at `--strong-df 113`), a possible confidence floor on `_vowel_run_min_for()` (Z5),
and projecting whatever annotation comes back. The table below is kept as the record.*

Stage 6 (the gold-scored rule sweep, unblocked by the `gold_clear_loss` fix — running), the R5
re-run, and the projection of whatever annotation comes back. Stage 5f, stage 7 and the
full-collection lexicon rebuild are done. Stage 8 is the short list the stage-7 re-read
produced, and **none of it needs Dana, David, a GPU or new annotation**:

| run    | what                                                                                                                                               | settles                                                                      |
|--------|----------------------------------------------------------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------|
| **8a** | `ab_constant_eval` over `SHORT_GARBAGE_WITNESS_ENABLE`, `--values false,true`, stage-7 tree, ONE directory, plus a `--no-postprocessing` companion | Whether the adoption gate passes (S5) **and** the 42→41→40 drift, in one run |
| **8b** | re-run 07b now that `_CACHES_FROM_FLAG` arms the flag                                                                                              | D26 — currently unmeasured, not null (S2)                                    |
| **8c** | re-run 07c on the same fix; then the per-collection concentration of the eight geminates                                                           | D27, and whether the de-gemination guard has a portable signal at all (S1)   |
| **8d** | `short_garbage_witness_report --by-group` — the witness's effect per (document, string) vote, not per line                                         | Gives H8 option 2 a corpus-wide number instead of a 22-line local one        |

**No flag flips until 8a exists**, and after 8a the flag decision is probably makeable
either way. That is a change from the previous state: what was blocked on annotation is
now blocked on one cheap run, and annotation is what makes the *result* trustworthy
rather than what makes it possible.

### Two fixes landed outside the annotation gate — neither touches the flag, so neither waited

**`quality_word_set()`'s cache (2026-09-20).** The same bug class as the one below, one
indirection further in, and already in the tree when that one was fixed. A zero-argument
`functools.lru_cache` freezes a flag's value on its first call exactly as a derived module
constant freezes it at import, and `ab_constant_eval.py` runs both arms in one process — so
07b's bit-identical result was an unarmed flag, not a null (S2). Fixed by `_CACHES_FROM_FLAG`
in `override_constants()`, clearing on entry and on exit, with a **source-level guard test**
that fails if a new zero-argument cache appears in `text_util` unregistered. That guard is the
part that matters: `_DERIVED_FROM_FLAG` fixed an instance, this fixes the class.

**`SHORT_GARBAGE_LEXICON_GEMINATE_MAX_DF` does not rescale with its table (2026-09-20).** Not
a bug in the guard — a bug in reading it. The constant is an absolute document count fitted to
a 4.4× gap on an 822-document table, and pointing `SHORT_GARBAGE_LEXICON_PATH` at the
113,100-document table silently makes it mean something else (right on 2 of 7 tokens instead of
7 of 7). **The value is unchanged**, deliberately: eight tokens is not a population to re-fit a
production threshold to. What landed is `geminate_cap_scale_warning()` on stderr, corrected
figures in the code comment and `setup/config.txt`, and a test pinning both tables. S1.

### One fix landed outside the annotation gate — it doesn't touch the flag, so it didn't wait

`override_constants()` did not rebuild constants derived from the flag it was overriding — the
same bug class as the config-path fix in the 2026-09-10 hardening pass, found this time by
chasing 07c's zero-effect result instead of auditing latent code. `_STRIP_CHARS` (D27) is built
once from `STRIP_SYMBOL_GLYPHS` at import time; overriding the flag alone left it untouched, so
07c's true-vs-false A/B never actually changed what any `.strip(_STRIP_CHARS)` call site saw.
Verified directly, fixed with a small `_DERIVED_FROM_FLAG` mapping that rebuilds and restores
`_STRIP_CHARS` alongside the flag, and covered by a new regression test
(`test_symbol_glyph_stripping_actually_arms_under_override`) exercising the override path the
old ships-off test never touched. Full suite green apart from two service-API modules this
environment has no `fastapi` path to, which is unrelated. **07c's "zero effect on gold" is void
and needs re-running before D27 can be read either way.** This is instrumentation, not a
category or scoring change, so it did not wait on H1/H2 the way a flag flip would.

## 🏁 Bottom line

The direction was right, it was cheaper than it looked, and it stopped being an argument and became measurements. Four independent gradings across three base commits agree at roughly 76–78% right against 11–16% wrong, and @DanaKriv's review of both collections fully recomputed came down in favour of adopting it.

What shipped is a **5.5 : 1 improvement carrying ~26,000 garbage lines into `Clear`**. That was accepted deliberately, in the open, with the strict xfail as the device that will go red the moment it is paid down.

The narrowing is now a **two-line change that is no longer blocked**. `_has_shape_garbage_evidence()` is in the tree, tested, inert, and its call site — the conditional the merge just created at `text_util.py:1361` — finally exists.

~~**And as of 2026-09-20 it is one run from a decision rather than one delivery.**~~
**Answered 2026-09-21 — the run happened, and it passes.** 08b is the paired A/B over the flag
itself: errors 513 → 503, `Clear`-loss **40 → 40**, cost 0.2917 → 0.2829, McNemar p = 0.01294.
The 42 → 41 → 40 drift resolves to 40 in both arms of one run, which is the answer to why it
used to be 42: three different trees, not three different measurements of one tree.

**And the decision moved anyway, for a reason nobody was looking for.** The exposure the flag
would be armed against, and the annotation ask sized from it, were both produced with the
vocabulary lexicon switched off — a configuration `setup/config.txt` says outright will never
ship. Roughly three quarters of the at-risk population it measured is exempt the moment the
lexicon is configured (T1). The flag decision is not blocked on annotation any more, and it is
not blocked on engineering; it is blocked on measuring the right population once, which is
stage 9.

~~**Two things still have to be requested rather than derived**~~ — ✅ **the annotation files arrived on 2026-09-10.** Both sets are in the tree as `tools/gold/sidecars/issue30_gold_2067.csv` (2,067 rows over 816 documents, 508 + 1,559), wired to the existing `--gold-column` path through `--gold-sidecar`. Raw perplexity for all 367,208 changed lines arrived with them and is read via `tools/issue30_perplex_report.py`; the file itself stays out of the repository.

~~**What actually remains:**~~ **Revised 2026-09-17.** Items 1 and 2 were blocked on a delivery that had already happened; item 3 was disputed and is now closed.

1. ✅ **Done 2026-09-18 — it passes the measurement, not the criterion.** The guarded re-run fixes 14 and breaks 1 (p = 0.00098), errors 513 → 500, cost down; rejected on Clear-loss +1, which the round-2 block shows is not the witness's at all. @david-spacil's roman-numeral break class is closed. ~~The 12/12 result predates the Roman-numeral exemption, and now the taxonomic one too.~~ ~~that needs the delivered `2907` batch, which is not here~~ — **the rows are on the cluster** (R2). This is runbook step 5a, and until 2026-09-17 the tool could not even express the comparison: `ab_constant_eval --values` parsed floats, so `false,true` was unsayable. **Until this is run the flag stays `false`**, and now that is a scheduling statement rather than a blocked one.
2. **A document-aware re-grade.** Every quality figure in §3.2 is per-line; `apply_document_postprocessing()` moves ~27% of these lines in production. ~~it needs the batch~~ — `evaluate_dataframe` applies the smoothing by default and the rows are there, so this falls out of runbook steps 1 and 6. `--split-cascade` measures the same boundary from the rule side.
3. ~~**The `30.audit.md` / `30.strategy.md` attachments**, still not in the tree.~~ **Closed as disputed.** @david-spacil replied that he took these requests for *"AI agent artifacts"* and noted they were K4TEL's own attachments from earlier in the thread ([`#issuecomment-5619021486`](https://github.com/ufal/atrium-alto-postprocess/issues/30#issuecomment-5619021486)). He was right that the request was circular. `30.review.md` supersedes the 2026-09-09 "they check out" confirmation, and this document plus [`../../30.runbook.md`](../../30.runbook.md) supersede the pair.

**And three new ones, all measurable and none blocked:**

4. ✅ **Done 2026-09-22 — stage 6 ran, 87h, and R3 is closed.** All 23 rules gold-scored against the 2,064-row sidecar. Baseline gold `Clear`-loss 40, cross-checked against 08b. **`rule_short_garbage` destroys 7 of those 40**; `rule_short_line` protects 31; `rule_reference_floor` protects 10. The "seven rules are better removed" column is mostly noise — the report has no significance testing, and five of the seven move less than one gold row. Its `DEAD` verdict on the witness was a flag artefact, fixed as D35.
5. ✅ **Done 2026-09-18, and re-checked 2026-09-19.** The token lexicon is rebuilt over both full collections (113,100 documents, 2,764,632 tokens, §"the full-collection lexicon"). The 07e lookup then settled one of the two named cases and left the other open: `Skelettmaterial` is genuinely `attested` at full scale — it was a thin-corpus artefact, not a rare word — while `Pinii` still returns only `edit1` neighbours (`linii`, `pizni`, `indii`), which is exactly the coincidental-match failure mode this document has already named, not a resolution (R6, updated).
6. **Re-measure the rotation-heavy divergence** (95 / 10,258), raised 2026-08-24 and never re-run after the `score_line` unification. Runbook step 2 is that measurement. It also underlies @DanaKriv's review, which compared against the shipped batch rather than a re-run.
7. ~~**The annotation — now the only thing that can settle the flag, and there are two queues waiting.**~~ **Revised 2026-09-20: one queue, and it is not the only thing.** There is one population (S3), and the ask against it is **293 decisions**, not 5,005 + 2,424 + 200. And the flag is no longer blocked on annotation alone: 07d already reports the adoption gate passing (S5), so **stage 8a — one A/B over the flag itself — may settle it without any new labels**. Annotation is what makes that result trustworthy across the other 20,055 lines, which is a different and still necessary job. See **What is needed from @DanaKriv and @david-spacil** above.

**And three found by re-reading stage 7 against its own delivery (2026-09-20):**

8. ✅ **Done 2026-09-21 — stage 8a ran as 08b, and the gate passes.** errors 513 → 503, `Clear`-loss 40 → 40, cost down, McNemar p = 0.01294, fixes 12 / breaks 2. 08c is the `--no-postprocessing` companion and shows the cascade is what makes it adoptable (`Clear`-loss 55 → 56 without it). What replaced it as the blocker is **T1**: 08f/08g were run lexicon-off, so the exposure and the ask behind the decision describe a population ~3.7× larger than the shipped configuration puts at risk. Stage 9 re-measures it.
9. ✅ **Done 2026-09-20 — the flag-freezing bug class, not one more instance.** `_DERIVED_FROM_FLAG` was a fix for derived module constants; `quality_word_set()`'s zero-argument `lru_cache` was the same defect already sitting in the tree, and it is what 07b measured (S2). `_CACHES_FROM_FLAG` plus a source-level guard test that fails on the next unregistered one. 07b and 07c both need re-running (8b, 8c).
10. ~~**The de-gemination guard needs a portable signal, and does not have one.**~~ **Closed 2026-09-21, in the negative.** 08a ran the per-collection experiment and it does not merely fail to separate — it **inverts**: `ppole`, the one confirmed abbreviation, is 3 ARUP / 226 ARUB, the most collection-concentrated token of the eight, because it is a convention of one institution's 2010s forms (T5). Neither ratio, nor absolute document frequency, nor per-collection concentration separates the classes. The guard has no portable signal left, and the remaining choice — keep it as a constant fitted to one table, or set `SHORT_GARBAGE_LEXICON_GEMINATE_MAX_DF = 0` and fall back to plain attestation — is @david-spacil's, because it turns on whether `ssuti` at 195 documents is still a scanning error. **✅ SUPERSEDED 2026-09-22 (W2/W3): the guard comes out.** @david-spacil: `ssuti`/`ssutí`/`ssutě` are an old spelling of *suť*, so the classes this experiment tried to separate were never one-against-seven — and the ratio gap the guard is fitted to has real language on both sides. He has no objection to dropping it, and V7 shows it fires on none of the eight at full scale. The choice described above is no longer a choice. Stage 11c.

---
_Digest of [ufal/atrium-alto-postprocess#30](https://github.com/ufal/atrium-alto-postprocess/issues/30). Revision of **2026-09-23 (second)**: @david-spacil confirmed **336/508 on `3b02959`** (15 moved: 11 fixed, 1 broken, 3 wrong either way; 0/508 change with the flag off), so the D45 restore is confirmed rather than expected and his item 1 in "What is needed" closes. The tree was then synced to the settled answers, text only apart from one parity fix: the advisory and the config witness block re-stated from corpus exposure (08f → 9b) instead of the retracted 1:3 / 1.7:1; `ppole` called an abbreviation wherever the tree still called it an artefact; the witness report now passes each row's `original_lang` to the witness, as production does since D44. Q8 is new. Nothing is armed. Previous revision of **2026-09-23**: adds § "D45" — @david-spacil's direct 508 re-check (335/508 on `master`, 11 fixed / 2 broken, H5 closed) and the regression one of its breaks exposed: both round-2 witness guards were deleted by `cc4990e` on the evening they were measured. The grid guard is restored and pinned by tests; S4's 05a-vs-05f comparison and the 5f paragraph carry 🛑 markers because they crossed the deletion. Previous revision of **2026-09-22 (seventh)**: adds § "After the tag" (AT1–AT3) — comment 61's `[allowed]` claim checked against the code and found to overstate the reach; four questions that were recorded as the data providers' and never asked, now asked as Q4–Q6 alongside Q1–Q3 and Q7; the collaborator documents re-synced to the tree. Stale status lines in "Where this stands" and "What is needed" carry dated 🛑 markers rather than rewrites. Previous revision of **2026-09-22 (sixth)**: adds § "Stage 10f + stage 11" (Z1–Z6). **Stage 10 and stage 11 are both finished and between them they close every open measurement in this issue.** Z1: per line the witness FAILS its own gate (`Clear`-loss 53 → 54, the tool prints REJECT), reproducing 08c — so the document-level dedup is a PRECONDITION for the witness and not a refinement of it, which makes @david-spacil's § 2 answer load-bearing. Z2: W6 closes, zero `ssut*` rows in the gold set. Z3: **X1 is confirmed** — exactly two gold-`Clear` rows are stored `Trash` and URL-shaped, which is the 40 → 38 shift precisely; the 42/41/40/38 drift is accounted for at last. The stage that confirmed it printed the WRONG VERDICT, because its pass condition counted every URL-shaped row instead of the ones stored `Trash` — this issue's own failure mode in miniature. Z4: **Y4 was wrong, in our favour** — the split gives **12 fixes / 1 break**, errors 503 → 502 and `Clear`-loss 38 → 37, both DOWN, so the adoption gate passes and does not need re-arguing. Y4's arithmetic was sound and its premise was not: a line that reads German was detected Afrikaans. Z5 is why that is fragile — the exemption rests on a `deu` label at 0.359 confidence and `http://www.arub.cz` is detected Cantonese, so the detector's label on this population is largely noise; a confidence floor on `_vowel_run_min_for()` is the open design question. Z6: **44.5% non-Czech is the wrong number to read and 5.8% is the right one** — the tail is Vietnamese, Estonian, Xhosa and Uzbek, which is the detector failing rather than foreign text, and the split acts only on `deu`+`fra`. Previous revision of **2026-09-22 (fifth)**: adds § "Stage 10e — the adoption gate, post-D33" (Y1–Y4). **The gate passes on the post-D33 tree** — errors 513 → 503, cost −0.0087, `Clear`-loss 38 → 38, `Trash`-recall 12.2% → 18.9%, fixes 12 / breaks 2, McNemar p = 0.01294 — which is what 08b said before D33 and what stage 9a was owed to confirm. Y2 is the finding that matters for X1: `Clear`-loss is 38 in BOTH arms, and 08b reported 40 in both of ITS arms, so the shift is flag-independent and belongs to the tree rather than to the witness, which is the shape D33 predicts. Y3: the witness's only two mistakes on gold are both vowel-run cases, and they are the same two rows 10d called its fixes — one run's fix is the other's break. Y4 computes what the language split would do instead of guessing: both breaks are `vowel_run`-only and so are exactly two of the twelve fixes, so the split turns on four language labels and most likely yields 11 fixes / 1 break — errors 503 → 504, `Clear`-loss 38 → 37. It trades a gold-`Clear` error for a gold-`Trash` one, which improves the composition and not the count, **and the adoption gate as written would reject it** because +1 error fails the test whatever `Clear`-loss does. That is worth knowing before the A/B rather than after. Also landed in-tree since the fourth revision: `setup/word_lists.txt`, the hand-maintained word lists, with eight lists migrated into it and `[allowed]` — the open-class layer that never existed — wired to the quality score and shipping empty. Previous revision of **2026-09-22 (fourth)**: adds § "Stage 10d read against its own delivery" (X1–X6), recomputed from `10d_vowelrun_gold_ab.log` and the four rows of `10d_discordant.csv`. **Two findings change what is written elsewhere.** `Clear`-loss reads **38**, not 40 — the fourth value this quantity has had, but the first whose drift is explainable from the delivery itself: `errors` is identical at 503, so two rows left the `Clear`→`Trash` cell without leaving the error count, which is D33's signature exactly. Recorded as a hypothesis with its check rather than as a conclusion. And **a global `VOWEL_RUN_MIN` of 4 is dead**: the adoption gate is indifferent (errors +0, cost +0, `Clear`-loss +0, fixes 2 / breaks 2, p = 1) while macro_f1 and `Trash`-recall both move the wrong way — so the stage that was written into the plan as "a regression guard rather than the decision" turned out to be the decision. X3 is the one to read before the next run: on this gold set the language split is not a different experiment from the threshold, it is the same experiment conditioned on a label, and which way it lands depends on FastText's verdict on four lines nobody has looked at — two of which point opposite ways, so a wash is the honest expectation. X4 replaces stage 11b with a cheaper measurement that answers more: emit `original_lang` for the 2,064 gold rows. **Also landed in-tree this revision** (five commits, unpushed): the de-gemination guard removed (D40), the dedup tie-break pinned on the production function, `DOMAIN_NOTATION_CATEG` (D43) and the vowel-run language split (D44), both shipping off, and the annotation guide's § 7.1/§ 7.2 corrected. Revision of **2026-09-22 (third)**: adds § "@david-spacil's answers" (W1–W6). **Four items close and two of them correct this document.** `ssuti`/`ssutí`/`ssutě` are an old spelling of *suť*, not scanning errors — so the doubled-letter split is four legitimate against four damaged, six tables here and in `docs/issue30/` are corrected, and D30's negative result gets a cleaner reason: concentration separates institutions, and each institution has its own vocabulary. And V5 listed `J. Vysoean` and `POSTKRANIAINY SKELET` among the correctly-read strings a higher vowel-run threshold would spare, two sections after V4's own table called them damaged; corrected in place, and the honest trade at min=4 is 299 confirmed-good lines against at least 226 confirmed-bad. **T9 resolves in favour of legibility** (`Trash` = illegible, regardless of usefulness), pending @DanaKriv, which also explains the gold set's 45 `Non-text` rows for the first time. **D40 resolves**: the de-gemination guard comes out. **D44 is new**: gate the vowel-run clause on the detected language rather than raise its threshold — the label exists per line but is not passed to `determine_category()`. **W6 is the one nobody asked for**: the gold sidecar predates the `ssut*` correction, so `Clear`-loss 40 carries an unquantified asterisk until a cluster-side check clears it. Both corrections were of things already measured and read past, not things unseen. Revision of **2026-09-22 (second)**: adds § "Stage 8 f/g re-run read against its own delivery" (V0–V11), recomputed from the re-run's `logs8.log` and four CSVs and from the re-cut `docs/issue30/{census,sample}.csv` / `frame.json`. **Stages 9b and 9c are done and the hold on @DanaKriv's request is lifted.** T1's 73.1% estimate is corrected to a measured **82.1%** and T2's projection is superseded; both are kept in place with the measurement attached, because the estimate being visible next to the answer is the only thing that makes the next estimate better. Four findings are new: **D33's corpus-wide effect is measured at last** (15,505 lines out of scope, a floor, and it falsifies the `unreleased` release row's "no categorisation change"); **`vowel_run` is the sole reason for 64.6% of everything the flag can damage**, and `SHORT_GARBAGE_WITNESS_VOWEL_RUN_MIN` 3 → 4 spares every correctly-read item in the head while keeping the page-stamp family (estimate, not yet the predicate); **the at-risk head is now German museum vocabulary rather than the archive's own words**, `Dauerleihe` on 286 `Clear` lines; and **H6 option 2 is closed by measurement** — 0 groups, 0 lines. Two reconciliation gaps between 08f and 08g are recorded rather than chased. Previous revision of **2026-09-22**: adds § "Stage 6 read against its own delivery" (U0–U8, recomputed from `logs6.log` and `06_rule_coverage_gold.json`). **R3 is closed** — the 23 rules are gold-scored at last, and the baseline agrees with 08b across two independent jobs. Three findings change what is written elsewhere: `rule_short_garbage` destroys 7 of the 40 gold `Clear`-losses the flag decision turns on; the sweep nominated `rule_short_garbage_witness` for retirement on a `fire_count == 0` that is a fact about a flag rather than about the corpus (fixed as D35, with D36 and D37 alongside); and the "seven rules are better removed" column carries no significance testing, with five of the seven moving less than one gold row. T1 in the stage-8 section is upgraded from inference to confirmed — `issue30_stage8_job.sh` shows 08b/08c arming the lexicon and 08f not. Previous revision of **2026-09-21**: adds § "Stage 8 read against its own delivery" (T1–T15, recomputed from the eleven attached CSVs and `logs8.log` rather than from the write-ups of them) and rewrites § "Where this stands" around it. The stage-8 A/Bs settle four questions — the adoption gate passes (08b), D26 is a decisive reject rather than a null (08d), D27 is a genuine null rather than void (08e), and D30's last portable signal is refuted (08a). **T1 is the finding that changes what happens next**: the exposure pass and the annotation ask were run with the lexicon off, which is not a configuration this project ships, so the 592-decision census is sized against a population ~3.7× larger than the real one. Two findings made earlier in this same read — T6 and T7, first written down as predicate defects — were then corrected by re-measuring them in the shipped configuration, which is T1's own mistake caught on this document's own work; only the URL/e-mail gap survived as real (D33, fixed here). Earlier revisions are kept with their corrections attached. Previous revision of **2026-09-20**: adds § "Where this stands" (the readable summary this document did not have) and § "Stage 7 read against its own delivery" (S1–S5, computed from the thirteen attached CSVs and the two logs rather than from the write-ups of them), and rewrites § "What is needed" around one population and 293 decisions. Three claims below are marked superseded where they stand — D25 holding at scale, D26's null, and the pack being a second population — and the passages are kept rather than deleted, because how they were misread is the recurring lesson. Previous revision covered the post-merge state at `3afaddd`. Nothing removed from the issue itself; this is a derived reading aid for `agent_dev_logs/digests/`._
