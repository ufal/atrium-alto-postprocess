# [\#2 Issue](https://github.com/ufal/atrium-alto-postprocess/issues/2) `open`: Update definition of text categories and their assignment logic
**Labels**: `enhancement`, `development`


#### <img src="https://avatars.githubusercontent.com/u/65828620?u=80b39d3cb2967c52e1d4af28dcefc7a1efdfdc79&v=4" width="50">[K4TEL](https://github.com/K4TEL) opened issue at [2026-03-13 15:14](https://github.com/ufal/atrium-alto-postprocess/issues/2):

Maybe add regex for: numbers attached to letters without spaces, symbols inside words - use as a separate measure

Maybe ignore (poorly scored) language during category assignment

#### <img src="https://avatars.githubusercontent.com/u/65828620?u=80b39d3cb2967c52e1d4af28dcefc7a1efdfdc79&v=4" width="50">[K4TEL](https://github.com/K4TEL) commented at [2026-03-14 19:28](https://github.com/ufal/atrium-alto-postprocess/issues/2#issuecomment-4061205649):

Commit 884316e tries to solve it

Updated result files are included

#### <img src="https://avatars.githubusercontent.com/u/20186892?u=81493d22a8cb5fce679ac0e5e32fd98a04d777c6&v=4" width="50">[motyc](https://github.com/motyc) commented at [2026-03-19 18:16](https://github.com/ufal/atrium-alto-postprocess/issues/2#issuecomment-4092291895):

@K4TEL Based on the last data sample you produced, Dana and Tomas did a review. Here is the CSV with added columns with their expert opinion on what the caetgory should be:

[Vse_260318.csv](https://github.com/user-attachments/files/26123720/Vse_260318.csv)

I then used the file to run quick analysis using ChatGPT, which turned out to find interesting paterns which you can try to use in your implementation. Don't take it as connonical, but it is definitely worthy a try.

---

# OCR Quality Classification – Analysis & Recommendations

## Purpose

This document summarizes analysis of OCR outputs (ALTO-XML extracted text) and proposes a robust, implementable approach for classifying OCR quality into five categories:

- Clear
- Noisy
- Trash
- Non-text
- Empty

The goal is to align automated classification with expert judgment and make behavior predictable and tunable.

---

## 1. Key Findings

### 1.1 Agreement Patterns

- Strong agreement exists for:
  - Clear
  - Non-text
  - Empty
- Most disagreement occurs between:
  - Noisy vs Clear
  - Noisy vs Trash
  - Non-text vs Trash

### 1.2 Core Insight

The main difference between categories is:

> **Recoverability of the original text**

---

## 2. Category Definitions (Human-readable)

### Clear

Text is linguistically correct and readable without effort.

- Valid words and syntax
- Minimal OCR errors
- Immediately usable

---

### Noisy

Text is mostly readable but contains OCR artefacts.

- Some broken characters or punctuation
- Meaning still recoverable
- Requires minor cleanup

---

### Trash

Text is heavily corrupted and unreliable.

- Broken words, random symbols
- Cannot confidently reconstruct original meaning

---

### Non-text

Content is not linguistic text.

- Numbers, codes, measurements
- Structured but not sentences

---

### Empty

No usable content.

---

## 3. Key Features for Classification

### 3.1 Symbol Ratio (most important)

Non-alphanumeric characters divided by total length.

- Low → Clear
- Medium → Noisy
- High → Trash

---

### 3.2 Valid Word Ratio

Fraction of words found in dictionary.

- High → Clear
- Medium → Noisy
- Low → Trash

---

### 3.3 Digit Ratio + Regex

Used to detect Non-text.

Examples:

- numeric-only strings
- measurements

---

### 3.4 Perplexity

Higher values indicate more natural language.

Use as supporting signal only.

---

## 4. Suggested Thresholds

These should be tuned on data, but starting points:

- symbol_ratio &lt; 0.04 → Clear
- 0.04–0.35 → Noisy
- &gt; 0.35 → Trash

- valid_ratio &gt; 0.75 → Clear
- 0.4–0.75 → Noisy
- &lt; 0.4 → Trash

- len &lt; 15 AND digit_ratio &gt; 0.4 → Non-text

---

## 5. Recommended Classification Pipeline

```python
if empty(text):
    return "Empty"

if is_non_text(text):
    return "Non-text"

symbol_ratio = compute_symbol_ratio(text)
valid_ratio = compute_valid_ratio(text)

if symbol_ratio > 0.5 and valid_ratio < 0.2:
    return "Trash"

if valid_ratio > 0.75 and symbol_ratio < 0.04:
    return "Clear"

if valid_ratio > 0.4:
    return "Noisy"

return "Trash"
```

---

## 6. Recommended Scoring Approach (Preferred)

Instead of strict rules, compute a quality score:

```python
score = (
    0.4 * valid_word_ratio +
    0.3 * (1 - normalized_symbol_ratio) +
    0.2 * normalized_perplexity +
    0.1 * length_score
)
```

Classification:

- &gt;0.75 → Clear
- 0.45–0.75 → Noisy
- 0.2–0.45 → Trash

---

## 7. Regex Patterns

### Trash indicators

```shell
([^\w\s]{2,})
([a-zA-Z][^a-zA-Z\s]+[a-zA-Z])
```

### Non-text indicators

```shell
^[\d\s\-–—/:.,()%]+$
```

---

## 8. Model-based Approach (Optional but Recommended)

If rule-based system is insufficient, train a simple model.

### Steps

1. Build dataset:
   - Use rows where experts agree (DK == TK)

2. Features:
   - symbol_ratio
   - valid_ratio
   - digit_ratio
   - perplexity
   - length

3. Train model:

    ```python
    from sklearn.tree import DecisionTreeClassifier

    clf = DecisionTreeClassifier(max_depth=3)
    clf.fit(X, y)
    ```

4. Evaluate:

    ```python
    from sklearn.metrics import classification_report
    print(classification_report(y_true, y_pred))
    ```

---

## 9. Evaluation Strategy

- Use expert consensus as ground truth
- Focus on:
  - F1 score per category
  - Confusion between Noisy and Trash

---

## 10. Final Recommendation

Start with:

- rule-based + scoring hybrid

Then:

- validate on expert-labelled data
- optionally upgrade to decision tree

---

## Summary

The classification should be based on:

> **Linguistic validity + noise density + recoverability**

This aligns automated behavior with expert judgment and minimizes ambiguity.

#### <img src="https://avatars.githubusercontent.com/u/65828620?u=80b39d3cb2967c52e1d4af28dcefc7a1efdfdc79&v=4" width="50">[K4TEL](https://github.com/K4TEL) commented at [2026-03-20 05:59](https://github.com/ufal/atrium-alto-postprocess/issues/2#issuecomment-4095813140):

Commit 391d574 tries to refine the categorization algorithm

Documentation updated

@motyc the samples were updated in the repository and the whole collection categorization has been launched for ARUP and ARUB

#### <img src="https://avatars.githubusercontent.com/u/65828620?u=80b39d3cb2967c52e1d4af28dcefc7a1efdfdc79&v=4" width="50">[K4TEL](https://github.com/K4TEL) commented at [2026-03-22 07:50](https://github.com/ufal/atrium-alto-postprocess/issues/2#issuecomment-4105748932):

Commit 5b198e2 expands summary result files with additional columns for average quality score and ww ratio

#### <img src="https://avatars.githubusercontent.com/u/65828620?u=80b39d3cb2967c52e1d4af28dcefc7a1efdfdc79&v=4" width="50">[K4TEL](https://github.com/K4TEL) commented at [2026-04-01 12:52](https://github.com/ufal/atrium-alto-postprocess/issues/2#issuecomment-4169888510):

Main work is performed on the [test branch](https://github.com/ufal/atrium-alto-postprocess/tree/test) for now

Fixing [problems_260327.csv](https://github.com/user-attachments/files/26406136/problems_260327.csv)

#### <img src="https://avatars.githubusercontent.com/u/65828620?u=80b39d3cb2967c52e1d4af28dcefc7a1efdfdc79&v=4" width="50">[K4TEL](https://github.com/K4TEL) commented at [2026-04-02 10:17](https://github.com/ufal/atrium-alto-postprocess/issues/2#issuecomment-4176178874):

Commit 5bb8b2b and previous ones try to solve the problem; calibration is needed

#### <img src="https://avatars.githubusercontent.com/u/65828620?u=80b39d3cb2967c52e1d4af28dcefc7a1efdfdc79&v=4" width="50">[K4TEL](https://github.com/K4TEL) commented at [2026-04-17 09:54](https://github.com/ufal/atrium-alto-postprocess/issues/2#issuecomment-4267006356):

#### Columns

The initial columns:
* `file` — document identifier 🆔
* `page_num` — page number 📄
* `line_num` — line number, starts from 1 for each page 🔢
* `text` — original text of the line 📝
* `split_ws` — hyphenated word prefix at the end of the line (split word start)
* `split_we` — hyphenated word suffix at the start of the line (split word end)
* `lang` — predicted ISO language code from the FastText model ([full list](https://github.com/facebookresearch/flores/tree/main/flores200#languages-in-flores-200)) 🌐
* `lang_score` — FastText confidence score for the predicted language 🎯
* `perplex` — DistilGPT2 perplexity score of the line 📉

The added columns:
* `word_count` — number of whitespace-delimited tokens in the line
* `char_count` — total character count of the line
* `garbage_density` — ratio of non-alphanumeric, non-standard-punctuation characters to total line length
* `symbol` — count of words containing disallowed internal symbols (see detectors below)
* `upper` — count of words with unexpected mid-word uppercase letters
* `repeated` — count of words where a non-standard character makes up ≥ 40% of the word
* `ldl_fuses` — count of words with a letter–digit–letter sandwich (e.g., `w0rd`)
* `gibberish` — count of words flagged as gibberish (all-caps, no vowels, or extreme vowel ratio)
* `word_weird` — mean per-word weirdness score in [0, 1]; combines strange-symbol, repeated-symbol, LDL-fusion, and mid-uppercase signals weighted per token (0 = fully clean)

The decision-making column (has not been applied yet)
* `quality_score` — composite quality score in [0, 1] based on valid-word ratio, symbol ratio, perplexity, and text length; higher = cleaner

The output column:
* `categ` — assigned category: **Clear** ✅, **Noisy** ⚠️, **Trash** 🗑️, **Non-text** 🔣, or **Empty** 🫙

##### CPU Pre-filter

Before any GPU or model inference, `pre_filter_line()` applies a fast CPU-side check and assigns `Empty` or `Non-text` directly, bypassing the ML pipeline entirely:

* Line is blank → **Empty**
* Fewer than 4 characters, or fewer than 3 unique non-whitespace symbols → **Non-text**
* Letter ratio below 30% of total characters → **Non-text**
* Matches the all-digits/symbols regex pattern → **Non-text**
* Otherwise → forwarded for ML classification as **Process**

##### Structural Detectors

Lines that pass the pre-filter are analysed by four structural detectors defined in `text_util_langID.py`:

| Detector | What it counts |
|---|---|
| `detect_strange_symbols` | Words containing any character that is not alphanumeric and not in the allowed set `{ . - , + ( ) " ' / _ — – : % }`. Edge punctuation is stripped before inspection. |
| `detect_letter_digit_letter` | Words with a **letter–digit–letter sandwich** — the fingerprint of OCR digit insertions mid-word (e.g., `vyt1ačená`, `nalez2í`). Legitimate patterns like `90,9g`, `80-90cm`, `26.IX.1957` do not trigger. |
| `detect_mid_uppercase` | Words with unexpected uppercase mid-word (`dalSÍ`, `obkLADem`) or an uppercase run at the start followed by lowercase (`XXWžkumu`). All-caps words and titles (`PhDr`, `MUDr`) are excluded. |
| `detect_repeated_chars` | Words where a single non-standard character makes up ≥ 40% of the word (e.g., OCR stutter like `bxxxoxx`). |
| `detect_gibberish_words` | Words of length ≥ 7 that are all-uppercase, contain no vowels, or have a vowel ratio below 15% or above 80%. |

##### Categorisation Logic (Cumulative Penalty System)

`categorize_line()` in `text_util_langID.py` uses a cumulative floating-point penalty score rather than a fixed decision tree. The full logic is evaluated as follows:

**Immediate Trash overrides** (checked first, before penalty accumulation):

* Garbage density > 0.35, or garbage density > 0.20 on lines of ≤ 3 words → **Trash**
* Perplexity > 500 **and** a structural weirdness ratio (`word_weird`) > 0.4 simultaneously → **Trash** *(hard override to catch severe, high-confidence garbage)*

**Penalty accumulation** (for lines that pass the overrides):

| Signal | Penalty added |
|---|---|
| Each word with a strange symbol (`sym_count`) | `sym_count × 0.4` |
| Two or more strange-symbol words | additional `+0.5` |
| Each LDL-fused token | `× 0.3` |
| Each mid-word uppercase word | `× 0.2` |
| Each word with repeated non-standard char | `× 0.4` |
| Each gibberish word | `× 0.5` |

**Perplexity penalty** (skipped for short phrases that are structurally clean):

A line with fewer than 5 words, whose language is in the `EXPECTED_LANGS` allowlist and whose structural penalty is zero, is treated as a "forgiven short phrase" and perplexity thresholds are not applied. For all other lines:

* Perplexity > `PERPLEXITY_THRESHOLD_MIN` (default 1500, scaled to `× 1.5` for lines < 5 words) → `+0.5`
* Perplexity > `PERPLEXITY_THRESHOLD_MAX` (default 5000) → additional `+1.0`

> [!NOTE]
> Perplexity is intentionally **not used** as a Trash signal in isolation. `distilgpt2` is an English model and
> assigns very high perplexity to legitimate short Czech strings (place names, postal codes, form-field labels),
> making it unreliable as a Trash indicator. It is applied only as an additive penalty with structural context.

**Language confidence penalty:**

* Predicted language is **not** in `EXPECTED_LANGS` and confidence < 0.60 → `+0.8`
* Predicted language **is** in `EXPECTED_LANGS` but confidence < 0.30 → `+0.5`

**Final classification** via normalized penalty:

```
normalized_penalty = total_penalties / max(1.0, word_count / 5.0)

normalized_penalty ≥ 1.2  →  Trash
normalized_penalty ≥ 0.3  →  Noisy
otherwise                 →  Clear
```

##### Post-Processing Smoothing

After all lines in a document are classified and written to CSV, a final data-smoothing pass is applied before the file is finalized to prevent unnatural categorization anomalies:

1. **Header/Footer Deduplication** — Resolves edge-case flip-flopping. If the exact same text string appears multiple times across a document, all instances are harmonized to share the statistical mode (most frequent) category assigned to that string.
2. **Context Smoothing (Rolling Window)** — Applies a 3-line rolling window. If a **Noisy** line is sandwiched between two consecutive **Trash** lines, it is automatically downgraded to **Trash** to prevent isolated "noisy" categorizations in otherwise heavily corrupted regions.

@DanaKriv, this is the explanation of the added columns (the latest currently implemented version of the results and categorization logic)


-------------------------------------------------------------------------------



[Export of Github issue for [ufal/atrium-alto-postprocess](https://github.com/ufal/atrium-alto-postprocess). Generated on 2026.07.12 at 10:33:58.]
