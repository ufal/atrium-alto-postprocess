# Strategy — Issue #2: Text-category definitions & assignment logic

> Issue [#2](https://github.com/ufal/atrium-alto-postprocess/issues/2) · Labels: `enhancement`, `development` · **Status: ✅ Finish — pilot, time-boxed (AMČR, #67); proposed box 2026-10-16** _(was 🔍 Tocheck)_

### 2026-09-26 — AMČR baseline ([atrium-project#67](https://github.com/ufal/atrium-project/issues/67))

**Bucket: ✅ finish, time-boxed.** Decided by motyc (AMČR) in #67, adopted by ÚFAL. **Time-box: proposed 2026-10-16, to be confirmed on 30 September** (AMČR set *finish, time-boxed* in [atrium-project#67](https://github.com/ufal/atrium-project/issues/67) without a date; ÚFAL proposes two weeks after @DanaKriv's ≈ 10-02 decisions on #30).

**What closes #2 at the time-box** — a done-criterion that no longer waits on the gold set (#5/B1 is deferred with
alto#23, after the pilot):
1. [ ] **A is mostly done — check and finish it.** `docs/categorization_logic.md` is the canonical definition now (the
   README links it): categories, the composite quality score, the gates, the detectors, smoothing, and the note that
   the cumulative-penalty description is historical (`:351-353`). Add the two ordering invariants by name —
   `CATEG_TRASH_SCORE_MAX < CATEG_NOISY_SCORE_MAX` and `SHORT_PPL_CAP < PERPLEXITY_THRESHOLD_MAX` (enforced in
   `validate_constants`).
2. [ ] **B:** mark the two opening prompts resolved in that document, with pointers (`detect_letter_digit_letter`,
   `detect_strange_symbols`; the language-confidence gating).
3. [ ] **C, re-worded:** boundary tuning stays with #3/#30; *objective validation against a gold set* is recorded as
   ⏸️ deferred (alto#23 / #5-B1), not as a condition for closing #2.
4. [ ] One line for the pilot: the **same categories and quality score apply to born-digital lines** (llm-enrich#10
   §12 W3 — one quality model), so the definition document names both originators' inputs.

D (a learned classifier) stays parked here, ⏸️ after the pilot.

## Context
#2 is the **definitional root** of the categorisation engine: it fixed the five categories
(Clear / Noisy / Trash / Non-text / Empty) on a *recoverability* axis, the feature-column
contract, and — following the expert review (Dana/Tomáš) and its ChatGPT pattern analysis —
the decision to prefer a **quality-score** over strict rules. On `test` HEAD that decision is
**implemented**: `determine_category(qs, …)` assigns categories from `quality_score` with
`CATEG_TRASH_SCORE_MAX = 0.55` / `CATEG_NOISY_SCORE_MAX = 0.80` as the boundaries (the
Noisy↔Clear boundary was lowered 0.85 → 0.80 by the #3 2026-07-02 pass, shipped in v0.20.1),
replacing the earlier cumulative-penalty system documented in the thread. The remaining work is not
*definition* but **boundary calibration** and **gold validation**, which is why #2 is
`Tocheck` and is continued operationally in #3 (calibration) and #5 (importance/ablation).

This plan keeps #2 as the **invariant-owner**: any change to what the categories *mean* or how
`quality_score` maps to them must be reasoned about here first, then calibrated in #3.

## Status snapshot
| Item                                                | State          | Note                                                         |
|-----------------------------------------------------|----------------|--------------------------------------------------------------|
| 5-category definitions (recoverability axis)        | ✅ **DONE**     | settled with expert review                                   |
| Feature/column contract                             | ✅ **DONE**     | `quality_score` is the decision column                       |
| Quality-score assignment (replaces penalty cascade) | ✅ **DONE**     | `646fc5e`: QS is the sole factor                             |
| Structural detectors + CPU pre-filter               | ✅ **DONE**     | strange-symbol / LDL / mid-uppercase / repeated / gibberish  |
| Post-processing smoothing                           | ✅ **DONE**     | dedup + 3-line window (now parameterised, analysed in #5/B5) |
| Boundary calibration (Noisy↔Clear / Noisy↔Trash)    | 🔄 **IN #3**   | 07-02 DanaKriv pass shipped in v0.20.1 (20 Trash→Noisy, 0 collateral); next round pending ARUP/ARUB review |
| Objective "better" validation                       | ⏸ **DEFERRED** | needs gold set (#5/B1) — self-reference trap                 |

---

## Strategic actions

### A — Lock the definition as a written contract (close the `Tocheck`)
1. Promote the 2026-04-17 column/detector documentation into the repo docs (README or a
   `docs/CATEGORIES.md`) as the **canonical definition**, so the issue thread stops being the
   source of truth. State explicitly that the **quality-score** system is current and the
   **cumulative-penalty** description in the thread is *historical*.
2. Record the two ordering invariants the engine relies on (from #5's findings):
   `CATEG_TRASH_SCORE_MAX < CATEG_NOISY_SCORE_MAX` and `SHORT_PPL_CAP < PERPLEXITY_THRESHOLD_MAX`.
   These are enforced in `validate_constants`; the definition doc should name them so a future
   threshold edit can't silently invert a boundary.

### B — Resolve the two original prompts explicitly
The issue opened with two concrete asks — close them in the doc:
- **digits-attached-to-letters / symbols-inside-words as a separate measure** → satisfied by
  `detect_letter_digit_letter` (`ldl_fuses`) and `detect_strange_symbols` (`symbol`). Mark done.
- **ignore poorly-scored language during assignment** → satisfied by the language-confidence
  gating in the score (low-confidence non-expected langs penalised, not hard-Trashed). Mark
  the decision and its rationale (avoid English-model perplexity false positives).

### C — Hand calibration + validation to #3/#5 (don't duplicate)
- Boundary tuning of `CATEG_TRASH_SCORE_MAX` / `CATEG_NOISY_SCORE_MAX` and the readability/
  low-ppl gates lives in **#3**; #2 only owns whether a *new boundary or category* is needed.
- The "is the current mapping objectively correct" question is blocked on a **gold set**
  (#5/B1, deferred). #2's done-criteria should reference that gold set, not attempt to
  re-derive it.

### D — Future: only if a 6th category or a learned mapping is wanted
The expert review floated a small decision-tree over agreed-label rows. Per #5's close-out,
the right successor is a **monotonic/interpretable classifier** over the same precomputed
features, *once gold exists* (#5/B6). #2 is where that "do we replace the threshold cascade?"
decision belongs — keep it parked here, implemented there.

## Verification / done-criteria
- A canonical `docs/CATEGORIES.md` (or README section) states the definitions, the
  quality-score mapping, the detectors, the smoothing, and the two ordering invariants.
- The two opening prompts are marked resolved with pointers to the implementing detectors.
- Boundary calibration explicitly delegated to #3; objective validation ~~gated on~~ **deferred with** #5/B1 gold (2026-09-26 — not a close condition).
- `tests/test_recategorize_parity.py` stays green (no definition change may break replay).

## Out of scope (other threads)
- Threshold calibration loop and expert edge-case confirmation — **#3**.
- Parameter importance, ablation, dead-code analysis, gold-set tooling — **#5**.
