# Strategy — Issue #6: Pipeline run script starting points

## Context
The end-to-end `run_pipeline.py` orchestrator previously forced a full execution from ALTO XML to final statistics. To improve development iteration and workflow resumption, the orchestrator has been updated to support skipping arbitrary stages, primarily targeting the expensive text extraction step.

## Status snapshot (Current state)

| Item                                       | State      | Note                                                              |
|--------------------------------------------|------------|-------------------------------------------------------------------|
| Implement per-stage `--skip-*` flags       | ✅ **DONE** | Available via CLI and `config_langID.txt`.                        |
| Implement `--start-from` functionality     | ✅ **DONE** | Automatically skips all preceding stages.                         |
| Dynamic environment variable propagation   | ✅ **DONE** | `LANGID_TEXT_DIR` correctly set for downstream consumption.       |
| Paradata logging for skipped stages        | ✅ **DONE** | Merged JSON accurately reflects only executed stages/licenses.    |
| Practical validation on full collection    | 🔄 active  | Pending real-world test run to confirm stability.                 |

## Next Immediate Steps
1.  **Practical Validation:** Execute a dry run and a resumed partial run on a sample corpus to verify that the classification stage correctly reads from an already populated `PAGE_TXT*` directory without attempting to reload extraction models.
2.  **Issue Close-Out:** Close the issue once the practical tests confirm the auto-tested implementation behaves as expected in the production environment.
