# Strategy — Issue #30: Algorithm change for 2010+ years documents

> Issue [#30](https://github.com/ufal/atrium-alto-postprocess/issues/30) · Labels: `enhancement`, `development`, `integration` · Milestone: Q1-27 [WP4: Pre-Closure Architecture Review]

> ## 🆕 2026-09-24 — two answers arrived on 2026-09-23 after this revision
>
> * **Q8 / H14 answered** (@david-spacil, 13:08, 5795411354): mostly German, but misread Czech is systematic, so
>   neither pure option fits. Of the **26,889** short lines (1–3 words) with `ä ö ü ß` and no Czech accent in the full
>   re-scored corpus (113,101 documents), a 150-line stratified sample weighted to the population is **~73 % German,
>   ~13 % OCR misreadings in Czech documents, ~10 % rubbish, ~5 % unclear**. The misreadings put `Ä`/`ä` for `Á`
>   (`NÄZEV trati`, `ZPRÄVA`, `Plän`) and also for `Ř`, `Č`, `Ú` and plain `A` (`ÄEZY` = `ŘEZY`, `äslo` = `číslo`); a
>   regex for six recurring forms finds 2,620 lines (9.7 %). Most are legible and already `Clear` (strictly `Noisy`),
>   so a German-letter exemption would keep a mild existing error rather than create a new one; what it would wrongly
>   protect is rubbish with an umlaut (`PM=Ää`, `57 Ä`). **Reading for the plan:** the veto D45 left out is worth its
>   gold A/B and exposure pass, with a guard against symbol-heavy umlaut lines; it stays off until measured.
> * **Q3 answered** (@DanaKriv, 16:08, 5798302434): the batch of the 357 decisions will be done by the end of next
>   week (≈ 2026-10-02). H1/H2 move from "awaiting answers" to "scheduled".
> * **Refs now:** `test` = `master` = `2e2794d` (#31 Phase 4 and its docs on top of the #30 work; no categorisation
>   change), still `v1.5.1-beta` released. Q1, Q2 and Q4–Q6 are open.
>
> ## 📍 Current state — 2026-09-23 (read this first; the blocks below are the record)
>
> **Refs.** Released: `v1.5.1-beta` (`09c9640`, 2026-09-22). `master` is `3b02959`; `test` is
> `68fcbcb`, the same tree plus an issue-log refresh. The post-tag #30 work is `5b27900` (D40
> removed, D43, D44, the tie-break test), `0c30517` (`setup/word_lists.txt`) and `3b02959` (D45:
> the grid guard restored).
>
> **The shape witness passes its gate and still ships off.** On the 2,064-row gold sidecar: errors
> 513 → 503 with `Clear`-loss 38 = 38 (10e), 502 / 37 with the D44 split (11b). On @david-spacil's
> 508, default config, no lexicon: 326 → **336/508**, one break (`Lokolieace: •VIII,`) —
> ✅ **confirmed by him on `3b02959`** (H5). What it waits on is people, not runs: @DanaKriv's Q1
> (the `Trash` definition, H9) and her 357 decisions (Q3, H1/H2), and the policy questions Q2 and
> Q4–Q6 (H3, H10–H13), which @david-spacil is taking through with her by e-mail. **H14 = Q8 is
> new** and optional.
>
> **Open on the maintainer side:** the German-diacritic veto D45 left out (Q8 decides whether it is
> worth measuring), a confidence floor on `_vowel_run_min_for()` (Z5), and the version bump in
> `CITATION.cff` + `setup/para_config.txt` before the next tag. **Nothing is armed:** every flag and
> key added for #30 ships off or empty, and re-scoring `data_samples/DOC_LINE_CATEG` moves 0
> categories.
>
> **2026-09-23 sync (this revision).** Code comments, config comments, the advisory, tool texts and
> the collaborator docs carried into line with the settled answers; the witness report now passes
> each row's language to the witness, as production does (D44 parity). No flag, threshold,
> `[allowed]` entry or config value changed.

> **Status: ✅ `Merged`.** @david-spacil's PR [#48](https://github.com/ufal/atrium-alto-postprocess/pull/48) landed on `test` as [`070620f`](https://github.com/ufal/atrium-alto-postprocess/commit/070620f), 2026-09-09 15:37 UTC, green — `954 passed` clean, `952 passed / 2 xfailed` with the patch. `test` HEAD is `3afaddd`; release path `v1.4.7-beta` 🛑 *(2026-09-23: it shipped in `v1.5.0-beta` — the merge was 15:37 UTC, six hours after `v1.4.7-beta` was published at 09:34)*. `rule_short_garbage` now requires `_has_strong_garbage_evidence()` (`text_util.py:1361-1370`).
>
> **What this round changes.** The plan moves from "should we take this?" to "how do we narrow it?". D12 (the red CI check) is closed by the merge. **D15 — the two-line wiring of `_has_shape_garbage_evidence()` into gate 6 — is unblocked**: it was blocked *only* because it lands inside PR #48's own hunk, and that hunk is now in `test`. Phase 3 is the live phase, and it is gated on one thing that is not a code change: the annotation files.
>
> **The headline figures were revised in the final round** and this document now carries the revision path: **367,208** promotions, not 378,735 (notation is lifted without the patch, −3.0%), and `is_domain_notation()` narrowed from 30 of 508 graded lines to **24** when the closed label lexicon landed, with six lines regressing to `Trash`.
>
> ---
>
> ## 2026-09-20 — what changed, and what it changes about this plan
>
> The stage-7 delivery was re-read against its own logs and CSVs rather than against the
> summaries written when it was produced. Five findings, in
> [`../digests/30.digest.md` § "Stage 7 read against its own delivery"](../digests/30.digest.md)
> as S1–S5. Three of them overturn a decision cell below.
>
> | #      | finding                                                                                                                                                                                                                                                                                                                    | what it moves here                                                                                               |
> |--------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------|
> | **S1** | `SHORT_GARBAGE_LEXICON_GEMINATE_MAX_DF` does not rescale with its lexicon. At full collection scale the 4.4× gap D25 was fitted to is **1.17×**, and by ratio `ppole` is fifth of seven with four confirmed artefacts below it. At the shipped `10` on that table the guard is right on **2 of 7** instead of 7 of 7       | **D25 reopened.** Value unchanged, advisory added, and the replacement signal is an experiment (8c), not an edit |
> | **S2** | 07b's null is **void**: `quality_word_set()` is a zero-argument `lru_cache`, both A/B arms run in one process, the second arm read the first arm's cached answer. The same defect as 07c, one indirection in                                                                                                               | **D26 has no measurement.** D27's "different bug" note is wrong. **D28** is the class-level fix                  |
> | **S3** | The 07f pack is **2,424 of the same 5,005 strings**, 17,635 of the same 20,078 lines — a re-cut of the witness queue, not a second population                                                                                                                                                                              | **H1/H2/H3 collapse into one ask of 293 decisions.** **D29** is the tool                                         |
> | **S4** | The modal-dedup blast radius is **4 groups / 17 `Clear` lines destroyed against 17 groups / 31 `Trash` lines rescued** — net **+14** in the cascade's favour. **Zero** of the 5,222 votes rest on a bare plurality, and a tie can never land on `Trash` (`mode()[0]` resolves alphabetically; `Clear` < `Noisy` < `Trash`) | **H6 option 2 is a no-op** — the case it removes does not occur. The choice is between 1 and 3                   |
> | **S5** | 07d reports the witness configuration at **503 errors / `Clear`-loss 40** against shipped 513 / 40 — `ADOPT-CANDIDATE`, the gate satisfied — while three earlier runs of the same nominal config reported 42, 41, 40                                                                                                       | **Phase 3's done-criterion may already be met.** Stage **8a** settles it and the drift together                  |
>
> **The practical consequence.** Phase 3 was blocked on annotation. It is now blocked on
> **one cheap re-score (8a)**, with annotation as what makes the result trustworthy beyond
> the 23 labelled rows rather than what makes it obtainable. Nothing in stage 8 needs
> @DanaKriv, @david-spacil, a GPU or a new label.

> ⚠️ **Provenance, and it is load-bearing.** Every corpus- and annotation-level figure in this document is attributed to the round that produced it and the commit it was measured on. **Nothing signal-dependent was re-measured for this revision** — the environment it was written in has no `pytest` and no `numpy`, so `classify_TEXT` will not import. The single exception is §7's shape-witness table, re-run directly because that predicate is a pure function of text. Harness divergence has produced confident wrong numbers in this repository three times (Risks table); the discipline is the mitigation.

## Context

The issue was opened as a 2010+ categorisation complaint. The cause was `rule_short_garbage` (gate 6 of `determine_category`, `text_util.py:1353-1371`), which returned `Trash` unconditionally for any 1–3 token line without Czech diacritics, weak language ID and any weirdness. It was the largest single `Trash` producer — 6,333 firings, 28.4% of all `Trash` lines — and it fires hardest on post-2000 documents because those are tabular, one value per cell.

@david-spacil proposed gating it behind `_has_strong_garbage_evidence()`, the same predicate six §9 rules use. That change is now merged. Measured over both collections recomputed twice on the same base, it moves **367,208 lines and demotes none** — `Trash`→`Clear` 153,353, `Noisy`→`Clear` 119,956, `Trash`→`Noisy` 93,899 — dropping `Trash` by 247,252 lines (−8.68%) across 113,101 documents and 71,815,774 lines, with `Non-text` and `Empty` untouched. 484 of the promoted lines scored blind by a human annotator (0 = right, 1 = borderline, 2 = wrong; key held aside on shuffled samples), design-weighted with decade strata, give **76.5% right / 9.6% borderline / 13.9% wrong** — a 5.5 : 1 ratio, and roughly **26,000 garbage lines reaching `Clear`**, the top category rather than a reviewable middle state. `tools/SWEEP_NOTES.md` classifies the rule `LOAD-BEARING`.

Two framings were tried and abandoned before the change was taken on its measurements. The **symmetry argument** (that the rule is the seventh sibling of six gated rules) does not hold: `50472fb` added `qs < thresh_trash` to sites that were already gated and never touched this rule, which sits among eight *early-exit* rules that all return unconditionally. And **"367,208 lifted / 0 demoted"** is a theorem rather than a measurement — deleting an unconditional `return Trash` is monotone by construction — so the only evidence about whether the change is *right* is the graded sample.

## Research

**1. What separates the two populations at 1–2 tokens.**
*Superseded in part by §5.* Of the signals the pipeline **passes to `determine_category`**, nothing separates them: `vansasaasasa` and `malakofauna` agree on valid-word ratio (1.000), weird ratio (0.350), garbage density (0.000), gibberish (0), ghost share (0) and raw quality score (0.647 vs 0.647). A quality-score gate was built and measured: dead, because raw pre-alignment scores overlap completely (garbage 0.52–0.65, notation 0.49–0.86, with `II/C` and `I-VIII-c` *below* most garbage). §5 shows that a signal the pipeline **computes but does not act on** does separate much of it.

**2. Whether perplexity can arbitrate.**
No, and it runs backwards. Both surviving routes gate on *low* language-ID confidence; FastText scores `oueussd` at 0.9163 and `malakofauna` at 0.56. With the cap lifted, real notation demotes at perplexity 3,000 while `oueussd` survives to 30,000. Recomputed values corroborate: `II/C` ≈ 6·10⁷ (correct) against `oueussd` ≈ 4,600 (garbage). Independently: because `SHORT_PPL_CAP` (850) < `HARD_SWEEP_PPL_MIN` (1,000) < `PPL_EXTREME_MIN` (3,000) < `PPL_GARBAGE_ABSOLUTE` (30,000), **no perplexity rule can fire at all on a 1–2 token line**. Four cap variants were then measured end to end and none is separable from the patch — see the accuracy ledger in [`../digests/30.digest.md`](../digests/30.digest.md) §3.5.

**3. Whether the existing structural protections could be widened.**
`is_structured_line()` (`text_util.py:2155`) dispatches to seven predicates; all seven returned `False` on all 35 thread examples (245 evaluations, zero matches). But widening *that* predicate is the wrong lever — it is read at eleven places and is an absolute veto at the first statement of `_has_strong_garbage_evidence()` (`text_util.py:1672`), so widening it would also disable `rule_allcaps`, `rule_garbage_density`, `rule_zero_alpha`, `rule_vowelless` and `rule_mid_uppercase`. Hence a separate, narrower predicate.

**4. Whether the wc/char boundary can isolate the problem lines.**
No, and this was asked directly in the thread. Error rates are flat — 31% / 28% / 25% by token count, 17–42% by character band with no monotone trend — and the tightest `wc <= 3, chars <= 24` box encloses 482 of 508 lines, 343 of them correct. `sektlll` and `edelite` (wrong) sit in the same band as `Kaaden` and `Pinii` (right). **That is why the narrowing is a shape predicate and not a boundary.**

**5. The pipeline already computes a discriminating signal, and the short-line path throws it away.**
This is the finding that unblocked the plan, and it falsifies the claim that stood for three rounds — carried by `tests/test_text_utils.py`, by `is_domain_notation()`'s own docstring, and by PR #48's *Known limitation* section — that no character-level rule separates `oueussd` from `malakofauna`.

`detect_fused_words()` (`text_util.py:773`), built on `_RE_FUSED_VOWEL_RUN` (`text_util.py:770`, 3+ vowel run) and `_RE_FUSED_CONSONANT_RUN` (`text_util.py:769`, 5+ consonant run), returns **1** for `oueussd`, `sektlll`, `cuxoaid`, `Tthts I` and **0** for `malakofauna`, `diapozitiv`, `Occipitale`, `Equus caballus`, `Kaaden`, `Pinii`, `kontext`, `Uniocrassus`, `Ossa tarsi`, `radius prox.sin.`, `1 ks`, `II/C`.

Section 7 (`rule_short_line`) **already reads it**, inside its `damage` term:

```python
or ((gibberish_present or detect_fused_words(text_source) > 0) and not structurally_clean)
```

with `structurally_clean = valid_word_ratio >= 1.0`. But `valid_word_ratio` is a **shape-only** heuristic — `compute_valid_ratio()` (`text_util.py:2405`) counts a token valid on length ≥ 3, ≥ 70% alphabetic, no strange character, no mid-word uppercase — so it is **1.0 for every line in this population**, `oueussd` included. The one witness that discriminates is suppressed by the one signal that cannot. The limitation was a **suppression bug in the short-line path**, not an absence of signal.

**6. On this population the merged gate is equivalent to deleting the rule.**
`_has_strong_garbage_evidence()` returned **False on 16 of 16** disputed lines. With `valid_word_ratio` 1.0, `weird_ratio ≈ 0.35`, gibberish 0 and density 0.0, its only live clause is `lang_score <= 0.20 and orig_lang_score <= 0.50` — i.e. `orig_lang <= 0.40` after `TRUST_TIER_UNKNOWN` (0.50) scales it; the last conjunct misses by 0.05 (`weird_ratio >= 0.40` against 0.35). `thresh_trash = CATEG_TRASH_SCORE_MAX + 0.35 = 0.90` is nearly always true, so the predicate is the whole gate. Measured through the real `determine_category`:

| variant                         | real vocabulary → `Trash`     | garbage → not-`Trash`                |
|---------------------------------|-------------------------------|--------------------------------------|
| baseline (pre-merge)            | 8 / 9 — the issue's complaint | 0 / 7                                |
| + the evidence gate (merged)    | 0 / 9                         | **7 / 7, all reaching `Clear`**      |
| + shape witness at gate 6 (D15) | 0 / 9                         | flags 9 / 9 of the reachable garbage |

So "require a second witness" is, on this population, a **suspension** of the rule rather than a narrowing: there is no second witness to consult, and the rule simply stops convicting. Pinned in-tree on production vectors by `tests/test_calibration.py::test_strong_evidence_is_false_on_the_entire_disputed_population`.

Corollaries that constrain any fix:

- **For `wc <= 2` the promotion happens in section 7, not gate 6.** `rule_short_line` (`text_util.py:1380-1439`) always returns, so nothing below it — including all six §9 gates and the §10 band routing — is reachable for 1–2 token lines. Its final branch returns `Clear` when `valid_word_ratio >= 1.0`.
- **`wc == 3` is a different regime.** `SHORT_PPL_CAP` covers `wc <= 2` only, so 3-token lines already reach the perplexity routes with raw perplexity, `damaged` becomes live, and §8–§10 are reachable. That, not "shorter is riskier" as a law, explains the 3.6 / 5.1 / 6.8 : 1 gradient in the grading — consistent with §4's flat error rates once borderline lines are included.
- **`check_rescues()` (`text_util.py:1462`) cannot supply the `Noisy` floor.** All three of its floors — `_trailing_fill_rescued()`, `is_forgiven_headline()`, `is_clean_reference()` — evaluate `False` on all 11 lines tested, so it returns `Trash` for every one, `malakofauna` and `1 ks` included. Routing gate 6 through it makes the symmetry argument literally true and changes **nothing** on the disputed population.

**7. The corrected witness, and what it costs to get it right.**
`_has_shape_garbage_evidence()` (`text_util.py:1770`) is a vowel-run test, an initial-consonant-geminate test, a triple-character test and a character-variety test, applied only to alphabetic sub-tokens of length ≥ `SHORT_GARBAGE_WITNESS_MIN_ALPHA` (4) not in `_NEUTRAL_LEXICON` / `SHORT_EXCEPTION_TOKENS` / `SHORT_VALID_WORDS`, and vetoed by `has_cz_diacs()` / `is_structured_line()` / `is_domain_notation()`.

Re-measured directly for this revision (pure text function, no scoring stack needed):

| set                                              | n  | flagged                                                                                                                                                           |
|--------------------------------------------------|----|-------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| thread garbage, reachable                        | 9  | **9** — `oueussd`, `cuxoaid`, `IDIDIDIDIDIDUOID`, `nnnooo` (vowel run), `sektlll`, `NINNNIC` (triple), `Tthts I`, `rragment` (geminate), `vansasaasasa` (variety) |
| vocabulary + notation + toponyms + names + units | 37 | **0** — incl. the whole `vrstva` family, `Skelettmaterial`, `Aachen`, `Schifffahrt`, `malakofauna`, `II/C`, `1 ks`, `mm`                                          |
| phonotactically legal residue                    | 6  | **0** — `edelite`, `vfetennl k.`, `Slaot-o hiezazzt`, by construction                                                                                             |
| known loan false-positive class                  | 7  | **5** — `Poaceae`, `Naiade`, `Beaune`, `Radiouhlik`, `Sauerstoffflasche`                                                                                          |
| every committed positive fixture row             | 37 | **0**                                                                                                                                                             |

Against exactly the lines printed in the §7 grading tables: **8 of 15** labelled *wrongly lifted* flagged, **0 of 25** *correctly lifted*, **0 of 4** borderline.

**These are the lines the thread happened to print, not a random sample.** It is a feasibility signal. If that recall held on the real changed population the wrong-lift share would fall from 13.9% toward 6–7% at no cost to the correct lifts — that must be measured on the 508 before it is claimed.

**8. What earlier revisions of this document got wrong.**
Both errors were the kind that pass review, neither was visible from reading the code, and both took running it. Recorded because the failure mode recurs, not to relitigate:

- **Placement.** An earlier revision put the witness in section 7's `damage` term and recorded that landing it there would turn D8's strict xfail into `XPASS(strict)`. It does not: section 7's `damage` branch returns `Noisy` (`text_util.py:1424-1425`) and the test asserts `== "Trash"`, so the test would have stayed `xfail` forever while the visible improvement (`Clear` → `Noisy`) was mistaken for the debt being paid. Traced on a patched module: `oueussd` and `sektlll` both go `Trash` → `Clear` (gate) → `Noisy` (gate + section-7 fix).
- **Signal.** An earlier revision specified a "run-only form of `detect_fused_words`". That is **two** regexes, and `_RE_FUSED_CONSONANT_RUN` (5+ consonants) matches `vrstva`, `vrstvy`, `vrstvou`, `vrstvami`, `ctvrtek`, `ctvrt`, `zmrzl`, `scvrkl` — ordinary diacritic-free Czech. `vrstva` ("layer") is the commonest noun in archaeological field documentation, and `vrstva 3` satisfies every condition of gate 6 at production signals, so a witness carrying that clause would `Trash` it: a demotion of clean text, the one outcome this thread exists to prevent. **Only the vowel run is safe**, and nothing is lost — `sektlll` is reached by the triple test and `Tthts I` by the geminate test. The geminate test also had to be narrowed to consonants (a bare `^(.)\1` takes `Aachen`) and the triple test length-capped (an uncapped one takes `Schifffahrt`).

## Design decisions

| #                                         | Decision                                                                                         | Recommendation                                                                                                                                                                                                                                             | Rationale                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Outcome                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
|-------------------------------------------|--------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| D1                                        | Notation predicate, separate from `is_structured_line()`                                         | A new shape-based `is_domain_notation()` consulted at a minimum of sites                                                                                                                                                                                   | Recovers grid refs and counts without the eleven-site blast radius of widening `is_structured_line()`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | ✅ Shipped in `8fadeb9`. Fires on **24** of 508 graded lines (4.2%) after the label lexicon closed — see the regression note below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| D2                                        | Scope: notation, not vocabulary                                                                  | Grid/context refs, labelled refs, counts, abbreviation chains only. `malakofauna`, `Equus caballus` explicitly out                                                                                                                                         | Binomials and bare foreign words need a lexicon; a regex claiming them would be the predicate quietly claiming to solve the harder half                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | ✅ Held. Documented in the predicate's docstring and `docs/categorization_logic.md` row 5b                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| D3                                        | Give short lines a usable perplexity                                                             | `apply_page_perplexity_blend()` — median of the page's long lines, geometric blend, behind `PAGE_PPL_BLEND_ENABLE` (default off)                                                                                                                           | Median resists the 6·10⁷ tail; log-space because perplexity spans six orders of magnitude                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | ⚠️ Shipped inert in `8fadeb9` — see D5. Fixed in `861409a`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| D4                                        | Preserve the uncapped perplexity                                                                 | New `perplex_raw` / `perplex_blend` columns (40 → 42)                                                                                                                                                                                                      | The capped value was the only one stored, so the blend could not be computed or re-tuned offline                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | ✅ Shipped in `8fadeb9`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| D5                                        | Let the blend reach the rules                                                                    | `score_line(..., apply_short_cap=False)` for the blend's re-score only                                                                                                                                                                                     | `score_line` re-applied `SHORT_PPL_CAP` to `wc <= 2` — the blend's only target population — pinning the blended number back to 850 before any rule saw it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | ✅ `861409a`. Default `True` keeps every other caller untouched                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| D6                                        | Make the perplexity routes notation-aware                                                        | Exempt `rule_extreme_ppl` and `rule_absolute_ppl`; **keep `rule_hard_sweep` armed**                                                                                                                                                                        | Those two convict on perplexity alone, which §2 disowns. Hard sweep additionally requires `orig_lang_score < HARD_SWEEP_LANG_MAX` — an independent second witness                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | ✅ `bd0a734`. Exempting all three let 65% of dot-mutated garbage escape; hard sweep armed trashed 600 of 600 at no cost to notation. Corpus-verified safe — see the note below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| D7                                        | Close the notation false positives                                                               | Capital initial per dotted segment in `_RE_NOTATION_ABBR`; closed label lexicon for `_RE_NOTATION_LABELLED`                                                                                                                                                | `^(?:[A-Za-z]{1,8}\.){2,6}$` constrained shape but not content — 100% of generated dot-chained garbage accepted                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | ✅ `bd0a734` + `861409a`. **With a measured cost**: six graded lines regressed to `Trash` — see the note below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| D8                                        | Resolve the red `oueussd` test                                                                   | `@pytest.mark.xfail(strict=True)` keeping `Trash` as expected, applied via `pytest.param`                                                                                                                                                                  | *"I would not rewrite the tests to claim that `Clear` is semantically correct… accepted technical debt, not newly correct labels."* A frozen `Clear` asserts a wrong answer is right                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | ✅ **Landed in the merge**, `tests/calibration_fixtures.py:156-170`. Exit condition is D15                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| D9                                        | ~~Un-suppress the witness in section 7~~                                                         | ~~Stop `valid_word_ratio >= 1.0` vetoing the fused/gibberish evidence in section 7's `damage` term~~                                                                                                                                                       | Research §5: the signal is already computed and already read at that exact site                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | ❌ **Withdrawn — placement was wrong.** Section 7 returns `Noisy`, not `Trash`. §8. Superseded by D15                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| D10                                       | A witness cascade, not a single test                                                             | Vowel-run + initial-geminate + triple-character + character-variety, guarded by a minimum alphabetic length (≥ 4) and the neutral/unit/function-word sets, vetoed by `is_structured_line()` / `is_domain_notation()` / `has_cz_diacs()`                    | Research §7. Each component targets a distinct OCR failure mode; the guards keep `mm` and notation out. The consonant run had to be dropped (§8)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | ✅ **Shipped as `_has_shape_garbage_evidence()`** behind `SHORT_GARBAGE_WITNESS_ENABLE` (default false) — D16                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| D11                                       | An explicit `Noisy` floor for `wc <= 2`, not `check_rescues()`                                   | If a floor is wanted, implement it in section 7's own return path. Do **not** route gate 6 through `check_rescues()` expecting one                                                                                                                         | Research §6: all three `check_rescues()` floors are `False` on this whole population                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | ❌ **Measured and rejected.** A blanket floor scores 31.1% [21.4; 40.7] — 28 fixes against **202 breaks**, applying to 227,536 lines (60%). It caps the good lifts with the bad                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| D12                                       | Unblock PR #48's CI by merging `test`                                                            | Merge `test` into the PR branch. No code change, no rebase                                                                                                                                                                                                 | The red `docker-build-smoke (api)` check was a base defect: the `api` stage's `ENTRYPOINT ["python", "service/text_api.py"]` puts `/app/service` on `sys.path[0]` and not `/app`, so the image died at `ModuleNotFoundError: No module named 'atrium_document'`                                                                                                                                                                                                                                                                                                                                                                                                                                           | ✅ **Closed by the merge.** `070620f` went in green, 954 passed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| D13                                       | Fix the test-suite blindness before trusting any of this                                         | Migrate `tests/test_smoke.py`'s harness; add **discriminating** golden edge cases; add a swept `Trash`-side counterpart                                                                                                                                    | Four independent blind spots let a category-flipping change through a green fast lane                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | ✅ **Done ahead of the merge.** 814 → 891 → 954 passing                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| D14                                       | A lexical signal only for the residue                                                            | Keep the lexicon out of the hot path. Use korektor (wrapped in `tools/quality_model/correct.py`, LINDAT REST) **offline to build** a closed lexicon, following the `_NOTATION_LABELS` + `_fold_diacritics` precedent, rather than consulting it at runtime | `edelite`-class residue is phonotactically legal, so only word knowledge separates it. korektor REST is "realistic for 10⁴–10⁵ lines, not 10⁶" and its models are CC BY-NC-SA, which would change the pipeline's output-licence story. `compute_valid_ratio(text, word_set=None)` already has the injection point and **no caller passes it** — note the `word_set` branch bypasses `_is_neutral_token`, so the branches need merging, not a flag flip                                                                                                                                                                                                                                                    | ✅ **SHIPPED 2026-09-17, on a different substrate — and measured.** The korektor route was abandoned for the licence reason recorded here. The lexicon is built from the **corpus's own token document-frequency** instead (`tools/build_token_lexicon.py`, CPU-only): a token in many distinct documents is vocabulary, one in a single document is what that scan did to some ink — a property of the data, not of the classifier, so it does not repeat the circular-objective mistake. Consulted two ways, gated separately: `_has_vocabulary_support()` as an exemption-only **veto**, and a `no_vocabulary` **conviction** clause behind `SHORT_GARBAGE_LEXICON_CONVICT` (`false`) that is the only thing in the module reaching `edelite`. **Measured on the real 822-document table: the mechanism works, `min_df` 3 does not** — it convicts 5 of 31 pinned vocabulary fixtures (`Pinii`, `Uniocrassus`, `Aachen`, `Hannah`, `Emma`). Flag stays off on evidence. Rebuild over both full collections before touching the threshold. Digest §6                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| D15                                       | Where the witness is consulted                                                                   | At **gate 6**, as a second disjunct beside `_has_strong_garbage_evidence()` — not in section 7                                                                                                                                                             | Section 7's `damage` branch returns `Noisy`; only gate 6 returns `Trash`. Two alternatives were rejected: a new keyword on `_has_strong_garbage_evidence` still edits the line the evidence gate adds, and computing the witness *inside* that predicate arms its other six call sites — `rule_wqx_rot`, `rule_bigram_run` and `rule_fragment_tokens` have no word-count bound, and the witness has no measurement on 4+ token lines                                                                                                                                                                                                                                                                      | ✅ **LANDED, flag still off.** Wired at gate 6 with `_fire("rule_short_garbage_witness")`; registry, ablation lists and `UNREACHABLE_RULES` updated; covered by `tests/test_short_garbage_witness_wiring.py` in BOTH flag states. Flag-on: `oueussd`+`sektlll` → `Trash`, `malakofauna`/`Equus caballus`/`diapozitiv`/`II/C`/`1 ks` unmoved, `edelite` still out of reach. Enabling still waits on gold                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| D16                                       | Ship the predicate inert, ahead of the wiring                                                    | `_has_shape_garbage_evidence()` + `SHORT_GARBAGE_WITNESS_ENABLE` (default `false`) + four config keys + tests, with **no call site**                                                                                                                       | Separates the reviewable part (does the predicate discriminate?) from the part that had to wait (where is it read?). The composed condition `strong or shape` is pinned on production vectors by `test_the_disjunction_the_gate_will_evaluate_separates_the_population`, so the wiring is a one-line change against a tested expression                                                                                                                                                                                                                                                                                                                                                                   | ✅ **Landed.** Flag-off re-score of every sample CSV: 0 categories changed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| D17                                       | Keep the four witness constants out of `TUNABLE_CONSTANTS` for now                               | Recorded in `_DELIBERATELY_NOT_TUNABLE` with the reason; moved to `_THRESHOLD_NAMES` + `SEARCH_SPACE` in the same commit that flips the flag                                                                                                               | A constant that cannot change an outcome sweeps as zero-variance, and every driver in `tools/` reads zero variance as an argument to **prune**. Registering them while the flag is off would manufacture four bogus prune recommendations                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | ✅ Landed, with the reminder in the exception map                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| D18                                       | Exempt Latin taxonomy above all four clauses                                                     | A closed suffix pattern (`aceae\|oideae\|eae\|iae\|aea\|oidea\|idae\|inae`) with a length floor, checked in the same place as the roman-numeral exemption                                                                                                  | The loan false-positive class was a stated precondition for flipping the flag and was never measured. Measured: the `vowel_run` clause convicts the botanical **family** suffix 10 of 10, so it was convicting a naming convention, not misfiring on rare spellings. Placement matters for the same reason as D15's roman numerals — `-aceae` is one clause away from the others on a longer name                                                                                                                                                                                                                                                                                                         | ✅ **Landed at zero cost in convictions — but necessary, not sufficient.** Every named garbage line still fires and every roman-numeral line still passes. The corpus shows the real class is Latin **binomials**, not family names: `Triticum monococcum`, `Lepus europaeus`, `Mammalia indet.`, `Papaver rhoeas/dubium`, convicted by `low_variety` and `vowel_run`. Genus+epithet endings are not enumerable and attestation clears all of them, so **no further shape rule should be proposed for this class**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| D19                                       | Document frequency, not line frequency, and not category                                         | Count the **distinct documents** a token appears in. Never read `categ` or `quality_score` when building the table                                                                                                                                         | OCR noise repeats freely inside the scan that produced it and — EXCEPT on pre-printed forms, see D22 — rarely across scans made years apart, so line frequency would let one bad document vote its garbage into the dictionary. Reading `categ` would rebuild the circular objective this issue already built once, in a new place                                                                                                                                                                                                                                                                                                                                                                        | ✅ Landed and measured at corpus scale. `vrstva` 429 against `oueussd` 1; 74.3% hapax. The witness alone scores 5,107 confirmations against 15,217 new convictions; with the veto 3,905 against 2,306. **That was read as "the veto is load-bearing, not a refinement" and the reading was wrong** — 90.5% of the gap is the single token `ppole`, an OCR doubling of `pole` in a form label that recurs across documents and so looks attested (df 35), and which `initial_geminate` was convicting CORRECTLY. Both figures are also counted against `categ`, so neither could distinguish a wrong witness from a wrong pipeline — the same circularity as the sweep objective. Guarded by `SHORT_GARBAGE_LEXICON_GEMINATE_RATIO` (D22). **The coupling decision stands on its own merits** — a veto moving ~12.9k lines either way must not be armed blind — enforced by an import advisory (not a gate — runbook 5a measures the shape-only case deliberately)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| D20                                       | Give the witness its own vowel-run length                                                        | `SHORT_GARBAGE_WITNESS_VOWEL_RUN_MIN`, defaulting to `FUSED_VOWEL_RUN_MIN` so shipped behaviour is identical                                                                                                                                               | The two were one constant, and `detect_fused_words()` feeds `fused_ratio` in the quality score and the `fused_words` CSV column — so the only clause that discriminates on this population could not be tuned without moving scores on every line in the corpus                                                                                                                                                                                                                                                                                                                                                                                                                                           | ✅ Landed, default unchanged. The trade at 4 is measured and asymmetric (`oueussd` stays, `cuxoaid` escapes, the three loans stop firing) and belongs to the sweep, not to a reading. Runbook step 5b                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| D21                                       | Add the gold verdict beside the structural figures, never over them                              | `_loo_metrics` runs a second, gold-scored pass and reports `gold_delta_macro_f1`; `decisive_count` and `clear_loss` keep their existing meaning                                                                                                            | Forwarding `gold_category_column` into the existing call was the obvious fix and the wrong one: with a gold column `flip_count` counts *disagreements with gold*, not lines the rule moved, and the baseline stops being zero — so `decisive_count` would have become a different quantity under the same name, in the tool that gates rule retirement                                                                                                                                                                                                                                                                                                                                                    | ✅ Landed, with a source-level test across all five `evaluate_dataframe` callers and `gold_column` / `decisive_scored_against` in the JSON payload. The **numbers** still need regenerating — digest R3                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| D22 🗑️ **REMOVED 2026-09-22 (D40 / W3)** | Attestation does not count when the token is a de-geminated artefact                             | `_is_geminate_artefact` / `SHORT_GARBAGE_LEXICON_GEMINATE_RATIO` (4.0): an attested doubled-initial token whose de-geminated form is >= 4x more frequent is not vocabulary                                                                                 | D19's premise — "OCR noise is idiosyncratic to the scan that produced it" — fails for a pre-printed form: the misread recurs once per document and accrues df like a word. `ppole` (OCR of `pole`, df 35 vs 163) was 57% of the whole witnessed population and 90.5% of the veto's measured effect, and `initial_geminate` had been convicting it CORRECTLY. Raising `MIN_DF` cannot reach it                                                                                                                                                                                                                                                                                                             | 🗑️ **REMOVED 2026-09-22**, owner-approved, code + config + tests. The record below is why it existed and why it went; nothing in the tree reads it any more. *(As it stood:)* ⚠️ **Landed, and wrong on its headline case.** Veto-only, so it can restore a conviction the shape clauses already reached and never create one; inert while no table is configured. **But `ppole` is an abbreviation, not an artefact** (@david-spacil, 2026-09-19), so this guard strips the veto from real vocabulary on 11,562 lines. Right on the other seven. D25 is the fix. **Not validatable by stage 5** — of the 25 gold-labelled rows in the witnessed population, zero contain `ppole` and 2 fire `initial_geminate`, so the guard cannot move those figures. Re-run stage 4, not stage 5. The artefact and Roman-numeral populations do not overlap on the real table (8 tokens >= 4x, 36 <= 2x, nothing between), so the threshold sits in an empty gap rather than being fitted                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| D23                                       | Annotate by distinct STRING, not by line                                                         | `short_garbage_witness_report.py --distinct` writes one row per string with an occurrence count; `--from-distinct` projects the filled result back onto every line as a joinable sidecar                                                                   | The queue is 20,324 lines but 5,243 strings, 94% of them hapax, and `ppole` alone is 11,562 lines. Per-line annotation spends an archivist's entire budget re-deciding one string. Top 20 strings settle 69.2% of the population, top 100 settle 73.4%                                                                                                                                                                                                                                                                                                                                                                                                                                                    | ✅ Landed with a round-trip test. This is what makes a gold set for this population affordable, and the gold set is the binding constraint on every figure in this issue                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| D24                                       | The A/B verdict gates on errors, cost and Clear-loss — never on macro_f1                         | `_print_gold_verdict` rejects any arm that raises total errors, `costed_score` or Clear-loss, whatever macro_f1 does; it also computes the exact McNemar and the effective n (`b + c`) it used to ask the operator for                                     | On an imbalanced gold set macro_f1 averages per-class F1, so moving the boundary toward the rare class pays for itself regardless of net accuracy. Stage 5c scored the BEST macro_f1 on the board (+0.0397) while getting 8 more lines wrong than changing nothing and costing 12% more, and the tool called it "better on gold". Third instrument-level error in this issue, after the self-referential sweep objective and the `categ`-scored exposure ratio — same error each time: the criterion, not the intuition                                                                                                                                                                                   | ✅ Landed with a 5c-shaped regression fixture. An ADOPT-CANDIDATE that rests on a handful of rows now carries `NOT SIGNIFICANT: n=…, p=…` on the verdict line itself, because stage 5a's four-decimal macro_f1 rested on 19 changed rows                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| D25 🗑️ **REMOVED with D22, 2026-09-22**  | Cap the de-gemination guard on the geminate's OWN frequency, not only the ratio                  | `SHORT_GARBAGE_LEXICON_GEMINATE_MAX_DF` (10): a doubled-initial token attested in more than N documents is a convention, not a scan error, whatever its ratio                                                                                              | @david-spacil: `ppole` is an abbreviation for *popelnicová pole*, not an artefact, so `initial_geminate`'s premise ("no European orthography opens a word that way") is false for abbreviations — `pp.`, `ss.` are the same convention. D22 convicts it. On the 822-doc table `ppole` has the HIGHEST own df (35) and the LOWEST ratio (4.7), while every confirmed artefact sits at df <= 8; 4.7 vs 5.2 is not a separation, 35 vs 8 is                                                                                                                                                                                                                                                                  | 🛑 **REOPENED 2026-09-20 (S1) — the run that was meant to confirm it refutes it.** The 2026-09-19 reading of 07a was wrong in three ways, and the counterexample was inside the sentence that made the claim. At full scale: `ppole` 229/8600 = **37.6x**, `ssuti` 195/1667 = 8.5x, `ssutí` 11.4x, `ssutě` 14.1x, `llocm` **7.6x**, `vvkop` 46.7x, `jjámy` 1479.9x. **By ratio `ppole` is fifth of seven**, not lowest — four artefacts sit below it. **By own df the 4.4x gap has collapsed to 1.17x** (229 against 195). And 4.7 → 37.6 is up, not "down from". The constant is an ABSOLUTE document count, so it does not rescale when the lexicon does: at the shipped `10` on the full-collection table the guard is right on **2 of 7** tokens instead of 7 of 7 — every artefact but `jjámy` keeps an exemption it should not have. **Not retuned**: eight tokens, seven labelled from one reading, is not a population to fit a production threshold to. `text_util.geminate_cap_scale_warning()` now says so on stderr whenever a table >4x the calibration corpus is configured; comment and `setup/config.txt` corrected; both tables pinned by tests. The portable signal is per-collection concentration — stage **8c** **🛑 And its premise is now known to be wrong in the other direction too (W2, 2026-09-22): `ssuti`/`ssutí`/`ssutě` are an old spelling of *suť*, so the ratio gap this constant is fitted to has real language on BOTH sides of it. It separates doubled-letter shapes, not damage from vocabulary. D40 resolves to removing the guard.** |
| D26                                       | Wire the corpus lexicon into `compute_valid_ratio`'s existing `word_set` parameter               | `QUALITY_VOCABULARY_ENABLE` (ships false); `classify_TEXT.py` passes the lexicon. Admits *attested OR one known transformation from attested*, not attested-only                                                                                           | The function already takes a vocabulary and production never passes one, so it falls back to shape: >=3 chars, >=70% alphabetic, no odd characters. `compute_valid_ratio('oueussd edelite sektlll')` returns **1.00**. That value feeds `compute_quality_score` and every threshold below it, so the pipeline's primary is-this-text signal is shape-only and maximally fooled by this issue's population                                                                                                                                                                                                                                                                                                 | 🐛 **VOID, not zero — 07b never armed the flag (S2, 2026-09-20).** The 07b and 07c tables are byte-identical to each other as well as across their own arms: same 513 errors, same 40 Clear-loss, same `KL` to five decimals, 0 discordant rows. Two unrelated constants agreeing to five decimal places is not two null results. `quality_word_set()` is `@functools.lru_cache(maxsize=1)` over a ZERO-ARGUMENT function reading `QUALITY_VOCABULARY_ENABLE`; `ab_constant_eval.py` runs both arms in one process, reference first, so the `False` arm cached `None` and the `True` arm was handed it back. Demonstrated directly, and the signal it gates is real either way — `compute_valid_ratio('oueussd edelite sektlll')` is **1.00** shape-only and **0.00** with a word set. Fixed by **D28**. **D26 therefore has no measurement at all**; the coverage argument the previous cell made may still be true and is now untested. Stage **8b** is the re-run. Ships off, unchanged                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| D27                                       | Strip symbol glyphs in tokenisation                                                              | Extend `_STRIP_CHARS` past ``.,;:!?()[]"'\/`` to bullet and marker glyphs                                                                                                                                                                                  | The full table ends in `♦zkoumaná`, `✓stopy`, `•nevelká`, `■♦hiiliilhltiilahl` — a marker fused to a real word. It corrupts the lexicon at build time AND makes the real word unfindable at lookup time, both ends of the same table                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | 🐛 **07c's zero-effect result was an instrumentation bug, not a null finding — found and fixed 2026-09-19.** `_STRIP_CHARS` is a constant DERIVED from `STRIP_SYMBOL_GLYPHS` at import time (`if STRIP_SYMBOL_GLYPHS: _STRIP_CHARS = _STRIP_CHARS + _SYMBOL_GLYPHS`), but `override_constants()` only `setattr`s the names it is given — `STRIP_SYMBOL_GLYPHS` was overridden, `_STRIP_CHARS` never was, so 07c's true-vs-false A/B toggled a flag no downstream `.strip(_STRIP_CHARS)` call site could see. Verified directly: overriding the flag left `♦` out of `_STRIP_CHARS` every time. Fixed by a `_DERIVED_FROM_FLAG` mapping inside `override_constants()` that rebuilds `_STRIP_CHARS` (and restores it) alongside the flag; a regression test now exercises the override path specifically (`test_symbol_glyph_stripping_actually_arms_under_override`), which is the gap that let this hide — the shipped-default test only ever checked the flag off. 07c needs re-running (stage **8c**); the reported zero-effect figure is void, not confirmed. **And the fix was for an instance, not the class (2026-09-20)**: `_DERIVED_FROM_FLAG` covers a module constant built from a flag at import, but a zero-argument `lru_cache` freezes a flag's value in exactly the same way, and one was already in the tree producing exactly this symptom in 07b. See D28                                                                                                                                                                                                    |
| D28                                       | Clear flag-dependent CACHES in `override_constants()`, not just derived constants                | `_CACHES_FROM_FLAG: {flag -> (cached callable, ...)}`, cleared on entry AND on exit, plus a source-level guard test that fails when a new zero-argument `lru_cache` appears in `text_util` unregistered                                                    | D27 fixed one WAY a flag's value gets frozen (a module constant built at import). A zero-argument `functools.lru_cache` freezes it identically, on first call, for the life of the process — and `tools/ab_constant_eval.py` runs both arms of an A/B in ONE process, reference value first. `quality_word_set()` was exactly that, already in the tree, and it is what 07b measured (S2). Registering the flag against the caches it feeds is the class-level form of the same fix                                                                                                                                                                                                                       | ✅ **Landed 2026-09-20.** `_read_token_lexicon` and `_compile_vowel_run` need no entry — both are keyed on their arguments, so a changed threshold is a changed cache key and neither could go stale. `SHORT_GARBAGE_LEXICON_PATH` and `..._MIN_DF` are registered alongside `QUALITY_VOCABULARY_ENABLE` because `quality_word_set()` resolves through `token_lexicon()`. Suite 1252 → 1273 passing, 0 failed, `ruff` clean. **The guard test is the point**: it is what makes the next instance loud instead of silent                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| D29                                       | Ask for a CENSUS of the head plus a PROJECTABLE SAMPLE of the tail, not the whole queue          | `tools/build_annotation_sample.py`: split the queue by whether a label can change the decision, enumerate the at-risk head and every self-contradictory family, draw a stratified random sample of the tail, and write the sampling frame beside it        | H1/H2/H3 were three overlapping asks — 5,005 strings, a 2,424-row pack billed as a different population but drawn from the same 5,005 (S3), and "~200 singletons" with no frame to project from. Measured: **3,223 of 4,807 families are already `Trash`**, where a label cannot move the decision; 93 decisions reach **94.8%** of at-risk exposure; the remaining 1,506 families need 200 sampled rows, not 1,506 decided ones                                                                                                                                                                                                                                                                          | ✅ **Landed 2026-09-20**, with 13 tests covering the properties that fail silently: the census is complete, the sample is exactly the size the frame promises (largest-remainder apportionment), the frame describes the draw that happened, the weights recover the stratum sizes, the draw is reproducible from the seed alone, no family is in both tranches, and no `gold_categ` is ever pre-filled                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| D30                                       | Do not retune `SHORT_GARBAGE_LEXICON_GEMINATE_MAX_DF` from the eight-token table                 | Leave the value at `10`; add `geminate_cap_scale_warning()` and pin both tables in tests. Replace the signal by measuring **per-collection concentration**, which `build_token_lexicon.py` already emits columns for                                       | The cap is an absolute document count fitted to a 4.4x gap on 822 documents; at 113,100 the gap is 1.17x and no ratio threshold works either (S1). Picking ~200 off that table would be the fourth time in this issue a constant was set from the data meant to test it. An artefact belongs to the scanning run that produced it; an abbreviation does not — that is a signal the global count cannot carry                                                                                                                                                                                                                                                                                              | ⚠️ **Landed as a warning and a correction, not a value change.** The guard is currently wrong on 5 of 7 tokens whenever the full-collection table is configured, which is a real defect and is now loud. Stage **8c** is the experiment that could close it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| D31                                       | Make "all of the collection documents" SAYABLE, and the dedup measurable in the unit it votes in | Three CLI additions: `short_garbage_witness_report --input-dir` REPEATS (plus `--recursive`), the same tool gains `--by-group`, and `recategorize_from_csv` exposes `--no-postprocessing`                                                                  | Stage 8 has to read both archives, and nothing could. There is no common parent holding only ARUP and ARUB, and a staging directory of symlinks **does not work**: `pathlib.Path.glob("**/*.csv")` does not follow directory symlinks before Python 3.13, so it returns an EMPTY LIST **without failing** — verified on the venv's 3.11, and precisely the silent-scope error this issue keeps producing. Separately, H6's decision needed the dedup's blast radius in (document, string) VOTES rather than lines, and the pre-cascade frame it should be measured on could not be written: `recategorize_dataframe` has always taken `apply_postprocessing`, and only `ab_constant_eval` ever exposed it | ✅ **Landed 2026-09-20**, 17 tests. The witness report prints a document count PER ARCHIVE before it reads a line, so "did it see all the documents" is answered in the log rather than assumed, and it refuses the same corpus reached twice. One test pins the symlink behaviour as a fact about the platform, so if a future Python fixes it the test fails and says so                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |

> **D7's measured cost.** Closing the label lexicon narrowed `is_domain_notation()` from 30 of the 508 graded lines to 24, and all six that dropped out land in `Trash`: `Orientace: SZ-JV`, `Orientace: SV-JZ` and `XII: 2` (annotated `Clear`), `Komponenta: H` and `XIV: 7` (annotated `Noisy`), `Bokalisace: B-XII-c` (annotated `Noisy`, previously lifted to `Clear`). **Five of the six were right before.** `orientace` and `komponenta` are simply not in the 22-item set; `XIV` / `XII` satisfy the label shape without being words. Weighted agreement of upstream alone moves 19.9% → 17.3%. It does **not** reach the patch — `f8ad4d8 + patch` is identical to `8fadeb9 + patch` on all 508 lines. Adding the two missing labels is a cheap, well-evidenced follow-up.

> **D6's corpus verification.** `is_domain_notation()` over both collections, 25.6M scored lines: 237,820 notation lines, 77,936 (32.8%) below `HARD_SWEEP_LANG_MAX` — against a corpus base rate of 11.7%, so notation is ~2.8× more likely to fall under it — 58,427 of those currently `Clear`, **0** carrying a perplexity the sweep would accept, and 50,221 sitting exactly at `SHORT_PPL_CAP`. Keeping hard sweep armed is safe **while the cap stands**. Those 58,427 lines are `Clear` because of the cap, not because the language gate cleared them; anyone lifting `SHORT_PPL_CAP` inherits that dependency, and it is the reason the cap experiments in §2 were not pursued further.

> **D8's exit condition.** The xfail is `strict=True` and keeps `Trash` as the expectation, so it goes red — as `XPASS(strict)` — the moment something fixes it. That requires the witness to reach a site that actually returns `Trash`: **gate 6, which is D15**. A section-7 fix would leave the test `xfail` forever while `oueussd` improved from `Clear` to `Noisy`, which is exactly the trap §8 records. When D15 lands, the correct response is to delete the `pytest.param` wrapper and restore the plain fixture row, **not** to relax the marker. The same flip will happen at the same moment in `test_inverted_trash_stays_trash_swept_bounds`, which is parametrized over the same list.

## Algorithm sketch — where a short line is decided now

1. **Section 1** — three perplexity routes. Notation is exempt from `rule_extreme_ppl` and `rule_absolute_ppl`; `rule_hard_sweep` still applies when FastText independently failed to place the line. At `wc <= 2` none of the three can fire at all, because `SHORT_PPL_CAP` (850) sits below every threshold.
2. **Sections 2–4** — inverted scan, all-caps, garbage density. Unchanged. Notation is not immune here: `Reg.Bez.Aussig.` sits at `rot_ratio` 0.583 against a 0.65 threshold, `II/C` at `garbage_density` 0.25 against 0.35.
3. **Gate 6 `rule_short_garbage`** (`text_util.py:1353-1371`) — skipped when `forgiven`, `structured`, or `is_domain_notation()`. **Now gated**: it fires (`_fire` is recorded above the gate, so telemetry still sees exposure) but returns `Trash` only when `qs < thresh_trash` **and** `_has_strong_garbage_evidence()`. On the disputed population that predicate is `False` throughout, so the rule convicts none of it (§6). **This is the only site in this path that returns `Trash`, and therefore the only place the witness can go.**
4. **Gate 7 `rule_short_line`** (`text_util.py:1380-1439`) — total for `wc <= 2`; every branch returns, so §8–§10 and `check_rescues()` are unreachable for that population. Its `damage` term already reads the discriminating witness and discards it via `structurally_clean` (§5); its `damage` branch returns `Noisy`, never `Trash`; its final branch returns `Clear` on `valid_word_ratio >= 1.0`, with `categorize_line` clamping the stored score *up* to `CATEG_NOISY_SCORE_MAX` (0.80) — `II/C` goes from a raw 0.4792 to a pinned 0.80.

Step 4 is where D11 would have belonged, and it is where the measurement killed it. **D15 belongs at step 3** — that was the error corrected in §8: step 4 cannot return `Trash`, so a witness placed there converts "garbage reaches `Clear`" into "garbage reaches `Noisy`" without ever restoring a `Trash` verdict, and the strict xfail that tracks the debt never flips.

## Phased plan

| phase                                                 | work                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | done-criteria                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
|-------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **0 — unblock** ✅                                     | D12: merge `test` into the PR branch to clear the inherited `docker-build-smoke (api)` failure. Hub-side cause also fixed — `docker-build-smoke` now runs on pushes to `test` and on `v*` tags, with both probe budgets derived from the `HEALTHCHECK` convention instead of guessed                                                                                                                                                                                                                            | ✅ Done. ⚠️ The hub fix reaches the five consumer repos only when the `v1` tag moves; it currently points at `ad1a788`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **1 — de-blind** ✅                                    | `ALL_FIXTURES` removed and fixture arities pinned (a `pytest.param` row breaks any positional unpack); three golden edge cases that can actually see the gate; `test_smoke.py` migrated off the third hand-rolled harness; a `Trash`-side swept-bounds counterpart; four stale `penalty_*` names fixed in both ablation drivers and pinned by a test; the `TUNABLE_CONSTANTS` drift hole closed                                                                                                                 | ✅ 814 → 891 passing. The golden entries went red when the gate landed — that was the artifact, and regenerating them was a separate reviewed commit inside the PR                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **2 — ship the trade** ✅                              | Merge the evidence gate as-is, xfail retained as recorded debt                                                                                                                                                                                                                                                                                                                                                                                                                                                  | ✅ **`070620f`, green.** The golden regeneration diff shows one desirable promotion (`malakofauna`, `Equus caballus`) beside one undesirable one (`oueussd`) — the trade in a single artifact. ⚠️ The interim cost (~26,000 garbage lines at `Clear`) belongs in the release notes, not only in this thread                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **3 — narrow it** 🔲 **LIVE, first measurement in**   | D15: wire `_has_shape_garbage_evidence()` into gate 6 (two lines) with `_fire("rule_short_garbage_witness")`, registering the name in `tools/rule_coverage_report.RULES`, `RULE_COVERAGE.md`, both ablation lists and `tests/test_pipeline_parity.UNREACHABLE_RULES` (the flag ships off, so it cannot fire in `EDGE_CASES`). Then measure on the 508 through `_rescore_row`, A/B with `tools/ab_constant_eval.py`, and **only then** flip the flag. De-stale the four in-tree notes in the same commit (Risks) | ⚠️ **The first measurement FAILED this criterion** — 12 fixed against 12 broken on the 508, not "no loss". ✅ **2026-09-18, guarded re-run: 14 fixed against 1 broken** on the 2,064-row gold set (errors 513 → 500, cost down, exact McNemar p = 0.00098). The roman-numeral break class @david-spacil identified is closed, and the single remaining break is not the witness's — it comes from the modal dedup in `apply_document_postprocessing()`, which no per-line guard can reach. **Still not "no loss", so the flag still does not flip**, and what it now waits on is annotation (H1) plus the dedup decision (H7), not more code. The roman-numeral exemption is the response and clears every named break, and a **taxonomic-suffix exemption** (D18) clears a second, larger class the criterion never tested. The criterion is met only once the 508 are re-scored with both. **2026-09-17: no longer blocked** — the rows are on the cluster and `ab_constant_eval` can now express a boolean, so this is runbook step 5a rather than a wait. Then: D17's four constants move into `_THRESHOLD_NAMES` + `SEARCH_SPACE` in the same commit as the flip; `oueussd` reports `XPASS(strict)` in **both** tests and the `pytest.param` wrapper is deleted; a both-collections recompute (~2 h GPU) for the record |
| **4 — the residue** ⚠️ **LANDED, OFF, MEASURED ONCE** | D14 shipped, on a different substrate than this plan assumed — corpus token document-frequency, not korektor, so the CC BY-NC-SA blocker never applied. Veto plus an unattested-token conviction clause, each behind its own key. Also the two missing label-lexicon entries from D7's regression note, which are cheap and well-evidenced                                                                                                                                                                      | The go/no-go is now **measurable** and has had its first reading: the mechanism separates the populations (`edelite` is reached at last), but `min_df` 3 on the 822-document table convicts 5 of 31 pinned vocabulary fixtures. **No-go at this setting.** Next: rebuild over both full collections — 822 documents is 0.7% of them — then score against gold, not against the pipeline's own labels                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |

Phase ordering mattered in one specific way: **de-blinding before narrowing**, which is why Phase 1 went first. Every offline tool in `tools/` grades against the pipeline's own stored `categ`, so a label-changing improvement scores as pure damage on all of them. (The `flip_rate == 0` baseline this document once cited is stale: `tests/test_recategorize_parity.py` now allows 5% globally and 30% per document, relaxed for the structural rule additions, while `tools/SWEEP_NOTES.md` still asserts the old figure — flagged there.) Without a gold set wired in, Phase 3 cannot tell success from regression, and the in-tree corpus is 15 lines.

## Risks / open questions

| Risk                                                                                | Mitigation                                                                                                                                                                                                                                                                                                                                                                                           | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
|-------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **The core trade is now shipped, unresolved**                                       | ~26,000 garbage lines reach `Clear` against a 5.5 : 1 improvement ratio. Accepted deliberately and in the open; D15 is the narrowing path and D8's strict xfail is the tracking device                                                                                                                                                                                                               | **Accepted, tracked, live in production** — Phase 3                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ~~**No gold set in the repo — the blocker for Phase 3**~~                           | Both sets arrived 2026-09-10 and are in the tree: `tools/gold/sidecars/issue30_gold_2067.csv`, 2,067 rows over 816 documents (508 `issue30_508` + 1,559 `calibration_1567`), joined onto a delivered batch with `--gold-sidecar` and scored through the existing `--gold-column` path                                                                                                                | ✅ **Closed.** ⚠️ The gold is here; **the rows it labels are not** — scoring still needs the delivered `2907` batch. And read `GOLD.md`'s provenance note first: the `issue30_508` labels were partly *derived* before 2026-09-10, and 55 of 508 changed on re-annotation. The `30.audit.md` / `30.strategy.md` attachments are still not in the tree                                                                                                                                                                                                                                                                                                                          |
| **The witness's separation was a smoke test — now measured, and it broke 12 lines** | Measured on the 508 (2026-09-10): flag-on moves 26, **12 fixed / 12 broken**, 2 borderline — not the projected "6–7% at no cost". Every break carried a roman numeral, a class the 0/37 smoke test contained none of. `_has_shape_garbage_evidence()` now exempts roman numerals **above all four clauses** (reusing `_RE_ROMAN_TOKEN`), which clears all 7 named breaks and keeps all 5 named fixes | **Open, narrowed twice, and no longer blocked.** ⚠️ **The 12/12 has NOT been re-run** against either exemption. ~~that needs the 508 scored through `_rescore_row`, i.e. the delivered batch~~ — the rows are on the cluster (2026-09-17); this is runbook step 5a. The flag stays `false` until it is run. Costs pinned in the suite: a 7-glyph all-numeral stutter (`DIDIDID`) is exempt, and the taxonomic exemption costs **zero** convictions                                                                                                                                                                                                                            |
| **Harness divergence — three instances, the mitigation is discipline**              | `tests/test_rotation_regression.py` fed the `LANG_SCORE_REMAP` cap where production feeds the trust score; `_categ` hardcoded `ces_Latn` (trust tier 1.0 against `TRUST_TIER_UNKNOWN` 0.50), which is what made the three fixture candidates look non-discriminating; `test_smoke.py` carried the same bug a third time plus omitted `orig_lang_score`/`garbage_density`                             | **All three closed — and the count is now six.** ~~`tools/short_garbage_witness_report.py` is text-only by construction so it cannot repeat it.~~ It did: it carried its own copy of the witness clauses, and the roman-numeral exemption landed in one and not the other (digest §5, instance 4). Instance 5 is `_loo_metrics` scoring against the pipeline's own output; instance 6 is a gold sidecar swept in as corpus rows, reporting 2,082 lines instead of 15. "Text-only by construction" was true and irrelevant — the bug was duplication. Any figure in this document not marked as re-measured must be re-run through `_rescore_row` before it is quoted publicly |
| ✅ **Four in-tree notes still describe PR #48 as open**                              | `text_util.py:1757-1765` ("NO CALL SITE YET… would collide with the one-hunk patch under review in PR #48"), `tools/short_garbage_witness_report.py:9-11` and `:278`, `docs/categorization_logic.md:315` and `:317`, `agent_dev_logs/DEVLOG.md:365-366`. All say the wiring is blocked; it is not                                                                                                    | ✅ **Closed — and there were six, not four.** The checklist below names them, including `tests/test_calibration.py:445` and `tests/test_short_garbage_witness_report.py:4`, which this row missed. Prose only, no behaviour. They belong with the wiring rather than in a separate sweep                                                                                                                                                                                                                                                                                                                                                                                       |
| The vowel-run clause's loan false positives                                         | `Poaceae`, `Naiade`, `Beaune`, `Radiouhlik`, `Sauerstoffflasche` are flagged. Most carry `weird_ratio == 0.0` and never reach gate 6 — a real but **thin** margin, because it depends on a signal outside the predicate                                                                                                                                                                              | **Open** — measuring that class is a precondition for flipping the flag, not a follow-up                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| Dot-chains still reach `Clear`                                                      | Independent of the merged patch: periods collapse a line to `wc == 1`, the cap flattens perplexity, gate 7 reads `valid_word_ratio >= 1.0` (`ALLOWED_INTERNAL` contains `.`)                                                                                                                                                                                                                         | **Open** — same root cause as #30, deserves its own issue. D15 would reach part of it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| The blend is uncalibrated                                                           | `PAGE_PPL_BLEND_WEIGHT`, `PAGE_PPL_LONG_MIN_WC`, `PAGE_PPL_MIN_LONG_LINES` have never been measured on a real collection; `data_samples/` holds 15 synthetic lines and zero notation                                                                                                                                                                                                                 | **Open** — flag stays off. `service/text_inference.py::_classify_lines` must also be wired before it is turned on                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| The blend is a page-consistency prior, not a garbage detector                       | On a mixed page it pulls a garbage token toward its clean neighbours                                                                                                                                                                                                                                                                                                                                 | Documented in `docs/categorization_logic.md` and pinned by `TestKnownLimits` (`tests/test_page_perplexity_blend.py:281`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| Notation still convicted by hard sweep with the cap lifted                          | **Intended, and now corpus-verified** — see D6's note. The predicate accepts >50% of capitalised dot-chained garbage, so it cannot carry a hard-sweep exemption. A vowel-per-segment rule (~40% remaining, loses `Kr.Hr.`/`St.Pol.`) and a short-majority rule (~64%) were both measured and rejected                                                                                                | **Closed** — `TestNotationIsNotExemptFromHardSweep` pins the hole as still present                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Ablation tooling entangled                                                          | `rule_domain_notation` is no longer independent of the perplexity routes, so ablating it re-arms two rules at once and `SWEEP_NOTES.md` fire counts (94 / 64 / 4) will shift                                                                                                                                                                                                                         | **Open** — no test asserts on ablation output; numbers need regenerating. Coverage was last regenerated on 2,171 lines: 16 LOAD-BEARING / 3 REDUNDANT-HERE / 3 DEAD                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `service/text_api.py` double-imports its own app                                    | `uvicorn.run("text_api:app", ...)` re-imports the module under a second name, so the module body runs twice and `app` / `_state` / `TextModelManager()` exist twice; the served `_state` is not the one `__main__` holds                                                                                                                                                                             | **Open** — separate from #30, deserves its own issue                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |

## Docs and assertions revised, and what still needs it

Done in earlier rounds:

- `tests/test_text_utils.py` and `is_domain_notation()`'s docstring both said separating `malakofauna` from `oueussd` needs a lexicon. **Reworded, not deleted** — the conclusion (notation predicates must not claim vocabulary) survives, the reason does not. The honest form, now written in both places, is that separating `malakofauna` from **`edelite`** needs a lexicon.
- The same claim in the block comment above `_RE_NOTATION_*`, likewise reworded.
- `docs/categorization_logic.md` row 5 records that gate 6 is the only structural `Trash` route without a second witness, and that requiring `_has_strong_garbage_evidence()` here is a suspension rather than a narrowing; row **5c** documents `_has_shape_garbage_evidence()` with its clauses, its two deliberate exclusions and its known limit.
- `tools/SWEEP_NOTES.md`'s captured run log carries a staleness warning instead of a rewrite: its figures were correct when taken, they describe 14 of today's 22 rules, and the `flip_rate == 0` baseline it asserts is no longer what the suite enforces.

✅ Still to do, in the D15 commit — *done with the D15 wiring: the stale notes de-staled (six, not
four) and the registry obligations met, per the checklist below; the calibration comment block
stands as instructed.*

- **De-stale the four post-merge notes** listed in the Risks table. Row 5 of `docs/categorization_logic.md` needs its "returns unconditionally" wording corrected as well — that is now false.
- Registry obligations for the new `_fire` name: `tools/rule_coverage_report.py`'s `RULES` is asserted equal to the `_fire()` call sites by `tests/test_rule_coverage.py`; both ablation lists are asserted to be subsets of it; and `tests/test_pipeline_parity.py::UNREACHABLE_RULES` needs an entry, because `EDGE_CASES` run at default config where the flag is off.
- `tests/calibration_fixtures.py`'s trust-tier comment block stays correct and load-bearing; extend it rather than replacing it.

## Note for whoever runs the sweep next

The tuner observation is correct and worth acting on. `SHORT_PPL_CAP` sweeps [300, 950], so no single value reaches the default `HARD_SWEEP_PPL_MIN` of 1,000 — the one-constant move is outside the space. But `HARD_SWEEP_PPL_MIN` sweeps [500, 3000], so the **joint** move is inside it. Verified:

```
SHORT_PPL_CAP = 950, HARD_SWEEP_PPL_MIN = 500  ->  validate_constants() passes
(coerce_constants raises PERPLEXITY_THRESHOLD_MAX to 1000 automatically)
```

At that setting the cap sits *above* the hard-sweep floor, so perplexity reaches `rule_hard_sweep` at `wc <= 2` without leaving the sweep space. That is the configuration the manual cap experiments could not express, and it is the one a joint sweep would find on its own. Note it interacts directly with D6: at `wc <= 2` it arms the route that convicts notation, and D6's corpus note quantifies exactly how many lines depend on the cap staying where it is (50,221 sitting on it). Research §2 and the cap variants in §3.5 of the digest are reasons to expect little from it — but the sweep should be allowed to say so itself.

## A decision-quality checklist for this class of change

Derived from what actually went wrong in this thread, and worth applying to D15 before it ships:

1. **Is the headline metric monotone by construction?** "367,208 lifted / 0 demoted" was — removing an unconditional `return Trash` cannot demote anything. A direction that was never in question is not evidence.
2. **Does the harness compute what production computes?** Three harnesses in this repo have fed `categorize_line` a value the pipeline never produces. All three are fixed; the fourth is the one to watch for.
3. **Can the test suite see the change?** The old golden `short_garbage` case was set up so that the gate under review could not alter its outcome.
4. **Does the new feature reach a rule?** `apply_page_perplexity_blend()` and `rule_domain_notation` both shipped in `8fadeb9` unable to change any outcome, for two different reasons. `_has_shape_garbage_evidence()` is the third, deliberately — and D15 is what ends that.
5. **Is the analogy load-bearing or decorative?** The "seventh sibling of six gated rules" symmetry did not survive reading `50472fb`'s diff.
6. **Is the signal absent, or present and discarded?** Research §5 is the case in point — three rounds concluded a lexicon was required while the discriminating witness sat two lines above the conjunct that suppressed it.
7. 🆕 **Did you run it, or read it?** Both errors in §8 passed code review and failed on execution. Neither was visible from reading.

## Pre-cluster-run hardening (2026-09-10)

Landed ahead of the next round of runs — parameter optimization and a
full-collection recategorization — because all of it is cheaper to fix than to
discover afterwards. Everything here was measured on `origin/test` @ `8a84108`.

### The sweep could not have produced a meaningful number

| # | Defect                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Fix                                                                                                                                                                                                       |
|---|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 1 | **`evaluate_dataframe` did not mask unannotated rows.** `normalize_category` maps a blank cell to `""`, a sidecar join leaves every unmatched row blank, and unmasked that becomes a sixth class. On a 15-row frame with 2 annotated: `line_count` **15**, `macro_f1` **0.028**, `flip_rate` **0.933**, `''` class support 13. On a real corpus the annotated share is under 1%, so `macro_f1` — the default `--metric` — measured **the annotation rate**, not agreement | `annotated_mask()` is now the single definition of "annotated" and both gold paths use it. `_gold_report` had always masked; the path every sweep trial takes had not                                     |
| 2 | **`--gold-sidecar` without `--gold-column` scored self-referentially**, after printing a reassuring "N labels matched" line                                                                                                                                                                                                                                                                                                                                               | Refused outright                                                                                                                                                                                          |
| 3 | **`run_optim_pipeline.sh` passed no gold flag at all** — the entry point `SWEEP_NOTES.md` recommends                                                                                                                                                                                                                                                                                                                                                                      | `GOLD_SIDECAR` / `GOLD_COLUMN` threaded to every stage, plus a warning banner when absent. Also `$PY_BIN` in all stages (the ≥3.11 guard validated a different interpreter) and `set -uo pipefail`        |
| 4 | **`rule_coverage_report.py` was the one loader with no sidecar join** — and it is stage 1 of 6                                                                                                                                                                                                                                                                                                                                                                            | Wired, via an explicit `gold_args` parameter                                                                                                                                                              |
| 5 | **Three swept constants could not move anything.** `PAGE_PPL_BLEND_*` were registered tunables while `PAGE_PPL_BLEND_ENABLE` ships false — the exact D17 failure mode the witness four were quarantined to avoid, missed on the rest of the registry. `rule_short_garbage_witness` sat in both ablation lists for the same reason                                                                                                                                         | Moved to `_DELIBERATELY_NOT_TUNABLE` (40 → 37 in both `_THRESHOLD_NAMES` and `SEARCH_SPACE`) with the same "restore in the commit that flips the flag" instruction. Rule dropped from both ablation lists |

### The gold set is inverted relative to the population

Decade parses from the document identifier. The sample is stratified, and its
strata are the opposite of the corpus's:

|                 | % of changed population | % of gold sample |              |
|-----------------|-------------------------|------------------|--------------|
| 1920s           | 0.1%                    | 5.4%             | **95x over** |
| 2010s           | 42.5%                   | 4.5%             | **0.1x**     |
| **2010s+2020s** | **55.5%**               | **9.3%**         | **6x under** |

Unweighted agreement over the 484 in-population gold rows is **62.8%**;
reweighted by decade population, **72.7%**. A ten-point swing from composition
alone, and `_metrics_from_labels` applied no sampling weight at all — the
`weighted_f1` it reports is weighted by *class support* and will be misread.

**The documents this issue is about are the thinnest-covered part of its own gold
set.** Pooling compounds it: 1,559 of the 2,067 rows are `calibration_1567`
lines the patch never moves, so 75% of a pooled objective comes from outside the
population being tuned.

Fixed: the gold report now always prints the breakdown by `gold_source` and by
decade beside the headline, warns when agreement varies by ≥10 points across
strata, and accepts an optional `gold_weight` column threaded as `sample_weight`
through every metric. **Weighting stays opt-in** — the design weights are the
annotator's, and inventing them here would be worse than reporting honestly that
the number is unweighted.

### The recategorization CLI was not shaped for 113k documents

`main()` globbed `*.csv` non-recursively with no `--recursive` flag (a
two-archive layout silently processed nothing and reported success) and defaulted
to **in-place overwrite**, destroying the baseline the run is measured against.
Now: `--recursive` with the output tree mirrored so same-named documents in
different archives cannot collide; `--out` or an explicit `--in-place` required;
per-file `try/except` so one malformed CSV cannot end a multi-hour run, with a
failed-file list and a non-zero exit; per-file progress; and an atomic
temp-then-rename write so a kill mid-write cannot leave a truncated CSV.

### The config path was resolved against the working directory

`text_util.py` and `classify_TEXT.py` both defaulted to the *relative*
`setup/config.txt`, so a process started anywhere but the repo root ran on
in-code defaults — `text_util` after one stderr line, `classify_TEXT` in total
silence — while `tools/recategorize_from_csv.py` resolved the same file
absolutely. One process, two configurations.

Measured: **0 of 84 scalar constants differ today**, so this was latent. That is
the argument for fixing it now rather than later — this round of runs exists to
*change* those constants, and the bug goes live the moment `setup/config.txt`
stops mirroring the defaults. Both are now anchored to their own module.

### `override_constants()` did not rebuild derived constants (2026-09-19)

The same class of bug as the config-path fix above, found while chasing 07c's
"zero effect on gold" result rather than latent and waiting. `_STRIP_CHARS`
(D27) is DERIVED from `STRIP_SYMBOL_GLYPHS` — built once, at import time, by
`if STRIP_SYMBOL_GLYPHS: _STRIP_CHARS = _STRIP_CHARS + _SYMBOL_GLYPHS` — the
same way `ROT_GHOSTLIST` and `_LANG_DIACRITICS` are already documented as
built once and *not* rebuilt by `override_constants()`. Those two are a known,
accepted limitation; `_STRIP_CHARS` was the same limitation, just never
recognised as an instance of it. `override_constants()` only `setattr`s the
names it is given, so a test or an A/B run that overrides `STRIP_SYMBOL_GLYPHS`
alone moves the flag and nothing downstream: `_STRIP_CHARS` keeps whatever it
was built with at import, and every `.strip(_STRIP_CHARS)` call site — 28 of
them — never sees the difference.

Verified directly against this repository: overriding `STRIP_SYMBOL_GLYPHS`
to `True` left `♦` out of `_STRIP_CHARS` on every check. This is why 07c
measured `STRIP_SYMBOL_GLYPHS` true vs false as bit-identical on all 2,064
gold rows — not a population-coverage gap, as first written up, but the A/B
harness never arming the flag it claimed to be testing.

**Fixed.** `override_constants()` now takes a small `_DERIVED_FROM_FLAG`
mapping (`{"STRIP_SYMBOL_GLYPHS": "_STRIP_CHARS"}`) and rebuilds — and
restores — the derived value in the same pass as the flag, generalising
cleanly if a future flag needs the same treatment. A regression test,
`test_symbol_glyph_stripping_actually_arms_under_override`, exercises the
override path specifically — the existing `test_symbol_glyph_stripping_ships_off`
only ever checked the flag at its shipped default, which is exactly the gap
that let this hide. Both pass, and the full suite (`pytest tests/`, minus the
two service-API modules this sandbox has no `fastapi`/`jsonschema`-free path
to) is otherwise green.

**07c is void, not zero.** The reported "zero effect on gold" result must be
re-run now that the flag actually reaches tokenisation; D27's status is
updated to reflect that above.

### Still not done, and gating the flag

**Revised 2026-09-17.** One of these is closed, one was closed by being measured
and failing, and the third turned out not to be blocked at all. Commands for all
of them are in [`../../30.runbook.md`](../../30.runbook.md). *(2026-09-23: that file never
entered the repository, so the link is dead. The steps lived only in the #30 cluster job scripts,
which are not tracked; the in-tree pointers now name those scripts and the plan or findings
section that holds each result.)*

- ✅ **The 508 re-scored against the narrowed predicate — done 2026-09-18**, as part of
  the full 2,064-row gold set: 14 fixed, 1 broken, p = 0.00098. Since the 508 are a
  subset, breaks within them are bounded at 1. @david-spacil re-scoring them directly
  is H5, which closes it without relying on set inclusion. ~~Still the gate, and the
  predicate has been narrowed twice since the measurement (roman numerals, then
  D18's taxonomy).~~ ~~`SHORT_GARBAGE_WITNESS_ENABLE` stays **false** for this
  round~~ — it still does, but for want of a run rather than a delivery: the rows
  are on the cluster, and `ab_constant_eval --values` can now take `false,true`,
  which it could not when this bullet was written. Runbook step 5a.
- ~~**The loan false-positive class** (`Poaceae`, `Naiade`, …) — still a stated
  precondition for flipping the flag, still unmeasured.~~ ✅ **Measured, and it
  failed.** `-aceae` is the botanical family suffix and the `vowel_run` clause
  convicted 10 of 10. D18 is the response, at zero cost in convictions. The three
  name-shaped loans (`Naiade`, `Beuern`, `Oueste`) are **not** closed: the
  lexicon does not reach them either (df 0/0/1 on the built table), so that
  sub-class has no mechanism and the plan should stop implying it does.
- ~~**The sidecar join against the real batch.**~~ ✅ **Now a first-class command,
  and the guard behind it was broken.** `--gold-preflight` reads only
  `(file, page_num, line_num)`, scores nothing, and splits the unmatched keys into
  *document absent* (wrong batch) and *locator absent* (page/line drift) — a
  distinction the old output could not make, because it never got past the first
  document: `attach_gold_sidecar`'s zero-match raise is a corpus-level check and
  was being applied per document, which killed an 822-document run at file 1.
  `data_samples/DOC_LINE_CATEG` still matches zero gold keys; that is correct, and
  the pre-flight now says so with a diagnosis instead of an exception.

**New, and gating the lexicon rather than the witness:** the token table has been
built once (822 documents, 12.7M lines, 347,097 tokens) and read. The mechanism
works — `edelite` is reached — and `min_df` 3 convicts 5 of 31 pinned vocabulary
fixtures, so `SHORT_GARBAGE_LEXICON_CONVICT` stays **false** on evidence. The next
step is a rebuild over both full collections, not a threshold tweak: 822 documents
is 0.7% of them, and `Skelettmaterial` at df 2 is as much a thin-corpus artefact as
a rare word.

## Owner-assigned actions — what only @DanaKriv or @david-spacil can do

> 🔁 **Rewritten 2026-09-20.** The old table had ten rows and the first three were the same
> population asked for three times (S3). It is now four asks for @DanaKriv and four for
> @david-spacil, and the annotation one is **293 decisions** rather than 5,005 + 2,424 + 200.
>
> **And the list is no longer what the flag is blocked on.** 07d already reports the
> adoption gate satisfied (S5); stage **8a** confirms or refutes that without a single new
> label. What annotation buys is confidence that the result generalises past the 23 labelled
> rows out of 20,078 — which is a different and still necessary job, but it is no longer
> the thing standing between here and a decision.

> ⏳ **Extended 2026-09-22 (evening).** H9–H13 are the questions put to the issue thread after
> [comment 61](https://github.com/ufal/atrium-alto-postprocess/issues/30#issuecomment-5783543756)
> as Q1 and Q4–Q6; Q2 is H3's dissent, Q3 is H1/H2, Q7 is H5's revision. H1–H3 and H5 carry their
> current status in the first column. **H14 (2026-09-23) is Q8**, put to @david-spacil alone.

Written-out instructions for both, in plain English and assuming no knowledge of this
thread, are in [`../../docs/issue30/issue30_annotation_guide.md`](../../docs/issue30/issue30_annotation_guide.md)
(@DanaKriv) and [`../../docs/issue30/issue30_review_request.md`](../../docs/issue30/issue30_review_request.md)
(@david-spacil). Those are the documents to attach; the table below is the index.

| #                                                                                                                                                                                                        | Owner         | Action                                                                                                                                                                                                                                                                                                                                                                         | Why it cannot be done here                                                                                                                                                              | Done when                                                                                                                                                                                                               |
|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|---------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **H1** ⏳ **delivered, awaiting answers (Q3)**                                                                                                                                                            | @DanaKriv     | **`census.csv` — 157 decisions, 1,217 lines (18.1% of at-risk exposure; 15.9% without the 39 `confirms_trash` checks)** — re-cut 2026-09-22 from the lexicon-on queue (`d9c41a5`); the 93 / 14,254 / 94.8% it replaces was sized on the lexicon-off queue (T1). The at-risk head of the queue plus every family the pipeline currently answers two ways. Complete, not sampled | Domain judgement on archival OCR. **23 of 20,078** lines the rule can reach carry a human label today (0.115%)                                                                          | The file comes back with `gold_categ` filled; blanks are skipped, never guessed                                                                                                                                         |
| **H2** ⏳ **delivered, awaiting answers (Q3)**                                                                                                                                                            | @DanaKriv     | **`sample.csv` — 200 decisions**, a stratified random draw from the 5,445-string at-risk tail (±6.9 pp; the 1,506-family tail was the lexicon-off queue's). Keep `frame.json` with it                                                                                                                                                                                          | The tail is 1.1 lines per family; enumerating it costs 1,506 decisions for one twentieth of the answer, and a sample without its frame is 200 anecdotes                                 | Returned together with `frame.json`, so the result can be projected with an interval                                                                                                                                    |
| **H3** ◐ **ANSWERED by @david-spacil (§ 2, option 1) — open only for @DanaKriv's dissent (guide § 7.1, Q2)**                                                                                             | @DanaKriv     | **Policy call on the modal dedup.** *(Full-archive figures since: 43,103 groups, 51 contested, 10 readable lines pulled down against 51 rescued — V6.)* Convicting some occurrences of a repeated string demotes *all* of them in that document. Acceptable, or should a bare plurality never demote `Clear` → `Trash`?                                                        | A decision about what the output is for, not a fact about it                                                                                                                            | Answered either way. **Now costed**: the mechanism's reach here is **7 groups / 22 lines** (S4), and removing it also gives up a cascade that removes 15 baseline `Clear`-losses and 2 of the witness's 3 breaks        |
| **H4**                                                                                                                                                                                                   | @DanaKriv     | **Re-review the rotation-heavy divergence (R5)** against a fresh re-run — 95 lines ARÚP, 10,258 ARUB                                                                                                                                                                                                                                                                           | The 2026-07-29 review compared against the shipped batch, not a re-run, so no assessment of current code exists                                                                         | Queued behind K4TEL producing the re-run                                                                                                                                                                                |
| **H5** ✅ **CLOSED 2026-09-23 — re-scored on `master`: 326 → 335/508, 11 fixed / 2 broken / 3 wrong either way (Q7); one break was D45. ✅ Confirmed after the restore: 336/508 on `3b02959`, 11 / 1 / 3** | @david-spacil | **Independently re-score the 508 against the guarded predicate**                                                                                                                                                                                                                                                                                                               | Round 2 bounds breaks within the 508 at ≤ 1 *by set inclusion* — sound but indirect, and it was his finding and his tooling                                                             | A direct figure on the 508 alone. Changed since his measurement: the roman-numeral, taxonomic-suffix and `S-VIIIb` exemptions, the de-gemination guard (since removed, D40), and — after the tag — D44's language split |
| **H6** ✅ **CLOSED 2026-09-22 — option 1 (accept as is); the bare-plurality guard (option 2 below, option 3 in the review request) dropped, the smoothing-key A/B not pursued**                           | @david-spacil | **Choose among the three dedup options** — now costed on both sides                                                                                                                                                                                                                                                                                                            | Changing a smoothing step that touches ~27% of this population on a reading rather than a measurement is how this issue has gone wrong three times                                      | One option chosen. On the delivered numbers the ranking is **1, then 3, then 2** — see below                                                                                                                            |
| **H7** ✅ **ANSWERED 2026-09-22 — four are real language, four are damage (W2)**                                                                                                                          | @david-spacil | **Confirm the eight doubled-initial tokens at full scale.** His 2026-09-19 reading (`ppole` is an abbreviation, the other seven are artefacts) was made against the 822-document table. At 113,100 documents the two populations no longer separate on any axis the code has (S1)                                                                                              | He is the only reader who can say whether `ssuti` at df 195 across 113,100 documents is still a scanning artefact, or whether corpus scale has turned some of them into conventions too | Each of the eight labelled *abbreviation / convention* or *scanning artefact*. That is what decides whether D30's per-collection experiment has a target worth fitting                                                  |
| **H8** ✅ **CLOSED 2026-09-22 — finished, nothing further intended**                                                                                                                                      | @david-spacil | **Confirm the raw-perplexity thread is finished** — 367,208 lines delivered via filesender, read through `tools/issue30_perplex_report.py`                                                                                                                                                                                                                                     | Only he knows whether more was intended                                                                                                                                                 | Closed, or a further delivery named                                                                                                                                                                                     |
| **H9** ⏳ **asked 2026-09-22 (Q1)**                                                                                                                                                                       | @DanaKriv     | **Agree or dissent with W4** — `Trash` = illegible, legible = `Clear`, decipherable = `Noisy`, usefulness does not count, `Non-text` left blank when labelling                                                                                                                                                                                                                 | @david-spacil framed it as his view; she is the other half of it, and the gold is scored against it                                                                                     | A sentence either way. On agreement: 11e closes, and Q4/Q6 become answerable                                                                                                                                            |
| **H10** ⏳ **asked 2026-09-22 (Q4)**                                                                                                                                                                      | both          | **What category every recognised web / e-mail address gets** — the value of `DOMAIN_NOTATION_CATEG` (D43, ships empty)                                                                                                                                                                                                                                                         | A policy call: the route cannot separate a damaged address (`e-mail: officeauappmost.cz`) from a correct one, so `Clear` over-rates one and `Noisy` under-rates the other               | A category named, or "leave it off". Then set, re-score, compare                                                                                                                                                        |
| **H11** ⏳ **asked 2026-09-22 (Q5a)**                                                                                                                                                                     | both          | **Which `[allowed]` candidates to switch on** in `setup/word_lists.txt` — the four @david-spacil confirmed (`ppole`, `ssuti`, `ssutí`, `ssutě`), all, a subset, or none                                                                                                                                                                                                        | The file hands it to the archive; `tests/test_word_lists.py` measures arming one as a category change, not a nudge                                                                      | A list. Then uncomment, update `test_nothing_is_allowed_in_the_shipped_configuration`, re-score, report                                                                                                                 |
| **H12** ⏳ **asked 2026-09-22 (Q5b)**                                                                                                                                                                     | both          | **How far a listed word reaches** — should `shape_garbage_clauses()` skip `[allowed]` tokens? Today it does not: with `ssuti` listed and the witness on, `ssuti vfetennl` is still `Trash` on `ssuti` alone (probed 2026-09-22)                                                                                                                                                | Deferred to them by the file header and `tests/test_word_lists.py`; comment 61 described it as already true                                                                             | Yes or no. "Yes" is a one-line veto beside `_has_vocabulary_support()` plus a test                                                                                                                                      |
| **H13** ⏳ **asked 2026-09-22 (Q6)**                                                                                                                                                                      | both          | **What `rule_short_garbage` should answer when a 1–3 word line might be decipherable** — its only verdict is `Trash`; under W4 decipherable damage is `Noisy`                                                                                                                                                                                                                  | `categorization_logic.md` recorded it as "a decision with an owner … not put to one"                                                                                                    | A preference (`Trash` / `Noisy` / wait for H1–H2). No change before the annotation is in                                                                                                                                |
| **H14** ✅ **answered 2026-09-23 13:08 (Q8)**                                                                                                                                                             | @david-spacil | **Are `ä` / `ö` / `ü` / `ß` reliable signs of German in this archive**, or does damaged Czech produce them (`á` read as `ä`, `ů` as `ü`)? It decides whether the German-diacritic veto D45 left out is worth its own gold A/B and exposure pass                                                                                                                                | A fact about the archive's scans, not the code. The veto would reach ~239 witnessed lines on the 822-document gold corpus, 156 of them kept today (`Grauer, geschlämmter Ton.`)         | *almost always German* / *often misread Czech* / *don't know*. Switches nothing on                                                                                                                                      |

**H6 in full, because it is the one real decision left on the review side.** Stage 5f
reported the per-line effect as **+2 `Clear`-loss for −11 errors, p = 0.013** — not the
+1/−13/p = 0.001 placeholder this row originally quoted from Round 2's with-cascade run.
What S4 adds is the size of what option 2 would fix and the direction of the cascade's own
effect:

|                                                             |                |
|-------------------------------------------------------------|----------------|
| (document, string) groups the dedup votes on, in this queue | 5,222          |
| of those, contested at all                                  | 22 (755 lines) |
| of those, a **bare plurality** — what option 2 targets      | **0**          |
| `Clear` lines the vote destroys / `Trash` lines it rescues  | 17 / **31**    |
| the witness's breaks, cascade **on** / **off**              | **1** / **3**  |
| baseline `Clear`-loss, cascade on / off                     | 40 / 55        |

1. **Accept** — with the cascade in place the observed cost is **+1**, not +2.
2. **Stop a bare plurality demoting `Clear` → `Trash`** — **a no-op.** Not one of the
   5,222 votes is decided by a bare plurality, and a tie already cannot land on `Trash`.
   It would also give up a cascade currently rescuing 31 `Trash` lines for the 17 `Clear`
   ones it destroys, and removing two of the witness's three breaks. The two lines that
   motivated it (`CTX194503132`, `MTX197902086`) are among those the cascade already
   smooths over.
3. **A/B the eight page-smoothing keys** — all config-driven, costs nothing to try, still
   not run.

**K4TEL's side, so the split is unambiguous:** stage 6 (the gold-scored rule sweep, running);
**stage 8** in full (below); the R5 re-run; and projecting whatever annotation returns. Stage
5f, stage 7 and the full-collection lexicon rebuild are done. **No flag flips before 8a.**

## Stage 7 — the next cluster round (finished 2026-09-19)

Stages 1-4 complete and stable. Stage 5 finished except 5c. **Stage 6 is running** (the
gold-scored rule sweep, unblocked by the `gold_clear_loss` fix — no output yet). **Stage 7 is
finished**, measuring D25-D27 and delivered the same way as the existing runbook: a standalone
`issue30_stage7_job.sh` with its own `#SBATCH` header and `.ok` markers, plus `7_logs.log`.

| stage | what                                                                             | outcome                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
|-------|----------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 07a   | `ocr_neighbours --lookup` for all eight doubled-initial tokens on the full table | 🛑 **D25 FAILS at scale — re-read 2026-09-20 (S1).** `ppole` is **fifth of seven by ratio**, not lowest: `llocm` 7.6x, `ssuti` 8.5x, `ssutí` 11.4x, `ssutě` 14.1x all sit below its 37.6x. By own document frequency the 4.4x gap becomes **1.17x** (229 vs `ssuti` 195). The cap is an absolute count and does not rescale with the table, so at the shipped `10` it is right on **2 of 7** tokens here instead of 7 of 7. The 2026-09-19 cell also said "down from 4.7x"; 4.7 → 37.6 is up. `oobjekt` downgrading to `recoverable` is the one part that stands                                                                                                                                                                                                                                                                                                                                                                                  |
| 07b   | `valid_word_ratio` A/B, lexicon off vs on (`QUALITY_VOCABULARY_ENABLE`)          | 🐛 **VOID — the A/B never armed the flag (S2, 2026-09-20).** Bit-identical on all 2,064 rows *and* byte-identical to 07c's table, `KL` included. `quality_word_set()` is a zero-argument `lru_cache`; both arms run in one process; the `True` arm read the `False` arm's cached `None`. Same defect as 07c, one indirection further in. Fixed by D28; needs re-running as **8b**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 07c   | symbol-strip A/B (`STRIP_SYMBOL_GLYPHS`)                                         | **Void — the A/B never armed the flag.** `_STRIP_CHARS` is derived from `STRIP_SYMBOL_GLYPHS` at import time and `override_constants()` didn't rebuild it, so both arms of 07c ran with the glyphs unstripped regardless of the flag. Found and fixed 2026-09-19 (see D27); needs re-running before the zero-effect figure means anything                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 07d   | re-run 5b from ONE clean output directory                                        | ⚠️ **Two findings, and the second was missed for a day (S5, 2026-09-20).** (a) The vowel-run comparison is parity again — `3.0` = 503 errors / Clear-loss 40, `4.0` = 503 / 40, fixes 2 breaks 2, p=1 — so 3.0 stands on the absence of a reason to move, and this is the **third** distinct figure for the same nominal config (504/42 → 502/41 → 503/40), still not root-caused. (b) **Both arms print `-> ADOPT-CANDIDATE`.** Against shipped (513 errors / Clear-loss 40 / cost 0.2917) this is −10 errors, lower cost and **`Clear`-loss unchanged** — D24's gate satisfied, in the tool's own words, while every other account still reads the flag as rejected on `Clear`-loss +1. Caveats: 07d's base config is not printed (witness-on with a lexicon is *inferred* from the `Trash` recall matching the veto arms), and both arms are witness-on so there is no paired test against flag-off. **Stage 8a settles (a) and (b) together** |
| 07e   | `--lookup Naiade Beuern Oueste Skelettmaterial Pinii Uniocrassus`                | Mixed. `skelettmaterial` is genuinely `attested` at full scale — not a thin-corpus artefact after all. `naiade`, `beuern`, `oueste` get `recoverable` edit1 suggestions; `pinii`→`linii`/`indii` looks like the coincidental-edit1 case already flagged elsewhere, not a real correction. `uniocrassus`, `kaukasus`, `feuersteinspan` stay `unattested` at any scale — the asymmetric-signal point again, not a new finding (R6, updated below)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 07f   | build the annotation pack                                                        | **Delivered, and it is a re-cut of the witness queue rather than a second population (S3, 2026-09-20).** All four tabs join back into `04_witness_distinct.csv` at **100%**: 2,424 of the same 5,005 strings, 17,635 of the same 20,078 lines, re-partitioned by recoverability with tab D down-sampled. `A_discarded_but_recoverable` (690 / 1,791), `B_clear_but_unrecoverable` (615 / 1,234), `C_clear_and_recoverable` (969 / 13,807), `D_trash_and_unrecoverable` (150 / 803, seed 30). `gold_categ` blank throughout, by design. It is the best available *view* of where the pipeline and the corpus disagree and should be read; it is **not** a second ask, and H1/H2 asked for it as one                                                                                                                                                                                                                                                |

**Writes only `07_*` / `issue30_stage7_out/*` into its own `$OUT`.** 07d exists because that
discipline was not kept in stage 5.

## Stage 8 — what the stage-7 re-read produced (2026-09-20; **run 2026-09-21**)

Four runs. **None needs @DanaKriv, @david-spacil, a GPU or a new label**, and the first one
is very likely the last thing standing between here and a flag decision. They are listed in
the order they should go, because 8a can make the others moot or urgent.

| run    | command shape                                                                                                                                                              | settles                                                                  | why now                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
|--------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------|--------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **8a** | `ab_constant_eval --constant SHORT_GARBAGE_WITNESS_ENABLE --values false,true` on the stage-7 tree, **one output directory**, plus the same run with `--no-postprocessing` | Whether the adoption gate passes (S5) **and** the 42 → 41 → 40 drift     | 07d says the gate is satisfied but was not an A/B over the flag; three runs of the same nominal config disagree by 2 lines and nobody knows why. Both questions have the same answer and the same run                                                                                                                                                                                                                                                                                                                                                                                         |
| **8b** | re-run 07b, `QUALITY_VOCABULARY_ENABLE false,true`                                                                                                                         | D26 — which currently has **no** measurement, not a null one (S2)        | The fix (D28) is in. Until this runs, the largest available use of the collection lexicon is unevaluated                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **8c** | re-run 07c (`STRIP_SYMBOL_GLYPHS`); then `build_token_lexicon` per-collection columns for the eight doubled-initial tokens                                                 | D27, and whether the de-gemination guard has a portable signal (S1, D30) | Both were void for the same reason. The second half is the only proposal on the table that could replace an absolute threshold that demonstrably does not travel                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **8d** | `short_garbage_witness_report --by-group`: the witness's effect per (document, string) **vote**, not per line                                                              | Gives H6 a corpus-wide denominator instead of an 822-document one        | S4 sized the mechanism inside the witness queue and found option 2's case does not occur there at all. The dedup runs corpus-wide, so retiring the option needs a corpus-wide denominator. ✅ **Landed 2026-09-20** as `--by-group`, with `--recursive` and a repeatable `--input-dir` beside it so both archives can be read in one invocation, and `recategorize_from_csv --no-postprocessing` so the PRE-cascade frame can be written (a delivered `DOC_LINE_CATEG` carries post-cascade labels, making any group count a lower bound). Shipped as stage 08f/08h of `issue30_stage8_job.sh` |

**Discipline carried forward from stage 5's mistake, which 07d exists because of:** one
output directory per stage, stage-prefixed filenames, and the base constants printed in the
log. Two directories sharing stage names is how a stale figure gets quoted as current, and
07d's un-printed base configuration is why S5 has to say "inferred" instead of "read".

**What stage 8 deliberately does not include.** Retuning
`SHORT_GARBAGE_LEXICON_GEMINATE_MAX_DF` from the eight-token table (D30: that is fitting to
one true positive), any change to `apply_document_postprocessing()` before H6 is answered,
and any flag flip before 8a.

### Outcomes, read off the delivery (2026-09-21)

The job ran as eight stages `08a`–`08h` rather than the four `8a`–`8d` this table names; the
mapping is `8a` → **08b** (plus **08c**, the `--no-postprocessing` companion), `8b` → **08d**,
`8c` → **08e** and **08a**, `8d` → **08f**. Figures quoted from `logs8.log`.

| run     | settles                                            | outcome                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
|---------|----------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **08a** | D30's per-collection concentration experiment      | 🛑 **Refuted, and it inverts.** `ppole` — the confirmed abbreviation — is **3 ARUP / 226 ARUB**, the most collection-concentrated of the eight; every confirmed artefact is *less* concentrated. T5 explains why: `ppole` is a convention of one institution's 2010s forms, so it concentrates by institution exactly as a scanner artefact would. **No signal separates the two classes.** The remaining choice is to keep the constant as fitted to one table or set it to `0`; that is @david-spacil's, not this plan's |
| **08b** | 8a — the adoption gate, and the 42 → 41 → 40 drift | ✅ **Gate passes.** errors 513 → 503, `Clear`-loss **40 → 40**, cost 0.2917 → 0.2829, macro_f1 0.6173 → 0.6344. Paired: fixes 12, breaks 2, exact McNemar **p = 0.01294**. `Trash`-recall 12.2% → 18.9%. Both arms report 40, so the drift was between trees, not within a measurement                                                                                                                                                                                                                                      |
| **08c** | the same A/B with the cascade off                  | ⚠️ **REJECT on `Clear`-loss 55 → 56.** The document cascade is what makes the witness adoptable, confirming S4/S5 from the other side. Note the baseline moves too (`Clear`-loss 40 → 55 with the cascade removed), so this arm is not comparable to shipped                                                                                                                                                                                                                                                               |
| **08d** | 8b — D26, which had no measurement at all          | 🛑 **Decisive REJECT.** 212 fixes against **540 breaks**; `Clear`-loss 40 → **180**; cost +0.3324; p ≈ 7.8e-34. It buys `Trash`-recall 78.3% by destroying 13.8% of all gold `Clear`. D26 is now measured. What it destroys is the corpus's telegraphic register — site codes, inventory numbers, citations, place names (digest T10), not a language class                                                                                                                                                                |
| **08e** | 8c — D27, previously void                          | ✅ **Genuinely null.** 5 fixes / 5 breaks, p = 1, errors unchanged. Closed; the flag can stay off on evidence rather than on a broken harness                                                                                                                                                                                                                                                                                                                                                                               |
| **08f** | 8d — H6's corpus-wide denominator                  | ✅ **Delivered, and it retires option 2.** 61,682 (document, string) groups over both archives; 61,359 unanimous, 143 strict majority, 180 tie. Cascade rescues **305** `Trash` lines and destroys **24** `Clear` — **net +281 in its favour**, a wider margin than the 31-against-17 on the 822-document queue. **Groups H6 option 2 would change: 0.** The `mode()[0]` tie-break remains alphabetical and load-bearing                                                                                                    |
| **08g** | the annotation ask, built from the real queue      | ⚠️ **Built, and sized against the wrong population — see T1.** 592 census + 200 sample = 792 decisions, advertised at 82.8% of at-risk exposure                                                                                                                                                                                                                                                                                                                                                                            |

**The defect this stage contains (digest T1).** 08f and 08g were run with
`SHORT_GARBAGE_LEXICON_PATH` unset. `setup/config.txt:199` says the witness and the lexicon are
one decision and the witness never ships without a table, so the exposure both files describe is
not the exposure the shipped configuration produces. The proof is in the queue rather than in a
missing log line: `ppole` is document frequency 229 at full scale, above the cap of 10, so
`_has_vocabulary_support()` already exempts it and a lexicon-on queue **cannot contain it** — yet
it is the largest row in the delivered one. Estimated from 08g's own `token_status` column,
**73.1% of the at-risk exposure (26,868 of 36,744 lines) is exempt once the table is configured**,
leaving 9,876 lines / 7,433 strings. The 592-row census then buys **30.7%** of the real
population rather than 82.8%, because the survivors are 1.33 lines per string and 93.7%
singletons — there is no head left to take a census of.

**08b–08e are unaffected by this.** They are gold-scored A/Bs over the 2,064-line sidecar and
their logs carry the geminate-scale warning, so the table was configured for them.

## Stage 9 — re-measure the right population, then decide (2026-09-21, not yet run)

Four runs, cluster-side as `issue30_stage9_job.sh`, same `stage()` / `.ok` / `FORCE` contract as
stages 6–8. **None needs @DanaKriv, @david-spacil, a GPU or a new label.** 09a is forced by the
predicate change; 09b is the run 08f should have been; 09c is what actually goes to @DanaKriv.

| run    | command shape                                                                                                                                       | settles                                                              | why now                                                                                                                                                                                                                                                                                                                          |
|--------|-----------------------------------------------------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **9a** | re-run 08b and 08c verbatim on the post-D33 tree, one output directory, **base constants printed in the log**                                       | whether the adoption gate still passes after D33                     | D33 changes `is_domain_notation()`, which `shape_garbage_clauses()` reads as a veto, so 08b's 503/40 was measured on a predicate that no longer exists. The expected delta is small — 08b's gold set reaches few URLs — but "expected small" is what 07b said. Printing the base config is the fix for S5's "inferred, not read" |
| **9b** | `short_garbage_witness_report --input-dir ARUP --input-dir ARUB --recursive --out/--distinct/--by-group`, **with `SHORT_GARBAGE_LEXICON_PATH` set** | the real exposure, and the 73.1% estimate                            | The run 08f should have been. Replaces a 100,824-line queue measured in a configuration that will not ship with the one that will, and turns T1's estimate into a measurement. Everything downstream depends on it                                                                                                               |
| **9c** | `ocr_neighbours --annotate --strong-df 113`, then `build_annotation_sample` on the 9b queue                                                         | the ask that actually goes to @DanaKriv                              | Two corrections in one run: the population (T1/T2) and the correction-target threshold. `--strong-df 10` is 0.0088% of 113,100 documents and 90% of its suggestions are `edit1`, which 08g's own log calls weak evidence and recommends against. The ask will be smaller and mostly sampled rather than census                   |
| **9d** | `--by-group` on the 9b queue                                                                                                                        | the dedup denominator on the population it will actually run against | 08f's +281 is measured on the lexicon-off queue. The conclusion (net rescuer, option 2 empty) is very unlikely to move, but it is quoted to @david-spacil as the basis for retiring an option, so it should be quoted from the right queue                                                                                       |

**Discipline carried forward, and one addition.** One output directory per stage, stage-prefixed
filenames — and now **the base configuration printed in every log**, including
`SHORT_GARBAGE_LEXICON_PATH`. T1 cost a delivery because a configuration had to be inferred from
which warnings did and did not appear. `recategorize_from_csv.py` and
`short_garbage_witness_report.py` should print the lexicon path and document count at startup
whether or not a table is configured; "no table configured" is the line whose absence was the
problem.

**What stage 9 deliberately does not include.** Retuning
`SHORT_GARBAGE_LEXICON_GEMINATE_MAX_DF` (08a removed the last candidate for a principled value —
that is now a decision, not an experiment), any change to `apply_document_postprocessing()`
before H6 is formally answered, and any flag flip before 9a and 9b are both in.

### D32–D34 — three predicate questions, one defect

Recorded together because the shape of the mistake is the same one T1 names, and two of the three
were made *by this read* before being caught.

**D32 — the binomial taxonomy class is not a predicate gap. NO CHANGE.**
Measured against the shipped predicate with no lexicon configured, `Lepus europaeus` fires
`vowel_run`, `Mammalia indet.` and `Triticum monococcum` fire `low_variety`, while `Poaceae` is
exempt — read as the `-aceae` suffix rule failing to travel to binomials. Re-measured with a
table armed at the real full-collection document frequencies, all of them are **exempt through
`_has_vocabulary_support()`**: `mammalia`, `indet`, `triticum`, `monococcum`, `lepus`,
`europaeus`, `arvicola`, `terrestris`, `linum`, `usitatissimum` are every one `attested` in
`08g_distinct_evidence.csv`. D14 already reaches this class; the first measurement was simply run
in the configuration T1 is about.
A generic "capitalised genus + lowercase epithet" exemption was drafted and then **measured and
rejected**: it matches 1,465 witness-fired strings, among them `Chenopoaium hycnaum`, `Loua
(oxkuku`, `Ooini hrana osira` and `Laaid. bazic 64999/`. It would exempt garbage to rescue text
the lexicon already rescues, which is the fifth version of the mistake the predicate's own
comment block names four times. Pinned in both configurations by
`tests/test_shape_witness_vocabulary.py`.

**D33 — URL and e-mail citations are a real gap. FIXED.**
Boilerplate URLs are attested by repetition (`http://www.arub.cz`, 5,309 lines of page footer) and
already exempt. What a document-frequency table can never attest is a citation quoted once, and
those survive a configured lexicon: 144 lines in the stage-8 at-risk population, none appearing
more than three times. `is_domain_notation()` now recognises the shape — **0 misses on the 144, 0
false positives against the other 7,289 at-risk survivors**, 0 against the predicate's own garbage
fixtures. It is `.search()` rather than an anchored `.match()`, the only shape in that predicate
that is, because a citation sits inside a sentence; the comment says so and says why that is safe
here specifically. Consulted at both of `is_domain_notation()`'s call sites, which is correct in
both: a URL is precisely the content for which perplexity is not a quality signal, which is the
exemption section 1 already exists for.

**D34 — the rare-real-word `low_variety` class. MEASURED, NOT FIXED.**
`Kaukasus`, `Hallstatthaus`, `Schuhleistenkeilbruchstueck`: real words, convicted by
`low_variety`, **unattested** at full scale because they are rare across documents — and
`issue30_annotation_guide.md` already uses two of them as its own "recoverability zero is not
evidence of garbage" example. The obvious fix is a length cap mirroring
`SHORT_GARBAGE_WITNESS_TRIPLE_MAX_ALPHA`; measured on the delivered queue **it does not
separate the classes**. Per convicting word: `vodovod` (real) and `PSSPPOP` (garbage) are both 7
letters at ratio 0.43; `Kaukasus` and `AltSattl.` both 8 at 0.50; `Vertreter` and `STRATSTAR`
both 9 at 0.44. Case shape does not separate them either — `VODOVOD` and `PSSPPOP` are both
ALLCAPS at 0.43. Retuning `SHORT_GARBAGE_WITNESS_VARIETY_MAX` or `_MIN_ALPHA` from two named
words is exactly what D25/D30 did with the geminate cap and what 07a then refuted at scale.
**Accepted, named debt**, with a test pinning it so a future change has to break a named line.

## Stage 6 — the gold-scored rule sweep (finished 2026-09-22, 87h)

The run that closes **R3**. All 23 rules re-scored against the 2,064-row gold
sidecar, `--split-cascade` on. Full reading in the digest's stage-6 section
(U0–U8); the outcomes that change this plan:

| finding | outcome                                                                                                                                                                                                                                                                                                                                                                                                                |
|---------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **R3**  | ✅ **Closed.** The `DEAD` / `LOAD-BEARING` / `clear_loss` figures are gold-scored. Baseline gold `Clear`-loss **40**, identical to 08b's shipped figure from a separate job with a different tool — the first cross-run consistency check this issue has had                                                                                                                                                            |
| **U1**  | 🛑 **The sweep nominated `rule_short_garbage_witness` for retirement.** `fire_count == 0` because `SHORT_GARBAGE_WITNESS_ENABLE` ships false, not because it has no population — 08f measured the same predicate at 100,824 lines and 08b passed its adoption gate. `_classify()` had no way to say so, `RULE_COVERAGE.md` called the criterion "config-independent", and the tool exits 1 on any `DEAD` rule. **D35** |
| **U2**  | `rule_mid_uppercase` is the other `DEAD` rule and is genuinely unreachable — gate 9d shadowed by gate 7, already in `UNREACHABLE_RULES`. **Neither `DEAD` rule is a retirement candidate.** And the class is sample-sensitive in fact: `rule_bigram_run` and `rule_vowelless` read DEAD at 1,471 scored lines and fire 52 and 1,086 at 4.9M                                                                            |
| **U3**  | Seven rules score better on gold when removed — **and the report has no significance testing of any kind**. Calibrated against 08b (+0.0171 over 10 net rows ≈ 0.0017/row), five of the seven move **less than one gold row**. Not actionable; `rule_short_line` (≈4 rows) and `rule_trailing_fill_rescue` (≈1.3) are candidates for a paired A/B, not for deletion                                                    |
| **U4**  | **`rule_short_garbage` destroys 7 of the 40 gold `Clear`-losses**; `rule_short_line` protects 31; `rule_reference_floor` protects 10. The 40 the flag decision turns on is partly the product of the rule the witness was built to narrow                                                                                                                                                                              |
| **U5**  | `gold_clear_loss` is an absolute count printed beside a delta, and its baseline was computed and discarded. **D36**                                                                                                                                                                                                                                                                                                    |
| **U6**  | `decisive_cascade` is not the cascade a rule's removal sets off — there is no smoothing-off baseline pass, so `decisive_line` carries the whole smoothing footprint (165,482 lines, visible as the floor under every row). **D37**                                                                                                                                                                                     |
| **U7**  | Stage 6 predates D33, so its `rule_domain_notation` row is stale by an unmeasured amount. **9e**                                                                                                                                                                                                                                                                                                                       |

### D35–D37 — three instrument defects, one of them expensive

**D35 — `INERT`, so a disabled rule cannot be reported as dead code. FIXED.**
`text_util.CONFIG_GATED_RULES` maps rule → gating flag, sited next to the flag;
`rule_is_config_gated_off()` resolves it at read time so `override_constants()`
is visible. `_classify()` consults it **before** the `fire_count == 0` branch and
returns `INERT`, which is excluded from the `DEAD` list and therefore from the
exit-1 set. The gate is consulted only when the count is zero, so a gated rule
that somehow fires is still reported normally — a flag must not hide a real
finding. `RULE_COVERAGE.md`'s criterion gains the matching clause and loses the
word "config-independent".

Worth recording because it is the honest version of the finding: **this was
known, twice, in places the artefact does not carry.**
`tests/test_pipeline_parity.py::UNREACHABLE_RULES` had the taxonomy, in a test no
tool can read; and `issue30_stage6_job.sh`'s own epilogue says it in plain words
— but it prints to the SLURM `.out`, not into `06_coverage.log`, which is the
file `tee` captures and the file that gets attached. D35 moves the knowledge into
the tool so it travels with the output.

**D36 — emit the baseline the gold pass already computes. FIXED.**
`evaluate_dataframe` builds `baseline_vs_gold`; `_loo_metrics` kept only the
macro-F1 delta and discarded its confusion. Now extracted as
`gold_clear_loss_baseline`, in the JSON and in the table
(`gold clear_loss N (baseline B, ±D)`), at zero extra cost. Twelve rules in the
stage-6 delivery report exactly 40 — that is the arm's absolute count, not twelve
rules each destroying 40, and nothing in the output said so.

**D37 — `decisive_cascade` says what it measures. DOCS ONLY.**
Docstring, table label (`page-cascade residual` → `smoothing residual`) and
`RULE_COVERAGE.md` corrected: it is a residual between two flip counts both
scored against post-smoothing labels, with no smoothing-off baseline pass
anywhere in the tool, so large negative values are the expected shape rather
than a signal. Adding the missing fourth pass would cost another full
recategorize over 12.7M lines and is recorded as an option, **not** proposed.

## Stage 9 — re-measure the right population, then decide (**9b/9c ran 2026-09-22**)

Rows 9a–9d as specified on 2026-09-21, plus 9e from stage 6. Cluster-side; the
stage-8 job already has the `stage()` / `.ok` / `FORCE` contract, so most of this
is deleting a marker and re-submitting rather than writing a new job.

| run       | command shape                                                                                                            | settles                                                             | why now                                                                                                                                                                                                                                                                                                                                                                                                   |
|-----------|--------------------------------------------------------------------------------------------------------------------------|---------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **9a**    | delete the `08b_witness_flag_ab.ok` / `08c_witness_flag_perline.ok` markers and re-submit                                | whether the adoption gate still passes after D33                    | 08b's 503/40 was measured on a predicate D33 has since changed. Expected delta small — 08b's gold set reaches few URLs — but "expected small" is what 07b said. ~5h                                                                                                                                                                                                                                       |
| **9b** ✅  | **add `ATRIUM_TEXT_UTILS_SHORT_GARBAGE_LEXICON_PATH="$LEXICON"` to the 08f stage command**, delete its marker, re-submit | the real exposure, and T1's 73.1% estimate                          | **RAN 2026-09-22.** At-risk **5,563 strings / 6,714 lines**, not the 7,433 / 9,876 estimated: the exempt share is **82.1%**, not 73.1%. Outcomes below                                                                                                                                                                                                                                                    |
| **9c** ⚠️ | add `--strong-df 113` to the `ocr_neighbours` call in 08g; re-run after 9b                                               | the ask that actually goes to @DanaKriv                             | **RAN 2026-09-22, half done.** The ask is re-cut and delivered (157 + 200, landed as `d9c41a5`) — but at **`--strong-df 10`**, not 113. The population correction landed; the threshold correction did not, and 08g's log repeats the warning. Affects the evidence columns the annotator reads, not which rows were chosen                                                                               |
| **9d** ✅  | `--by-group` is already in 08f, so 9b delivers this                                                                      | the dedup denominator on the population it will run against         | **Delivered with 9b, and it closed H6 option 2**: 0 groups changed, 0 `Clear` lines saved. The cascade is a net rescuer by **+41** lines corpus-wide (51 rescued, 10 destroyed)                                                                                                                                                                                                                           |
| **9e**    | rule coverage re-run, post-D33                                                                                           | `rule_domain_notation`'s stale row, and the witness's real coverage | **Not recommended as a full re-run** — 87h to refresh one stale row and a classification D35 already corrects on paper. If the witness's own coverage figures are wanted, `issue30_stage6_job.sh` is explicit that arming the flag must be a *separate* job (it refuses to start otherwise), and `NOCASCADE=1` brings it to ~59h. 08b already answers the decision question, so the marginal value is low |

**Discipline carried forward, and one addition.** One output directory per stage,
stage-prefixed filenames, base configuration printed in every log — and now
**the job epilogue's caveats belong in the artefact, not beside it** (D35's
lesson). A caveat printed to the SLURM `.out` does not reach the person reading
the `.log` that was attached to the issue.

### Outcomes, read off the re-run (2026-09-22)

Full reading in the digest, § "Stage 8 f/g re-run read against its own delivery" (V0–V11).
What changes this plan:

| what             | before (lexicon off)      | after (lexicon on)                            | consequence                                                            |
|------------------|---------------------------|-----------------------------------------------|------------------------------------------------------------------------|
| at risk          | 8,529 str / 37,555 ln     | **5,563 str / 6,714 ln**                      | the ask is re-cut and **the hold is lifted**                           |
| the ask          | 592 + 200 = 792 decisions | **157 + 200 = 357**                           | delivered at `d9c41a5`; covers 516 distinct spellings                  |
| the at-risk head | `ppole`, `ARCHAIA`, taxa  | **`Dauerleihe`, `FEUILLETON.`, `J. Vysoean`** | the question is now about a clause, not about the archive's vocabulary |
| H6 option 2      | open                      | **closed, 0 lines**                           | remove it from the open list                                           |
| D33 corpus-wide  | unmeasured                | **≥ 15,505 lines**                            | the release is a minor bump, not a patch                               |

**9a is still owed.** The confirming re-run of 08b/08c on the post-D33 predicate did not
happen in this round, and the re-run does not substitute for it: 08f/08g measure a
population, 08b/08c score gold. Cheap, ~5h, no annotation.

### D38–D41 — what the re-run decided

| #                                 | decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
|-----------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **D38**                           | **D33's corpus-wide effect is 15,505 lines, and that is a floor.** Both runs read 56,599,631 lines; in-scope moved 7,493,429 → 7,477,924, and D33 is the only change to the three scope vetoes between them. The witness's scope also demands `word_count <= 3`; `rule_domain_notation` does not, so the production figure is larger. **The `unreleased` release row's "no categorisation change" is false and is rewritten.**                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **D39**                           | **`vowel_run` is the witness, numerically.** It is the sole reason for 4,305 of 6,668 at-risk lines (64.6%) and is the least discriminating of the four clauses (79.8% agreement with the pipeline's existing answer, against `triple`'s 97.9%). `SHORT_GARBAGE_WITNESS_VOWEL_RUN_MIN` 3 → 4 is estimated to cut at-risk exposure by 56.6% while keeping the page-stamp family. **Not adopted** — it is a regex estimate, it is not gold-scored, and fitting a threshold to four nameable words is precisely the D25/D30 mistake. It becomes stage 10a/10b.                                                                                                                                                                                                                                                                                                                  |
| **D40** ✅ **RESOLVED 2026-09-22** | **Remove the de-gemination guard.** @david-spacil, on W2/W3: three of the eight are an old spelling of *suť* rather than damage, and *"if the dictionary already covers all eight, it looks redundant – no objection to dropping it."* V7 is the measurement that says it does — zero of 42,853 queue rows carry any of the eight, so `SHORT_GARBAGE_LEXICON_GEMINATE_MAX_DF` fires on nothing at full scale. It is a threshold fitted to an 822-document table that now changes nothing, and with `ssut*` real, keeping it risks convicting real words for no measured benefit. Stage **11c**. The original text of this decision follows.                                                                                                                                                                                                                                  |
| **D40** (as first written)        | **The eight doubled-letter tokens are out of the annotation ask entirely.** Zero rows of 42,853 in the lexicon-on queue: three are vetoed by `has_cz_diacs()` and can never be in scope, the rest are exempt by document frequency. § 4 of the review request stops blocking @DanaKriv and becomes a question about whether the de-gemination guard should exist. Still @david-spacil's, still one line, no longer urgent.                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **D42**                           | **A run's artefact has to name the configuration that produced it.** `short_garbage_witness_report.py`'s banner printed four witness constants, omitted `SHORT_GARBAGE_WITNESS_VOWEL_RUN_MIN` — the one stage 10 varies — and never named its lexicon state, which is how 08f's lexicon-off run had to be inferred from an *absent* warning (T1). Both print now, the lexicon line whether or not a table is configured, guarded by a source-level test over the clause body's own constant reads. `issue30_stage10_job.sh` refuses to start without them.                                                                                                                                                                                                                                                                                                                   |
| **D43**                           | **The five categories are defined by legibility, not by usefulness.** @david-spacil (W4): *"`Trash` = illegible. Anything legible is `Clear`, easily decipherable is `Noisy` – regardless of how useful the line is to us."* Pending @DanaKriv. Three consequences that are not merely editorial: `http://www.arub.cz` is `Clear` (5,309 lines, and D33 left it at `Noisy`); `Non-text` is out of scope for annotation because it cannot be judged without the page image, which finally explains the gold set's 45 `Non-text` rows against 1,302 `Clear`; and `Trash` means re-OCR rather than delete, which the root `README.md` already said. T9 resolves. Stage **11e**.                                                                                                                                                                                                 |
| **D44**                           | **Gate the vowel-run clause on the detected language; do not raise its threshold.** @david-spacil (W5): a three-vowel run is a fact about Czech phonotactics, so applying it to German and French is a category error, and raising the threshold to 4 answers the wrong question — it buys two German words (`Dauerleihe` 286 lines, `FEUILLETON.` 13) by releasing at least 226 lines of confirmed Czech damage. **Cost, checked:** `lang` / `original_lang` exist per line in `DOC_LINE_CATEG`, but `determine_category()` takes only `lang_score`, `orig_lang_score` and `is_upright_czech`, and that last one reduces to a Czech word-list hit on this population (the scope has already vetoed every diacritic line) — which both `Dauerleihe` and the damaged `J. Vysoean` fail. So it is a signature change plus its own A/B, conditional on **11b** sizing it first. |
| **D41**                           | **The 08f/08g reconciliation gap is bounded and recorded, not chased.** 08f writes 42,853 distinct strings, 08g's queue reports 42,248 (605 dropped by its own normalisation); 08g's at-risk is 5,563 / 6,714 against 5,695 / 6,668 recomputed from its own CSV. 2.3% of strings, 0.7% of lines, no conclusion affected. **But `annotation_ask_README.md` quotes 18.1% against a denominator that cannot be reproduced from the delivered files**, so the document says so rather than presenting it as exact.                                                                                                                                                                                                                                                                                                                                                               |

## Stage 10 — settle the clause, then decide the flag (**launched 2026-09-22**)

Written as `issue30_stage10_job.sh`, same `stage()` / `.ok` / `FORCE` contract as stages 6–8, ~9h of
default stages, running now. **Every stage arms the lexicon** — there is no lexicon-off stage in
this job, which is T1's lesson made structural.

| run                                                               | command shape                                                                                                         | settles                                                   | why now                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
|-------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------|-----------------------------------------------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **10a**                                                           | 08f re-run with `ATRIUM_TEXT_UTILS_SHORT_GARBAGE_WITNESS_VOWEL_RUN_MIN=4`, both archives, `--distinct` + `--by-group` | the real trade at the new threshold                       | ~1h. **Not a filter of 08f's CSV**, though that shortcut is nearly exact: with a table armed `_has_vocabulary_support()` exempts sub-tokens, so an off-cluster recompute with no table finds clauses the armed run suppressed — measured, 191 of 42,853 rows, every one over-keeping. A lower bound, not the measurement                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **10b**                                                           | diff the min=3 and min=4 distinct queues                                                                              | V5's estimate, replaced                                   | Seconds. Carries an invariant guard: raising the threshold cannot ADD convictions, so a string present at 4 and absent at 3 exits 3 rather than printing a plausible table                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **10c**                                                           | intersect the spared set with the delivered `census.csv` / `sample.csv`                                               | **whether the ask already in hand settles the threshold** | Seconds, and the most consequential stage in the job. Measured off-cluster: **184 of 357 decisions (51.5%)** touch a string the move spares, and **none is partly spared**, so every row scores cleanly either way                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **10d** ✅ **RAN 2026-09-22 — and it decided rather than guarded** | `ab_constant_eval`, `SHORT_GARBAGE_WITNESS_VOWEL_RUN_MIN` 3 vs 4, witness + lexicon armed, `--dump-discordant`        | whether the gate still passes at 4, post-D33              | **Outcome: a global 4 is dead.** errors 503 = 503, cost 0.2810 = 0.2810, `Clear`-loss 38 = 38, fixes 2 / breaks 2, exact McNemar **p = 1** — and both secondary measures move the WRONG way: macro_f1 −0.0020, `Trash`-recall 34/180 → 32/180. So the blunt move is a null that costs recall, and the stage written up as a guard closed the question instead. **It also moved the baseline: `Clear`-loss is 38, not 40, with `errors` identical at 503** — two rows left the `Clear`→`Trash` cell without leaving the error count, which is D33's signature; digest X1 carries the hypothesis and its check. Four discordant rows named: digest X3. *(As written before the run:)* ~2.5h, and **a regression guard rather than the decision**: 07d already ran this A/B and both arms were identical (503 errors / `Clear`-loss 40 / cost 0.2829, n=4, p=1). What is new is D33 underneath it, and four discordant rows named instead of counted |
| **10e** ✅ **RAN 2026-09-22 — the gate survived D33**              | 9a — 08b re-run on the post-D33 predicate                                                                             | whether the adoption gate survived D33                    | **Outcome: it passes, and wider than before.** errors 513 → 503, cost 0.2897 → 0.2810, `Clear`-loss 38 → 38, `Trash`-recall 12.2% → 18.9%, fixes 12 / breaks 2, exact McNemar **p = 0.01294**. All three arms of the acceptance test hold. It also corroborates X1: `Clear`-loss is 38 in BOTH arms where 08b had 40 in both of its own, so the shift is flag-independent and belongs to the tree. **And its two breaks are 10d's two fixes** — the same rows from the other side of the same clause, so everything the witness gets wrong on gold is `vowel_run` (digest Y3/Y4). *(As written before the run:)* ~2.5h, still owed. The 08f/08g re-run does **not** substitute: 08f/08g measure a population against stored labels, 08b/08c score 2,064 annotated lines                                                                                                                                                                           |
| **10f**                                                           | the same A/B with `--no-postprocessing`                                                                               | the cascade's share of that result                        | ~2.5h. Read as a pair with 10e; the difference between them is the cascade                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **10g**                                                           | `RUN_RECUT=1` — re-cut the ask at the new threshold, at `--strong-df 113`                                             | a third annotation ask                                    | **Conditional and deliberately last.** Only if 10c says the delivered ask cannot settle it. It also carries 9c's missed half — `--strong-df 113` rather than 10                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |

**How @david-spacil's answers change the reading of this job, without changing the job.** W5
corrects V5: the strings min=4 spares are **not** all correctly read. `J. Vysoean` and
`POSTKRANIAINY SKELET` are scanning errors, so the honest trade is 299 confirmed-good lines against
at least 226 confirmed-bad. **10b's spared set is therefore a mixture, not a rescue list**, and
10c's overlap with the delivered ask is how the mixture gets quantified — the 184 rows come back
labelled and the split is read off them. **10d matters less than it looked**: it was already a
regression guard, and now the question it guards is one we no longer expect to adopt.

**A code change the job refuses to start without (D42).** `short_garbage_witness_report.py`'s banner
printed `MIN_ALPHA`, `VARIETY_MIN_ALPHA`, `VARIETY_MAX` and `TRIPLE_MAX_ALPHA` — and not
`VOWEL_RUN_MIN`, the one constant this round varies — and said nothing at all about its lexicon
state. A run whose artefact cannot name the value that produced it is T1's failure mode exactly, and
this job diffs two queues produced at different values. Both print now, the lexicon line
unconditionally (`NONE CONFIGURED` is the case whose absence was the problem), with a source-level
test that fails when a constant the clause body reads does not reach the banner.

## Stage 11 — what @david-spacil's answers created (2026-09-22, not yet run)

| run                                                                       | what                                                                                                                               | why it is on this list                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
|---------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **11a** ✅ **RAN 2026-09-22 — ZERO, W6 closes**                            | **check the gold sidecar for `ssut*` rows labelled `Trash`** — join `issue30_gold_2067.csv`'s locators back to the corpus and look | **First, and it is small but it touches the number everything turns on.** He annotated before discovering `ssut*` is real language (W2, W6). Exposure is 1,667 `ssuti` lines in 56.6M over an 822-document gold corpus, so almost certainly zero — which is why it should be *seen* to be zero rather than argued to be                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **11b** ✅ **RAN 2026-09-22 — and it overturned Y4 in our favour (Z4–Z6)** | ~~emit the `lang` distribution of the witness queue~~ → **emit `original_lang` for the 2,064 gold rows**                           | Decides whether D44 is worth building at all. **The queue-wide distribution answers the sizing question; the gold-row join answers the question the flag actually turns on**, settles 10d's four discordant rows, and yields the 3%-or-40% figure as a by-product. Cluster-side, but a join and not a run. The queue column is **done in-tree anyway** — `short_garbage_witness_report.py` now prints the distribution, reading `original_lang` and not the remapped `lang`, since `remap_lang()` rewrites unknown bases to Czech and would answer a phonotactic question with a policy default                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **11c** ✅ **DONE 2026-09-22**                                             | **remove the de-gemination guard** (D40 / W3)                                                                                      | Owner-approved. Zero of 42,853 queue rows carry any of the eight, so it fires on nothing at full scale; with `ssut*` real, keeping it risks convicting real words for no measured benefit. Needs a test and a re-measurement, not just a deletion. **Landed:** `_is_geminate_artefact`, both constants, both config keys, `geminate_cap_scale_warning()` and its two calibration constants are gone; `lexicon_document_count()` stays (the report banner reads it). The test that asserted attested artefacts LOSE their veto is inverted into one asserting every attested token KEEPS it. Suite 1316 → 1312 passing (13 guard tests out, 9 parametrised in), `ruff` clean; `recategorize_from_csv --report-only` over `data_samples/DOC_LINE_CATEG` changes 0 categories. **Re-measured rather than argued.** The same eight tokens were probed through `_has_vocabulary_support` / `shape_garbage_clauses` on the pre-removal tree and on this one, with a table carrying 07a's full-collection frequencies: before, the guard withdrew attestation from exactly **two** of the eight — `jjámy` (df 10) and `oobjekt` (df 3, thin-table value; 07a does not list it) — and left the other six attested. **Neither of the two can be convicted either way**: `jjámy` carries a Czech diacritic so the whole predicate bails out before any clause, and `oobjekt` is a doubled VOWEL, which `_RE_INITIAL_CONSONANT_GEMINATE` does not match. So the guard's live effect on a witness verdict at full scale was nil, which is stronger than V7's 'fires on zero queue rows' and reaches it from the predicate rather than from the queue. Note 5d's RATIO=0 A/B is behaviourally identical to the removal, but **re-running that A/B now would be vacuous** — `override_constants()` gates on `hasattr` and silently ignores a deleted constant, which is D28's failure mode. |
| **11d** 🔧 **BUILT 2026-09-22, NOT YET SCORED**                           | **plumb the language label into `determine_category()`** and A/B the gated clause (D44)                                            | The plumbing is done: `determine_category()` and `categorize_line()` take a keyword-only `lang` defaulting to `None`, `classify_TEXT` passes the RAW `original_lang`, and the clause reads `3 vowels outside deu,fra` / `4 vowels anywhere` via `SHORT_GARBAGE_WITNESS_VOWEL_RUN_EXEMPT_LANGS` and `..._MIN_EXEMPT`. `None` gets the STRICT threshold, so every existing caller is byte-identical. **The A/B is not done and 10d says what to expect**: on this gold set the split is not a different experiment from the threshold, it is the same experiment conditioned on a label (X3), and two of the four discordant rows point opposite ways — a wash is the honest expectation. **10e sharpened that to a computation (Y4), and 11b then MEASURED it and Y4 was wrong.** The four labels are `deu` / **`afr`** / `fin` / `eng`, so only the German break is spared and every fix survives: **12 fixes / 1 break, errors 503 → 502, `Clear`-loss 38 → 37, both DOWN.** The adoption gate passes and the "the gate is what needs deciding" framing is **withdrawn** — the split does not ask the gate for anything. **But read Z5 before arming it:** it passes partly by accident. `deutendes. Alhimiaal` keeps its conviction only because the detector called it Afrikaans rather than German, `Frauenzimmerbad` is exempted on a `deu` label at 0.359, and `http://www.arub.cz` is detected Cantonese. The label on a short damaged line is largely noise. **Open: a confidence floor on `_vowel_run_min_for()`**, which would put `Frauenzimmerbad` back at risk — a real trade needing its own measurement                                                                                                                                                                                                                                                        |
| **11e** ◐ **PARTLY DONE 2026-09-22**                                      | reconcile the five category descriptions with W4                                                                                   | `README.md`, the annotation guide and `docs/categorization_logic.md` describe the five categories in two incompatible ways. W4 settles which one wins. Documentation, but it decides how the annotations get scored. **Landed:** `categorization_logic.md` carries the legibility note and names the two routes that send readable text to `Trash`; the annotation guide's § 7.1 is marked answered with the LEXICON-OFF figures replaced (61,682 / 24 / 305 → 43,103 / 10 / 51), and § 7.2 is reframed as confirm-or-dissent instead of asking Dana what § 4 of the same document already answers. **Still open:** @DanaKriv's agreement (H9 / Q1). ~~and the `README.md` weight table, which describes five quality-score terms where the code has nine~~ — *checked 2026-09-22: the root `README.md` has listed all nine `QS_WEIGHT_*` terms, and says "based on **9** combined signals", since `9121c4c` (2026-09-16); nothing to reconcile.* **Also landed 2026-09-22 (evening):** `categorization_logic.md`'s top note named a `rule_domain_notation_clear` / `SHORT_LINE_URL_CLEAR_ENABLE` that does not exist — rewritten to the real D43 route, with its own gate row (0b), and row 5c brought up to D44                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |


**What stage 10 deliberately does not include.** Arming
`SHORT_GARBAGE_WITNESS_ENABLE` before 10d and @DanaKriv's answers are both in; removing
`SHORT_GARBAGE_LEXICON_GEMINATE_MAX_DF` (approved by @david-spacil in W3, and it belongs
in stage 11 with a test rather than in a running job); and any change to
`apply_document_postprocessing()` — H6 option 2 is now closed by measurement, and the
alphabetical tie-break decides 17 groups rather than 180.

> **Both of those are done as of 2026-09-22.** The guard is removed (11c). And the tie-break
> is no longer undefended: `tests/test_page_postprocess.py::test_a_tied_dedup_vote_never_lands_on_trash`
> pins it on the production function, with the sort order asserted rather than assumed and a
> Trash-majority control so it reads as a tie-break and not a refusal to convict. Until then
> the only test of it was in `tests/test_short_garbage_witness_report.py`, against the offline
> tool's own re-implementation of the vote — i.e. the behaviour the §2 decision rests on was
> asserted only against a copy of itself. The vote site in `classify_TEXT.py` now says out loud
> that the ordering is an accident of the spelling of five labels and that renaming one would
> flip every tie silently.
>
> **10a re-confirms option 3 at the other threshold.** With `VOWEL_RUN_MIN=4` the contested
> population is 38 groups of 27,244 and the bare-plurality rule still changes **0**. One caveat
> worth carrying: **30 of those 38 groups are in a single document** (`CTX201604155`, the
> `OUOI*`/`OUUI*` page stamps), so the corpus-wide "+27 in the cascade's favour" at min=4 is
> close to a property of one document's stamps rather than a corpus regularity. It does not
> change the decision — 27,206 of 27,244 groups are unanimous — but it should be quoted with
> the caveat.

## What landed in the tree on 2026-09-22 (five commits, unpushed)

Recorded here because the decisions above were prose until this round, and a plan that
says "approved" without saying "landed" is the drift this document guards against.

| decision           | what shipped                                                                                                                                                                                                                                                                                                                                                                                                     | armed?                                                                   |
|--------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|--------------------------------------------------------------------------|
| **D40 / W3**       | the de-gemination guard removed — `_is_geminate_artefact()`, both constants, both `setup/config.txt` keys, `geminate_cap_scale_warning()` and its two calibration constants. `lexicon_document_count()` kept (the report banner reads it)                                                                                                                                                                        | n/a, it is a deletion                                                    |
| **§ 2 / H6**       | the alphabetical tie-break pinned on `apply_document_postprocessing()` itself, with the sort order asserted rather than assumed and a `Trash`-majority control. Until now the only test of it was against the offline tool's *copy* of the vote                                                                                                                                                                  | no behaviour change                                                      |
| **D43 / W4**       | `DOMAIN_NOTATION_CATEG` — a **category NAME in config**, not a switch. Every address `_RE_NOTATION_URL` catches gets it, and the rule runs FIRST, above the hard sweep                                                                                                                                                                                                                                           | **ships empty = off**                                                    |
| **D44 / W5**       | the vowel-run language split — 3 vowels outside `deu,fra`, 4 anywhere. `lang` plumbed through `determine_category()` / `categorize_line()`, `classify_TEXT` passes raw `original_lang`                                                                                                                                                                                                                           | **ships behind the witness flag, which is off**                          |
| **11e (part)**     | the legibility reconciliation in `categorization_logic.md`, and the annotation guide's § 7.1 / § 7.2                                                                                                                                                                                                                                                                                                             | documentation                                                            |
| **the word lists** | `setup/word_lists.txt` — one hand-editable file, eight lists migrated into it (`_NEUTRAL_LEXICON`, `_NOTATION_LABELS`, `_DOCUMENT_HEADER_LABELS` from code; `SHORT_VALID_WORDS`, `SHORT_EXCEPTION_TOKENS`, `LDL_UNITS`, `ROT_WHITELIST`, `GHOST_WORD_COLLISIONS` from config), plus `[allowed]`, the open-class layer that never existed. Config keys stay as empty overrides so nothing loses its override path | **`[allowed]` ships empty**; the eight migrated lists are byte-identical |

Suite **1,316 → 1,391** passing, `ruff` clean, `check_version` agrees, and
`recategorize_from_csv --report-only` over `data_samples/DOC_LINE_CATEG` changes **0**
categories at every step — which it must, since everything new ships off.

**Three things the work itself found, each recorded because it contradicts something
that was written down first.**

1. **A test can fail by passing.** `test_the_ratio_can_be_disabled` stayed green after the
   guard's removal, because `override_constants()` gates on `hasattr` and silently ignores
   a constant that no longer exists. It was deleted rather than kept. The same mechanism
   means **re-running `ab_constant_eval --const SHORT_GARBAGE_LEXICON_GEMINATE_RATIO
   --values 4.0,0.0` is NOT valid confirmation of the removal** — both arms would run
   identical code and report a perfect null. That is D28's failure mode, third instance.
2. **The first URL route asserted a separation the code cannot make.** It claimed a damaged
   address would stay `Noisy`; its own test showed `detect_fused_words` fires on the address
   SHAPE, correct and damaged alike, and that `e-mail: officeauappmost.cz` — the same label
   with its `@` lost to the scan — already reads `Clear` today without any new route. The
   test now pins the limit instead.
3. **Every word list the code carried was CLOSED-class.** Czech function words, units, academic
   titles, catalogue markers. The OPEN-class vocabulary this issue keeps mis-convicting —
   `Dauerleihe`, `Kaukasus`, `Schuhleistenkeilbruchstueck`, `ssuti`, `ppole`, the Latin taxonomy —
   existed only in `tests/`, and the one mechanism meant to cover it,
   `SHORT_GARBAGE_LEXICON_PATH`, ships EMPTY with no frequency table anywhere in the repository.
   So the shipped configuration had **no open-class protection at all**. `setup/word_lists.txt`'s
   `[allowed]` section is that missing layer, not a duplicate of an existing list.
4. **Three of the eleven lists could not migrate**, each for a concrete reason rather than for
   lack of time: `ACADEMIC_TITLES` is matched case-sensitively, `METADATA_MARKERS` carries
   load-bearing trailing spaces, and `LDL_ALLOWED_FOLLOW` is punctuation rather than words. Each
   is fixable only by making the file harder to edit by hand, which is the one thing it is for.
5. **The migration nearly removed the override path.** `test_tier1_key_roundtrip_from_alternate_config`
   caught it: after the move, an alternate `LANGID_CONFIG` no longer reached `ROT_WHITELIST`. The
   precedence is now layered — config/env, then file, then in-code default — and the keys stay in
   `setup/config.txt` empty, meaning "not overridden, use the file".
6. **The guard's live reach was nil, not small.** Probed on the pre-removal tree against a
   full-collection table, it withdrew attestation from exactly two of the eight tokens —
   `jjámy` and `oobjekt` — and neither can be convicted either way: `jjámy` carries a Czech
   diacritic so the predicate bails out before any clause, and `oobjekt` is a doubled VOWEL,
   which `_RE_INITIAL_CONSONANT_GEMINATE` does not match. Stronger than V7's "fires on zero
   queue rows", and reached from the predicate rather than from the queue.

## 2026-09-23 — @david-spacil's 508 re-check, and two guards lost since 2026-09-18 (D45)

**H5 closes.** His answer to Q7 is `master`, and he ran the tag as well. Whole documents re-scored
with `tools/recategorize_from_csv.py`, witness on, **no lexicon** — the shipped default:

| revision                                        | agreement with gold |
|-------------------------------------------------|--------------------:|
| `4ed309a` witness off                           |             326/508 |
| `09c9640` (`v1.5.1-beta`) witness on            |             334/508 |
| `4ed309a` witness on                            |             335/508 |
| `4ed309a` witness on, `VOWEL_RUN_EXEMPT_LANGS=` |             334/508 |

Tag and `master` differ on `Frauenzimmerbad` alone (D44). On `master` the witness moves 16 lines:
11 fixed, 2 broken, 3 wrong either way — the "≤ 2 by set inclusion" bound, now measured directly.

**D45 — the two round-2 guards were deleted by an unrelated commit, and nothing noticed.** One of
the two breaks is `S-VIIIb`, and he asked whether removing `_RE_FUSED_GRID_REF` in `cc4990e` was
intended. **It was not.** The history:

| commit                | what it did                                                                                                                                                                                                                          |
|-----------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `9bc218b` 09-18 08:03 | added two witness-local guards — `_RE_FUSED_GRID_REF` (`S-VIIIb`) and `has_expected_lang_diacs()` (`Frauenzimmerbad`, a German-diacritic veto) — and the round-2 stage-5a re-run measured them: **14 fixed / 1 broken, p = 0.00098** |
| `cc4990e` 09-18 17:14 | "edit lexicon-related code for full collection" — deleted **both** guards and their veto line; the rest of its `text_util.py` diff is lexicon parsing. No test pinned either guard, so the suite stayed green                        |
| `031fa58` 09-18 17:55 | put back the pre-round-2 `setup/config.txt` witness block ("DO NOT TURN IT ON WITHOUT A LEXICON") that round 2 had replaced — the signature of a stale working copy, not a decision                                                  |

No plan, digest or DEVLOG entry records dropping either guard, and this plan went on listing the
`S-VIIIb` exemption as present (H5's "Done when", the review request's § 3). **Every measurement
from 5f onward scored the guard-less witness.** Three things follow:

1. **The 05a-vs-05f comparison crossed the deletion.** 05a (cascade on, guards in) broke 1 line;
   05f (cascade off, guards gone) broke 3 — and its two extra breaks are exactly the two lines the
   guards vetoed. So S4's "the cascade removes two of the witness's errors" is confounded. The
   conclusion that the cascade is a precondition still stands, on same-tree evidence (08c, 10f).
2. **D44 re-solved a line the lost German veto had already solved** — `Frauenzimmerbad` carries
   `ä` twice. D44 remains the right rule for Czech-vs-German phonotactics, but its fix of that line
   rests on a `deu` label at 0.359 (Z5), where the veto rested on the letters themselves.
3. **`S-VIIIb` survived every stage-8+ run only because those runs had a lexicon** (`viiib`
   attested). On the shipped config it goes to `Trash`.

**Landed 2026-09-23 (maintainer decision): the grid guard only.** `_RE_FUSED_GRID_REF` is back in
`shape_garbage_clauses()`'s line-level veto, witness-local, with the history in its comment, and it
is **pinned three ways**: the seven-string series in `tests/test_shape_witness_vocabulary.py`, a
narrowness test (anchored; `Aa/III 116`, `OUUITN`, `Lokolieace: •VIII,` stay outside), and an
`S-VIIIb` row in `tests/test_short_garbage_witness_wiring.py`'s end-to-end keep list. Verified
that the tests fail with the guard removed (9 failures) and pass with it. Expected effect on his
run: **336/508, `Lokolieace: •VIII,` the only break** — ✅ **confirmed on his side**, 2026-09-23
11:05 UTC, on `3b02959`: 336/508, 15 moved (11 fixed, 1 broken, 3 wrong either way), and with the
flag off 0 of 508 change against `4ed309a` — so the restored guard is inert until the flag flips.

**Open (maintainer, not data providers): `has_expected_lang_diacs()`.** Not restored. For it: it
makes the `Frauenzimmerbad` fix independent of the language guess, and Z5 is exactly that
fragility. Against it: it changes the witness's reach on ~239 corpus lines with a German diacritic
(83 currently `Trash`), it overlaps D44, and its zero-argument cache would need registering in
`_CACHES_FROM_FLAG` (D28's guard test would refuse it otherwise). Needs its own gold A/B plus the
exposure pass before a decision.

**Also flagged, not changed:** the `setup/config.txt` lexicon block `031fa58` restored. Round 2
had called it falsified against gold — but round 2's own argument leaned on `ppole` being garbage,
which W2 overturned. Neither text is right as it stands; it needs rewriting from the current
evidence, not reverting to either version.

## Verification / done-criteria

- [x] The evidence gate merged — `070620f`, green: 954 passed clean, 952 passed / 2 xfailed with the patch.
- [x] The red check diagnosed and attributed to the base, not the PR (D12) — and the hub-side cause fixed: `docker-build-smoke` now runs on pushes to `test`, with budgets derived from the `HEALTHCHECK` convention instead of expiring 30 s before an unhealthy verdict and 5 s before uvicorn's own deadline.
- [x] `_has_strong_garbage_evidence()` verified `False` on the disputed population — **pinned in-tree** on production vectors, not measured in a harness.
- [x] The suppression path in section 7 identified and confirmed in code (Research §5).
- [x] The "run-only form" spec falsified before it shipped — `_RE_FUSED_CONSONANT_RUN` matches `vrstva` and the rest of ordinary diacritic-free Czech, and was dropped (§8).
- [x] Corrected witness measured: **9/9** reachable garbage, **0/37** vocabulary/notation/toponym/name/unit negatives, **0/6** phonotactic residue, **0/37** committed positive fixture rows, **5/7** on the known loan false-positive class.
- [x] D8's exit condition corrected: section 7 returns `Noisy`, so a witness placed there never flips the strict xfail.
- [x] The test suite can see this class of change: three discriminating golden edge cases, the mechanism pinned at predicate level, the third hand-rolled harness retired, a `Trash`-side swept-bounds guard. 814 → 891 → 954 passing.
- [x] `_has_shape_garbage_evidence()` shipped inert behind `SHORT_GARBAGE_WITNESS_ENABLE=false`; flag-off re-score of every sample CSV changes **0** categories.
- [x] `check_rescues()` verified inert on this population (Research §6).
- [x] The `Noisy` floor (D11) measured and rejected: 28 fixes against 202 breaks on the 508.
- [x] `rule_hard_sweep` staying armed verified against the corpus, not argued (D6's note).
- [x] The witness wired into gate 6 — **done**, behind the flag, with the registry obligations and a wiring test covering both flag states.
- [x] **The annotation files arrived** (2026-09-10) — both sets, 2,067 rows, in the tree as `tools/gold/sidecars/issue30_gold_2067.csv` and wired to the existing gold path via `--gold-sidecar`. The blocker named in this checklist since Phase 1 is gone. ⚠️ The gold is here; the delivered `2907` rows it labels are not.
- [x] **The witness measured on the 508** — and it did **not** hold up: 26 lines move, **12 fixed / 12 broken**, 2 borderline. The projected "wrong-lift share falls to 6–7% at no cost" was wrong. All 12 breaks carried a roman numeral.
- [x] **The roman-numeral class fixed at predicate level** — exemption placed above all four clauses (`_RE_ROMAN_TOKEN`, uppercase-only, capped at 7). Verified here on `1fca8eb`: all 7 named broken lines stop firing, all 5 named fixed lines keep firing, and every positive the predicate's comment block names survives. Suite 1038 → 1053 passing, 13 skipped / 2 xfailed, 0 failed.
  - The thread's diagnosis — *"the triple-character clause reads `III` as a stutter"* — is **incomplete**: `I` is a vowel so `III` is also a 3-vowel run, and `CCVI` / `CCLXII` fire the initial-consonant-geminate clause instead. Exempting the triple clause alone would have fixed nothing. This is why the exemption sits above all four.
  - **Consistent with D2, not a reversal of it.** D2 kept roman numerals out of `is_domain_notation()`'s label lexicon because admitting one there **claims the line is clean**. The witness exempting one merely **declines to claim it is garbage**. A predicate whose job is convicting on positive evidence loses nothing by refusing to convict; `XIV: 7` and `XII: 2` stay out of the lexicon.
- [x] **The 508 re-scored against the narrowed predicate** — ✅ **done 2026-09-23 by @david-spacil, directly** (H5): default config, no lexicon, witness on, `master` `4ed309a` — 335/508 against 326 off; 16 moved, 11 fixed / 2 broken / 3 wrong either way. The breaks were `S-VIIIb` (D45 — a lost guard, restored) and `Lokolieace: •VIII,` (H13's case). ✅ **Confirmed on `3b02959`** with the guard restored: 336/508, 15 moved (11 fixed / 1 broken / 3 wrong either way), `Lokolieace: •VIII,` the only break, 0/508 change with the flag off. Original item: **not done.** The 12/12 figure predates the exemption, and the named class no longer fires, but that is not the same as a re-measurement. ~~Needs the delivered batch through `_rescore_row`.~~ **2026-09-17: the rows are on the cluster** (all 816 gold documents, in `data_samples/DOC_LINE_CATEG`), the predicate has been narrowed a second time since (D18), and `ab_constant_eval --values` can now take `false,true` — which it could not when this line was written, so the comparison was literally unsayable. Runbook step 5a. **`SHORT_GARBAGE_WITNESS_ENABLE` stays `false` until this exists.**
- [ ] **A document-aware re-grade** — not done. Every quality figure is per-line; `apply_document_postprocessing()` moves ~27% of these lines in production. `evaluate_dataframe` applies the smoothing, so the sidecar makes this possible — ~~it needs the batch, not more annotation.~~ **Neither, as of 2026-09-17** — the rows are on the cluster, so this falls out of runbook steps 1 and 6. `rule_coverage_report --split-cascade` measures the same boundary from the rule side.
- [x] **Stage 5f run (2026-09-19)** — the per-line effect, `apply_document_postprocessing()`
  skipped: **+2 Clear-loss for −11 errors, p = 0.013** (17 discordant rows,
  `issue30_outs/05f_discordant_perline.csv`), not the +1/−13/p = 0.001 placeholder H7 was
  drafted against. It also complicates, rather than confirms, Round 2's claim that the witness
  produces no new Clear-loss on its own — see the digest's dedicated section. H7's three-way
  dedup choice is now costed; the choice itself is still @david-spacil's.
- [x] **Two lexicon constants checked against gold (2026-09-19)** —
  `SHORT_GARBAGE_LEXICON_GEMINATE_RATIO` (0.0 vs 4.0) is identical on every one of the 2,064
  scored rows: the guard's effect is real but doesn't land inside this gold set.
  `SHORT_GARBAGE_LEXICON_MIN_DF` (2 vs 3) differs by one row, not significant (p = 1), and in the
  opposite direction from the fixture-level argument for the default. Neither changes a
  decision already on the books; `MIN_DF` stays 3 on the fixture evidence alone.
- [ ] Both collections recomputed on `test + witness` — **not run**; needs the contributor's corpora (~2 h GPU). Only worth spending once a flag actually moves, which on current evidence neither does.
- [x] **The token lexicon rebuilt over both full collections** — done 2026-09-18: 113,100 documents, 72,306,182 lines, 2,764,632 distinct tokens (against 822 / 12.7M / 347,097 on the thin table). The distribution barely moved (attested-in-≥3 15.7%→15.2%, hapax 74.3%→75.9%); what changed is per-token resolution, not shape. Still does not settle `SHORT_GARBAGE_LEXICON_MIN_DF` on gold — 05e's 2-vs-3 A/B differs by one row, not significant (see D26 note). `tools/build_token_lexicon.py`, runbook step 3
- [x] **Stage 7 run (2026-09-19)** — D25 confirmed at full scale, D26 measured at zero effect on the current gold sample, R6's three tokens looked up on the full collection, and the recoverability annotation pack (07f) built and delivered as H2. D27's zero-effect figure turned out to be void — the A/B harness could not arm the flag at all — and is fixed in code, pending a re-run. Details in the digest's dedicated section
- [x] **The rule figures regenerated against gold** — **done 2026-09-22, stage 6, 87h.** All 23 rules gold-scored against the 2,064-row sidecar; baseline gold `Clear`-loss 40, matching 08b from a separate job. R3 closed. Three instrument defects found in the output and fixed as D35–D37; the headline substantive number is that **`rule_short_garbage` destroys 7 of the 40 gold `Clear`-losses** the flag decision turns on.
- [ ] **The rotation-heavy divergence re-measured** (95 ARÚP / 10,258 ARUB) — raised 2026-08-24, never re-run after the `score_line` unification. Runbook step 2 *is* that measurement.
- [x] The post-merge stale notes de-staled — **done**. There were **six**, not four: the plan's list missed `tests/test_calibration.py:445` and `tests/test_short_garbage_witness_report.py:4`, both of which still asserted "no call site yet". `agent_dev_logs/DEVLOG.md` keeps its historical wording with a superseding note appended rather than a rewrite.
- [x] The two missing label-lexicon entries (`orientace`, `komponenta`) — **done**, and re-measured: they recover **3 of the 6** regressed lines, not 5. `XIV: 7` and `XII: 2` satisfy the label shape without being words and are deliberately left (admitting Roman numerals would be the predicate claiming vocabulary, against D2); `Bokalisace: B-XII-c` is annotated `Noisy`, so lifting it to `Clear` was never right either. Lexicon is now **26** entries — the thread's "22-item set" was already wrong.
- [ ] The `v1` tag re-pointed so the hub CI fix reaches the five consumer repos — a five-repo change, deliberately left to the maintainer.
- [x] **Stage 7 re-read against its own delivery (2026-09-20)** — the thirteen attached CSVs and both logs pulled down and recomputed rather than read from the summaries written when they were produced. Five findings (S1–S5 in the digest); three overturn a decision cell above. **Four of the six instrument-level errors this issue has logged would have been caught the same way**, and that is now the standing instruction for a delivery: read the artefact, not the report of the artefact.
- [x] **The flag-freezing bug class closed, not one more instance (D28)** — `_CACHES_FROM_FLAG` in `override_constants()` plus a source-level guard test that fails when a new zero-argument `lru_cache` appears in `text_util` unregistered. Suite 1252 → 1273 passing, 0 failed, `ruff` clean. `quality_word_set()` was the instance; the guard is the fix.
- [x] **The annotation ask sized and built (D29)** — `tools/build_annotation_sample.py`, 13 tests. **293 decisions** (93 census + 200 sampled) reaching 94.8% of at-risk exposure exactly and the remainder to ±6.9 points, against three overlapping asks totalling 7,629 rows.
- [x] **`SHORT_GARBAGE_LEXICON_GEMINATE_MAX_DF`'s non-portability made loud (D30)** — `geminate_cap_scale_warning()`, corrected comment and config block, both tables pinned by tests. Value unchanged: it is a threshold that cannot be honestly set from eight tokens.
- [x] **Stage 8a — the witness-flag A/B on the stage-7 tree, one directory.** Ran 2026-09-21 as **08b**: errors 513 → 503, `Clear`-loss **40 → 40**, cost 0.2917 → 0.2829, fixes 12 / breaks 2, exact McNemar **p = 0.01294**. The adoption gate passes, and the 42 → 41 → 40 drift resolves to 40 in both arms. **08c** is the `--no-postprocessing` companion: `Clear`-loss 55 → 56, so the cascade is what makes it adoptable.
- [x] **Stage 8b / 8c — 07b and 07c re-run now that the flags actually arm.** Ran 2026-09-21. **08d**: `QUALITY_VOCABULARY_ENABLE` is a decisive **reject** — 212 fixes against 540 breaks, `Clear`-loss 40 → 180, p ≈ 7.8e-34; D26 is measured rather than void. **08e**: `STRIP_SYMBOL_GLYPHS` is a genuine **null** — 5 fixes / 5 breaks, p = 1; D27 closed.
- [x] **Stage 8 is written and dry-run green (2026-09-20)** — `issue30_stage8_job.sh`, eight stages, same `stage()` / `.ok` / `FORCE` contract as the stage-6 and stage-7 jobs. Validated end to end against a synthetic two-archive layout: all seven default stages OK, every stage SKIPs on re-submit, `FORCE=<stage>` re-runs exactly one, a wrong archive path exits 2 before anything runs, and removing `_CACHES_FROM_FLAG` from the tree makes it refuse to start. `bash -n` and `shellcheck` clean apart from the house SC2016 (the deliberate late-expansion pattern every job in this series uses).
- [x] **The three CLI gaps stage 8 needed are closed (D31)** — repeatable `--input-dir` and `--recursive` on the witness report, `--by-group`, and `--no-postprocessing` on the re-scorer. Suite 1273 → 1288 passing.
- [x] **Stage 8 submitted and read against its own delivery.** All eleven CSVs and `logs8.log` pulled down and recomputed rather than read from the write-ups (digest T1–T15). Four questions settled; one defect found in the delivery itself (**T1** — 08f/08g ran lexicon-off, so the exposure and the annotation ask describe a population ~3.7× larger than the shipped configuration puts at risk).
- [x] **D33 — URL and e-mail citations exempted from the witness.** `is_domain_notation()` extended; 144 uniquely-quoted citations in the stage-8 at-risk population that no document-frequency table can ever attest, 0 false positives against the other 7,289 survivors. Suite green, `ruff` clean.
- [x] **D32 and D34 measured and deliberately not changed.** The binomial class is already reached by `_has_vocabulary_support()` once a lexicon is armed (the first measurement was run without one — T1's own mistake); the rare-real-word `low_variety` class has no length, ratio or case-shape cut that separates it from garbage of the same shape. Both pinned by tests rather than patched.
- [x] **D35 — flag-gated rules classify `INERT`, not `DEAD`.** The stage-6 sweep nominated `rule_short_garbage_witness` for retirement; it is switched off, not dead. `CONFIG_GATED_RULES` + a fourth class + the matching clause in `RULE_COVERAGE.md`'s criterion, with tests pinning both flag states and the exit code.
- [x] **D36 — `gold_clear_loss_baseline` emitted.** The gold pass already computed it and `_loo_metrics` discarded it; twelve rules reporting "40" were absolute counts with no baseline printed.
- [x] **D37 — `decisive_cascade` documented as what it measures.** No smoothing-off baseline pass exists, so it is a residual between two flip counts and its large negative values are the expected shape, not a signal.
- [ ] **Stage 9e — rule coverage re-run post-D33.** `rule_domain_notation`'s stage-6 row predates D33 by an unmeasured amount. **Not recommended as an 87h re-run** for that alone; see the stage-9 table. **Not run.**
- [x] **Stages 10f and 11 ran (2026-09-22)** — stage 10 and stage 11 are finished. 10f: per line the witness FAILS its gate (`Clear`-loss 53 → 54, REJECT), so the dedup cascade is a PRECONDITION, confirming 08c. 11a: zero `ssut*` rows, W6 closes. 11c: X1 confirmed — exactly two gold-`Clear` rows are stored `Trash` and URL-shaped, which is the 40 → 38 shift; the drift is accounted for. 11b: **the split measures 12 fixes / 1 break, errors and `Clear`-loss both DOWN**, so the gate passes — Y4's premise was wrong and its conclusion with it. See Z5 for why the result is fragile.
- [x] **Stage 10e ran (2026-09-22)** — the adoption gate survives D33: errors 513 → 503, cost −0.0087, `Clear`-loss 38 → 38, `Trash`-recall 12.2% → 18.9%, fixes 12 / breaks 2, p = 0.01294. Stage 9a is closed. Digest Y4 computes the language split's effect on the same 14 rows rather than guessing it: most likely 11 fixes / 1 break, i.e. errors +1 and `Clear`-loss −1, **which the adoption gate as written would reject**. 10f (`--no-postprocessing`) was still running.
- [~] **Stage 10d ran (2026-09-22)** — the threshold question is closed against gold: a global 4 is a null that costs `Trash`-recall (errors 503 = 503, `Clear`-loss 38 = 38, fixes 2 / breaks 2, p = 1, macro_f1 −0.0020, recall 34/180 → 32/180). **And `Clear`-loss is 38, not 40**, with `errors` unchanged — digest X1 carries the D33 hypothesis and the cheap check that would confirm it. **10e was still running** when this revision was written.
- [ ] **Stage 9a — 08b/08c re-run on the post-D33 tree, base config printed.** The gate result was measured on a predicate D33 has since changed, and the 9b/9c re-run does **not** substitute for it: 08f/08g measure a population, 08b/08c score gold. **Not run**, now carried as stage 10e.
- [x] **Stage 9b — the exposure pass with `SHORT_GARBAGE_LEXICON_PATH` configured.** **Ran 2026-09-22.** At risk **5,563 strings / 6,714 lines**; the exempt share is **82.1%**, not T1's estimated 73.1%. Delivered `--by-group` with it (9d), which closed H6 option 2 at 0 groups / 0 lines.
- [~] **Stage 9c — the annotation ask re-cut from the 9b queue.** **Ran 2026-09-22, half done.** The ask is re-cut and in the repository (157 census + 200 sample, `d9c41a5`) and **the hold on @DanaKriv is lifted** — but it was cut at `--strong-df 10`, not the recommended 113, so the recoverability evidence is unchanged in character. That half is still open and is carried as a caveat in the annotation guide § 5 (the `nearest_attested` paragraph) rather than as a blocker.
- [x] **The eight doubled-initial tokens re-confirmed by @david-spacil at full scale (H7)** — ✅ **answered 2026-09-22 (W2): `ppole` and the three `ssut*` forms are real language, `llocm`, `vvkop`, `jjámy`, `oobjekt` are damage; the guard came out on the same answer (D40).** Original item: his labels were given against the 822-document table, where the populations separated. At 113,100 they do not, and whether that is a scale artefact or a real change in what those strings are is a question only a reader of the archive can answer.

## Out of scope (other threads)

- Issue #23 (`tools/quality_model/`) — the offline distillation harness. D14 would borrow its korektor wrapper and its `gold_gate()`, but wiring a trained model into the pipeline is #23's decision, not this one.
- The dot-chain `Clear` leak and the `service/text_api.py` double-import each deserve their own issue.
- Whether the `api` container probe should run on pushes is a hub change (`ufal/atrium-project`), not an `alto-postprocess` one — and it is already made; only the `v1` tag move remains.

---

_Revision of **2026-09-23 (second)**. @david-spacil confirmed **336/508 on `3b02959`** (15 moved: 11 fixed, 1 broken, 3 wrong either way; 0/508 change with the flag off), so H5 and the D45 restore are confirmed rather than expected. A text-only sync then carried the settled answers into the tree: the advisory and the `setup/config.txt` witness block re-stated from corpus exposure (37,555 → 6,714 at-risk lines, 08f → 9b) instead of the retracted 1:3 / 1.7:1; config comments brought up to 08d, 08e, 10d, 05e and 5c; `ppole` described as an abbreviation wherever the tree still called it an artefact; the findings doc given a D45 section and 🛑 markers; the review request, the folder README and `CONTRIBUTING.md` brought up to date. One parity fix is code: `tools/short_garbage_witness_report.py` now passes each row's `original_lang` to the witness as production does since D44. **H14 = Q8** is new. No flag, threshold, `[allowed]` entry or config value changed, and the re-score of the sample corpus still moves 0 categories. Previous revision of **2026-09-23**. H5 closes on @david-spacil's direct re-check of the 508 (335/508 on `master`, 11 fixed / 2 broken), and one of its two breaks exposes **D45**: both round-2 witness guards were deleted by `cc4990e`, an unrelated lexicon commit, on the evening they were measured, and every run since scored the guard-less witness. The grid guard is restored and pinned by tests; the German-diacritic veto is left as an open maintainer item. Previous revision of **2026-09-22 (fourth)**. A documentation sync after comment 61 — no code, no flag, no word-list entry changed. The owner table gains **H9–H13**, the questions put to the thread after comment 61 (Q1, Q4–Q6), and H1–H3 / H5 carry their real status: the ask is 157 + 200 decisions, not 93 + 200; H3 is answered pending @DanaKriv's dissent; H5's revision is Q7. H6's "option 3 dropped" named the wrong option under this table's own numbering and now names the bare-plurality guard. The H7 checklist item is ticked (W2). 11e's "README weight table" item was already done (`9121c4c`). One finding is recorded because it contradicts something written first: **comment 61 told the data providers that an `[allowed]` entry stops the program discarding a word; the shape witness never consults the list**, so a mixed line can still be convicted on the listed word alone (H12). Previous revision of **2026-09-22 (third)**. Stage 10 and stage 11 are finished. **The measurement programme this plan exists to run is complete**, and what remains is decisions rather than runs. 10f shows the witness fails its gate per line, so the dedup cascade is a precondition and § 2's answer is load-bearing. 11a closes W6 at zero. 11c confirms X1 and accounts for the 42/41/40/38 drift at last — while printing the wrong verdict, because its own pass condition counted the wrong rows. And 11b **overturns Y4 in our favour**: the split measures 12 fixes / 1 break with errors and `Clear`-loss both down, so the adoption gate passes and the "decide the gate first" recommendation is withdrawn — replaced by Z5's caution, that it passes partly because a German-reading line was detected Afrikaans. Previous revision of **2026-09-22 (second)**. Adds § "What landed in the tree" — five unpushed commits turning @david-spacil's answers into code, config, docs and tests (D40 removed, the tie-break pinned, D43 and D44 built and shipping off) — and folds in **stage 10d**, which closed a question the plan had written off as a guard. **Two numbers change.** A global `VOWEL_RUN_MIN` of 4 is dead: the gate is indifferent and both secondary measures move the wrong way, so the language split was the right thing to build and the blunt threshold is not a fallback. And **`Clear`-loss is 38, not 40**, with `errors` identical at 503 — the fourth value this quantity has had, but the first whose drift is explainable from the delivery itself. Digest X3 is the one to read before 11d: on this gold set the split is the same experiment as the threshold conditioned on a label, two of the four discordant rows point opposite ways, and a wash is the honest expectation. 11b is replaced by a cheaper measurement that answers more — `original_lang` for the 2,064 gold rows. Three findings from the implementation are recorded because each contradicts something written first, including a test that failed by passing (D28's third instance). Previous revision of **2026-09-22**. Adds the stage-6 outcomes, D35–D37 and stage 9e, and
closes R3 — the 23 rules are gold-scored at last, and the baseline agrees with
08b across two independent jobs. The substantive finding is that
`rule_short_garbage` destroys 7 of the 40 gold `Clear`-losses the flag decision
turns on; the instrument findings are that the sweep nominated the witness for
retirement on a `fire_count == 0` that is a fact about a flag (D35), that
`gold_clear_loss` was printed without the baseline the same pass computed (D36),
and that `decisive_cascade` is not the quantity three places said it was (D37).
T1 in the digest is upgraded from inference to confirmed: `issue30_stage8_job.sh`
arms the lexicon in 08b/08c and not in 08f. Previous revision of **2026-09-21**.
Adds the stage-8 outcomes, stage 9, and D32–D34. Stage 8 settles
four questions: the adoption gate passes (08b), D26 is a decisive reject rather than a null
(08d), D27 is a genuine null rather than void (08e), and D30's per-collection experiment is not
merely inconclusive but inverted (08a). It also contains a defect — 08f and 08g were run with the
lexicon off, a configuration `setup/config.txt` says will never ship, so the annotation ask is
sized against a population roughly 3.7x larger than the real one. Stage 9 re-measures it before
anything goes to @DanaKriv. D32 and D34 are recorded as measured-and-not-changed rather than as
fixes: both were first written down as predicate defects here and then refuted by re-measuring
them in the shipped configuration, which is the same mistake T1 documents, made on this plan's
own work. Previous revision of **2026-09-20**. Adds the stage-7 re-read summary at the head, D28–D30, stage 8,
and a rewritten owner table built around one population and 293 decisions. D25 is reopened,
D26's outcome is void rather than null, and D27's "different bug from D26's" note is
corrected — all three on evidence that was inside the delivered logs when they were first
written up. Earlier revisions are left in place with their corrections attached, because the
recurring failure in this issue is not the wrong answer, it is the confident summary of a run
nobody re-read._
