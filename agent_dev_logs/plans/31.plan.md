# Strategy — Issue #31: any text-bearing input → ordered pages × lines

> Issue [#31](https://github.com/ufal/atrium-alto-postprocess/issues/31) · Labels: `development`,
> `integration` · Milestone: **Q1-27 [WP4: Pre-Closure Architecture Review]** _(relabelled 2026-09-08; was Q3 [WP8 / WP7])_ ·
> **Status: 🔍 `Tocheck`.**
> * **2026-09-24, after the push:** everything below is on `test` = `master` = `2e2794d` (Phase 4: `267e334`, `3bd10f9`, `2e2794d`), CI green, unreleased. Round 5 added the formats section to `docs/text_inputs.md`, documented the ALTO-v3-only splitter (§8) and the TEITOK page numbering (§2), and fixed the hub's ALTO-only pages; remaining cross-repo items: `KNOWN_PIPELINE_SUFFIXES`, `test_document_required.py`, the `ORIGIN_ORIGINATORS` `digital-born` prefix, llm-enrich V-1.
> * **Phase 1–2 (generic JSON):** landed on `test` in `v1.2.1-beta`; its `doc.json` side was
>   finished by #37.
> * **Phase 3 (`--method text-lines`: PDF, DOCX, ODT, XLSX, PPTX, EPUB, RTF, HTML/hOCR, PAGE XML,
>   TEI, CSV, Markdown, TXT, …):** implemented 2026-09-23; **on `test` since `103e30a`** (2026-09-24,
>   unreleased). Suite 1448 → **1581 passed** (11 skipped, 2 xfailed, 0 failed); `ruff` clean;
>   coverage 72.7%.
> * **TEITOK follow-ups** (below Phase 3): implemented 2026-09-24; **on `test` since `fb72526`**
>   (suite 1588 passed, +3 new tests).
> * **Phase 4 (hardening and more dialects, end of this file):** implemented 2026-09-24 — ABBYY/DjVu
>   XML, Tesseract TSV, SRT/VTT, EML/MBOX, gzip/bzip2/xz, ZIP bundles of page files, footnotes,
>   `partial`, safe page directories, `SOURCE_ORIGIN_BY_KIND`, the service on `[TEXT_INGEST]`, doc ids
>   in classify/aggregate. **On `test` since `267e334`** (unreleased); its docs and dev logs, the
>   Apple `.pages` refusal fix and the restore of `tests/test_env_contract.py` follow in the next
>   commit. Suite 1584 → **1707 passed** (11 skipped, 2 xfailed, 0 failed); `ruff` clean.

## ✅ Status reconciliation (2026-09-23)

This file used to say "design only, no production code touched yet", and the digest still said so
after the code had landed. That was wrong from 07-25 onwards:
* **Phase 1–2, the JSON half (D1–D8 below), is implemented exactly as designed:**
  `page_split.split_json_document()` (Family A/B/C), `json_stats_create.py` page ids from split file
  names, and `run_pipeline.py --method json-keys` with `PAGE_JSON_DIR`.
* **#37 then added** the `doc.json` accretion, the deferred `--force-single-page` (a Risks item below)
  and `tests/test_json_subprocess.py`.

The sections from **Context** to **Out of scope** are kept as originally written, as the record of
the JSON design. **Phase 3** at the end is the new work: the rest of the issue ("digest *any*
text-containing format"), which K4TEL re-scoped on 2026-09-23. The new scope:
* DOCX and PDF must be accepted as ordered pages of text;
* every sequentially readable textual format should be;
* extracted lines become CSV rows;
* pages and lines encode reading order and blocks for non-paged formats;
* input handling must be strict;
* readers should be lightweight;
* the ALTO workflow must be preserved.

## Context

Issue #31 asks the pipeline to digest any text-containing OCR-engine output — ALTO XML or generic
JSON — into ordered plain text. The ALTO half of that is done: `page_split.py` already splits a
document-level `<file>.alto.xml` (multiple `<Page>` elements under one `<Layout>`) into one
`<base>-<page>.alto.xml` per page (`page_split.py:39-87`), and everything downstream
(`alto_stats_create.py`, the `extract_*_2_TXT.py` family) is built on the assumption that it is
handed one page per file.

The JSON half of the pipeline was built on the **same one-file-one-page assumption**, but nothing
actually enforces or splits toward it — it's just hardcoded:

- `run_pipeline.py`: `INPUT_FORMATS["json"]["split_script"] = None`, with the stage 1 comment
  *"not applicable; each JSON file is already one page"*, and a second comment tagged **`(#31)`**
  explicitly noting the stats-CSV builder has to scan the raw `input_dir` because there is no
  split output to scan.
- `json_stats_create.py`: every JSON file becomes exactly one CSV row with `"page": 1` hardcoded
  (`_process_single_json()`), documented in the module docstring as *"the pipeline's page model for
  JSON is 'one file = one page'"*.
- `extract_JSON_2_TXT.py`: one file in → one `.txt` out, no page concept at all.
- There is no real sample JSON anywhere in `data_samples/` — only synthetic test fixtures
  (`{"page": {"lines": [...]}}`, `{"text": "..."}`). The extractor was deliberately built
  schema-agnostic (`TARGET_KEYS` leaf-walk) rather than against one vendor's format, and **we
  confirmed with K there is no concrete engine/sample to design against yet** — this plan is
  intentionally generic and will be validated against real data whenever a sample shows up.

**Decision (confirmed with K):** unify page splitting into `page_split.py` itself rather than a
parallel script — Step 1 of the pipeline becomes "split whatever page-bearing document you were
given," dispatched by file extension, with the existing ALTO path untouched.

## Research: how OCR/Doc-AI engines represent multi-page documents

Surveyed several real engines; the multi-page shapes fall into three families:

**Family A — nested pages array (most common).** One JSON per document; a top-level (or
one-level-nested) key holds an array of page objects, each appearing in document order with its
own structural fields. Azure Document Intelligence's `analyzeResult.pages` is a list of pages
represented sequentially, each carrying its own orientation angle and dimensions
([Microsoft Learn: Document layout analysis](https://learn.microsoft.com/en-us/azure/ai-services/document-intelligence/prebuilt/layout?view=doc-intel-4.0.0)).
docTR follows the identical shape — blocks/lines/words nested under an indexed `pages` list,
accessed as `output['pages'][0]['blocks']`
([docTR OCR extraction walkthrough](https://medium.com/quantrium-tech/text-extraction-using-doctr-ocr-471e417764d5)).
Google Document AI and most PDF-text-layer dumps (PyMuPDF/pdfplumber-style exports) use the same
`"pages": [...]` nesting.

**Family B — flat element list tagged with a page number.** AWS Textract is the counterexample:
no nesting — a single flat `Blocks` array, where a `PAGE`-type block exists per page and every
other block carries a `Page` field pointing back to it; the total count lives separately in
`DocumentMetadata.Pages`
([AWS Textract: Pages](https://docs.aws.amazon.com/textract/latest/dg/how-it-works-pages.html)).
Splitting this means grouping a flat list by a field value, not slicing a nested array.

**Family C — already one file per page.** This is what the pipeline assumes today, and it's a
reasonable default for this project's domain specifically: pero-ocr — the Brno (DCGM) tool
commonly used on Czech historical/handwritten material — processes images one at a time via its
CLI and emits Page XML/ALTO XML per image, not a bundled multi-page JSON
([pero-ocr GitHub](https://github.com/DCGM/pero-ocr)). Even cloud OCR APIs like OCR.space return
one `ParsedResults` entry per page for multi-page inputs
([OCR.space API docs](https://ocr.space/ocrapi)) rather than one nested blob.

Net: we need to detect Family A and Family B generically and fall back to Family C (today's
behaviour) when neither fires — never assume, never break the current fixtures.

## Design decisions

| #  | Decision                   | Recommendation                                                                                                                                                                                                                                                                    | Rationale                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
|----|----------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| D1 | Where the logic lives      | Extend `page_split.py` in place; add `split_json_document()` alongside `split_alto_xml()`, dispatched by extension in `main()`. `split_alto_xml()` is untouched.                                                                                                                  | Confirmed with K. Keeps "Step 1: split into pages" a single conceptual stage regardless of input format; purely additive to the ALTO path (minimal blast radius).                                                                                                                                                                                                                                                                                                                                       |
| D2 | Detection strategy         | Generic, schema-agnostic heuristic (Family A first, then Family B, then Family C passthrough). No vendor-specific parsers.                                                                                                                                                        | No real sample exists yet (confirmed with K). Mirrors the existing philosophy already stated in `extract_JSON_2_TXT.py`'s docstring — "no assumption about a particular OCR engine's JSON schema."                                                                                                                                                                                                                                                                                                      |
| D3 | Output convention          | Always write `<output_dir>/<base_name>/<base_name>-<page_number>.json`, for 1 page or N — never a passthrough at the original path.                                                                                                                                               | Exactly mirrors `split_alto_xml()`'s contract (same naming pattern, same directory shape, same `int` page-count return). Downstream stages never need to know whether a split "actually happened."                                                                                                                                                                                                                                                                                                      |
| D4 | Family C fallback          | If neither A nor B is detected, still route through the same writer as a single `<base>-1.json`.                                                                                                                                                                                  | Preserves 100% of current behaviour/tests while giving JSON output the identical shape as ALTO's PHYSICAL_IMG_NR-or-index-fallback path.                                                                                                                                                                                                                                                                                                                                                                |
| D5 | Detection depth            | Container-key / page-tag scan limited to **top level + one level of nesting** (depth ≤ 2), not a full recursive walk.                                                                                                                                                             | Mirrors the existing "scan root + one level of subdirectories" convention already used by `alto_stats_create.py`/`json_stats_create.py`. A shallow scan also avoids false-triggering on an unrelated nested list that happens to use a whitelisted key name (e.g. a bibliography list with per-entry "page" ranges buried three levels down). Azure's real shape (`analyzeResult.pages`) needs exactly one level, so this isn't just conservatism — it's required for Family A's most common real case. |
| D6 | Family B trigger threshold | Only fires if the tagged flat list has **>1 distinct page-tag value**. A single distinct value (or none) falls through to D4.                                                                                                                                                     | A flat list of dicts that all share one page value is not evidence of a multi-page document — avoids false positives on ordinary single-page JSON that happens to have a "page": 1 field on every line.                                                                                                                                                                                                                                                                                                 |
| D7 | `json_stats_create.py` fix | Drop the hardcoded `"page": 1`; derive `file`/`page` from the split filename exactly like `alto_stats_create._process_single_xml()` already does (`basename.split(".")[0].split("-")`), and scan the split output directory instead of the raw input dir.                         | `json_stats_create.py`'s directory walk ("root of the folder and one level of subdirectories") already matches `PAGE_ALTO`'s shape — this is a very small, targeted change, not a rewrite.                                                                                                                                                                                                                                                                                                              |
| D8 | Pipeline wiring            | `INPUT_FORMATS["json"]["split_script"]` flips from `None` to `"page_split.py"`; new `[PIPELINE].PAGE_JSON_DIR` config key (default `data_samples/PAGE_JSON`), parallel to the existing `PAGE_ALTO_DIR`. `stats_scan_dir` becomes the split output dir for both formats uniformly. | Keeps the per-format-default-dir convention already used everywhere else in `config.txt` (`OUTPUT_TXT_JSON`, `WORKERS_MAX_JSON`, etc.) instead of overloading `PAGE_ALTO_DIR`.                                                                                                                                                                                                                                                                                                                          |

## Detection whitelists (proposed, case-insensitive matching — same style as `TARGET_KEYS`)

```python
# Family A: top-level (or one-level-nested) key whose value is a non-empty list of dicts.
PAGE_LIST_KEYS = {"pages", "page_results", "parsedresults", "page_list", "document_pages", "documentpages"}

# Field checked inside each candidate page-dict / flat-list element (Family A page numbering,
# and Family B grouping key).
PAGE_NUMBER_FIELD_KEYS = {
    "pagenumber",
    "page_number",
    "page_num",
    "page",
    "pageid",
    "page_id",
    "pageno",
    "page_no",
    "physical_img_nr",
}
```

No collision with `extract_JSON_2_TXT.TARGET_KEYS` (text-leaf whitelist) — `"pages"` isn't in that
set, so splitting and text-extraction stay independent concerns operating on the same JSON shape.

## Algorithm sketch for `split_json_document(input_file_path, output_dir) -> int`

1. Load JSON. (No DOCTYPE-style hardening needed — that was ALTO/XML-specific entity-expansion
   protection; standard `json.load` has no equivalent attack surface.)
2. **Family A scan** (depth ≤ 2): find the first key (top-level, or nested one level under a
   dict-valued top-level key) matching `PAGE_LIST_KEYS` whose value is a non-empty list where
   every item is a `dict`. If found and `len(list) >= 1`: that's the page list. Page number per
   item = first matching `PAGE_NUMBER_FIELD_KEYS` field, else 1-based index (mirrors ALTO's
   `PHYSICAL_IMG_NR`-or-index fallback). Each output file = all sibling top-level keys (the JSON
   "header," analogous to `Description`/`Styles`) + that one page object in the container key's
   place.
3. **Family B scan** (only if A found nothing; depth ≤ 2): find any list of dicts where a
   majority of items share a `PAGE_NUMBER_FIELD_KEYS` field, and the field has **>1 distinct
   value** (D6). Group items by value; each output file = full original document with that list
   key replaced by only the matching subset.
4. **Fallback** (D4): neither pattern found → one output file, page number `1`, full document
   unchanged.
5. Write `<output_dir>/<base_name>/<base_name>-<page_number>.json` per page (`json.dump`,
   `ensure_ascii=False`, matching the rest of the repo's JSON I/O style). Return the page count,
   same contract as `split_alto_xml()`.

## File-by-file changes

- **`page_split.py`** — add `split_json_document()` (above); extend `main()`'s file-discovery loop
  to also match `*.json` and dispatch to it; extend the paradata `output_types` to be format-aware
  (`["xml"]` vs `["json"]`) instead of hardcoded. `split_alto_xml()` itself: **no changes**.
- **`json_stats_create.py`** — `_process_single_json()`: replace hardcoded `"page": 1` with
  filename-derived `file_id`/`page` (D7); `process_json_files()`/`main()`: scan the split-output
  directory (mirrors `alto_stats_create.py`'s existing root+one-level walk almost exactly).
- **`run_pipeline.py`** — `INPUT_FORMATS["json"]["split_script"] = "page_split.py"`; add
  `page_json_dir` to `_DEFAULTS`/`resolve_settings()`/CLI (`--page-json-dir`, parallel to
  `--page-alto-dir`); `stats_scan_dir` resolution becomes format-agnostic (split output dir for
  both `alto` and `json`); `outputs["split"]` stops being `None` for json.
- **`setup/config.txt`** — new `[PIPELINE].PAGE_JSON_DIR = data_samples/PAGE_JSON`.
- **`README.md`** — extend the "Step 1: Split Document-Specific ALTOs into Pages" section (or
  retitle it format-neutrally) to document the JSON path and link to the new `PAGE_JSON` sample
  dir once one exists.
- **Tests to update** (behaviour changes, not just additions):
  - `tests/test_run_pipeline.py::test_resolve_settings_json_keys_format` and
    `::test_build_plan_json_keys_forces_split_skip_and_routes_stats` — both currently assert
    `outputs["split"] is None` / the split stage is force-skipped for json; this flips.
- **Tests to add**:
  - `tests/test_page_split.py`: Family A (nested `pages` list, ≥2 pages, page-number field
    present and absent → index fallback), Family B (flat tagged list, >1 distinct value),
    Family B non-trigger (single distinct value → falls through to D4), Family C fallback
    (today's fixtures must still produce exactly one `<base>-1.json>` unchanged), header/sibling-key
    preservation, empty-list edge case, malformed-JSON skip-and-continue (mirrors
    `test_main_survives_malformed_document`).
  - `tests/test_json_stats_create.py`: page number correctly derived from split filenames instead
    of hardcoded `1`.

## Risks / open questions

| Risk                                                           | Mitigation                                                                                                                                                                                                                          |
|----------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| No real sample to validate against                             | Ship the generic heuristic now (per K); revisit whitelist thresholds the moment a real engine sample is available. Flagged explicitly in the docstring as provisional.                                                              |
| Family A/B false positives on a genuinely single-page JSON     | D6 threshold (>1 distinct value) for Family B; D5 depth cap for both; Family A additionally requires every list item to be a `dict` (rules out e.g. a `"pages": [1, 2, 3]` page-count field misfiring).                             |
| Two container keys both match `PAGE_LIST_KEYS` in one document | Take the first by depth-then-declaration-order (top-level before nested, first-seen key order); document this precedence explicitly in the docstring since it's the one place behaviour is a judgment call rather than a hard rule. |
| Silent misfire once a real engine is known                     | Worth a config/CLI escape hatch (`--force-single-page` or a config flag) to disable auto-detection per run — **deferred to implementation**, flagging here so it isn't forgotten.                                                   |

## Verification / done-criteria

- `pytest -m "not slow"` fully green, including the two updated `test_run_pipeline.py` cases.
- New `test_page_split.py` cases cover Family A, Family B, Family B non-trigger, and the Family C
  fallback (today's exact fixtures still produce unchanged single-page output).
- `ruff` / pre-commit clean.
- README Step 1 section documents both formats.

## Out of scope (this issue)

- Vendor-specific hardcoded parsers (e.g. a dedicated Textract or Azure adapter) — only revisit if
  a real sample shows the generic heuristic is insufficient.
- Any change to `extract_JSON_2_TXT.py`'s `TARGET_KEYS` text-leaf extraction logic — orthogonal
  concern, already works on split output unchanged.
- The `--force-single-page` escape hatch flagged above — worth doing alongside the main
  implementation, but not blocking it.

---

# Phase 3 — `--method text-lines`: any text-bearing input (2026-09-23)

## Context

K4TEL's re-scope asked for:
* **DOCX and PDF** accepted as an ordered list of texts split into pages;
* **any textual format that can be read sequentially** accepted;
* ordered lines becoming **CSV rows**;
* for non-paged formats, pages and lines that encode reading order and block succession (JSON child
  objects, sheets);
* **strict** handling of user input;
* **lighter** readers where they exist;
* **no change** to the ALTO workflow;
* no categorization run.

The goal is text-quality categorization (OCR text first) for any shape of input. Deliverables were
full files in chat: no patch files, no remote actions.

The sibling repos were read first:
* **nlp-enrich** has no PDF/DOCX reader. Its flexiconv→TEITOK adapter was non-functional per its #10 *(fixed on
  2026-09-23, released in nlp-enrich v0.21.0; see "TEITOK follow-ups" below)*.
* **llm-enrich** has `api_util/digital_to_json.py` (role `digital-convert`, pdfplumber + python-docx).
  It chooses the format by extension only, has no corrupt/encrypted handling, treats DOCX as one
  page, and belongs to another role: its output is llm-enrich's record, not this repo's
  categorization input.
* **Reused rather than rebuilt:**
  * the PDF text-layer thresholds of llm-enrich's `pdf_to_md._page_ocr_reason` (`MIN_TEXT_CHARS=3`,
    `GARBLE_THRESHOLD=0.15`);
  * its hand-rolled deterministic PDF/ZIP fixture builders (MIT, attributed);
  * its licence policy: permissive only, no PyMuPDF (AGPL).

## Decisions (D9–D20)

| #   | Decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Why                                                                                                                                                                                                                                                                                                   |
|-----|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| D9  | A **new method** `text-lines` (input format `text`) with its own stage-1/2/3 scripts: `text_split.py`, `text_stats_create.py`, `extract_TEXT_2_TXT.py`. `page_split.py`, both older stats scripts, the ALTO/JSON extractors and `aggregate_STAT.py` are **not modified**.                                                                                                                                                                                                                                                                                                                          | Keeps the ALTO path byte-identical; `test_page_split::test_main_ignores_non_xml_files` pins `page_split` ignoring `.txt`. Three stages rather than one keep `--skip-split` (hand-made page TXT in any encoding), `--start-from extract` and resume working, and mirror `PAGE_ALTO` → `PAGE_TXT`.      |
| D10 | One reader library, `text_formats.py`, that detects the format **by content**. Magic bytes and ZIP members decide for containers; the root element for XML; a parse for JSON. The extension only chooses among text dialects (CSV/TSV/MD/JSONL).                                                                                                                                                                                                                                                                                                                                                   | Users misname files. Content-sniffing CSV would misfire on any prose with commas, so the extension decides only there. Mismatches are noted in the report, not fatal.                                                                                                                                 |
| D11 | **Pages** are real pages where the format has them (PDF, ALTO, PAGE XML, hOCR, DOCX/ODT breaks). Otherwise a page is the natural block: sheet, slide, JSON child, JSONL record, EPUB chapter, form-feed section. **Lines** are the format's own units: physical line, paragraph, cell, row.                                                                                                                                                                                                                                                                                                        | The re-scope's page and block semantics. Chosen with K4TEL: native units plus a safety cap (D13).                                                                                                                                                                                                     |
| D12 | **Lightweight readers:** stdlib `zipfile` + lxml (already a dependency) for DOCX/XLSX/PPTX/ODF/EPUB/HTML/XML; **pypdfium2** for PDF; **charset-normalizer** for legacy encodings. No python-docx, openpyxl, pdfplumber, docling or flexiconv. Both new packages are lazy imports and `conditional` rows in `para_config.txt` (pypdfium2 recorded as Apache-2.0: `para_licenses` cannot parse `OR`).                                                                                                                                                                                                | K4TEL asked to use lighter alternatives where they exist. pypdfium2 is a 3.7 MB wheel with no Python dependencies; pdfplumber adds pdfminer.six and Pillow on top of the same pypdfium2. The `>=5.9` floor matches llm-enrich's pdfplumber pin, so a shared environment resolves one version.         |
| D13 | **Normalization** is centralized in `text_formats.py`. `normalize_line()` at split: every separator except `\f` becomes `\n`; NFC; ligatures expanded; zero-width, bidi and control characters removed; soft hyphen at end of line becomes `-`. `shape_lines()` at extract and in the service: blank lines dropped, lines over `MAX_LINE_CHARS=1000` wrapped at a word boundary.                                                                                                                                                                                                                   | `classify_TEXT` numbers lines by `readlines()`, so the line table stays aligned with `DOC_LINE_CATEG` by construction. The cap is also a GPU safety limit, because `calculate_perplexity_batch` pads to the longest line.                                                                             |
| D14 | **Strictness:** each file fails alone, with a stable **reason code** (`REASON_CODES`) in `ingest_report.csv` and paradata. Caps are enforced: `MAX_FILE_MB`, zip-bomb checks on declared sizes before reading, `MAX_PAGES`, `MAX_LINES_PER_PAGE`. XML is parsed with no entities, DTD or network, and entity declarations are refused. PDFs are read in a **subprocess** with a timeout. Page directories are written atomically. doc_id collisions (case-folded) and invalid ids are refused. Symlinks, FIFOs, lock and metadata files are ignored. `--strict` / `STRICT` controls the exit code. | "Strict enough to handle users' inputs." A plain `subprocess` rather than multiprocessing spawn, so the child never re-imports the service's `__main__`.                                                                                                                                              |
| D15 | **`source.origin` is truthful per class** (chosen with K4TEL): `ocr:page-xml` / `ocr:hocr` / `ABBYY-ALTO` / `ocr:pdf-text-layer` (mostly invisible text) / `ocr:generic` (TXT, MD, CSV, JSON, TEI, XML), and `digital-born-<kind>` otherwise. `page_count` is only written for formats with real pages. **`document_hook.write_document_block` guard:** a record whose origin authorises another originator gets no `pages/content/lines/tables` from any stage of this repo, unless it carries the `needs_ocr` hand-off.                                                                          | When not strict, `atrium_document` §1a only *warns* and still writes (`_complain`), so the unchanged classify/aggregate would have put OCR categories into digital-convert's records. The hub's e2e-digital-smoke asserts that alto-postprocess must not run there. The CSVs are produced either way. |
| D16 | `classify_TEXT.load_page_index()` reads `file` as a string (`keep_default_na=False, na_values=[""]`): the one change to classify.                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | User-named ids such as `0001` or `NA` were silently skipped (int/NaN). `page` stays numeric; ALTO `CTX…` ids are unaffected.                                                                                                                                                                          |
| D17 | **Service** `/process` accepts every text-lines kind as `task_type=document` (chosen with K4TEL). `auto` keeps `.txt`/`.json` and decides `.xml` and everything else by content. Classification runs per page in batches of 128 lines. Unsupported files are 400 and unreadable ones 422, both with the reason code. Born-digital uploads do not accrete. `release.yml` bundles `text_formats.py` + `page_split.py`.                                                                                                                                                                               | One reader and one shaping path for batch and API. The previous "`.pdf` → 400" test now uses an unknown binary.                                                                                                                                                                                       |
| D18 | **Line tables** in `DOC_LINES_TEXT/<doc>.csv` (`file,page_num,line_num,text,page_label`). The script refuses `[CLASSIFY].OUTPUT_LINES_LOG` / `[AGGREGATE].RAW_LINES_CSV` as their directory.                                                                                                                                                                                                                                                                                                                                                                                                       | "Ordered lines become CSV rows" even without running categorization. The key columns match `DOC_LINE_CATEG`, so nlp-enrich/llm-enrich can read them. Classify's resume check would have skipped every document whose line table sat in its output directory.                                          |
| D19 | `run_pipeline.py`: `INPUT_FORMATS[...]["page_dir"]` replaces both binary alto/json choices; `--page-text-dir` / `[PIPELINE].PAGE_TEXT_DIR`; the `_single_input_doc_id` text branch uses `text_split.iter_candidate_files`; a **warning** when `--input-csv` differs from `[EXTRACT]`/`[CLASSIFY].INPUT_CSV`.                                                                                                                                                                                                                                                                                       | ALTO/JSON dry-run output is byte-identical before and after (checked). The `--input-csv` trap predates this work: the flag only moves the stats output, and a json-keys run on the sample extracted 0% until the config was fixed. Warning only, so the stages still agree with each other.           |
| D20 | **Fixtures and samples:** `tests/text_format_fixtures.py` builds reproducible DOCX/XLSX/PPTX/ODF/EPUB/PDF in memory, so no binary fixtures are committed. `data_samples/TEXT/` holds one sample per family (CTX000000004–14) and `data_samples/JSON/CTX000000015.json` an Azure-style sample. Tests never read `data_samples/`.                                                                                                                                                                                                                                                                    | Closes the "no real JSON sample / `data_samples/JSON` does not exist" gap. `tests/test_alto_tools.py` now selects ALTO samples by root element, so the new PAGE XML sample is not mistaken for ALTO (same 10 files as before).                                                                        |

## Files

* **New:**
  * `text_formats.py`, `text_split.py`, `text_stats_create.py`, `extract_TEXT_2_TXT.py`
  * `docs/text_inputs.md`
  * `tests/text_format_fixtures.py`, `tests/test_text_formats.py`, `tests/test_text_split.py`,
    `tests/test_text_stats_create.py`, `tests/test_extract_text.py`, `tests/test_text_subprocess.py`
  * `data_samples/TEXT/*`, `data_samples/JSON/CTX000000015.json`
* **Modified:**
  * `run_pipeline.py`, `classify_TEXT.py` (`load_page_index` only), `document_hook.py` (guard)
  * `service/text_inference.py`, `service/text_api.py`, both frontends' `index.html`,
    `service/requirements.txt`, `service/README.md`
  * `.github/workflows/release.yml`
  * `setup/config.txt` (`[TEXT_INGEST]`, `PAGE_TEXT_DIR`, `OUTPUT_TXT_TEXT`, `OUTPUT_LINES_TEXT`,
    the `SOURCE_ORIGIN` comment)
  * `setup/requirements.txt`, `setup/requirements-test.txt`, `setup/para_config.txt`
  * `README.md`, `CONTRIBUTING.md`, `data_samples/README.md`
  * tests: `test_run_pipeline.py`, `test_doc_id_parity.py`, `test_classify.py`,
    `test_document_hook.py`, `test_service_api.py`, `test_text_inference.py`, `test_alto_tools.py`
  * this plan, the digest, `DEVLOG.md`
* **No hub-canonical file changed, and no new env var** (`test_env_contract`).

## Gap register

| Gap                                                                                                                                         | Kind                | Handling                                                                                                                            |
|---------------------------------------------------------------------------------------------------------------------------------------------|---------------------|-------------------------------------------------------------------------------------------------------------------------------------|
| #31 plan/digest said "no code changed" after the JSON phase landed                                                                          | docs                | reconciled above and in the digest                                                                                                  |
| DOCX/PDF and every other non-ALTO/JSON format unsupported                                                                                   | code                | Phase 3 (D9–D14)                                                                                                                    |
| No input-format matrix anywhere                                                                                                             | docs                | `docs/text_inputs.md` + README Step 1/3 sections                                                                                    |
| README: method list omitted json-keys; "`page_split` emits no paradata" (it does); dead `data_samples/PAGE_TXT_JSON` link; Step 2 ALTO-only | docs                | fixed                                                                                                                               |
| `run_pipeline` binary alto/json branches; stale `split_script=None` comment                                                                 | code                | table-driven (D19)                                                                                                                  |
| `--input-csv` reaches only the stats stage                                                                                                  | code (pre-existing) | warning (D19); proper propagation needs CLI flags on every extractor, so it is a follow-up                                          |
| `pd.read_csv` type inference drops `0001`/`NA` doc ids in classify                                                                          | code                | `load_page_index` (D16). The same trap in `extract_JSON_2_TXT.main` and `aggregate_STAT` is left for a follow-up (JSON/ALTO parity) |
| §1a only warns when not strict, so OCR blocks can land in digital-born records                                                              | code                | `document_hook` guard (D15)                                                                                                         |
| `split("-")` in `alto_stats_create`/`json_stats_create` breaks hyphenated doc ids                                                           | code (pre-existing) | new text path is hyphen-safe; the ALTO/JSON scripts are left untouched (ALTO parity) and flagged for a follow-up                    |
| json-keys copies the document header (Azure `content`) and word leaves onto every page                                                      | code (pre-existing) | documented in the README; `text-lines` reads the same JSON page-only and prefers lines over words                                   |
| `data_samples/JSON` (config default) did not exist; no realistic sample                                                                     | config/data         | `CTX000000015.json`                                                                                                                 |
| Service accepted `.xml/.txt/.json` only; frontends said so                                                                                  | code/docs           | D17                                                                                                                                 |
| `--force-single-page` "deferred"                                                                                                            | —                   | done in #37                                                                                                                         |

## Cross-repo follow-ups (not done here — other repos)

* **Hub `atrium_document.KNOWN_PIPELINE_SUFFIXES`:** add `.pdf .docx .odt .ods .odp .xlsx .pptx
  .epub .rtf .html .htm .hocr .jsonl .tsv .md`. Unknown multi-dot names currently truncate at the
  first dot (`scan.2019.pdf` → `scan`). text_split refuses the resulting collisions instead of
  merging, but ids should not fork across repos.
  *(Phase 4: `.md` — like `.csv`, `.txt`, `.json`, `.xml` — is already in the hub list; see the Phase 4
  follow-ups for the current list.)*
* **Hub `tests/test_document_required.py`:** it names alto's record writers (`page_split.py`,
  `extract_ALTO_2_TXT.py` "and its three twins"). Add `text_split.py` (source only) and
  `extract_TEXT_2_TXT.py` (pages + content), the fifth twin.
* **Hub docs that describe alto inputs as ALTO-only:** `docs_site/pipelines.md`, `index.md`,
  `ecosystem/repository-map.md`, `_generators/repos.py`, `docs/document_schema.md` origin table,
  `docs/agent_skill_strategy.md:139` (`/process` "ALTO XML · TXT"), `docs/k8s_deployment.md`.
* **llm-enrich V-1:** `json_to_md.DROP_CATEGORIES` ignores alto's `Trash`. Pre-existing, but
  text-lines produces more alto-categorized lines for llm-enrich to consume.
* **llm-enrich #13 TODO:** "normalise alto-postprocess `program` names". Stale: every stage stamps
  `program="alto-postprocess"` and records the script under `config.script`. Propose closing it.
* Once a real OCR-engine export arrives (Azure/Textract/docTR/pero), re-check the JSON page
  detection thresholds (still the Phase 1 risk).

## Verification (done in-session; categorization never run)

* Fast suite: 1448 → **1581 passed**, 11 skipped, 2 xfailed, **0 failed**. `ruff check` and
  `ruff format --check` are clean; coverage **72.7%** (floor 61; `text_formats.py` 87%,
  `text_split.py` 92%).
* `run_pipeline.py --dry-run` for `layoutreader`, `alto-tools`, `glm` and `json-keys` is
  **byte-identical** before and after.
* `run_pipeline.py --method text-lines --input-dir data_samples/TEXT --skip-classify
  --skip-aggregate` with a scratch config: 11/11 documents split, 21 pages, line tables written.
  The line-table invariant was checked for every row; every `doc.json` validates against the
  schema; born-digital records carry `source` only.
* A mixed directory with a corrupt PDF, zip bomb, OLE2 `.doc`, PNG, entity-bomb XML, FIFO,
  subdirectory, lock file and a case-folded id collision: every file got its reason code and the
  run continued.

## TEITOK follow-ups (2026-09-24, with atrium-nlp-enrich's round 4) — ✅ implemented the same day
1. ✅ `text_formats.read_tei`: every `<pb/>` starts a page, a blank one too, labelled with `pb@n` (else its ordinal);
   in a tokenized document (`<tok>` + `<lb/>`) `<s>` is inline, so lines follow `<lb/>` and a sentence is split at
   an inner `<lb/>`/`<pb/>`. `parse_xml_bytes` repairs a legacy `</n>` exactly (note `name_close_repaired`)
   before falling back to recover mode.
2. ✅ Tests in `tests/test_text_formats.py`, with inline format-2 documents rather than a copied fixture (the file
   builds every fixture in memory): page labels and blank pages kept; lines = physical lines, text faithful to its
   spacing, a `<pb/>` inside `<s>` and `<name>`; the `</n>` repair noted and not recovered.
3. ✅ `docs/text_inputs.md`: the TEI/TEITOK row says how pages and lines are derived; the `</n>` repair is noted.
4. ✅ Comments that cited `atrium-project#13` for the document-JSON contract → `atrium-llm-enrich#13` (six code
   comments and `setup/config.txt`).
5. No change to `DOC_LINE_CATEG`'s columns; nlp-enrich now relies on its `page_num` (see the digest).

Evidence: `pytest` 1588 passed, 13 skipped, 2 xfailed, 0 failed (the same environment gives 1585 passed at `103e30a`;
the issue's 1581/11 came from another environment); `ruff check` / `ruff format --check` clean.

---

# Phase 4 — hardening and more dialects (2026-09-24)

## Context

K4TEL asked for the #31 subtask to be re-inspected against the dev logs of every issue and against
the sibling repos, and for its gaps in docs, code and config to be closed. Requirements: strict
handling of users' inputs; every sequentially readable textual format accepted; ordered lines as CSV
rows; pages and lines that encode reading order and blocks for non-paged formats. The ALTO workflow
stays unchanged, and categorization is never run. Deliverables are full files in chat.

The audit ran at `fb72526` on all four repos, fetched at their `test` HEADs. Besides the stale status
lines, it found:
* **Correctness gaps.**
  * `0001` doc ids were coerced to `1` by classify's and aggregate's re-reads, and mixed int/str ids
    crashed aggregate's final sort.
  * Transkribus PAGE-XML table text was dropped.
  * A stray `"` in a CSV/TSV swallowed the rest of the file.
  * Tesseract TSV came out one word per line.
  * JSON line and word text was duplicated (Azure Read v3, Textract).
  * A failed re-run left stale pages that flowed into classify.
  * text_split's `rmtree` was unguarded: output dir `.` plus an input `setup.txt` deleted `./setup`.
  * One bad document aborted the extract and stats stages.
* **Service gaps.** The service ignored `[TEXT_INGEST]`, read `.txt` as UTF-8 only, and mapped
  400/422 by code path rather than by reason.
* **Frontends.** Their `accept=` lists had drifted.
* **Ecosystem gap.** Born-digital kinds that digital-convert cannot read (xlsx, pptx, odt, ods, odp,
  epub, rtf, html) could only ever be `source`-only records.

Decisions taken with K4TEL:
1. a per-kind origin override;
2. fix the id handling in classify/aggregate, ALTO-neutral and never run;
3. footnotes on their page;
4. add all four dialect groups: OCR exports, ZIP bundles, compression wrappers, subtitles + e-mail.

## Decisions (D21–D35)

| #   | Decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Why                                                                                                                                                                                                   |
|-----|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| D21 | `[DOCUMENT].SOURCE_ORIGIN_BY_KIND` (`xlsx = ocr:generic, …`; config only, no new env var), parsed by `document_hook.parse_origin_by_kind`: an unknown kind, a duplicate, an empty value or an origin no originator claims exits 2. Precedence (`resolve_input_origin`): `--source-origin` > `DOCUMENT_SOURCE_ORIGIN` > by-kind > `SOURCE_ORIGIN` > truthful default. Used by text_split and the service.                                                                                                                                                                                                                                                                                                                                                       | K4TEL's choice. The flag and the env var are per-run statements about every input; within the config the per-kind entry is the more specific one. A typo must not silently switch §1a off for a kind. |
| D22 | The born-digital ingest note only when `[DOCUMENT].JSON_DIR` is set; for kinds outside `DIGITAL_CONVERT_KINDS = {pdf, docx}` it names the override.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Without a record nothing is held back; for the other kinds no ecosystem originator writes the plane (llm-enrich `digital_to_json.py` reads .pdf/.docx only).                                          |
| D23 | Ingest status `partial`: read with a lossy note (`text_formats.LOSSY_NOTE_PREFIXES`: `xml_recovered`, `decode_replacement`, `jsonl_bad_records`, `csv_unbalanced_quote`, `tsv_bad_rows`, `xlsx_bad_shared_string`, `sheet_repeat_capped`, `zip_members_skipped`, `epub_spine_skipped`, `email_bad_messages`, `lone_surrogates_dropped`, the `page_load_failed` flag). Processed downstream; `--strict` counts it.                                                                                                                                                                                                                                                                                                                                              | "Strict enough": text that may be missing must not pass as `ok`. Informational notes (encoding detected, `</n>` repaired, overflow split) stay out, so the pinned E2E summary holds.                  |
| D24 | Page directories: only a real directory holding nothing but `<doc>-<n>.txt` is replaced or removed (else `output_failed`); `.tmp-` staging, `.old-` rename-aside; the record written before the swap; a failure after id validation removes the document's previous pages (`stale_pages_removed`). Docs stop saying "atomic".                                                                                                                                                                                                                                                                                                                                                                                                                                  | The `./setup` deletion hazard; stale pages of a now-failing document reached classify. A POSIX directory swap cannot be atomic.                                                                       |
| D25 | Per-document isolation in `extract_TEXT_2_TXT` (record, line table, CSV read) and `text_stats_create` (reports, subdirectories); `--strict/--no-strict` (`BooleanOptionalAction`) on text_split and extract; `STRICT` through `text_formats.config_bool`; stats decodes with `FALLBACK_ENCODINGS`; page labels per page directory; paradata records the reader options and the by-kind map.                                                                                                                                                                                                                                                                                                                                                                    | One bad document aborted a whole stage; `--strict` could not be negated; a `STRICT = ture` typo crashed with a traceback.                                                                             |
| D26 | classify: `read_doc_line_csv(path, file_id)` reads `file` as text and pins it to the task's id; aggregate: `file` as text, a missing id refilled from the CSV's stem, `sort_page_stats()` with a natural key. Edited, never run.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `0001` → 1 on the re-read; mixed id types raised at the final sort. Pinned byte-identical on `data_samples/DOC_LINE_CATEG(_gpt)` and for all-numeric ids (tests).                                     |
| D27 | Service: `ingest_settings()` (lru-cached, `LANGID_CONFIG`) loads `[TEXT_INGEST]` + `SOURCE_ORIGIN(_BY_KIND)`, validated at start-up; `.txt` decoded and shaped like the batch path (`plain_text` shape kept); one mapping, `UNSUPPORTED_REASONS` → 400, every other code → 422; a zero-line document → 422 `no_text`; a compressed ALTO → document path.                                                                                                                                                                                                                                                                                                                                                                                                       | Batch and API must yield the same lines; a cp1250 upload was a 500; sniff-time `corrupt` was a 400 but document-time a 422.                                                                           |
| D28 | Both frontends' `accept=` lists equal `text_formats.supported_extensions()`, pinned by `tests/test_service_frontends.py`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Ten supported extensions were missing; static HTML drifts.                                                                                                                                            |
| D29 | `run_pipeline`: for text-lines `--input-csv` reaches `extract_TEXT_2_TXT` and `--strict/--no-strict` both stages; `--source-origin` reaches the split stage of any method; `[PIPELINE].INPUT_DIR_TEXT` (→ `INPUT_DIR` → `data_samples/TEXT`); help texts format-neutral; the dry-run `[no log]` tag kept.                                                                                                                                                                                                                                                                                                                                                                                                                                                      | A bare `--method text-lines` ingested the ALTO samples. Every change is opt-in or text-only, so the ALTO/JSON dry-runs stay byte-identical.                                                           |
| D30 | Sniffing over bytes (`sniff_bytes`): compression magic first, then the inner name; `%PDF-` must lead unless it is not a text file (a stored PDF no longer makes a ZIP a "pdf"); known XML roots first (+ ABBYY, DjVuXML, `TEI.2`); dialect extensions beat an unknown root and the JSON probe; `.html` with any root → html; tar/7z/RAR/zstd refused; a BOM before `{\rtf`; a `.txt` that merely starts with `<` is plain text. New reason code `unreadable`.                                                                                                                                                                                                                                                                                                  | CODE-5/21/23/25 of the audit; OOXML/ODF/EPUB and all ALTO/JSON/TEI/HTML results unchanged.                                                                                                            |
| D31 | New kinds: `tesseract-tsv` (`ocr:tesseract`), `abbyy-xml` (`ocr:abbyy-finereader`), `djvu-xml` (`ocr:djvu`), all native pages; `srt`/`vtt` (`ocr:generic`); `eml`/`mbox` (`digital-born-eml/-mbox`: Subject + text/plain, html fallback, attachments counted); gzip/bzip2/xz read through, capped by `MAX_FILE_MB` and `ZIP_MAX_RATIO` (no new keys). `TextDocument.origin_hint`.                                                                                                                                                                                                                                                                                                                                                                              | K4TEL's choice: OCR engine exports first; every sequential text format accepted.                                                                                                                      |
| D32 | `zip-bundle`: one document of its members in natural path order, read by content; metadata/images/READMEs ignored; one stem in several formats → one kind by precedence (page-xml > alto > abbyy > hocr > djvu > tesseract > tei > json > … > txt); PDFs and nested containers skipped (never read in-process); a failing member skipped and noted (`partial`); origin and page nativeness from the members; an office/EPUB-looking ZIP without its marker, a package with only a preview image, never a bundle.                                                                                                                                                                                                                                               | "A folder of per-page files" was a documented limitation; Transkribus/eScriptorium export exactly this.                                                                                               |
| D33 | `[TEXT_INGEST].NOTES = page \| end \| skip` (default `page`): DOCX footnotes/endnotes (separators by `w:type`), ODT `text:note`; a footnote's lines end the page that cites it (a `_Flow` page tail flushed at each break), endnotes end the document; headers/footers counted, not read.                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | K4TEL's choice. Notes were silently lost.                                                                                                                                                             |
| D34 | Report-only page flags, never categories: `mojibake_cp1252` (llm-enrich's codec-derived CP1250↔CP1252 table and ≥2-hit / <0.90 rule, made conservative: a shared Czech letter required, Western-only letters exclude), `mirrored_text=N` / `rotated_text=N` (PDF text-object matrices composed with form XObjects, ~1° tolerance).                                                                                                                                                                                                                                                                                                                                                                                                                             | The garble test (FFFD/controls) cannot see a wrong-but-valid decode. The shared vocabulary keeps the tools' category sets disjoint.                                                                   |
| D35 | Reader fixes: PAGE-XML table cells and nested regions; CSV odd-quote re-parse, TSV unquoted, numeric columns never the text, word rows joined by (page, block, par, line), `field_size_limit` raised once; JSON line granularity at any ancestor, Textract LINE/WORD, string arrays, blobs, an own copy of the text keys (JSON no longer imports pandas); sheets continue on `+1` pages under a text budget; ODS comments and non-string cells dropped, repeat cap noted; hidden sheets/slides counted; RTF per-font code pages and surrogate pairs; lone surrogates dropped; TEI corpora and `<choice>`; generic `line` elements; EPUB `%`-hrefs; the PDF child writes ASCII JSON; ratio thresholds in [0, 1]; lxml elements tracked by identity, not `id()`. | The audit's CODE items; the `id()` one was a real flake (lxml frees element proxies, so an id can be reused and a line skipped).                                                                      |

## Files

* **Code**
  * `text_formats.py`
  * `text_split.py`, `text_stats_create.py`, `extract_TEXT_2_TXT.py`, `run_pipeline.py`
  * `document_hook.py`: D21 helpers, `DIGITAL_CONVERT_KINDS`, and a docstring rewrite. The stale
    per-stage program names and the dead `digests/13.digest.md` link are gone.
  * `classify_TEXT.py` and `aggregate_STAT.py`: D26 only.
  * `service/text_inference.py`, `service/text_api.py`, both `service/frontend*/index.html`
* **Config**
  * `setup/config.txt`: `SOURCE_ORIGIN_BY_KIND`, `NOTES`, `INPUT_DIR_TEXT` and the `STRICT` and
    service comments.
  * `.gitignore`: `!data_samples/TEXT/*.zip`.
  * `.env.example`: a comment.
  * `tests/env_contract_data.py`: the reason text.
* **Tests**
  * `test_text_formats.py`, `text_format_fixtures.py`, `test_text_split.py`, `test_extract_text.py`,
    `test_text_stats_create.py`, `test_run_pipeline.py`, `test_document_hook.py`, `test_classify.py`,
    `test_aggregation.py`, `test_text_inference.py`, `test_service_api.py`
  * new: `test_service_frontends.py`
* **Samples**
  * new: `data_samples/TEXT/CTX000000016.tsv` … `CTX000000024.zip` (Tesseract, ABBYY, DjVu, SRT, VTT,
    EML, MBOX, `.txt.gz`, bundle).
  * `write_samples()` makes the two binaries, and the committed binaries stay byte-identical.
* **Docs**
  * `docs/text_inputs.md` (rewritten: detection order, matrix, JSON granularity, bundles, blank pages,
    page ids, notes, decode order, reports, HTTP statuses, flags, origin override, service,
    limitations)
  * `README.md`, `CONTRIBUTING.md`, `service/README.md`, `data_samples/README.md`
  * this plan, the digest, `DEVLOG.md`

**Tests that flip:**
* `test_build_plan_text_lines_routes_all_three_new_stages`: extract gains `--input-csv`.
* `test_odt_explicit_and_soft_page_breaks`: the footnote lands on page 2.
* `…refused_with_a_reason[pack.gz]`: now a `.tar.gz`.

## Gap register (Phase 4)

| Gap                                                                                                                                          | Kind                | Handling                                         |
|----------------------------------------------------------------------------------------------------------------------------------------------|---------------------|--------------------------------------------------|
| Plan/digest/DEVLOG said "not on `test`" for `103e30a` and then `fb72526`; plan header mangled; digest title typo "othet"                     | docs                | reconciled (header, digest, DEVLOG second entry) |
| `0001` ids coerced in classify's/aggregate's re-reads; mixed id types crash aggregate's sort                                                 | code                | D26                                              |
| PAGE-XML TableRegion/TableCell text lost; nested regions misordered                                                                          | code                | D35                                              |
| CSV/TSV stray quote swallows the file; Tesseract TSV one word per line; a numeric `line` column taken as text                                | code                | D35, D31                                         |
| JSON line+word duplication (Azure Read v3, Textract); string arrays one page per item; blobs as lines; JSON needs pandas                     | code                | D35                                              |
| Failed re-run leaves stale pages; unguarded `rmtree`; not atomic                                                                             | code                | D24                                              |
| One bad document aborts extract/stats; `--strict` not negatable; `STRICT` unvalidated                                                        | code                | D25                                              |
| Service ignores `[TEXT_INGEST]`, `.txt` UTF-8 only, 400/422 by path, no-text 200                                                             | code                | D27                                              |
| Frontend `accept=` drift                                                                                                                     | code/docs           | D28                                              |
| text-lines default input dir = ALTO samples; `--input-csv` not reaching extract; no `--strict`/`--source-origin` pass-through                | code                | D29                                              |
| `%PDF-` anywhere in 1 KiB (a ZIP storing a PDF became "pdf"); `.md`/`.csv` starting with `<`/`[`; HTML5 fragments; sniff OSError             | code                | D30                                              |
| DOCX/ODT notes silently lost; headers/footers silent                                                                                         | code                | D33                                              |
| Sheet over `MAX_LINES_PER_PAGE` refused though documented to continue; ODS numbers/comments; hidden sheets/slides silent                     | code/docs           | D35                                              |
| RTF `\fcharset` ignored; surrogate pairs crashed the page write                                                                              | code                | D35                                              |
| TEI P4, `teiCorpus`, `<choice>`                                                                                                              | code                | D35                                              |
| Word/character-level XML (ABBYY, DjVu) one character per line                                                                                | code                | D31, D35                                         |
| A folder of per-page files could not be one document; compressed files refused                                                               | code                | D32, D31                                         |
| No sibling-style decode check (CP1250 mojibake); no mirrored/rotated text signal                                                             | code                | D34                                              |
| Born-digital kinds that digital-convert cannot read end source-only                                                                          | code/config         | D21, D22; hub follow-up below                    |
| `document_hook` docstring (per-stage program names, dead link); `env_contract_data` reason                                                   | docs                | fixed                                            |
| `docs/text_inputs.md`: service claims, 422 claims, sheet continuation, ODS, decode order, jsonl check, blank pages, "atomic", report columns | docs                | rewritten                                        |
| `extract_JSON_2_TXT.main` reads doc ids with type inference                                                                                  | code (pre-existing) | left for JSON/ALTO parity                        |
| `split("-")` in the ALTO/JSON stats scripts; json-keys copies header/words onto every page                                                   | code (pre-existing) | left for ALTO/JSON parity                        |
| classify still reads only `[CLASSIFY].INPUT_CSV` (and overwrites the shared `test_alto_stats.csv` with the stock config)                     | code (pre-existing) | documented; a fix needs a classify CLI flag      |
| classify's resume skips a changed, re-ingested document; outputs of deleted inputs remain                                                    | code (pre-existing) | documented                                       |
| The dry-run tags `page_split` `[no log]` though it logs                                                                                      | docs                | documented; kept for ALTO/JSON byte-identity     |

## Cross-repo follow-ups (not done here — other repos)

* **Hub `KNOWN_PIPELINE_SUFFIXES`:**
  * `.md`, `.csv`, `.txt`, `.json` and `.xml` are already there; the Phase 3 list above asked for `.md`
    again.
  * Add `.pdf .docx .odt .ods .odp .xlsx .pptx .epub .rtf .html .htm .hocr .jsonl .tsv .srt .vtt .eml
    .mbox .tei`, and the compressed forms if ids like `scan.txt.gz` → `scan.txt` are wanted.
  * Update the skeleton mirror in the same commit, re-vendor into all five repos and retag `v1`; this
    repo's collision test and `docs/text_inputs.md` then change with it.
* **Hub `tests/test_document_required.py`:** add `text_split.py` (source only) and
  `extract_TEXT_2_TXT.py` (pages + content). Its line references have drifted
  (`extract_ALTO_2_TXT.py:197`, `classify_TEXT.py:1152` at `fb72526`).
* **Hub docs that describe alto as ALTO-only:**
  * `docs_site/pipelines.md`, `index.md`, `ecosystem/repository-map.md`, `external-tools.md`
  * `_generators/repos.py`
  * `docs/document_schema.md`: its origin table lacks bare `pdf`
  * `docs/agent_skill_strategy.md:139`: still "ALTO XML · TXT" at `eec0682`
  * `docs/k8s_deployment.md:172`
  * the hub #66 Galaxy record "ALTO XMLs post-processing"
* **Hub `ORIGIN_ORIGINATORS`:** the `digital-born` prefix routes every kind to digital-convert, which
  reads only pdf/docx. Either narrow it to `digital-born-pdf`/`-docx`, or extend digital-convert.
  `SOURCE_ORIGIN_BY_KIND` is this repo's workaround.
* **llm-enrich V-1:** `json_to_md.DROP_CATEGORIES` still ignores alto's `Trash`.
* **Resolved on 2026-09-24, note only:**
  * llm-enrich treats a missing `quality_score` as unknown (#13 P5.1), so `DOC_LINES_TEXT` tables are
    consumable; its 6/7-argument `_should_process_line` bug is fixed.
  * nlp-enrich takes pages from these tables' `page_num` (#38), so see the page-id note in
    `docs/text_inputs.md` §2.
  * The llm-enrich #13 "program names" TODO is stale; `document_hook.py`'s docstring now says so.
* Once a real OCR-engine export arrives, re-check the JSON page detection and the Tesseract/ABBYY/DjVu
  readers against it. The samples are synthetic.

## Verification (done in-session; categorization never run: torch/fasttext are not installed)

* **Suite.** 1584 (baseline at `fb72526`, this environment) → **1707 passed**, 11 skipped, 2 xfailed,
  **0 failed**. `ruff check` and `ruff format --check` clean (ruff 0.15.18).
* **Dry-runs.** `run_pipeline.py --dry-run` is **byte-identical** to `fb72526` for eight variants:
  default, `layoutreader`, `alto-tools`, `glm`, `json-keys` (with `--input-dir data_samples/JSON`),
  `--skip-split`, `--start-from classify`, and `json-keys --skip-split`.
* **Doc-id replay.** classify's re-read and aggregate's sort reproduce the old output byte for byte on
  `data_samples/DOC_LINE_CATEG(_gpt)`, and for all-numeric ids (tests).
* **E2E.** `run_pipeline.py --config <scratch> --method text-lines --skip-classify --skip-aggregate
  --strict` on `data_samples/TEXT`:
  * all 20 documents `ok`, exit 0;
  * the line-table invariant holds for all 94 rows;
  * all 20 `doc.json` records validate, and the born-digital ones carry `source` only;
  * with `SOURCE_ORIGIN_BY_KIND = xlsx = ocr:generic`, CTX000000013 gains `pages`/`content`;
  * no `DOC_LINE_CATEG` directory is created.
* **Hostile inputs.** The directory held a corrupt PDF, a zip bomb, a gzip bomb, OLE2, a PNG, entity
  XML, a FIFO, a symlink to `/etc/passwd`, a lock file, a case-folded collision, a bad JSONL, cp1250
  text, an empty file, binary data, a 36k-character line, a bundle with `../evil.txt`, and `setup.txt`
  next to an existing `out/setup/config.txt`.
  * Every file got a row with its reason code.
  * Nothing was written outside `out/`, and `setup/config.txt` is intact, also with output dir `.`.
  * Damaging a file and re-running gives `stale_pages_removed`; `--strict` exits 1.
  * The stats and extract stages continue past the damaged entries.
* **Hub drift (`267e334`).** Paradata Canonical Drift failed: `tests/test_env_contract.py`, a hub-canonical file,
  held the contents of `tests/env_contract_data.py` (a file mapped to the wrong path on apply), and the data file kept
  its old text. The restored test is byte-identical to the hub `v1` copy (`eec0682`, `docs/templates/shared/`). A
  local replay of the workflow's `MANIFEST.json` loop on the corrected tree passes all 17 files, and
  `tests/test_env_contract.py` passes (12 tests).
