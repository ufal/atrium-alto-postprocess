line-length = 100
target-version = "py311"

[lint]
select = ["E", "F", "W", "I", "B"]
# Ignore lines that are slightly too long but otherwise clean
ignore = ["E501"]

[format]
quote-style = "double"
indent-style = "space"